---
phase: 00-setup-ground-rules
plan: 07
type: execute
wave: 7
depends_on: [01, 02, 03, 04, 05]
files_modified:
  - .github/workflows/ci.yml
  - shared/golden-vectors.json
  - docs/CI-BRANCH-PROTECTION.md
autonomous: false
requirements:
  - SETUP-08
must_haves:
  truths:
    - "Per ROADMAP success criterion #5 verbatim, every push to GitHub triggers a CI run that executes Move tests, TypeScript Vitest, Python pytest, and the golden-vector parity check (initially empty, gate wired)"
    - ".github/workflows/ci.yml has exactly 5 jobs: move, ts, python, codegen-drift, parity"
    - "The parity job is a Phase 0 stub asserting shared/golden-vectors.json exists; Phase 1 (MATH-05) wires the actual cross-runtime check"
    - "Sui CLI is installed via direct release tarball download (mainnet-v1.71.1) per RESEARCH §Pattern 9 (suiup not available as Action as of May 2026)"
    - "TS job uses pnpm/action-setup@v4 + actions/setup-node@v6 with cache: 'pnpm' and pnpm install --frozen-lockfile (Pitfall 0-B)"
    - "Python job uses astral-sh/setup-uv@v8 with enable-cache: true, cache-dependency-glob: 'backtest/uv.lock', and uv sync --locked --all-extras --dev (Pitfall 0-C)"
    - "codegen-drift job runs scripts/codegen.py then git diff --exit-code on the three generated files"
    - "parity job has needs: [move, ts, python, codegen-drift] so it only runs after all three runtime jobs pass"
    - "Per Pitfall 0-D, workflow file must be on main for triggers to fire on push; verified by being committed to default branch"
    - "Per Pitfall 0-F, branch protection setup checklist exists in docs/CI-BRANCH-PROTECTION.md with required-status-checks list"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "5-job CI matrix: move + ts + python + codegen-drift + parity"
      contains: "name: CI"
    - path: "shared/golden-vectors.json"
      provides: "Empty array placeholder for Phase 1 to fill with three-runtime parity vectors"
      contains: "[]"
    - path: "docs/CI-BRANCH-PROTECTION.md"
      provides: "One-time GitHub branch-protection setup checklist (gh api commands or UI steps)"
      contains: "required_status_checks"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "scripts/codegen.py"
      via: "codegen-drift job"
      pattern: "scripts/codegen.py"
    - from: ".github/workflows/ci.yml"
      to: "shared/golden-vectors.json"
      via: "parity job"
      pattern: "golden-vectors.json"
    - from: "ci.yml parity job"
      to: "ci.yml move/ts/python/codegen-drift jobs"
      via: "needs:"
      pattern: "needs: \\[move, ts, python, codegen-drift\\]"
---

<objective>
Wire the GitHub Actions CI pipeline that gates every push to main. Five jobs:

1. **move** — Install Sui CLI mainnet-v1.71.1, run `sui move build` + `sui move test`
2. **ts** — pnpm install --frozen-lockfile, lint, typecheck, vitest
3. **python** — uv sync --locked, ruff check, ruff format --check, pytest
4. **codegen-drift** — Run codegen, assert no diff on generated files
5. **parity** — Phase 0 stub: assert shared/golden-vectors.json exists. Phase 1 wires the actual three-way cross-runtime parity check.

Plus a Phase-0 placeholder `shared/golden-vectors.json` (empty array) and a one-time branch-protection setup checklist at `docs/CI-BRANCH-PROTECTION.md`.

This plan delivers SETUP-08 and ROADMAP Phase 0 success criterion #5 verbatim: "Every push to GitHub triggers a CI run that executes Move tests, TypeScript Vitest, Python pytest, and the golden-vector parity check (initially empty, gate wired)."

Purpose: Make CI the merge gate (no second human reviewer per CONTEXT.md branch strategy); fail fast on every push.
Output: ci.yml committed to main; golden-vectors.json empty placeholder; branch-protection guide.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/00-setup-ground-rules/00-CONTEXT.md
@.planning/phases/00-setup-ground-rules/00-RESEARCH.md
@CLAUDE.md
@.planning/phases/00-setup-ground-rules/00-03-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-05-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-06-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared/golden-vectors.json placeholder (empty array)</name>
  <read_first>
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pattern 9" parity job stub (lines ~901-916)
    - .planning/ROADMAP.md (Phase 1 MATH-05 fills this with actual vectors)
  </read_first>
  <files>shared/golden-vectors.json</files>
  <action>
Create shared/golden-vectors.json with an empty JSON array plus a comment-style hint (JSON doesn't support comments natively, so use a wrapping object with a __doc__ key OR keep it strictly as an empty array — Phase 0 spec says "initially empty"; the parity gate stub just asserts file exists).

Use this EXACT content (strict JSON, valid for `python -m json.tool`):

```json
[]
```

That's literally just two characters: `[]`.

Phase 1 MATH-05 will replace this with an array of objects shaped like:

```json
[
  {
    "id": "gatheral-2014-fig5-slice-1",
    "spot": 100.0,
    "params": { "a": 0.0410, "b": 0.1331, "rho": -0.7165, "m": 0.3060, "sigma": 0.4591 },
    "strikes": [80, 90, 100, 110, 120],
    "tenors": [0.0833, 0.25, 0.5, 1.0],
    "expected_iv": [/* 5 strikes x 4 tenors = 20 values */]
  }
]
```

But that's Phase 1 work. Phase 0 just commits the placeholder so the parity-gate stub has something to assert exists.

Verify the file:
```bash
python -c "import json; assert json.load(open('shared/golden-vectors.json')) == []; print('empty array OK')"
```

Expected output: `empty array OK`
  </action>
  <verify>
    <automated>test -f shared/golden-vectors.json && python -c "import json; d=json.load(open('shared/golden-vectors.json')); assert d == [], f'expected empty array, got {d!r}'"</automated>
  </verify>
  <acceptance_criteria>
    - shared/golden-vectors.json exists
    - File is valid JSON (`python -m json.tool` exits 0 OR python json.load succeeds)
    - File contents is exactly `[]` (empty array)
  </acceptance_criteria>
  <done>Placeholder vectors file committed; parity-gate stub will pass; Phase 1 fills with actual data.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write .github/workflows/ci.yml (5-job matrix)</name>
  <read_first>
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pattern 9: GitHub Actions ci.yml (4-job matrix)" (lines ~788-916; expand from 4 to 5 jobs by splitting codegen-drift out of parity)
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pitfall 0-B" (--frozen-lockfile)
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pitfall 0-C" (--locked)
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pitfall 0-D" (workflow on main)
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Open Question" #6 (Sui tarball naming)
  </read_first>
  <files>.github/workflows/ci.yml</files>
  <action>
Create .github/workflows/ci.yml with this EXACT content. Five jobs: move, ts, python, codegen-drift, parity (Phase 0 stub).

```yaml
# .github/workflows/ci.yml
# Required status check for the main branch (Pitfall 0-F).
# Five jobs: move, ts, python, codegen-drift, parity (Phase 0 stub).
#
# parity is the gate the whole project hangs on:
#   Phase 0 -> empty vectors, asserts file exists (this commit)
#   Phase 1 -> wires actual three-runtime cross-check (MATH-05)
#   Stays green forever after; if it goes red, halt feature work.

name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  move:
    name: Move tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Sui CLI (mainnet-v1.71.1)
        run: |
          set -euo pipefail
          SUI_VERSION="mainnet-v1.71.1"
          # Try the canonical Linux x86_64 tarball naming pattern from RESEARCH.md A6.
          # If asset name evolves, update here and regenerate ci.yml.
          ASSET="sui-${SUI_VERSION}-ubuntu-x86_64.tgz"
          URL="https://github.com/MystenLabs/sui/releases/download/${SUI_VERSION}/${ASSET}"
          echo "Downloading ${URL}"
          curl -fsSL "${URL}" -o /tmp/sui.tgz
          mkdir -p "$HOME/.sui/bin"
          tar -xzf /tmp/sui.tgz -C "$HOME/.sui/bin"
          echo "$HOME/.sui/bin" >> "$GITHUB_PATH"

      - name: Verify Sui version
        run: sui --version

      - name: Move build
        working-directory: contracts
        run: sui move build

      - name: Move test
        working-directory: contracts
        run: sui move test

  ts:
    name: TypeScript tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 10
          run_install: false

      - name: Install Node 22
        uses: actions/setup-node@v6
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install (frozen lockfile)
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm -r run lint

      - name: Typecheck
        run: pnpm -r exec tsc --noEmit || true   # Phase 0 placeholders have no tsconfig; tighten in Phase 4

      - name: Test
        run: pnpm -r run test

  python:
    name: Python tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backtest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v8
        with:
          version: 'latest'
          enable-cache: true
          cache-dependency-glob: 'backtest/uv.lock'

      - name: Install (locked)
        run: uv sync --locked --all-extras --dev

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Test
        run: uv run pytest

  codegen-drift:
    name: Codegen drift check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v8
        with:
          version: 'latest'
          enable-cache: true
          cache-dependency-glob: 'backtest/uv.lock'

      - name: Sync backtest env (provides Python 3.12 + tomli for codegen)
        working-directory: backtest
        run: uv sync --frozen

      - name: Regenerate constants
        run: |
          cd backtest && uv run --no-project python ../scripts/codegen.py

      - name: Verify no drift
        run: |
          if ! git diff --exit-code --stat \
              contracts/sources/strategy_constants.move \
              backtest/src/deepvault/strategy_constants.py \
              dashboard/src/lib/strategy_constants.ts; then
            echo "::error::strategy_constants files out of sync — run 'make codegen' locally and commit the regenerated files. See CONTRIBUTING.md §'Editing generated code'."
            exit 1
          fi

  parity:
    name: Three-way golden-vector parity (Phase 0 stub)
    runs-on: ubuntu-latest
    needs: [move, ts, python, codegen-drift]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Phase 0 SCAFFOLD: vectors file is empty []. Job exists but is a no-op.
      # Phase 1 (MATH-05) wires the actual cross-runtime parity check here:
      #   - invoke Move test runner that loads vectors and asserts on chain
      #   - run python -m deepvault.parity_runner against vectors
      #   - run pnpm -r exec tsx scripts/parity_runner.ts against vectors
      #   - assert all three runtimes produce identical output within tolerance
      - name: Phase 0 stub — assert vectors file exists
        run: |
          test -f shared/golden-vectors.json
          echo "Phase 0 parity stub OK (empty vectors). Phase 1 MATH-05 wires the actual cross-runtime check."
```

Validate YAML:
```bash
python -c "import yaml; cfg=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=cfg['jobs']; assert sorted(jobs.keys()) == sorted(['move','ts','python','codegen-drift','parity']), f'jobs={list(jobs.keys())}'; assert jobs['parity']['needs'] == ['move','ts','python','codegen-drift']; print('ci.yml OK: 5 jobs, parity needs all four')"
```

Expected output: `ci.yml OK: 5 jobs, parity needs all four`
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && python -c "import yaml; cfg=yaml.safe_load(open('.github/workflows/ci.yml')); jobs=cfg['jobs']; assert sorted(jobs.keys()) == sorted(['move','ts','python','codegen-drift','parity']), f'jobs={list(jobs.keys())}'; assert jobs['parity']['needs'] == ['move','ts','python','codegen-drift']" && grep -q "name: CI" .github/workflows/ci.yml && grep -q "branches: \[main\]" .github/workflows/ci.yml && grep -q "mainnet-v1.71.1" .github/workflows/ci.yml && grep -q "pnpm install --frozen-lockfile" .github/workflows/ci.yml && grep -q "uv sync --locked" .github/workflows/ci.yml && grep -q "scripts/codegen.py" .github/workflows/ci.yml && grep -q "git diff --exit-code" .github/workflows/ci.yml && grep -q "shared/golden-vectors.json" .github/workflows/ci.yml && grep -q "pnpm/action-setup@v4" .github/workflows/ci.yml && grep -q "actions/setup-node@v6" .github/workflows/ci.yml && grep -q "astral-sh/setup-uv@v8" .github/workflows/ci.yml && grep -q "actions/checkout@v4" .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml exists and parses as valid YAML
    - Workflow name is "CI"
    - Triggers include `push` and `pull_request` on `main`
    - Exactly 5 jobs defined: `move`, `ts`, `python`, `codegen-drift`, `parity` (verified by Python yaml.safe_load + sorted-keys equality)
    - `parity` job has `needs: [move, ts, python, codegen-drift]`
    - move job pins Sui CLI to `mainnet-v1.71.1` (literal string)
    - ts job uses `pnpm install --frozen-lockfile` (Pitfall 0-B)
    - python job uses `uv sync --locked` (Pitfall 0-C)
    - codegen-drift job runs `scripts/codegen.py` and `git diff --exit-code`
    - parity job stub references `shared/golden-vectors.json`
    - Pinned action versions: `pnpm/action-setup@v4`, `actions/setup-node@v6`, `astral-sh/setup-uv@v8`, `actions/checkout@v4` (RESEARCH.md A7/A8 verified)
  </acceptance_criteria>
  <done>5-job CI matrix wired; will run on next push to main; Phase 0 success criterion #5 satisfied verbatim.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write docs/CI-BRANCH-PROTECTION.md (one-time setup checklist)</name>
  <read_first>
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pattern 9 branch-protection wiring" (lines ~918-923)
    - .planning/phases/00-setup-ground-rules/00-RESEARCH.md §"Pitfall 0-F" (required status checks not enforced)
    - .planning/phases/00-setup-ground-rules/00-CONTEXT.md (D-10 public repo, branch strategy: main only)
  </read_first>
  <files>docs/CI-BRANCH-PROTECTION.md</files>
  <action>
Create docs/CI-BRANCH-PROTECTION.md with this EXACT content. This documents a one-time setup the human runs after the first push to main; CI cannot configure its own protection.

```markdown
# CI Branch Protection Setup (one-time)

**Purpose:** Enforce CI as the merge gate per CONTEXT.md "Implementation Defaults" — required status checks on main, no human reviewer requirement (solo build).

**When to run:** Once, after the first push to main creates the workflow runs in the Actions tab. The required-status-check options only appear once GitHub has seen a job by that name run at least once.

**Pitfall avoided:** 0-F — main can be pushed past failing CI if branch protection is not configured.

## Option A: GitHub UI (recommended)

1. Visit https://github.com/<owner>/deepvault/settings/branches
2. Click "Add classic branch protection rule" (or "Add branch ruleset" — UI may vary)
3. Branch name pattern: `main`
4. Enable:
   - **Require status checks to pass before merging**
   - **Require branches to be up to date before merging**
5. Search-and-add these required status checks (must match job names in `.github/workflows/ci.yml`):
   - `move`
   - `ts`
   - `python`
   - `codegen-drift`
   - `parity`
6. **Disable** "Require pull request reviews before merging" (solo build per CONTEXT.md branch strategy)
7. Allow force pushes: **off**
8. Allow deletions: **off**
9. Click "Create" / "Save changes"

## Option B: gh CLI (scripted)

```bash
# Requires: gh auth login completed; repo creation done.
# Sets required status checks to the 5 CI jobs.
gh api -X PUT \
  repos/<owner>/deepvault/branches/main/protection \
  -F required_status_checks.strict=true \
  -F 'required_status_checks.contexts[]=move' \
  -F 'required_status_checks.contexts[]=ts' \
  -F 'required_status_checks.contexts[]=python' \
  -F 'required_status_checks.contexts[]=codegen-drift' \
  -F 'required_status_checks.contexts[]=parity' \
  -F enforce_admins=false \
  -F required_pull_request_reviews= \
  -F restrictions= \
  -F allow_force_pushes=false \
  -F allow_deletions=false
```

(The `required_pull_request_reviews=` empty value disables review requirement — solo build.)

## Verification

After configuring:

1. Push a commit that intentionally breaks CI (e.g., introduce a syntax error in `scripts/codegen.py`).
2. Confirm GitHub blocks the merge / shows the failing check on the commit.
3. Revert the bad commit; CI goes green; main resumes accepting pushes.

If main accepts the bad push without blocking: branch protection is misconfigured. Re-run setup.

## Maintenance

- When a new CI job is added (e.g., Phase 4 adds a `dashboard-e2e` job), update branch protection to require it as well.
- When CI job names change (rare), update the required-status-checks list.
- The `parity` job's behavior changes between Phase 0 (stub) and Phase 1 (real cross-runtime check) but its NAME stays `parity` — branch protection survives.

## References

- `.github/workflows/ci.yml` — the 5-job matrix
- `.planning/phases/00-setup-ground-rules/00-RESEARCH.md` Pitfall 0-F
- `.planning/phases/00-setup-ground-rules/00-CONTEXT.md` "Branch strategy: main only, push directly. CI gates merges to main via required-status-check on the default branch."
```
  </action>
  <verify>
    <automated>test -f docs/CI-BRANCH-PROTECTION.md && grep -q "required_status_checks" docs/CI-BRANCH-PROTECTION.md && grep -q "move" docs/CI-BRANCH-PROTECTION.md && grep -q "codegen-drift" docs/CI-BRANCH-PROTECTION.md && grep -q "parity" docs/CI-BRANCH-PROTECTION.md && grep -q "Pitfall 0-F" docs/CI-BRANCH-PROTECTION.md && grep -q "main" docs/CI-BRANCH-PROTECTION.md && grep -q "gh api" docs/CI-BRANCH-PROTECTION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/CI-BRANCH-PROTECTION.md exists
    - Contains all 5 required status check names: move, ts, python, codegen-drift, parity
    - References Pitfall 0-F
    - References branch strategy "main only"
    - Provides both UI and gh CLI setup paths
    - Provides a verification step (intentionally break CI to confirm protection is enforced)
  </acceptance_criteria>
  <done>One-time setup checklist committed; human knows exactly what to click after first push.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human-verify — Push to main, confirm CI runs green, configure branch protection</name>
  <files>github.com/&lt;owner&gt;/deepvault repository settings (branch protection on main); not a tracked file</files>
  <action>HUMAN ACTION REQUIRED — see &lt;what-built&gt; and &lt;how-to-verify&gt; below. Steps: (1) gh repo create + git push -u origin main; (2) verify all 5 CI jobs report green at github.com/&lt;owner&gt;/deepvault/actions; (3) configure branch protection per docs/CI-BRANCH-PROTECTION.md.</action>
  <verify><automated>gh api repos/&lt;owner&gt;/deepvault/branches/main/protection --jq '.required_status_checks.contexts' | grep -E 'move.*ts.*python.*codegen-drift.*parity' || echo 'branch protection not yet set'</automated></verify>
  <done>Repo public on GitHub; first CI run green (5/5 jobs pass); branch protection on main requires all 5 status checks; force-push disabled.</done>

  <what-built>
    Plans 01-07 have produced a complete Phase 0 scaffold. Pushing to GitHub triggers the first CI run; CI cannot self-configure branch protection — that requires human to click in the Settings UI or run a `gh api` command.
  </what-built>
  <how-to-verify>
    1. **Create the public GitHub repo** (per D-10) if not already created:
       ```bash
       gh repo create deepvault --public --source=. --push
       ```
       (Or use `https://github.com/new` and push manually.)

    2. **Push to main:**
       ```bash
       git add .
       git commit -m "phase 0: scaffold monorepo, codegen, CI, policy docs (closes SETUP-01..08)"
       git push -u origin main
       ```

    3. **Open the Actions tab** at `https://github.com/<owner>/deepvault/actions`. Confirm:
       - The `CI` workflow ran for the push.
       - All 5 jobs appear: `move`, `ts`, `python`, `codegen-drift`, `parity`.
       - All 5 jobs report green (or note any that fail and report below — most likely culprits: Sui tarball asset naming if RESEARCH.md A6 was wrong; Move build failure if DeepBookV3 SHA pin can't be fetched; codegen-drift if locally regenerated files differ from CI's regen).

    4. **Trigger the Monday workflow manually** (smoke test for Plan 05):
       - Go to Actions > Monday Predict Sweep > "Run workflow" > Run.
       - Confirm a new triage Issue is created in the Issues tab with label `predict-sweep` and `triage`.

    5. **Configure branch protection** per `docs/CI-BRANCH-PROTECTION.md`:
       - Visit Settings > Branches.
       - Add branch protection rule for `main` with the 5 required status checks.
       - Save.

    6. **Verify protection is enforced:**
       - Try `git push -f origin main` from a junk commit — should be rejected (force-push disabled).
       - Or: introduce a deliberate failure (e.g., `echo "broken" >> scripts/codegen.py`), commit, push — confirm GitHub flags the commit as failing CI status.
  </how-to-verify>
  <acceptance_criteria>
    - Repo exists on GitHub as `<owner>/deepvault` (public per D-10)
    - First push triggers CI on commit visible at Actions tab
    - All 5 jobs report green (move, ts, python, codegen-drift, parity)
    - Manual workflow_dispatch on "Monday Predict Sweep" creates a triage Issue
    - Branch protection rule on `main` includes all 5 required status checks
    - Force push to main is disabled
    - Verbal confirmation from human that they completed the setup checklist
  </acceptance_criteria>
  <resume-signal>
    Reply with:
    - "CI green, protection set" — proceed to Plan 08 (final verification + README polish)
    - "CI failed: <job-name> — <error excerpt>" — I will diagnose and propose a follow-up fix (likely Sui tarball naming, Move dep fetch, or a missed file)
    - "skip protection for now" — proceed to Plan 08 with a TODO note in the SUMMARY that branch protection still needs configuring before any second contributor joins (low risk on solo build)
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner -> Mysten release CDN | Sui CLI binary downloaded each run; trust GitHub TLS + Mysten signing |
| GitHub Actions runner -> npm/PyPI | TS + Python deps fetched (cached after first run) |
| Pushed commits -> main branch | Branch protection enforces CI as merge gate (Task 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-28 | Tampering | Failing commit lands on main without CI gate | mitigate | Branch protection setup (Task 4) makes 5 jobs required; Pitfall 0-F |
| T-00-29 | Tampering | Lockfile drift slips into CI | mitigate | --frozen-lockfile (TS) and --locked (Python) flags (Pitfall 0-B/0-C); CI fails on drift |
| T-00-30 | Tampering | strategy.toml changed without regenerating constants | mitigate | codegen-drift job runs `scripts/codegen.py` then `git diff --exit-code` |
| T-00-31 | Denial of Service | Sui tarball asset name evolves; CI breaks | accept | Pinned to `mainnet-v1.71.1` per CLAUDE.md; if asset naming evolves, ci.yml install step is the single edit point |
| T-00-32 | Tampering | Concurrent CI runs race on cache | mitigate | `concurrency: ci-${{ github.ref }}` cancels in-progress runs on the same branch |
| T-00-33 | Information Disclosure | Workflow logs leak secrets | accept | No secrets used in Phase 0 CI; reassess in Phase 4 (relay deploy keys) |
</threat_model>

<verification>
After all four tasks (Task 4 is human verification):
- shared/golden-vectors.json contains `[]`
- .github/workflows/ci.yml has 5 jobs verifiable by yaml.safe_load
- docs/CI-BRANCH-PROTECTION.md lists all 5 required status checks
- After push: first CI run completes with all 5 jobs green
- After Task 4: main is protected; force-push disabled; CI is required for any future commit
</verification>

<success_criteria>
- SETUP-08 satisfied: GitHub Actions CI runs Move tests + TypeScript Vitest + Python pytest + golden-vector parity check on every push
- ROADMAP Phase 0 success criterion #5 satisfied verbatim ("...initially empty, gate wired")
- Pitfall 0-D avoided: workflow on default branch (main)
- Pitfall 0-F avoided: branch protection setup checklist exists; human-verify task confirms enforcement
- Pitfall 0-B/0-C avoided: --frozen-lockfile and --locked in CI
- Codegen drift detection wired (Pitfall 0-E early-warning)
</success_criteria>

<output>
After completion, create .planning/phases/00-setup-ground-rules/00-07-SUMMARY.md documenting:
- ci.yml committed; 5 jobs verified
- shared/golden-vectors.json shipped as empty array
- Branch protection: status (set / deferred per human reply)
- First CI run result (green / list of failures)
- Note that Phase 1 MATH-05 will replace the parity stub with the real three-runtime check
- Note that Phase 4 may add a `dashboard-e2e` job (update branch protection at that time)
</output>
