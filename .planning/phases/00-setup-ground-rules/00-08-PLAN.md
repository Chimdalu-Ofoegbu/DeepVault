---
phase: 00-setup-ground-rules
plan: 08
type: execute
wave: 8
depends_on: [01, 02, 03, 04, 05, 06, 07]
files_modified:
  - README.md
autonomous: false
requirements:
  - SETUP-01
  - SETUP-02
  - SETUP-03
  - SETUP-04
  - SETUP-05
  - SETUP-06
  - SETUP-07
  - SETUP-08
must_haves:
  truths:
    - "README.md is a polished entry point: laypitch, glossary, prerequisites, build/test commands, hosting URLs (TBD until Phase 4), reproducible run script, build log skeleton — all derived from CONTEXT.md D-10..D-16"
    - "Per D-10/D-12, README links the public GitHub repo URL, MIT license, and append-only build log section"
    - "Per ROADMAP Phase 0 success criterion #1, a fresh `git clone` followed by `make install && make codegen && make test && make lint` produces a working monorepo — verified by running these four commands in clean shell"
    - "All 5 ROADMAP Phase 0 success criteria are checked end-to-end via a final verification task"
    - "Append-only build log entry for Week 1 records Phase 0 completion (per D-12 discipline + CONTRIBUTING.md build-log convention)"
  artifacts:
    - path: "README.md"
      provides: "Polished project entry point cold-readable by judges"
      contains: "## Laypitch"
  key_links:
    - from: "README.md"
      to: "CONTRIBUTING.md, docs/HEDGE-POLICY.md, docs/MAINNET-FUNDING.md, docs/DEV-BOOTSTRAP.md"
      via: "References section"
      pattern: "CONTRIBUTING|HEDGE-POLICY|MAINNET-FUNDING|DEV-BOOTSTRAP"
    - from: "README.md build log"
      to: "Phase 0 closure"
      via: "Week 1 entry"
      pattern: "Week 1|Phase 0 completed"
---

<objective>
Final Phase 0 plan: polish README.md (replace the Plan 01 skeleton with full content) and run the end-to-end Phase 0 verification — a single shell sequence that proves all 5 ROADMAP success criteria pass on a fresh-state repo.

This plan delivers:
1. The full README.md — laypitch, glossary, prerequisites, repository layout, build/test/lint, hosting, demo placeholder, build log Week 1 entry, references.
2. Execute `make install && make codegen && make test && make lint` end-to-end and capture exit codes.
3. Cross-check all 5 ROADMAP success criteria against the actual repo state.
4. Update the build log with Phase 0 closure entry.

Purpose: Close Phase 0 with a verifiable proof that scaffold is correct; produce the README a judge will read first; commit the build log baseline.
Output: Polished README; verification report in plan SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-setup-ground-rules/00-CONTEXT.md
@.planning/phases/00-setup-ground-rules/00-RESEARCH.md
@CLAUDE.md
@.planning/phases/00-setup-ground-rules/00-01-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-02-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-03-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-04-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-05-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-06-SUMMARY.md
@.planning/phases/00-setup-ground-rules/00-07-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Polish README.md (replace Plan 01 skeleton with full content)</name>
  <read_first>
    - README.md (Plan 01 skeleton — replace verbatim)
    - .planning/phases/00-setup-ground-rules/00-CONTEXT.md (D-12 build log discipline; D-13/D-15 hosting URLs)
    - CONTRIBUTING.md (Plan 06 — for cross-references)
    - docs/HEDGE-POLICY.md (Plan 06 — for hedge framing in laypitch)
  </read_first>
  <files>README.md</files>
  <action>
Replace the current README.md (created as a skeleton in Plan 01) with this EXACT content. Keep the build-log entry from Plan 01 if it already added Week 1 hash mark; otherwise initialize.

```markdown
# DeepVault

> A composable structured-product vault on Sui DeepBook Predict that fuses PLP yield with automated tail-risk hedging, paired with an institutional-grade SVI volatility-surface dashboard. Built for Sui Overflow 2026.

[![CI](https://github.com/<owner>/deepvault/actions/workflows/ci.yml/badge.svg)](https://github.com/<owner>/deepvault/actions/workflows/ci.yml)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)

**Status:** Phase 0 — Setup & Ground Rules (complete) · Phase 1 — Math Foundation (next)
**Ship target:** 2026-06-16 (Sui Overflow 2026 submission)

## Laypitch

DeepVault sells "PLP yield minus crash insurance" as a single deposit.

You put USDsui in. The vault routes ~90% to DeepBook Predict's PLP for yield, and ~10% buys binary tail hedges priced from a live SVI volatility surface (Gatheral & Jacquier 2014). When BTC tanks more than ~15%, the hedges pay; otherwise you collect the PLP fees minus a small hedge cost.

The flagship demo is a single Programmable Transaction Block (PTB) that opens three positions atomically — Margin borrow + vault deposit + Predict hedge mint — showcasing what "Sui composability" actually means at the protocol layer.

## Glossary

- **PLP** — Predict Liquidity Provider; the LP role inside DeepBook Predict's binary-options venue.
- **SVI** — Stochastic Volatility Inspired; a 5-parameter volatility-surface parameterization (Gatheral & Jacquier 2014).
- **Vault share** — `Coin<VAULT_SHARE>` representing pro-rata claim on vault NAV.
- **PTB** — Programmable Transaction Block; Sui's atomic multi-call primitive.
- **Hedge ratio** — Fraction of each new deposit routed to the hedge book (locked at 10% per `docs/HEDGE-POLICY.md`).
- **NAV** — Net Asset Value per share, anchored to the vault's PLP balance + hedge book mark-to-market.

## Quick start

```bash
git clone https://github.com/<owner>/deepvault.git
cd deepvault

# Install all toolchains (Move, TS, Python) — see docs/DEV-BOOTSTRAP.md if first time
make install

# Regenerate strategy_constants from shared/strategy.toml
make codegen

# Run all tests
make test

# Run lints + format checks
make lint
```

If any step fails, see `docs/DEV-BOOTSTRAP.md` for one-shot machine setup (Sui CLI, pnpm, uv, wallets).

## Demo

```bash
make demo   # Phase 6 fills this in — reproduces demo end-to-end from fresh clone
```

The demo target prints a placeholder until Phase 6. After submission, it will produce the same artifact judges see in the demo video.

## Repository layout

| Path | Purpose | Phase |
|------|---------|-------|
| `contracts/` | Sui Move package (`deepvault::`) | Phase 0 + 2 |
| `indexer/` | Node.js event relay | Phase 4 (placeholder) |
| `dashboard/` | React + Vite SVI Risk Studio | Phase 4 (placeholder) |
| `backtest/` | Python uv project, lookahead audit | Phase 0 + 1 + 3 |
| `shared/` | `strategy.toml` (source of truth), `golden-vectors.json` | Phase 0 + 1 |
| `scripts/` | `codegen.py`, `predict-diff.sh`, vendored DeepBookV3 fork | Phase 0 |
| `config/` | `testnet.toml`, `mainnet.toml` (TBD slots filled in Phases 2/5) | Phase 0 |
| `docs/` | CONTRIBUTING, HEDGE-POLICY, MAINNET-FUNDING, DEV-BOOTSTRAP, CI-BRANCH-PROTECTION | Phase 0 |
| `.github/workflows/` | CI (5-job matrix) + Monday Predict sweep cron | Phase 0 |

## Architecture (high level)

```
shared/strategy.toml ──> codegen.py ──┬──> contracts/sources/strategy_constants.move
                                      ├──> backtest/src/deepvault/strategy_constants.py
                                      └──> dashboard/src/lib/strategy_constants.ts

vault::supply / redeem / rebalance ──> predict::supply / mint
                                  └──> oracle_svi::OracleSVIUpdated event ──> indexer ──ws──> dashboard
```

Full architecture diagram: `.planning/research/ARCHITECTURE.md` (Phase 6 produces a polished PNG/SVG version per DEPLOY-07).

## Hosting

| Component | Tier | URL |
|-----------|------|-----|
| Dashboard (React + Vite) | Vercel free tier | `[TBD-vercel-subdomain].vercel.app` (Phase 4) |
| Event relay (Node.js + WS) | Render free tier | `[TBD-render-subdomain].onrender.com` (Phase 4) |
| Sui RPC (testnet) | Public Mysten | `https://fullnode.testnet.sui.io:443` |
| Sui RPC (mainnet, Phase 5) | Public Mysten | `https://fullnode.mainnet.sui.io:443` |

Per CONTEXT.md D-13/D-15/D-16: default Vercel/Render subdomains, no custom domain. Hosting URLs are filled in `config/{testnet,mainnet}.toml` `[hosting]` section as each component deploys.

## Key policies (locked in writing)

- **Hedge-ratio policy** (10% allocation, -15% OTM, 14-day tenor, fixed sizing): `docs/HEDGE-POLICY.md`
- **Code freeze** (2026-05-30): `CONTRIBUTING.md §"Code freeze: 2026-05-30"`
- **No refactor after vault ships** (Pitfall 18 mitigation): `CONTRIBUTING.md §"No refactor after vault ships"`
- **No dashboard before vault** (Pitfall 19 mitigation): `CONTRIBUTING.md §"No dashboard work before vault feature-complete"`
- **Weekly Monday Predict sweep** (Pitfall 6 mitigation): `CONTRIBUTING.md §"Weekly Monday Predict sweep"`
- **Mainnet redeploy mechanical playbook**: `docs/MAINNET-FUNDING.md`

## Build log

Append-only weekly bullets per `CONTRIBUTING.md` build-log discipline. Never edit history; never delete entries.

### Week 1 (2026-05-09 to 2026-05-15)

- **Phase 0 completed** — monorepo scaffold, codegen, CI (5-job matrix), policy docs (CONTRIBUTING, HEDGE-POLICY, MAINNET-FUNDING, DEV-BOOTSTRAP, CI-BRANCH-PROTECTION).
- Toolchains pinned: Sui CLI mainnet-v1.71.1, Move 2024 edition, Node 22 LTS, pnpm 10, Python 3.12, uv 0.5+.
- DeepBookV3 vendored at `scripts/deepbookv3/` via git subtree --squash; Move.toml SHA-pinned.
- Hedge-ratio policy committed: 10% allocation, -15% OTM, 14-day tenor, fixed sizing — frozen until Phase 3 backtest re-tune (then permanent).
- Two-wallet split provisioned: testnet dev + mainnet deploy (UNFUNDED until Phase 5).
- Slack remaining: 37 days (Day 2 of 39).

## References

- **For developers:** `docs/DEV-BOOTSTRAP.md` (one-shot setup), `CONTRIBUTING.md` (rules)
- **For judges:** Laypitch above, `docs/HEDGE-POLICY.md` (strategy lock), demo video (Phase 6)
- **For deploy:** `docs/MAINNET-FUNDING.md` (Phase 5 playbook), `docs/CI-BRANCH-PROTECTION.md` (one-time CI setup)
- **For research:** `.planning/research/SUMMARY.md`, `.planning/research/STACK.md`, `.planning/research/PITFALLS.md`
- **For roadmap:** `.planning/ROADMAP.md`, `.planning/REQUIREMENTS.md`

## License

MIT — see [LICENSE](LICENSE).

---

*Built for Sui Overflow 2026 by a solo builder. Hard ship: 2026-06-16. Code freeze: 2026-05-30.*
```

Note: replace `<owner>` with the actual GitHub owner once known. If unknown at this plan execution time, leave as `<owner>` placeholder and add a TODO comment — Plan 07 Task 4 (Human-verify) captures this.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "## Laypitch" README.md && grep -q "## Glossary" README.md && grep -q "## Quick start" README.md && grep -q "make install" README.md && grep -q "make codegen" README.md && grep -q "make test" README.md && grep -q "make lint" README.md && grep -q "## Repository layout" README.md && grep -q "## Hosting" README.md && grep -q "## Key policies" README.md && grep -q "## Build log" README.md && grep -q "Week 1" README.md && grep -q "Phase 0 completed" README.md && grep -q "10%" README.md && grep -q "\-15%" README.md && grep -q "14-day" README.md && grep -q "2026-05-30" README.md && grep -q "2026-06-16" README.md && grep -q "MIT" README.md && grep -q "docs/HEDGE-POLICY.md" README.md && grep -q "docs/MAINNET-FUNDING.md" README.md && grep -q "docs/DEV-BOOTSTRAP.md" README.md && grep -q "CONTRIBUTING.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists
    - Has all required sections: Laypitch, Glossary, Quick start, Repository layout, Hosting, Key policies, Build log, References, License
    - Quick start contains all four make commands: install, codegen, test, lint
    - Build log Week 1 entry contains "Phase 0 completed" string
    - Locked hedge values present: 10%, -15%, 14-day
    - Hard-lock dates present: 2026-05-30 (code freeze), 2026-06-16 (ship)
    - References to all four key docs: CONTRIBUTING.md, HEDGE-POLICY.md, MAINNET-FUNDING.md, DEV-BOOTSTRAP.md
    - Indicates MIT license
  </acceptance_criteria>
  <done>README polished; cold-readable by a judge in 60 seconds; build log has Phase 0 closure entry.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify — Run end-to-end verification (make install/codegen/test/lint) on a fresh shell</name>
  <files>/tmp/deepvault-verify (fresh clone target); not a tracked file</files>
  <action>HUMAN ACTION REQUIRED — see &lt;what-built&gt; and &lt;how-to-verify&gt; below. Run the fresh-clone + 4-make-command + 5-criterion cross-check sequence on the dev machine and report exit codes / PASS-FAIL output.</action>
  <verify><automated>cd /tmp &amp;&amp; rm -rf deepvault-verify &amp;&amp; git clone https://github.com/&lt;owner&gt;/deepvault.git deepvault-verify &amp;&amp; cd deepvault-verify &amp;&amp; make install &amp;&amp; make codegen &amp;&amp; git diff --exit-code contracts/sources/strategy_constants.move backtest/src/deepvault/strategy_constants.py dashboard/src/lib/strategy_constants.ts &amp;&amp; make test &amp;&amp; make lint</automated></verify>
  <done>Fresh clone reproduces working monorepo: make install/codegen/test/lint all exit 0; codegen idempotency verified; all 5 ROADMAP Phase 0 success criteria PASS.</done>

  <what-built>
    Plans 01-08 produce a complete Phase 0 scaffold. The final acceptance test is whether ROADMAP Phase 0 success criterion #1 holds verbatim: "A fresh git clone followed by pnpm install and uv sync produces a working monorepo with Move/TypeScript/Python toolchains pinned to exact versions."

    The most rigorous version: clone the repo to a brand-new directory, run `make install && make codegen && make test && make lint`, capture exit codes.
  </what-built>
  <how-to-verify>
    Run this sequence on the dev machine (Git Bash on Windows, native bash on Linux/Mac). Use a fresh clone to a sibling directory:

    ```bash
    # 1. Clean clone (sibling dir, doesn't disturb working tree)
    cd /tmp
    rm -rf deepvault-verify
    git clone https://github.com/<owner>/deepvault.git deepvault-verify
    cd deepvault-verify

    # 2. Install
    make install
    # Expected: pnpm install --frozen-lockfile succeeds; cd backtest && uv sync --locked succeeds.
    # Exit code 0.

    # 3. Codegen (verify reproducibility)
    make codegen
    git diff --exit-code contracts/sources/strategy_constants.move \
                          backtest/src/deepvault/strategy_constants.py \
                          dashboard/src/lib/strategy_constants.ts
    # Expected: codegen runs; git diff is empty (idempotency).
    # Exit code 0.

    # 4. Test
    make test
    # Expected:
    #   sui move test          -> 0 tests pass (Phase 0 has no Move tests yet; OK)
    #   pnpm -r run test       -> "Phase 4 fills this in" placeholder echo, exits 0
    #   uv run pytest          -> 0 tests collected (Phase 0 has no Python tests yet; OK)
    # Exit code 0.

    # 5. Lint
    make lint
    # Expected:
    #   pnpm -r run lint       -> "Phase 4 fills this in" placeholder echo, exits 0
    #   uv run ruff check .    -> 0 errors (no Python source yet)
    #   uv run ruff format --check . -> no changes needed
    # Exit code 0.

    # 6. Print versions for the SUMMARY
    sui --version
    node --version
    pnpm --version
    uv --version
    bash --version
    ```

    Then run the 5-criterion ROADMAP cross-check:

    ```bash
    cd /tmp/deepvault-verify

    echo "=== Success criterion #1: pinned toolchains ==="
    grep -q "rev = \"[0-9a-f]\\{40\\}\"" contracts/Move.toml && echo "  PASS: DeepBookV3 SHA-pinned"
    grep -q "mainnet-v1.71.1" .github/workflows/ci.yml && echo "  PASS: Sui CLI pinned in CI"
    grep -q "requires-python = \">=3.12\"" backtest/pyproject.toml && echo "  PASS: Python 3.12+"

    echo "=== Success criterion #2: codegen wired ==="
    python scripts/codegen.py --check && echo "  PASS: codegen idempotent"
    grep -q "AUTO-GENERATED" contracts/sources/strategy_constants.move && \
    grep -q "AUTO-GENERATED" backtest/src/deepvault/strategy_constants.py && \
    grep -q "AUTO-GENERATED" dashboard/src/lib/strategy_constants.ts && \
    echo "  PASS: All three runtimes have generated headers"

    echo "=== Success criterion #3: predict-diff + Monday cron ==="
    test -f scripts/predict-diff.sh && bash -n scripts/predict-diff.sh && echo "  PASS: predict-diff.sh"
    test -f .github/workflows/monday-predict-check.yml && echo "  PASS: Monday cron workflow"
    test -f .predict-diff-state && echo "  PASS: state file initialized"

    echo "=== Success criterion #4: CONTRIBUTING.md policy locks ==="
    grep -q "2026-05-30" CONTRIBUTING.md && echo "  PASS: code freeze date"
    grep -q "fixed-ratio v1\|Fixed (v1)" CONTRIBUTING.md && echo "  PASS: hedge-ratio policy"
    grep -q "No refactor after vault ships" CONTRIBUTING.md && echo "  PASS: no-refactor rule"

    echo "=== Success criterion #5: CI parity gate ==="
    test -f shared/golden-vectors.json && echo "  PASS: golden-vectors.json placeholder"
    grep -q "needs: \[move, ts, python, codegen-drift\]" .github/workflows/ci.yml && echo "  PASS: parity job dependencies"
    ```

    Verification:
    1. `make install` exits 0 on a fresh clone
    2. `make codegen` exits 0 AND `git diff --exit-code` shows no changes (idempotency)
    3. `make test` exits 0 (placeholder tests pass; no real tests yet)
    4. `make lint` exits 0 (no source yet to lint)
    5. All 5 ROADMAP success criteria checks emit "PASS"
    6. Versions reported match: Sui 1.71.1, Node v22.x.x, pnpm 10.x.x, uv 0.5+
  </how-to-verify>
  <acceptance_criteria>
    - Fresh clone of repo to /tmp/deepvault-verify succeeds (proves D-10 public repo accessible)
    - `make install` exits 0
    - `make codegen` followed by `git diff --exit-code` exits 0 (idempotency)
    - `make test` exits 0
    - `make lint` exits 0
    - All 5 ROADMAP Phase 0 success criteria cross-check emits "PASS"
    - Versions emitted: `sui --version` reports 1.71.1; `node --version` v22.x; `pnpm --version` 10.x; `uv --version` 0.5+
    - First CI run on push to main is green (verified in Plan 07 Task 4)
    - Branch protection configured (verified in Plan 07 Task 4)
  </acceptance_criteria>
  <resume-signal>
    Reply with one of:
    - "all green" — Phase 0 closes; create Phase 0 closure SUMMARY (Task 3); proceed to Phase 1.
    - "fail: <criterion> — <error>" — I diagnose and write a follow-up Plan 09 to close the gap, OR re-run a specific earlier plan.
    - "skip-verify" — Phase 0 closes with TODO note in SUMMARY that end-to-end verification was deferred (acceptable on solo build but loses the Phase 0 success criterion #1 verbatim guarantee).
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write Phase 0 closure SUMMARY (cross-cutting)</name>
  <read_first>
    - .planning/phases/00-setup-ground-rules/00-01-SUMMARY.md through 00-07-SUMMARY.md (each plan's individual closure)
    - .planning/ROADMAP.md (Phase 0 success criteria — confirm all 5 satisfied)
    - .planning/REQUIREMENTS.md (SETUP-01..08 — confirm all 8 closed)
  </read_first>
  <files>.planning/phases/00-setup-ground-rules/00-08-SUMMARY.md</files>
  <action>
This is the final per-plan SUMMARY. Use the standard template + an additional "Phase 0 closure" section that aggregates across all 8 plans.

The SUMMARY MUST include:

1. **Per-plan rollup table:**

   | Plan | Files Modified | Status | Notable Output |
   |------|----------------|--------|-----------|
   | 00-01 | 9 (root files + workspace placeholders) | green | LICENSE, .gitignore, README skeleton |
   | 00-02 | 8 (Move skeleton, Python uv, Makefile, DEV-BOOTSTRAP) | green/checkpoint | DeepBookV3 SHA: <SHA>; testnet+mainnet wallets provisioned |
   | 00-03 | 6 (strategy.toml, codegen.py, 3 generated, Makefile) | green | All locked values 1000/1500/1209600/172800/"fixed" parity-checked |
   | 00-04 | 2 (config/{testnet,mainnet}.toml) | green | Schema parity verified |
   | 00-05 | 4 (DeepBookV3 subtree, predict-diff.sh, .predict-diff-state, monday-predict-check.yml) | green | Vendored SHA: <SHA>; cron 0 14 * * 1 wired |
   | 00-06 | 3 (CONTRIBUTING.md, HEDGE-POLICY.md, MAINNET-FUNDING.md) | green | 2026-05-30 verbatim; D-01..D-09 referenced |
   | 00-07 | 3 (ci.yml 5-job, golden-vectors.json [], CI-BRANCH-PROTECTION.md) | green/checkpoint | First CI run green; branch protection set |
   | 00-08 | 1 (README.md polish) | green/checkpoint | Build log Week 1 entry recorded |

2. **All 8 SETUP requirements closed:**
   - [x] SETUP-01 (Plan 01): Repo scaffolded as TS monorepo with `pnpm` + `uv`
   - [x] SETUP-02 (Plan 02): Move.toml SHA-pinned to predict-testnet-4-16; Sui CLI mainnet-v1.71.1
   - [x] SETUP-03 (Plan 03): shared/strategy.toml + codegen to Move/Python/TS
   - [x] SETUP-04 (Plan 04): config/{testnet,mainnet}.toml scaffold (schema parity)
   - [x] SETUP-05 (Plan 05): predict-diff.sh + Monday cron
   - [x] SETUP-06 (Plan 06): hedge-ratio policy committed (HEDGE-POLICY.md ADR + CONTRIBUTING.md summary)
   - [x] SETUP-07 (Plan 06): code-freeze 2026-05-30 + no-refactor rule in CONTRIBUTING.md
   - [x] SETUP-08 (Plan 07): GitHub Actions CI 5-job matrix + branch protection

3. **All 16 D-XX decisions honored** — list each with the plan that delivered it:
   - D-01 (10% allocation) → Plan 03 (strategy.toml) + Plan 06 (HEDGE-POLICY.md, CONTRIBUTING.md)
   - D-02 (-15% OTM) → Plan 03 + Plan 06
   - D-03 (14-day tenor, 2-day roll) → Plan 03 + Plan 06
   - D-04 (parameterized fixed sizing) → Plan 03 + Plan 06
   - D-05 (re-tuning Phase 3 only) → Plan 06 (HEDGE-POLICY.md)
   - D-06 (two-wallet split) → Plan 02 (DEV-BOOTSTRAP.md) + Plan 06 (MAINNET-FUNDING.md)
   - D-07 ($80 budget + risk flag) → Plan 06 (MAINNET-FUNDING.md)
   - D-08 (Cetus DEX swap) → Plan 06 (MAINNET-FUNDING.md)
   - D-09 (no third wallet, ephemeral CI keypairs) → Plan 06 (MAINNET-FUNDING.md)
   - D-10 (public GitHub day 1, MIT) → Plan 01 (LICENSE) + Plan 07 Task 4 (gh repo create)
   - D-11 (MIT license) → Plan 01 (LICENSE) + Plan 08 (README)
   - D-12 (minimal build log) → Plan 01 (README skeleton) + Plan 08 (Week 1 entry)
   - D-13 (Vercel free tier) → Plan 04 (config[hosting]) + Plan 08 (README hosting table)
   - D-14 (local Vite recording target) → Plan 06 (MAINNET-FUNDING.md demo recording section)
   - D-15 (Render free tier + keepalive) → Plan 04 (config[hosting] keepalive_path) + Plan 08 (README hosting table)
   - D-16 (no custom domain) → Plan 04 (config) + Plan 08 (README hosting table)

4. **Phase 0 success criteria cross-check** (from Task 2 verification output):
   - [PASS/FAIL] #1: fresh clone produces working monorepo
   - [PASS/FAIL] #2: codegen wired, drift detection in CI
   - [PASS/FAIL] #3: predict-diff.sh + Monday cron
   - [PASS/FAIL] #4: CONTRIBUTING.md policy locks (verbatim 2026-05-30)
   - [PASS/FAIL] #5: CI 5-job matrix green on first push

5. **Open items / Phase 1 handoff:**
   - Phase 1 fills `shared/golden-vectors.json` with Gatheral-paper test cases (MATH-01)
   - Phase 1 wires the actual three-runtime parity check in ci.yml `parity` job (MATH-05)
   - Phase 1 Day-1 spike resolves RESEARCH Open Question #2 (DeepBookV3 fork path structure for `OracleSVIUpdated` event)
   - Phase 4 fills `[hosting]` URLs in config + README
   - Phase 5 fills mainnet `[deepvault]` deploy outputs in config/mainnet.toml
   - Branch protection status (set / deferred) noted from Plan 07 Task 4 reply

6. **Time consumed:** budget was 2 days (Day 1-2 of 39); actual = ?
  </action>
  <verify>
    <automated>test -f .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md && grep -q "SETUP-01" .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md && grep -q "SETUP-08" .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md && grep -q "D-01" .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md && grep -q "D-16" .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md && grep -q "Phase 0 success criteria\|success criterion" .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/00-setup-ground-rules/00-08-SUMMARY.md exists
    - Contains all 8 SETUP-XX requirement closure markers
    - Contains all 16 D-XX decision attributions
    - Contains 5-criterion ROADMAP success cross-check (each marked PASS or FAIL)
    - Lists open items for Phase 1 handoff
  </acceptance_criteria>
  <done>Phase 0 closes with traceability matrix (every requirement and decision attributed); Phase 1 handoff is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fresh clone -> reproducible build | Phase 0 closure depends on this; if make install fails on a clean clone, Phase 0 is not complete |
| Build log -> ground-truth schedule signal | "Slack remaining: M days" is the early-warning for cuts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-34 | Tampering | Build log retroactively edited | mitigate | CONTRIBUTING.md "build log discipline" + per-plan SUMMARY captures Week 1 baseline |
| T-00-35 | Repudiation | Phase 0 declared complete with hidden gaps | mitigate | Task 2 5-criterion cross-check on fresh clone produces verifiable PASS/FAIL output |
| T-00-36 | Information Disclosure | README references hosting URLs that turn into open targets | accept | TBD until Phase 4; default subdomains per D-13/D-16 are hackathon-correct |
</threat_model>

<verification>
Final Phase 0 verification:
- README.md polished (cold-readable in 60 seconds)
- Fresh clone + 4 make commands all exit 0 (Task 2)
- 5 ROADMAP success criteria all PASS
- 8 SETUP-XX requirements all closed
- 16 D-XX decisions all attributed
- Build log Week 1 entry committed
- CI green on main; branch protection set (Plan 07 Task 4)
</verification>

<success_criteria>
- All 8 SETUP-XX requirements closed
- All 16 D-XX decisions reflected in code/docs
- All 5 ROADMAP Phase 0 success criteria PASS
- Phase 0 budget (2 days = Day 1-2 of 39) on track
- Phase 1 handoff is clean: known unknowns documented (Phase 1 spike on Open Question #2; Phase 4 hosting URLs; Phase 5 mainnet deploy outputs)
</success_criteria>

<output>
This plan's task 3 IS the Phase 0 closure SUMMARY (.planning/phases/00-setup-ground-rules/00-08-SUMMARY.md).

After Plan 08 completes:
- Update STATE.md: progress = 1/7 phases complete; current focus moves to Phase 1.
- Update ROADMAP.md: mark Phase 0 checkbox `[x]` (`- [x] **Phase 0: Setup & Ground Rules** ...`).
- Run `/gsd-discuss-phase 1` (or equivalent) to gather context for Phase 1 (SVI math foundation).
</output>
