---
phase: 01-math-foundation-svi-parity-gate
plan: 09
type: execute
wave: 7
depends_on:
  - "01-04"
  - "01-05"
  - "01-08"
files_modified:
  - contracts/tests/svi_view_test.move
  - scripts/golden_emit.py
autonomous: true
gap_closure: true
requirements:
  - MATH-02
  - MATH-04
tags:
  - svi
  - move
  - arb-violating
  - gap-closure
  - cr-01
  - phase-1
  - wave-7

must_haves:
  truths:
    - "MATH-02 / Success Criterion #2 — `svi_view::*` aborts when fed each of the 10 arb-violating Tier-B golden vectors (B-arb-091..B-arb-100), proven by per-row `#[test, expected_failure(...)]` tests."
    - "MATH-04 / Success Criterion #4 — Move-side rejection contract (D-04) is exercised against ALL 10 generated arb-violating vectors, not just one hand-crafted (a=0, b=0) sample; downstream Phase 2 vault.rebalance can rely on this contract."
    - "scripts/golden_emit.py emits a defensive assertion (WR-02) that the count of arb-violating vectors with `params_valid=false` matches the count of arb-violating vectors generated, so future evaluator changes that silently flip a row to valid trigger an emit-time failure."
    - "All 311 TS tests + 61 Python tests + the 5 surviving Move tests in svi_view_test.move (golden_vectors_total_variance_all_pass, golden_vectors_binary_price_all_pass, zero_a_zero_b_aborts_zero_variance, zero_forward_aborts, k_out_of_range_aborts) continue to pass after the new tests are added (no regressions). The 6th pre-existing test (golden_vectors_arb_violating_all_reject) is DELETED — it violated the vendored DeepBook code-review rule by counting data without exercising svi_view::*."
    - "Plan 01-09 closes CR-01 from 01-REVIEW.md and the `partial` truth in 01-VERIFICATION.md frontmatter `gaps:`; re-running `/gsd-verify-work 1` should produce status `verified` with score 5/5 (or note the human-verification CI gates as before, but with no `partial` truths and no BLOCKER anti-patterns)."
  artifacts:
    - path: "contracts/tests/svi_view_test.move"
      provides: "10 new per-row `#[test, expected_failure]` functions, one per arb-violating golden vector (B-arb-091..B-arb-100); each pulls its row from golden_vectors_data and feeds it to svi_view::*"
      contains: "arb_violating_091_aborts"
    - path: "scripts/golden_emit.py"
      provides: "Emit-time defensive assertion (per WR-02) that arb-violating count with params_valid=false matches len(arb_violating)"
      contains: "len(arb_violating)"
  key_links:
    - from: "contracts/tests/svi_view_test.move (new per-row tests)"
      to: "contracts/tests/golden_vectors_data.move"
      via: "golden_vectors_data::all_inputs() indexed at the row offset for each B-arb-09{1..9}/B-arb-100 vector"
      pattern: "golden_vectors_data::all_inputs"
    - from: "contracts/tests/svi_view_test.move (new per-row tests)"
      to: "contracts/sources/svi_view.move"
      via: "Per-row svi_view::binary_price_from_params(...) or total_variance_from_params(...) call inside #[expected_failure(abort_code = svi_view::EZeroVariance)] test"
      pattern: "expected_failure(abort_code = svi_view::"
    - from: "scripts/golden_emit.py (defensive assertion)"
      to: "the in-memory `arb_violating` list / Tier-B vectors sub-list"
      via: "assert sum(1 for v in vectors if v['source'].endswith('arb-violating') if not v['expected']['params_valid']) == len(arb_violating)"
      pattern: "len(arb_violating)"
---

<objective>
Close CR-01 (BLOCKER) from `.planning/phases/01-math-foundation-svi-parity-gate/01-REVIEW.md` and the single `partial` truth in `01-VERIFICATION.md` frontmatter `gaps:` block.

The verified gap: `contracts/tests/svi_view_test.move:102-122` (`golden_vectors_arb_violating_all_reject`) loops the 10 arb-violating Tier-B golden vectors (B-arb-091..B-arb-100) only to count how many have `params_valid=false`, asserting `invalid_count == 10`. It NEVER calls `svi_view::total_variance_from_params` or `svi_view::binary_price_from_params` on those rows. Per the vendored DeepBook code-review rule (`scripts/deepbookv3/.claude/rules/code-review.md`): **"Every generated test vector must be exercised against the contract. If generated data isn't passed to the function under test, delete it."**

The current single rejection test (`zero_a_zero_b_aborts_zero_variance`) only proves ONE hand-crafted (a=0, b=0) call aborts — it does NOT loop the 10 generated arb-violating vectors. If `golden_emit.py` ever produces `params_valid=false` rows whose params do NOT trigger an abort, the Move test would silently pass while on-chain returned garbage. This weakens MATH-02 invalid-vector rejection coverage and MATH-04 Success Criterion #4 Move-side completeness for arb-violating slices. D-04 (CONTEXT.md) — the Move evaluator's hard-rejection contract that Phase 2 `vault.rebalance` depends on — relies on `svi_view::*` aborting on EVERY arb-violating golden vector, not just one hand-crafted sample.

Phase 1 was otherwise verified at 4/5 must-haves with 1 partial. Plans 01-01 through 01-08 are committed; this plan does NOT modify them.

Defense-in-depth fix: land BOTH (a) Move per-row `expected_failure` tests for B-arb-091..B-arb-100 (PRIMARY — closes the trust-boundary gap on the Move side), and (b) a Python defensive emit-time assertion in `scripts/golden_emit.py` (SECONDARY — closes the loop on the Python side and reduces the risk of a future evaluator change silently flipping a row to `params_valid=true` without test signal).

The fix is small (~30 lines of Move test code, mechanical) per `01-VERIFICATION.md` Gaps Summary. Two tasks. No architectural change. No re-run of any prior plan needed.

Output: 10 new per-row Move rejection tests + 1 Python emit-time assertion + this plan's SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-CONTEXT.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-RESEARCH.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-PATTERNS.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-VERIFICATION.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-REVIEW.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-05-SUMMARY.md
@.planning/phases/01-math-foundation-svi-parity-gate/01-08-SUMMARY.md
@contracts/tests/svi_view_test.move
@contracts/tests/golden_vectors_data.move
@contracts/sources/svi_view.move
@scripts/golden_emit.py
@scripts/deepbookv3/.claude/rules/code-review.md
@CLAUDE.md

<interfaces>
<!-- From contracts/sources/svi_view.move (the function under test): -->
- public fun binary_price_from_params(a: u64, b: u64, rho: I64, m: I64, sigma: u64, forward: u64, strike: u64): u64
- public fun total_variance_from_params(a: u64, b: u64, rho: I64, m: I64, sigma: u64, k: &I64): u64
- Error codes (verified via Read tool):
    EZeroForward: u64 = 3;
    ECannotBeNegative: u64 = 4;
    EZeroVariance: u64 = 5;
    EParamOutOfRange: u64 = 6;
    EKOutOfRange: u64 = 7;
    EZeroStrike: u64 = 8;

<!-- From contracts/tests/golden_vectors_data.move (the data table): -->
- public fun vector_count(): u64 { 141 }
- public fun all_inputs(): vector<vector<u64>>      // each row is [a, b, rho_mag, rho_neg, m_mag, m_neg, sigma, k_mag, k_neg, forward, strike]
- public fun all_params_valid(): vector<bool>
- The 10 arb-violating vectors are at consecutive indices in the middle of the data table at zero-indexed array offsets **111..120** (NOT 131..140 — those are C2 stubs). File-line correspondence: array offsets 111..120 are at file lines 126..135 of golden_vectors_data.move. All 10 rows have `a=0` and `b=0` per the source data; this triggers the EZeroVariance abort path documented in svi_view.move:104.

<!-- From contracts/tests/svi_view_test.move (existing tests; structure to mirror): -->
- existing helper: i64::from_parts(magnitude: u64, is_neg: bool) -> I64
- existing rejection test pattern: `#[test, expected_failure(abort_code = svi_view::EZeroVariance)] fun zero_a_zero_b_aborts_zero_variance() { ... let _ = svi_view::binary_price_from_params(...); abort 999 }`
- existing module decl: `#[test_only] module deepvault::svi_view_tests;`

<!-- From scripts/golden_emit.py (the loop to wrap): -->
- The arb-violating list is named `arb_violating` and lives inside `def tier_b_vectors() -> list[dict]:` at lines 167-178.
- Loop body lines 179-203 appends one entry per arb-violating tuple to `vectors` (the function-local accumulator).
- After line 203 (loop end) and before line 205 (`return vectors`) is where the defensive assertion lands.
- `vectors` is the function-local list; `v['source']` is `"synthetic-arb-violating"` for these rows; `v['expected']['params_valid']` is `False` for all 10.
</interfaces>

<gap_evidence>
<!-- From 01-VERIFICATION.md frontmatter, gaps[0]: -->
status: partial
truth: "The Move deepvault::svi_view evaluator hard-rejects invalid params (D-04) such that arb-violating golden vectors are demonstrably exercised against svi_view::* in the Move test suite."
artifacts: contracts/tests/svi_view_test.move
issue: "golden_vectors_arb_violating_all_reject loops the data table only to count invalid rows; does not call svi_view::* on any of them. The assertion is `invalid_count == 10` not a per-row evaluator-aborts assertion."

<!-- From scripts/deepbookv3/.claude/rules/code-review.md (governing rule): -->
"Every generated test vector must be exercised against the contract. If generated data isn't passed to the function under test, delete it."

<!-- From 01-VERIFICATION.md gaps[0].missing[]: -->
1. "Add per-row #[test, expected_failure(abort_code = svi_view::EZeroVariance)] tests for each of the 10 arb-violating golden vectors (B-arb-091..B-arb-100) — minimum 10 separate test functions, each pulling its specific row from golden_vectors_data and feeding it to svi_view::binary_price_from_params or total_variance_from_params and asserting the documented abort code."
2. "Alternatively, add a defensive emit-time assertion in scripts/golden_emit.py per WR-02: after the arb-violating loop, assert `sum(1 for v in vectors if v['source'].endswith('arb-violating') if not v['expected']['params_valid']) == len(arb_violating)` — this closes the loop on the Python side and reduces (but does not eliminate) the Move-side coverage gap."

This plan lands BOTH for defense-in-depth.
</gap_evidence>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add per-row arb-violating rejection tests to svi_view_test.move + Python defensive assertion to golden_emit.py</name>
  <files>contracts/tests/svi_view_test.move, scripts/golden_emit.py</files>
  <read_first>
    - contracts/tests/svi_view_test.move (the file containing the broken `golden_vectors_arb_violating_all_reject` test at lines 102-122; mirror the existing per-row pattern in `zero_a_zero_b_aborts_zero_variance` at lines 126-143)
    - contracts/tests/golden_vectors_data.move (the data table — the 10 arb-violating rows are at file lines 126-135 corresponding to ZERO-INDEXED ARRAY OFFSETS 111..120; B-arb-091 is offset 111, B-arb-100 is offset 120. Layout: A-01..A-21 fill offsets 0..20, B-001..B-090 fill offsets 21..110, B-arb-091..B-arb-100 fill offsets 111..120, C-01..C-10 fill offsets 121..130, C2-01..C2-10 fill offsets 131..140. Cross-check by re-reading the inline `// B-arb-NNN` comments at file lines 126-135 of golden_vectors_data.move before writing the tests; do NOT confuse file line numbers with array offsets)
    - contracts/sources/svi_view.move (confirm the abort code names and values: EZeroForward=3, ECannotBeNegative=4, EZeroVariance=5, EParamOutOfRange=6, EKOutOfRange=7, EZeroStrike=8; for the arb-violating rows the EXPECTED abort is EZeroVariance because all 10 rows have a=0, b=0 → total_var = a + math::mul_div_round_down(b, magnitude, F) = 0 + 0 = 0 → `assert!(total_var > 0, EZeroVariance)` at svi_view.move:104 fires)
    - scripts/golden_emit.py lines 160-205 (the `arb_violating` list and loop inside `tier_b_vectors()`)
    - .planning/phases/01-math-foundation-svi-parity-gate/01-VERIFICATION.md frontmatter `gaps[0].missing` (verbatim assertion text)
    - scripts/deepbookv3/.claude/rules/code-review.md (the governing rule that drives CR-01's BLOCKER classification)
  </read_first>
  <action>
**Step A — Move per-row tests (PRIMARY).**

In `contracts/tests/svi_view_test.move`:

1. **DELETE** the existing `golden_vectors_arb_violating_all_reject` test (lines 102-122) — this is the test that violates the vendored code-review rule per CR-01. Per the rule: "If generated data isn't passed to the function under test, delete it."

2. **ADD** 10 new per-row `#[test, expected_failure(abort_code = svi_view::EZeroVariance)]` test functions, one per arb-violating golden vector (B-arb-091 through B-arb-100). Each test:
   - Pulls its specific row from `golden_vectors_data::all_inputs()` at the appropriate row index.
   - Unpacks the row using the same field layout as `golden_vectors_total_variance_all_pass` (svi_view_test.move:42-54): `[a, b, rho_mag, rho_neg, m_mag, m_neg, sigma, k_mag, k_neg, forward, strike]`.
   - Calls `svi_view::binary_price_from_params(a, b, rho, m, sigma, forward, strike)` (the same callsite as the existing `zero_a_zero_b_aborts_zero_variance` test at line 133).
   - Includes a trailing `abort 999` to satisfy the Move test framework's expected-failure pattern (matches the existing reference at svi_view_test.move:142).

3. **Row indices (zero-indexed array offsets, NOT file line numbers).** The 141-vector table layout is: A-01..A-21 → offsets 0..20, B-001..B-090 → offsets 21..110, **B-arb-091..B-arb-100 → offsets 111..120** (these are the rows you want), C-01..C-10 → offsets 121..130, C2-01..C2-10 → offsets 131..140. So: offset 111 = B-arb-091, offset 112 = B-arb-092, ..., offset 120 = B-arb-100. **VERIFY** this by reading file lines 126-135 of `contracts/tests/golden_vectors_data.move` (the inline `// B-arb-NNN` comments label each row by id) BEFORE writing the tests. **DO NOT** use offsets 131..140 — those rows are C2 (PredictTests-stub) vectors with non-zero a/b that would NOT abort with `EZeroVariance` and would cause every per-row `expected_failure` test to fail. If the row indices you read in the file differ from offsets 111..120, use the indices you find in the file.

4. **Naming convention.** Use `arb_violating_NNN_aborts_when_passed_to_svi_view` (e.g. `arb_violating_091_aborts_when_passed_to_svi_view`). 10 functions total.

5. **Abort code per row.** All 10 rows have `a=0, b=0` (verified in golden_vectors_data.move lines 126-135 — every row begins with `vector[0, 0, ...]`). With a=0 and b=0:
   - `total_variance_from_params` returns `a + math::mul_div_round_down(b, magnitude, F)` = `0 + 0` = `0`.
   - `binary_price_from_params` calls `binary_price_from_k`, which at svi_view.move:104 does `assert!(total_var > 0, EZeroVariance)` — this fires.
   - Therefore the expected abort code for ALL 10 rows is `svi_view::EZeroVariance`.

6. **Template for each test (substitute the row index N where N is the zero-indexed array offset 111..120 — NOT 131..140 — and NNN is the vector id 091..100; specifically: arb_violating_091 → N=111, arb_violating_092 → N=112, ..., arb_violating_100 → N=120):**

```move
/// Per-row rejection coverage for B-arb-NNN (closes CR-01 from 01-REVIEW.md).
/// Per scripts/deepbookv3/.claude/rules/code-review.md: every generated test
/// vector must be exercised against the contract. The data row's a=0, b=0
/// triggers EZeroVariance via assert!(total_var > 0, ...) at svi_view.move:104.
#[test, expected_failure(abort_code = svi_view::EZeroVariance)]
fun arb_violating_NNN_aborts_when_passed_to_svi_view() {
    let inputs = golden_vectors_data::all_inputs();
    let row = inputs[N];
    let a = row[0];
    let b = row[1];
    let rho = i64::from_parts(row[2], row[3] == 1);
    let m = i64::from_parts(row[4], row[5] == 1);
    let sigma = row[6];
    let forward = row[9];
    let strike = row[10];
    let _ = svi_view::binary_price_from_params(a, b, rho, m, sigma, forward, strike);
    abort 999
}
```

7. **Top-of-section comment.** Replace the deleted `golden_vectors_arb_violating_all_reject` block with a one-paragraph block-comment explaining that the per-row tests below close CR-01 by exercising each generated arb-violating row against `svi_view::*` per the vendored code-review rule, and noting that all 10 rows hit `EZeroVariance` because their (a=0, b=0) construction in `golden_emit.py:167-178` is a deliberate choice to land on the only reachable rejection path documented in Plan 01-03/01-04 mathematical analysis (ECannotBeNegative is unreachable for sigma > 0; EZeroVariance via a=0+b=0 is the reachable path).

**Step B — Python defensive emit-time assertion (SECONDARY, complementary).**

In `scripts/golden_emit.py`, inside `def tier_b_vectors()`:

After the arb-violating loop terminates (current line 203, immediately before `return vectors` at line 205), add a defensive assertion that closes the WR-02 loop:

```python
    # WR-02 / Plan 01-09 defensive assertion: every entry pushed by the
    # arb_violating loop above MUST have params_valid=False. If a future
    # evaluator change silently flips a row to True (e.g. by altering the
    # a=0, b=0 -> EZeroVariance contract), this assertion fires at emit time
    # and prevents the regression from reaching CI's parity job. Mirrors the
    # Move-side per-row #[expected_failure] tests in svi_view_test.move that
    # close CR-01 from 01-REVIEW.md.
    arb_violating_emitted = sum(
        1 for v in vectors
        if v['source'].endswith('arb-violating')
        and not v['expected']['params_valid']
    )
    assert arb_violating_emitted == len(arb_violating), (
        f"WR-02 / Plan 01-09: arb-violating count mismatch. "
        f"Expected {len(arb_violating)} entries with params_valid=False; "
        f"found {arb_violating_emitted}. A row may have silently passed validation."
    )
```

The assertion text is taken VERBATIM from `01-VERIFICATION.md` frontmatter `gaps[0].missing[1]` (with variable names adapted to match the actual function-local list `vectors` and the actual `source` field values used by `_make_vector_explicit`). DO NOT paraphrase the structural form `sum(1 for v in vectors if v['source'].endswith('arb-violating') if not v['expected']['params_valid']) == len(arb_violating)` — keep the comprehension shape and the equality check identical so the assertion is recognizable as the documented WR-02 remediation.

**Step C — Run the full test suite to confirm no regressions.**

Run from repo root:
1. `cd backtest && uv run python ../scripts/golden_emit.py` — must complete without raising the new assertion (i.e., the current 10 arb-violating rows still produce `params_valid=False`).
2. `cd backtest && uv run python ../scripts/golden_emit.py --check` — must exit 0 (no diff regenerated, the file is bit-identical because we only added an assertion, not changed any output).
3. `cd backtest && uv run pytest` — must report 61 passed (no regression from Plan 01-08 baseline).
4. `cd dashboard && pnpm test` — must report 311 passed (no regression).
5. `cd backtest && uv run python -m deepvault.parity_runner` — must report `PARITY OK: 141 vectors pass within tolerance <= 1.`
6. `cd dashboard && pnpm exec tsx src/lib/parity_runner.ts` — must report `PARITY OK: 141 vectors pass within tolerance <= 1.`

Do NOT attempt to run `sui move test` locally — Sui CLI is unavailable in the execution environment per Plan 01-05 and Plan 01-07 SUMMARYs. The first CI run after this commit is the empirical confirmation of the Move-side tests; this is a documented-conditional gate, not a freshly discovered uncertainty (called out in 01-VERIFICATION.md `human_verification[0]`).

**Commit prefix.** Use `MATH(01-09): close CR-01 — per-row arb-violating Move tests + WR-02 emit assertion` per CONTRIBUTING.md §6 (this is a parity-affecting change because it strengthens the Move-side rejection contract that Phase 2 vault.rebalance depends on).
  </action>
  <verify>
    <automated>cd backtest && uv run python ../scripts/golden_emit.py --check && uv run pytest -q && cd ../dashboard && pnpm test && cd .. && grep -c "expected_failure(abort_code = svi_view::" contracts/tests/svi_view_test.move | grep -qE "^1[2-9]$|^[2-9][0-9]+$" && grep -c "arb_violating_[0-9]\{3\}_aborts_when_passed_to_svi_view" contracts/tests/svi_view_test.move | grep -qE "^10$" && grep -q "len(arb_violating)" scripts/golden_emit.py && ! grep -q "fun golden_vectors_arb_violating_all_reject" contracts/tests/svi_view_test.move</automated>
  </verify>
  <done>
    - 10 new `#[test, expected_failure(abort_code = svi_view::EZeroVariance)]` test functions exist in `contracts/tests/svi_view_test.move`, named `arb_violating_091_aborts_when_passed_to_svi_view` through `arb_violating_100_aborts_when_passed_to_svi_view`.
    - The old `golden_vectors_arb_violating_all_reject` test (the one that only counted, never called `svi_view::*`) is DELETED.
    - The pre-existing `zero_a_zero_b_aborts_zero_variance`, `zero_forward_aborts`, and `k_out_of_range_aborts` tests are preserved unchanged.
    - `scripts/golden_emit.py` contains a new defensive `assert ... == len(arb_violating)` after the arb-violating loop and before `return vectors`.
    - `cd backtest && uv run python ../scripts/golden_emit.py --check` exits 0 (file is bit-identical).
    - `cd backtest && uv run pytest` reports 61 passed (no regression).
    - `cd dashboard && pnpm test` reports 311 passed (no regression).
    - Both parity_runners report `PARITY OK: 141 vectors pass within tolerance <= 1.`
  </done>
</task>

<task type="auto">
  <name>Task 2: Re-run full Phase 1 test suites + write 01-09-SUMMARY.md declaring CR-01 closed</name>
  <files>.planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md</files>
  <read_first>
    - .planning/phases/01-math-foundation-svi-parity-gate/01-08-SUMMARY.md (mirror frontmatter shape and section structure — this plan is also gap-closure-style; minimal scope, mechanical change)
    - .planning/phases/01-math-foundation-svi-parity-gate/01-VERIFICATION.md (the gap source — quote the `partial` truth being closed and the missing[] paths landed)
    - .planning/phases/01-math-foundation-svi-parity-gate/01-REVIEW.md (CR-01 details)
    - $HOME/.claude/get-shit-done/templates/summary.md (the canonical SUMMARY template shape)
  </read_first>
  <action>
**Step A — Re-run all parity-bound test suites a final time** to confirm Task 1's changes are durable across the full Phase 1 surface (not just the new arb-violating tests). Capture the exit codes / pass counts in the SUMMARY:

```
cd backtest && uv run pytest                              # expect: 61 passed
cd backtest && uv run python -m deepvault.parity_runner   # expect: PARITY OK: 141 vectors
cd backtest && uv run python ../scripts/golden_emit.py --check   # expect: exit 0
cd backtest && uv run python ../scripts/codegen.py --check       # expect: exit 0
cd dashboard && pnpm test                                 # expect: 311 passed
cd dashboard && pnpm exec tsx src/lib/parity_runner.ts    # expect: PARITY OK: 141 vectors
```

Forbidden-token grep on parity-bound TS evaluator files (regression guard from Plan 01-07):

```
grep -nE "Number\(|parseFloat\(|Math\.(sqrt|exp|log|pow)\(" dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts
```

Expected: exit 1 (no matches). If exit 0, a regression has been introduced — STOP and investigate; do not proceed with the SUMMARY.

**Step B — Write `.planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md`** following the shape of `01-08-SUMMARY.md`. Required sections / frontmatter content:

1. **Frontmatter:** `phase`, `plan: 09`, `subsystem: math-foundation`, `tags: [svi, move, arb-violating, gap-closure, cr-01, phase-1, wave-7]`. Dependency graph: `requires: [01-04 (golden_vectors_data + arb-violating sub-tier source), 01-05 (svi_view.move + existing test scaffold + EZeroVariance abort code), 01-08 (Plan 08 finalized min_g_k via arb_checker.check_arb)]`. Provides: `[contracts/tests/svi_view_test.move (10 new per-row tests + 1 deleted misuse-test), scripts/golden_emit.py (WR-02 defensive assertion)]`. `affects: [phase-2-vault-rebalance (Move-side D-04 rejection contract now empirically exercised on every generated arb-violating row, not just one hand-crafted sample)]`. `requirements-completed: [MATH-02, MATH-04]` (data-coverage strengthening; both were CONDITIONAL/PARTIAL after 01-08 and are now FULLY SATISFIED-CONDITIONAL on first CI run).

2. **Body sections:** "What CR-01 was", "What was landed (Move side)", "What was landed (Python side)", "Why both — defense in depth", "Test pass counts (before/after)", "Commit hash", "Self-Check: PASSED", "Concerns / flags forwarded to STATE.md".

3. **Concerns section MUST explicitly state:**
   - **Sui CLI unavailable locally — first CI run after this commit is the empirical confirmation gate** for the 10 new per-row `#[expected_failure(abort_code = svi_view::EZeroVariance)]` tests + the existing `golden_vectors_total_variance_all_pass` + `golden_vectors_binary_price_all_pass` + `zero_a_zero_b_aborts_zero_variance` + `zero_forward_aborts` + `k_out_of_range_aborts` tests (15 tests total). This is a documented-conditional gate (per Plan 01-05 and Plan 01-07 SUMMARYs and `01-VERIFICATION.md` `human_verification[0]`), NOT a freshly discovered uncertainty. The user observes the first CI run; expected outcome is `sui move test --filter golden_vectors` exits 0.
   - **Forbidden-token grep CI step — first CI run is the empirical confirmation** that `dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts` remain clean (per `01-VERIFICATION.md` `human_verification[1]`). Local equivalent already exits 1 (clean). This is a documented-conditional gate, NOT a freshly discovered uncertainty.

4. **DO NOT update `01-VERIFICATION.md`** — the verifier owns that file; the SUMMARY just records what was done. The next `/gsd-verify-work 1` invocation will refresh `01-VERIFICATION.md` from the new ground truth.

5. **DO NOT mark this plan as introducing new requirements** — `requirements-completed: [MATH-02, MATH-04]` is a strengthening of pre-existing satisfaction (PARTIAL → FULL on the data-coverage axis), not a new satisfaction. Phase 1 closure metrics are unchanged: 6/6 MATH-XX requirements remain satisfied.

**Step C — Self-check before declaring done:**

Verify the following invariants by inspection:
- All 10 per-row Move tests are present and named consistently (`arb_violating_NNN_aborts_when_passed_to_svi_view` for NNN ∈ {091..100}).
- Each test calls `svi_view::binary_price_from_params(...)` (NOT just `total_variance_from_params(...)` alone — `binary_price_from_params` calls into `binary_price_from_k` which transitively calls `total_variance_from_params` AND then asserts `total_var > 0, EZeroVariance` — this is the rejection path with the documented abort code).
- Each test pulls its row from `golden_vectors_data::all_inputs()` (NOT hand-crafted; the entire point of CR-01 is that generated data must be exercised).
- The Python `assert ... == len(arb_violating)` uses the structural shape from VERIFICATION.md missing[1], not a paraphrase.
- The deleted `golden_vectors_arb_violating_all_reject` test is genuinely removed (not just commented out).
- `01-09-SUMMARY.md` exists with the required frontmatter + sections + Concerns block forwarding the two documented-conditional CI gates.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md && grep -q "CR-01" .planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md && grep -q "Sui CLI" .planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md && cd backtest && uv run pytest -q && cd ../dashboard && pnpm test</automated>
  </verify>
  <done>
    - `.planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md` exists with the documented frontmatter (phase/plan/subsystem/tags/requires/provides/affects/requirements-completed/duration/completed) and body sections.
    - SUMMARY's Concerns block explicitly forwards the two documented-conditional CI gates (Sui CLI Move test confirmation + forbidden-token grep) per `01-VERIFICATION.md` `human_verification[0..1]`.
    - All Phase 1 test suites green: backtest pytest 61, dashboard vitest 311, both parity_runners report PARITY OK 141 vectors, golden_emit drift exits 0, codegen drift exits 0, forbidden-token grep exits 1 (no matches).
    - Self-check confirms all 10 per-row Move tests are present + the WR-02 Python assertion is present + the misuse-test is deleted.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated golden-vector data → Move test execution | The 10 arb-violating Tier-B rows in `golden_vectors_data.move` MUST be exercised against `svi_view::*` (per the vendored code-review rule). CR-01 was the violation of this trust boundary; this plan closes it. |
| Python golden-vector emitter → on-chain rejection contract (D-04) | `golden_emit.py` constructs the 10 arb-violating rows with `a=0, b=0` to deliberately land on the EZeroVariance abort path. WR-02 was the absence of an emit-time assertion that this construction continues to produce `params_valid=False`. This plan adds the assertion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-45 | Tampering | `contracts/tests/svi_view_test.move` (per-row tests must actually call `svi_view::*`) | mitigate | Per-row tests use `let _ = svi_view::binary_price_from_params(...)` with explicit `let _ =` capture — this guarantees the call is not optimized out and the abort triggers the `expected_failure` matcher. The trailing `abort 999` is the convention from the existing `zero_a_zero_b_aborts_zero_variance` test (svi_view_test.move:142). |
| T-01-46 | Tampering | `scripts/golden_emit.py` arb-violating loop (future evaluator change might silently flip a row to params_valid=True) | mitigate | The new defensive `assert sum(...) == len(arb_violating)` fires at emit time, BEFORE the JSON or Move companion is written, BEFORE CI's parity job runs. Triggers a hard failure rather than a silent bypass. |
| T-01-47 | Spoofing | New per-row test names accidentally match an existing test name | mitigate | Naming convention `arb_violating_NNN_aborts_when_passed_to_svi_view` is unique relative to existing tests (verified: existing tests are `golden_vectors_total_variance_all_pass`, `golden_vectors_binary_price_all_pass`, `zero_a_zero_b_aborts_zero_variance`, `zero_forward_aborts`, `k_out_of_range_aborts`). |
| T-01-48 | Information Disclosure | n/a | accept | No PII; tests are public. |
| T-01-49 | DoS | 10 additional Move tests slow CI | accept | Each test runs in microseconds (single function call + Move VM abort). 10 additional tests are insignificant against the existing parity-loop test that iterates 131 valid vectors. |
| T-01-50 | Repudiation | Plan 01-09 SUMMARY does not record the WR-02 assertion + per-row tests landed | mitigate | Task 2 explicitly requires the SUMMARY to enumerate the 10 test names + the Python assertion + cite the governing rule (`scripts/deepbookv3/.claude/rules/code-review.md`). |

`security_block_on: high` clears — no HIGH-severity threats. T-01-45 (tests must actually call the function) is the headline mitigation; the per-row pattern with explicit `let _ = svi_view::*` capture is the defensive shape.
</threat_model>

<verification>
- [ ] `cd backtest && uv run python ../scripts/golden_emit.py --check` exits 0 (no diff regenerated; emitter changes are assertion-only).
- [ ] `cd backtest && uv run pytest` reports 61 passed (no regression from Plan 01-08).
- [ ] `cd dashboard && pnpm test` reports 311 passed (no regression).
- [ ] `cd backtest && uv run python -m deepvault.parity_runner` reports `PARITY OK: 141 vectors`.
- [ ] `cd dashboard && pnpm exec tsx src/lib/parity_runner.ts` reports `PARITY OK: 141 vectors`.
- [ ] `grep -c "expected_failure(abort_code = svi_view::" contracts/tests/svi_view_test.move` returns ≥ 12 (the 3 pre-existing rejection tests + 10 new per-row tests = ≥ 13; allow ≥ 12 to tolerate any minor refactor inside the existing reject tests).
- [ ] `grep -c "arb_violating_[0-9]\{3\}_aborts_when_passed_to_svi_view" contracts/tests/svi_view_test.move` returns exactly 10.
- [ ] `! grep -q "fun golden_vectors_arb_violating_all_reject" contracts/tests/svi_view_test.move` (the misuse-test is deleted).
- [ ] `grep -q "len(arb_violating)" scripts/golden_emit.py` (the WR-02 assertion is present).
- [ ] `01-09-SUMMARY.md` exists and contains the words "CR-01" and "Sui CLI" (the two documented-conditional CI gates).
- [ ] Forbidden-token grep on `dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts` exits 1 (no matches; regression guard from Plan 01-07).
</verification>

<success_criteria>
Plan 01-09 is complete when:

1. **Move-side coverage gap closed (PRIMARY).** `contracts/tests/svi_view_test.move` exercises EVERY one of the 10 generated arb-violating Tier-B golden vectors (B-arb-091..B-arb-100) against `svi_view::*` via per-row `#[test, expected_failure(abort_code = svi_view::EZeroVariance)]` tests. The misuse-test (`golden_vectors_arb_violating_all_reject`) that violated the vendored code-review rule is DELETED.

2. **Python-side defensive assertion landed (SECONDARY).** `scripts/golden_emit.py` contains an emit-time `assert sum(1 for v in vectors if v['source'].endswith('arb-violating') if not v['expected']['params_valid']) == len(arb_violating)` — closing WR-02. Future evaluator changes that silently flip a row to `params_valid=True` will trigger an emit-time failure.

3. **No regressions.** All Phase 1 test suites green: backtest pytest 61 passed, dashboard vitest 311 passed, both parity_runners report PARITY OK 141 vectors, golden_emit drift + codegen drift both exit 0, forbidden-token grep on parity-bound TS files exits 1 (no matches).

4. **CR-01 closed.** Re-running `/gsd-verify-work 1` should produce status `verified` with score 5/5 must-haves (no `partial` truths, no BLOCKER anti-patterns). The two documented-conditional CI gates (Sui CLI Move test confirmation + forbidden-token grep) remain documented but are NOT new gaps — they are pre-existing acknowledged-conditional gates from Plans 01-05 and 01-07 SUMMARYs.

5. **Phase 1 closure stays intact.** All 6 MATH requirements (MATH-01..MATH-06) remain satisfied across 9 plans (Plans 01-01 through 01-09). Phase 2 (vault::rebalance) remains UNBLOCKED, with the additional confidence that the Move-side D-04 rejection contract is now empirically exercised on every generated arb-violating row, not just one hand-crafted sample.

6. **No scope creep.** This plan does NOT touch any of WR-03..WR-11 or IN-01..IN-04 from `01-REVIEW.md` (those are non-blocking and tracked for future cleanup, not in scope for Phase 1 closure). This plan does NOT modify Plans 01-01 through 01-08 source files (other than the explicitly listed `contracts/tests/svi_view_test.move` and `scripts/golden_emit.py`). This plan does NOT introduce any new dependency, library, runtime, or schema.
</success_criteria>

<output>
After completion, create `.planning/phases/01-math-foundation-svi-parity-gate/01-09-SUMMARY.md`.

Records: list of 10 new Move per-row tests + the WR-02 Python assertion text + the deleted misuse-test name + before/after test counts (Move: existing 6 - 1 deleted + 10 new = 15 tests; Python: 61 unchanged; TS: 311 unchanged) + commit hash + the two documented-conditional CI gates forwarded to STATE.md (Sui CLI Move test confirmation + forbidden-token grep) + explicit closure declaration: "CR-01 from 01-REVIEW.md and the `partial` truth in 01-VERIFICATION.md frontmatter `gaps:` are CLOSED."
</output>
