---
phase: 01-math-foundation-svi-parity-gate
verified: 2026-05-09T18:00:00Z
status: gaps_found
score: 4/5 must-haves verified (1 partial)
overrides_applied: 0
re_verification:
  previous_status: not_applicable
  previous_score: not_applicable
gaps:
  - truth: "The Move deepvault::svi_view evaluator hard-rejects invalid params (D-04) such that arb-violating golden vectors are demonstrably exercised against svi_view::* in the Move test suite."
    status: partial
    reason: "CR-01 (BLOCKER from code review): contracts/tests/svi_view_test.move:102-122 (golden_vectors_arb_violating_all_reject) only counts how many params_valid=false rows exist (asserts invalid_count == 10) — it never feeds those rows to svi_view::total_variance_from_params or svi_view::binary_price_from_params. Per scripts/deepbookv3/.claude/rules/code-review.md: 'Every generated test vector must be exercised against the contract. If generated data isn't passed to the function under test, delete it.' The single rejection test (zero_a_zero_b_aborts_zero_variance) only proves ONE hand-crafted (a=0, b=0) call aborts — it does not loop the 10 generated arb-violating Tier-B vectors. If golden_emit.py ever produces params_valid=false rows whose params do NOT trigger an abort, the Move test would silently pass while on-chain returned garbage. This weakens MATH-02's invalid-param rejection coverage and Success Criterion #4's Move-side completeness for arb-violating slices. Note: MATH-02 valid-vector parity (131 valid vectors) and Python+TS arb-violating rejection are FULLY proven; only the Move arb-violating leg is data-coverage-incomplete."
    artifacts:
      - path: "contracts/tests/svi_view_test.move"
        issue: "golden_vectors_arb_violating_all_reject loops the data table only to count invalid rows; does not call svi_view::* on any of them. The assertion is `invalid_count == 10` not a per-row evaluator-aborts assertion."
    missing:
      - "Add per-row #[test, expected_failure(abort_code = svi_view::EZeroVariance)] tests for each of the 10 arb-violating golden vectors (B-arb-091..B-arb-100) — minimum 10 separate test functions, each pulling its specific row from golden_vectors_data and feeding it to svi_view::binary_price_from_params or total_variance_from_params and asserting the documented abort code."
      - "Alternatively, add a defensive emit-time assertion in scripts/golden_emit.py per WR-02: after the arb-violating loop, assert `sum(1 for v in vectors if v['source'].endswith('arb-violating') if not v['expected']['params_valid']) == len(arb_violating)` — this closes the loop on the Python side and reduces (but does not eliminate) the Move-side coverage gap."
      - "Either path requires a follow-up plan to land in Phase 1 closure or as the first action of Phase 2 wiring (svi_view's rejection contract is what vault::rebalance relies on)."
human_verification:
  - test: "First CI run on the parity branch — confirm `sui move test --gas-limit 100000000000 --filter golden_vectors` exits 0 (Move-side parity gate green on real Sui CLI)."
    expected: "Both `golden_vectors_total_variance_all_pass` and `golden_vectors_binary_price_all_pass` pass; the 4 rejection tests (zero_a_zero_b, zero_forward, k_out_of_range, golden_vectors_arb_violating_all_reject) also pass."
    why_human: "Sui CLI is unavailable in the local environment per Plans 01-05/01-07 SUMMARY.md. Move-side parity was verified via static review against the vendored Predict source at SHA 1159d79af33c70e09e406310e1d8f067832ede9d. The first CI run is the empirical confirmation. Listed for completeness — this is a known-conditional gate, NOT a freshly discovered uncertainty."
  - test: "First CI run on the parity branch — confirm the parity job's `Forbidden-token grep on TS evaluator` step exits 0."
    expected: "grep finds no Number(/parseFloat(/Math.{sqrt,exp,log,pow}( in dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts."
    why_human: "Local equivalent grep returns exit 1 (clean), but CI's grep step uses a slightly different shell context. The `arb_checker.ts` is intentionally excluded from this grep (documented in its file header) but the parity-bound 5 files must remain clean."
must_haves:
  truths:
    - "MATH-01 / Success Criterion #1 — Python SSVI evaluator reproduces Gatheral & Jacquier 2014 paper-derived vectors within float tolerance, and the audit script (`uv run pytest` + `uv run python -m deepvault.parity_runner`) shows all 141 vectors pass on a fresh clone."
    - "MATH-02 / Success Criterion #2 — Move `deepvault::svi_view` evaluator produces output identical to Python on the same 141 golden vectors within 1 unit at 1e9 (D-14 re-routed tolerance), executable via `sui move test` (algorithm cloned + tests written; first CI run is the empirical confirmation per `Sui CLI unavailable` documentation in Plan 01-05 SUMMARY)."
    - "MATH-03 / Success Criterion #3 — TypeScript `dashboard/src/lib/svi.ts` evaluator produces output identical to Python on the same vectors, executable via `pnpm test` (Vitest reports 311 passed)."
    - "MATH-04 / Success Criterion #4 — Arbitrage-free checker emits a 200-element g(k) array (not just a boolean) for both runtimes, returns min_g_k < 0 for arb-violating slices and min_g_k >= 0 for Gatheral-paper-valid slices."
    - "MATH-05 / Success Criterion #5 — CI three-way parity gate is wired (.github/workflows/ci.yml `parity` job runs Python parity_runner + TS parity_runner + Move sui move test --filter golden_vectors + forbidden-token grep); job key `parity` preserved per branch-protection invariant."
---

# Phase 1: Math Foundation (SVI Parity Gate) — Verification Report

**Phase Goal:** A single SSVI evaluator algorithm implemented in three runtimes (Move, Python, TypeScript) producing bit-for-bit identical output on a shared golden-vector suite, with a working arbitrage-free checker.

**Verified:** 2026-05-09
**Status:** gaps_found
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth (= ROADMAP Success Criterion)                                                                                                                                                                              | Status                | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Python SSVI evaluator reproduces every published Gatheral & Jacquier 2014 test case within float tolerance — developer reruns audit script and sees "all 100+ vectors PASS" on a fresh clone.                    | VERIFIED              | Locally ran `cd backtest && uv run python -m deepvault.parity_runner` → "PARITY OK: 141 vectors pass within tolerance <= 1." Locally ran `cd backtest && uv run pytest` → "61 passed in 5.92s" (test_isqrt + test_phi_against_scipy + test_svi + test_gatheral_paper_vectors + test_arb_checker). 21 Tier-A vectors all cite Gatheral in the `source` field. The Cody Φ implementation is independently cross-checked against scipy.stats.norm.cdf at 8 reference points within 1e-7. |
| 2   | Move `deepvault::svi_view` evaluator produces output identical to Python on the same 100+ golden vectors within 1 wei (1 unit at 1e9 per re-route D-14), executable via `sui move test`.                         | VERIFIED-CONDITIONAL  | `contracts/sources/svi_view.move` (131 lines), 5 helpers under `contracts/sources/helpers/` (i64/math/isqrt/phi/ln, 467 lines total). `contracts/tests/svi_view_test.move::golden_vectors_total_variance_all_pass` and `::golden_vectors_binary_price_all_pass` loop all 141 vectors; tolerance 1 unit at 1e9. Sui CLI is unavailable locally; CI's parity job runs `sui move test --gas-limit 100000000000 --filter golden_vectors`. Static review against vendored SHA 1159d79a passes. |
| 3   | TypeScript `dashboard/src/lib/svi.ts` evaluator produces output identical to Python on the same vectors, executable via `pnpm test`.                                                                             | VERIFIED              | Locally ran `cd dashboard && pnpm test` → "Test Files 4 passed (4) / Tests 311 passed (311)". Locally ran `cd dashboard && pnpm exec tsx src/lib/parity_runner.ts` → "PARITY OK: 141 vectors pass within tolerance <= 1." Forbidden-token grep on `math.ts/isqrt.ts/phi.ts/ln.ts/svi.ts` returns exit 1 (no matches — bigint-only).                                                                                                                                                  |
| 4   | Arbitrage-free checker visualizes a violating g(k) curve (not just a boolean) when fed an arb-violating SVI slice, AND passes when fed Gatheral-paper-valid slices.                                              | VERIFIED-WITH-WARNING | `arb_checker.py` and `arb_checker.ts` both expose ArbResult / ArbResult with `g_k_array` / `gK` of length 200 (= SVI_GRID_POINTS_FOR_ARB_CHECK). Empirical: extreme-rho slice produces min_g_k=-29_019_236_358 with negative g(k) elements; sane slice (the one tested in test_valid_slice_passes) produces min_g_k=296_558_869 with all-positive g(k). All 21 Tier-A Gatheral-derived vectors have min_g_k >= 0. WARNING: Move-side arb-violating data-coverage is incomplete (CR-01). |
| 5   | CI three-way parity gate is green; any change in any runtime that breaks parity blocks the phase from advancing.                                                                                                 | VERIFIED-CONDITIONAL  | `.github/workflows/ci.yml` parity job (lines 163-258) replaces Phase 0 stub with: forbidden-token grep, Python parity_runner, TS parity_runner, Move `sui move test --filter golden_vectors`, Move full sanity suite, summary. Job key `parity` preserved (line 164). `needs: [move, ts, python, codegen-drift]` preserved (line 166). Awaiting first push to confirm CI green.                                                                                                       |

**Score:** 4/5 fully VERIFIED + 1 PARTIAL (criterion #4 gap is on the Move-side data coverage of the arb-violating sub-tier; the criterion's "visible g(k) curve" delivery and "passes valid slices" both VERIFIED).

### Required Artifacts

| Artifact                                                                  | Expected                                                       | Status     | Details                                                                                                                                                                            |
| ------------------------------------------------------------------------- | -------------------------------------------------------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `shared/svi-spec.md`                                                      | 8-section locked Phase 1 contract; cites SHA 1159d79a          | ✓ VERIFIED | 331 lines; all 8 sections present; SHA cited in 45 files across the repo                                                                                                           |
| `shared/strategy.toml [svi]`                                              | parameterization=raw_svi_5param, scale=9, k bounds, per-param  | ✓ VERIFIED | All 9 fields confirmed via grep                                                                                                                                                    |
| `shared/cody_phi_coefficients.toml`                                       | Cody 1969 coefficients verbatim from helper/math.move:31-65    | ✓ VERIFIED | `schema_version=1`, `upstream_sha`, [small]+[medium] tables                                                                                                                        |
| `shared/golden-vectors.json`                                              | ≥120 vectors: A≥20, B≥80, C≥10, C2≥10, arb-violating sub-tier  | ✓ VERIFIED | 141 vectors total; A=21, B=100 (90 valid + 10 arb-violating), C=10, C2=10; all Tier A cite Gatheral; 131 vectors have populated min_g_k from Plan 01-08 arb_checker pass            |
| `contracts/sources/helpers/{i64,math,isqrt,phi,ln}.move`                  | Line-for-line clones of vendored helpers; SHA cited            | ✓ VERIFIED | 5 files, 467 lines combined; all import deepvault::phi_coefficients (no hand-coded numbers); SHA cited in module docstrings                                                        |
| `contracts/sources/svi_view.move`                                         | binary_price (production) + binary_price_from_params (test) + total_variance_from_params; clones oracle.move:400-429 | ✓ VERIFIED | 131 lines; production entry FULLY FUNCTIONAL (per Plan 05 SUMMARY decision after re-reading oracle.move:225-262 confirmed accessors are public); SHA cited                         |
| `contracts/tests/{i64,isqrt,phi,svi_view}_test.move`                      | Unit tests + golden-vector parity loop                         | ⚠️ ISSUE   | 4 test files exist (647 lines combined). svi_view_test has 6 tests including 2 large parity loops. **CR-01 BLOCKER:** `golden_vectors_arb_violating_all_reject` only counts data; does not feed invalid rows to svi_view::*. |
| `contracts/tests/golden_vectors_data.move`                                | Move companion: vector_count, all_inputs, all_expected_w, all_expected_binary_price, all_params_valid             | ✓ VERIFIED | 595 lines (auto-generated); contains all 5 required public funs                                                                                                                    |
| `backtest/src/deepvault/{isqrt,phi,ln,svi}.py`                            | Python canonical evaluator triple+ln; pure int                 | ✓ VERIFIED | 4 files, 516 lines combined; all cite SHA 1159d79a; no math/numpy/scipy imports in evaluator (only test_phi_against_scipy.py uses scipy as ground truth)                           |
| `backtest/src/deepvault/arb_checker.py`                                   | check_arb returning ArbResult with g_k_array length 200        | ✓ VERIFIED | 193 lines; numpy used only for grid generation; output Python int (Pitfall A respected)                                                                                            |
| `backtest/src/deepvault/parity_runner.py`                                 | CLI consuming shared/golden-vectors.json                       | ✓ VERIFIED | 142 lines; `python -m deepvault.parity_runner` exits 0 with PARITY OK 141 vectors                                                                                                  |
| `dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts`                            | bigint-only TS evaluator; n suffix on every literal            | ✓ VERIFIED | 5 files, 475 lines combined; forbidden-token grep clean; all cite SHA                                                                                                              |
| `dashboard/src/lib/arb_checker.ts`                                        | bigint mirror of Python arb_checker; gK bigint[]               | ✓ VERIFIED | 139 lines; uses Number/Math.sqrt INSIDE the visualization helpers (intentional and excluded from CI grep per file header)                                                          |
| `dashboard/src/lib/parity_runner.ts`                                      | CLI consuming shared/golden-vectors.json                       | ✓ VERIFIED | 191 lines; `pnpm exec tsx src/lib/parity_runner.ts` exits 0 with PARITY OK 141 vectors                                                                                             |
| `dashboard/src/lib/__tests__/{isqrt,phi,svi,arb_checker}.test.ts`         | Vitest tests including per-vector golden-vector loops          | ✓ VERIFIED | 4 test files; 311 tests pass                                                                                                                                                       |
| `backtest/tests/{test_isqrt,test_phi_against_scipy,test_svi,test_gatheral_paper_vectors,test_arb_checker}.py` | pytest covering all evaluator paths   | ✓ VERIFIED | 5 test files; pytest reports "61 passed in 5.92s"                                                                                                                                  |
| `backtest/tests/test_isqrt_random_snapshot.txt`                           | 100 deterministic random sqrt inputs (seed=42)                 | ✓ VERIFIED | 100 lines, format `<32-hex>=<32-hex>`; first 20 inlined into Move isqrt_test for cross-runtime parity                                                                              |
| `backtest/tests/fixtures/jackjacquier_ssvi_outputs.json`                  | Tier C JackJacquier cross-check fixture                        | ⚠️ INFO    | 5 vectors with metadata; per WR-10 the expected_w values are derived from deepvault.svi (self-referential — documented as future work)                                             |
| `scripts/golden_emit.py`                                                  | Canonical emitter; --check drift-mode; runs deepvault.arb_checker for min_g_k                              | ✓ VERIFIED | Re-running with --check exits 0; produces both JSON and Move companion atomically                                                                                                  |
| `scripts/codegen.py`                                                      | Triple-emit codegen extended for SVI fields + Cody coefficients | ✓ VERIFIED | --check exits 0                                                                                                                                                                    |
| `.github/workflows/ci.yml [parity job]`                                   | Three-runtime cross-check + forbidden-token grep              | ✓ VERIFIED | Lines 163-258; job key `parity` preserved; needs `[move, ts, python, codegen-drift]`                                                                                               |
| `.github/workflows/ci.yml [codegen-drift job]`                            | Extended to include 6 generated files + 2 golden vectors      | ✓ VERIFIED | 8 files in git diff list (3 strategy_constants + 3 phi_coefficients + 2 golden vectors); `Regenerate golden vectors` step added                                                    |
| `CONTRIBUTING.md §6` + commit-prefix policy                               | MATH: prefix discipline; cites svi-spec.md + SHA                | ✓ VERIFIED | Section 6 at line 70; commit-conventions bullet at line 106                                                                                                                        |

### Key Link Verification

| From                                              | To                                                          | Via                                                                | Status            | Details                                                                                                                                                       |
| ------------------------------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------------ | ----------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `backtest/src/deepvault/svi.py`                   | `backtest/src/deepvault/{isqrt,phi,ln}.py`                  | `from .isqrt import isqrt_u128` etc.                               | ✓ WIRED           | Imports verified; svi.py:34-36                                                                                                                                |
| `backtest/src/deepvault/phi.py`                   | `backtest/src/deepvault/phi_coefficients.py`                | `from .phi_coefficients import SMALL_THRESHOLD, ...`               | ✓ WIRED           | Verified (Plan 02 codegen output)                                                                                                                             |
| `dashboard/src/lib/svi.ts`                        | `dashboard/src/lib/{isqrt,phi,ln,math}.ts + strategy_constants.ts` | `import {...} from './...';`                              | ✓ WIRED           | svi.ts:35-39                                                                                                                                                  |
| `dashboard/src/lib/phi.ts`                        | `dashboard/src/lib/phi_coefficients.ts`                     | `import { PHI_COEFFICIENTS } from './phi_coefficients';`           | ✓ WIRED           | All Cody coefficients via codegen output                                                                                                                      |
| `contracts/sources/svi_view.move`                 | `deepbook_predict::oracle::OracleSVI`                       | `use deepbook_predict::oracle::{Self, OracleSVI};`                 | ✓ WIRED           | svi_view.move:22; production entry binary_price reads forward_price + svi via public accessors                                                                |
| `contracts/sources/helpers/phi.move`              | `contracts/sources/phi_coefficients.move`                   | `use deepvault::phi_coefficients;`                                 | ✓ WIRED           | All Cody coefs imported (no hand-coded numbers per Plan 05 SUMMARY)                                                                                           |
| `contracts/tests/svi_view_test.move`              | `contracts/tests/golden_vectors_data.move`                  | `use deepvault::golden_vectors_data;`                              | ⚠️ PARTIAL        | Wired for valid-vector parity loops (lines 38-95); BUT for arb-violating coverage (lines 102-122) only counts data — does not feed it to svi_view::* (CR-01)  |
| `dashboard/src/lib/__tests__/svi.test.ts`         | `shared/golden-vectors.json`                                | `JSON.parse(readFileSync(VECTORS_PATH, 'utf-8'))`                  | ✓ WIRED           | Per-vector `.each` loop confirmed                                                                                                                             |
| `dashboard/src/lib/__tests__/isqrt.test.ts`       | `backtest/tests/test_isqrt_random_snapshot.txt`             | `readFileSync(SNAPSHOT_PATH)` cross-runtime parity vs Python       | ✓ WIRED           | 100 inputs match; 311 vitest tests pass                                                                                                                       |
| `scripts/golden_emit.py`                          | `backtest/src/deepvault/arb_checker.py`                     | `check_arb(svi).min_g_k` populates `expected.min_g_k`              | ✓ WIRED           | 131 valid vectors carry structured min_g_k; all 10 arb-violating have neg min_g_k                                                                             |
| `.github/workflows/ci.yml [parity]`               | `backtest/src/deepvault/parity_runner.py`                   | `uv run python -m deepvault.parity_runner`                         | ✓ WIRED           | Step at ci.yml:236-238                                                                                                                                        |
| `.github/workflows/ci.yml [parity]`               | `dashboard/src/lib/parity_runner.ts`                        | `pnpm exec tsx src/lib/parity_runner.ts`                           | ✓ WIRED           | Step at ci.yml:240-242                                                                                                                                        |
| `.github/workflows/ci.yml [parity]`               | `contracts/tests/svi_view_test.move`                        | `sui move test --gas-limit 100000000000 --filter golden_vectors`   | ✓ WIRED           | Step at ci.yml:244-246                                                                                                                                        |
| `.github/workflows/ci.yml [parity]`               | `dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts`              | `grep -nE "Number\(\|parseFloat\(\|Math\..*\("`                    | ✓ WIRED           | Step at ci.yml:223-234; local equivalent passes (no matches)                                                                                                  |

### Behavioral Spot-Checks

| Behavior                                              | Command                                                                                                  | Result                                              | Status |
| ----------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | --------------------------------------------------- | ------ |
| Python full test suite passes                         | `cd backtest && uv run pytest`                                                                           | "61 passed in 5.92s"                                | ✓ PASS |
| Python parity runner: 141 vectors                     | `cd backtest && uv run python -m deepvault.parity_runner`                                                | "PARITY OK: 141 vectors pass within tolerance <= 1" | ✓ PASS |
| TS full test suite passes                             | `cd dashboard && pnpm test`                                                                              | "Test Files 4 passed (4) / Tests 311 passed (311)"  | ✓ PASS |
| TS parity runner: 141 vectors                         | `cd dashboard && pnpm exec tsx src/lib/parity_runner.ts`                                                 | "PARITY OK: 141 vectors pass within tolerance <= 1" | ✓ PASS |
| Forbidden-token grep on parity-bound TS files         | `grep -nE "Number\(\|parseFloat\(\|Math\..*\(" dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts`             | exit 1 (no matches)                                 | ✓ PASS |
| Codegen drift: strategy + phi coefs                   | `cd backtest && uv run python ../scripts/codegen.py --check`                                             | exit 0                                              | ✓ PASS |
| Golden-vectors drift: re-emit produces no diff        | `cd backtest && uv run python ../scripts/golden_emit.py --check`                                         | exit 0                                              | ✓ PASS |
| Arb-checker on valid slice: min_g_k >= 0              | Python `check_arb(SVIParams(50M, 250M, -100M, 0, 400M))`                                                 | min_g_k=296,558,869; len(g_k_array)=200             | ✓ PASS |
| Arb-checker on extreme slice: min_g_k < 0             | Python `check_arb(SVIParams(10M, 2_000M, -995M, 0, 10M))`                                                | min_g_k=-29,019,236,358; any g<0=True               | ✓ PASS |
| All Tier A (Gatheral-derived) vectors arb-clean       | `[v for v in d if v['tier']=='A']` → all min_g_k >= 0                                                    | 21/21 with min_g_k >= 0                             | ✓ PASS |
| All arb-violating vectors flagged                     | 10 B-arb-* vectors in golden-vectors.json; all have neg min_g_k                                          | 10/10 negative                                      | ✓ PASS |
| Move build / test                                     | `sui move build && sui move test`                                                                        | n/a — Sui CLI unavailable locally                   | ? SKIP |

### Requirements Coverage

| Requirement | Source Plan(s)                  | Description                                                                                                                                                  | Status                | Evidence                                                                                                                                                       |
| ----------- | ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| MATH-01     | 01-01, 01-02, 01-03             | Python SSVI evaluator audited against Gatheral & Jacquier 2014 published test cases                                                                          | ✓ SATISFIED           | `test_gatheral_paper_vectors.py` + `test_phi_against_scipy.py`; pytest 61/61 passes; parity_runner OK on 141 vectors including 21 Tier A Gatheral-derived      |
| MATH-02     | 01-01, 01-02, 01-04, 01-05      | Move `deepvault::svi_view` SSVI evaluator producing identical output to Python on 100+ golden vectors (within 1 wei tolerance)                              | ✓ SATISFIED-CONDITIONAL | svi_view_test.move loops all 141 vectors; static review against vendored Predict source + first CI run is empirical confirmation. Valid-vector path FULLY proven. |
| MATH-03     | 01-01, 01-02, 01-04, 01-06      | TypeScript `dashboard/lib/svi.ts` SSVI evaluator producing identical output to Python on the same 100+ golden vectors                                       | ✓ SATISFIED           | svi.test.ts per-vector parity loop; pnpm test 311/311; parity_runner.ts OK on 141 vectors                                                                      |
| MATH-04     | 01-01, 01-08                    | Arbitrage-free checker with closed-form butterfly check + ≥200-point g(k) grid scan + calendar-monotonicity test, with diagnostic visualization (visible g(k) plot, not boolean) | ⚠️ PARTIAL          | 200-element g_k_array delivered in both runtimes; Python and TS both flag arb-violating slices and pass valid slices. **Move-side Plan 05 D-04 invalid-param exercise is incomplete (CR-01).** |
| MATH-05     | 01-01, 01-04, 01-07             | Three-way parity gate enforced in CI — failing the gate blocks any further phase work                                                                        | ✓ SATISFIED-CONDITIONAL | ci.yml parity job replaced with real implementation; job key preserved for branch protection. First CI run is the empirical confirmation.                       |
| MATH-06     | 01-01, 01-03                    | Theoretical binary-price function derived from SVI parameters at target strike (powers vault hedge pricing AND backtest)                                    | ✓ SATISFIED           | `binary_price` exposed in all 3 runtimes; per-vector parity bit-equal at 1 unit at 1e9; D-08 (theoretical fair value) implemented per spec                     |

**Coverage:** 6/6 IDs accounted for. 4 fully SATISFIED, 2 SATISFIED-CONDITIONAL on first CI run (Move build + parity job), 1 PARTIAL (MATH-04 has a Move-side data-coverage gap on invalid params).

### Anti-Patterns Found

| File                                            | Line(s)             | Pattern                                                              | Severity   | Impact                                                                                                                                                                                                                  |
| ----------------------------------------------- | ------------------- | -------------------------------------------------------------------- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `contracts/tests/svi_view_test.move`            | 102-122             | Test loops the data table to count, never feeds it to function under test | 🛑 BLOCKER | CR-01: arb-violating Tier-B vectors are NOT exercised against `svi_view::*`. Goal-impact: weakens MATH-04 invalid-param coverage and the Move-side rejection contract that Phase 2 vault.rebalance will depend on.  |
| `backtest/src/deepvault/svi.py`                 | 102-129             | Python `binary_price` skips param-bounds + k-range validation that Move/TS perform | ⚠️ WARNING | WR-01: Python evaluator runtime semantics for out-of-bounds params differ from Move/TS. All 141 golden vectors are within bounds so parity passes; "bit-equal" claim about runtime behavior on invalid params is not exact. |
| `scripts/golden_emit.py`                        | 179-203             | No defensive assertion that arb-violating cases actually raise        | ⚠️ WARNING | WR-02: Future evaluator changes could silently flip a row's `params_valid` to True without test signal.                                                                                                                  |
| `contracts/sources/helpers/phi.move`            | 30                  | `MAX_EXP_INPUT` magic constant tied to codegen-emitted LN2_U128       | ⚠️ WARNING | WR-03: A LN2 bump via `MATH:` could leave this stale; CI codegen-drift won't catch.                                                                                                                                       |
| `dashboard/src/lib/isqrt.ts`                    | 21-33               | `isqrtInitialGuess(0n)` returns `0n`; Move and Python return `1`     | ⚠️ WARNING | WR-04: Cross-runtime contract drift. Not load-bearing today (callers short-circuit zero) but parity claim weakened.                                                                                                       |
| `contracts/tests/i64_test.move`                 | (filename vs module)| Module `i64_tests` vs file `i64_test.move` (singular vs plural)      | ℹ️ INFO    | WR-05: maintainability smell.                                                                                                                                                                                             |
| `contracts/sources/svi_view.move`               | 30-35               | Error codes start at 3 — codes 0/1/2 unexplained                      | ℹ️ INFO    | WR-06: Either fill or document.                                                                                                                                                                                            |
| `dashboard/src/lib/svi.ts`                      | 81, 105, 136        | `_validateParams` only called in `binaryPrice`; `totalVariance` skips it | ⚠️ WARNING | WR-07: Off-chain bypass-validation path; matches Move's semantics but weakens the threat model.                                                                                                                            |
| `backtest/src/deepvault/svi.py` + 2 mirrors     | (`ECannotBeNegative`)| Defense-in-depth path is unreachable per analysis but not property-tested | ⚠️ WARNING | WR-08: Refactor regression risk.                                                                                                                                                                                            |
| `backtest/src/deepvault/parity_runner.py`       | 118-125             | Outer except can hide unexpected exceptions on arb-violating vectors  | ⚠️ WARNING | WR-09: Diagnostic noise on real failures.                                                                                                                                                                                  |
| `backtest/tests/fixtures/jackjacquier_ssvi_outputs.json` | 5, 15-49 | `expected_w` values derived from deepvault.svi (self-referential)     | ⚠️ WARNING | WR-10: Tier C "cross-check" passes against itself; documented as future work.                                                                                                                                              |
| `dashboard/src/lib/arb_checker.ts`              | 34-38, 46-49, 57-60, 93 | `Number()` on bigint inputs that may exceed `Number.MAX_SAFE_INTEGER` | ⚠️ WARNING | WR-11: Currently safe at SVI bounds; risk if bounds loosen.                                                                                                                                                                  |
| `contracts/sources/helpers/phi.move`            | 145-154             | `exp_overflow_aborts` tested but `exp_at_max_input_succeeds` missing  | ℹ️ INFO    | IN-01: soft coverage gap.                                                                                                                                                                                                    |
| `dashboard/src/lib/parity_runner.ts`            | 75-83               | Argv parsed without bounds-checking                                   | ℹ️ INFO    | IN-02: confusing error path on malformed input.                                                                                                                                                                              |
| `scripts/golden_emit.py`                        | 152, 197            | `B-arb-{counter}` continues from grid counter; ID jump 90→91 without comment | ℹ️ INFO | IN-03: cognitive smell.                                                                                                                                                                                                       |
| `backtest/src/deepvault/arb_checker.py`         | 115                 | `-F` sentinel collides with legitimate `g(k) = -1.0` value            | ℹ️ INFO    | IN-04: ambiguity at boundary; non-load-bearing.                                                                                                                                                                              |

**Note:** All anti-patterns above are documented in `01-REVIEW.md` (the just-completed code review report). Severity classification matches that report: 1 BLOCKER, 11 WARNING, 4 INFO. Of these, only CR-01 (BLOCKER) materially affects Phase 1 goal achievement; the WARNINGs and INFOs are cleanup items that don't block phase advance but should be tracked.

### Human Verification Required

The two items below are documented for completeness — they're already known-conditional gates per Plan 05/07 SUMMARY documentation, not freshly discovered uncertainties.

#### 1. First CI run on the parity branch — Move test confirmation

**Test:** Push the phase-completion commit to a branch and observe CI's `Move tests` and `parity` jobs.
**Expected:** Both `move` job (`sui move build` + `sui move test`) and `parity` job (`sui move test --filter golden_vectors`) exit 0. The 4 svi_view_test rejection tests pass: `zero_a_zero_b_aborts_zero_variance`, `zero_forward_aborts`, `k_out_of_range_aborts`, `golden_vectors_arb_violating_all_reject`.
**Why human:** Sui CLI is unavailable in the local environment per Plan 01-05 / 01-07 SUMMARY.md "Sui CLI unavailable" sections. Move-side parity was verified via static review against the vendored Predict source at SHA `1159d79af33c70e09e406310e1d8f067832ede9d`. The first CI run is the empirical confirmation. This is a documented-conditional gate, NOT a freshly discovered uncertainty.

#### 2. First CI run — Forbidden-token grep step

**Test:** CI's `parity` job's `Forbidden-token grep on TS evaluator (Pitfall B)` step.
**Expected:** exit 0 (no `Number(`/`parseFloat(`/`Math.{sqrt,exp,log,pow}(` in `dashboard/src/lib/{math,isqrt,phi,ln,svi}.ts`).
**Why human:** Local equivalent grep returns exit 1 (clean), but CI uses bash with `set -euo pipefail` and the inverted-`if` semantics produce exit 0 only when grep finds NO matches. Confirming on first CI run rules out shell-context surprises.

### Gaps Summary

**One BLOCKER gap (CR-01):** The Move test `golden_vectors_arb_violating_all_reject` does NOT actually feed the 10 generated arb-violating golden vectors to `svi_view::*`. It only counts how many `params_valid=false` rows exist in the data table. The reachable rejection path (`a=0, b=0` → `EZeroVariance`) is exercised by ONE hand-crafted test (`zero_a_zero_b_aborts_zero_variance`), not by per-vector tests over the 10 generated vectors.

**Why this matters for the goal:**

- Success Criterion #2 (Move parity within 1 wei) is mostly proven — 131 valid vectors are bit-equal-tested. The arb-violating leg is the gap.
- Success Criterion #4 (arb-checker visible g(k) for violating slices) is mostly proven — Python and TS both produce the visible array; both correctly flag violators. The Move-side rejection-path coverage of the same 10 violating vectors is the gap.
- D-04's "Move evaluator hard-rejects invalid params" claim depends on `svi_view::*` aborting on EVERY arb-violating golden vector, not just one hand-crafted (a=0, b=0) sample.

**Why this is BLOCKER not WARNING:** The vendored DeepBook code-review checklist (which Phase 1 explicitly inherits per `scripts/deepbookv3/.claude/rules/code-review.md`) is unambiguous: "Every generated test vector must be exercised against the contract. If generated data isn't passed to the function under test, delete it." The current state — `let n = ...; while (i < n) { count++; }` without ever calling `svi_view::*` — is exactly the pattern that rule prohibits.

**Recommendation:** A follow-up plan should land the per-row `expected_failure` tests (10 separate tests, one per `B-arb-091..B-arb-100`) OR a defensive emit-time assertion in `golden_emit.py` (per WR-02) before this phase is declared closed. The fix is small (~30 lines of Move test code, mechanical).

**Other concerns (NON-blocking, documented):**

- Sui CLI unavailable locally → first CI run is empirical confirmation of MATH-02 + MATH-05 (documented in Plans 05/07 SUMMARY).
- Tier C JackJacquier fixture is self-referential (WR-10) — documented as future work in the fixture's own metadata.
- Several maintainability smells (WR-03..WR-11, IN-01..IN-04) — none affect goal achievement but should be tracked for cleanup.

---

_Verified: 2026-05-09_
_Verifier: Claude (gsd-verifier)_
