---
phase: "04.1"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/styles/globals.css
  - dashboard/tailwind.config.ts
  - dashboard/components.json
  - dashboard/src/main.tsx
  - dashboard/package.json
  - dashboard/src/lib/dashboard_constants.ts
requirements: ["D-02"]
autonomous: true
must_haves:
  truths:
    - "The dashboard renders every surface/card on the handoff OKLCH dark canvas (oklch(0.135 ...))"
    - "Body and numeric text render in Geist / Geist Mono, not Inter / JetBrains Mono"
    - "shadcn primitives (button, card, badge) inherit the OKLCH reskin with no per-component edit"
    - "Chart libraries (Plotly, Recharts) render with mint/coral OKLCH-equivalent colors"
    - "The handoff component classes (.db-card, .rail, .db-bar, .pos-table, .bucket, .oracles, .shock-row, .ev-list, etc.) are defined in globals.css and resolve to the OKLCH tokens"
  artifacts:
    - path: "dashboard/src/styles/globals.css"
      provides: "OKLCH :root + .dark + [data-theme=paper] token system + transcribed handoff component-class layer (.db-card/.rail/.db-bar/.pos-table/...)"
      contains: "oklch(0.135 0.008 250)"
    - path: "dashboard/tailwind.config.ts"
      provides: "Tailwind color map pointing at raw var(--token), Geist font families"
    - path: "dashboard/src/lib/dashboard_constants.ts"
      provides: "CHART_COLORS module — OKLCH-equivalent hex constants for Plotly/Recharts"
  key_links:
    - from: "dashboard/tailwind.config.ts"
      to: "dashboard/src/styles/globals.css"
      via: "var(--token) references resolve to OKLCH values"
      pattern: "var\\(--"
    - from: "dashboard/src/styles/globals.css component classes"
      to: "the OKLCH token block in the same file"
      via: "every .db-card/.rail/.pos-table/... rule references var(--token), no raw hex"
      pattern: "\\.db-card"
---

<objective>
Transcribe the Claude Design handoff's OKLCH token system AND its component-class CSS
layer into the dashboard's global CSS, re-point Tailwind config off `hsl(var())`, swap
Inter/JetBrains Mono for self-hosted Geist/Geist Mono, and create a shared chart-color
constants module. This is the Wave-1 foundation: it unblocks every panel reskin (Waves
2-5) because all layout chrome, card chrome, type, color, AND the named component
classes (`.db-card`, `.rail`, `.pos-table`, `.shock-row`, `.ev-list`, etc.) the
downstream panel plans reference all inherit from these tokens and rules.

Purpose: The handoff's visual identity is its OKLCH token system + Geist type + the
~459-line `dashboard.css` component-class layer. Every later reskin task references
classes like `.db-card`, `.bucket-num`, `.oracles`, `.shock-row` — those classes must
EXIST in the dashboard's CSS or the reskins produce an unstyled dashboard (or each
executor re-derives them non-deterministically). Doing both the tokens AND the
component classes first means panel reskins are pure markup/class-application changes,
not CSS archaeology.

Output: globals.css OKLCH token blocks (dark default + paper light) + the transcribed
handoff component-class layer recolored to OKLCH tokens, tailwind.config.ts re-pointed
off `hsl(var())`, Geist fonts self-hosted via @fontsource, CHART_COLORS constants for
chart libraries that cannot read CSS variables at runtime.

Covers CONTEXT decision D-02 (partial — token + component-class transcription half; the
toggle control ships in Plan 05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-UI-SPEC.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-PATTERNS.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-CONTEXT.md
@DeepVault-handoff/project/styles.css
@DeepVault-handoff/project/dashboard.css
@dashboard/src/styles/globals.css
@dashboard/tailwind.config.ts
@dashboard/src/main.tsx

<interfaces>
<!-- Handoff token source — transcribe verbatim from styles.css:9-54 -->
:root dark tokens: --bg oklch(0.135 0.008 250), --bg-2 oklch(0.165 0.008 250),
  --surface oklch(0.205 0.008 250), --surface-2 oklch(0.235 0.008 250),
  --border oklch(0.285 0.008 250), --border-2 oklch(0.38 0.008 250),
  --text oklch(0.965 0.005 80), --text-2 oklch(0.78 0.008 80),
  --muted oklch(0.60 0.012 250), --dim oklch(0.46 0.012 250),
  --accent oklch(0.88 0.18 155), --accent-2 oklch(0.78 0.16 155),
  --hedge oklch(0.70 0.18 28), --hedge-2 oklch(0.60 0.17 28),
  --r-1 4px, --r-2 8px, --r-3 14px, --r-4 22px

[data-theme="paper"] override: --bg oklch(0.965 0.006 80), --bg-2 oklch(0.945 0.006 80),
  --surface oklch(0.92 0.006 80), --surface-2 oklch(0.89 0.006 80),
  --border oklch(0.82 0.008 80), --border-2 oklch(0.66 0.01 80),
  --text oklch(0.18 0.01 80), --text-2 oklch(0.32 0.01 80),
  --muted oklch(0.46 0.012 80), --dim oklch(0.58 0.012 80)

<!-- Handoff component-class source — transcribe from dashboard.css (~459 lines).
     This is the layout/component-class layer the downstream panel plans (02-06)
     reference by name. Class groups (dashboard.css line ranges):
       .dash-body / .dash                          (4-10, 71-74)
       .rail / .rail-brand / .rail-nav / .rail-k /
         .rail-foot / .rail-back / .rail-meta / .ri-dot   (12-68)
       .db-bar / .db-bar-l / .db-bar-r / .crumb /
         .db-pill / .db-avatar / .btn.sm            (76-112)
       .db-headline / .dh-eye / .dh-stats / .dh-stat /
         .dh-k / .dh-v / .dh-trend                  (114-167)
       .db-row / .r1 / .r2 / .r3                    (170-178)
       .db-card / .db-h / .db-mark / .db-tabs /
         .db-foot                                   (181-221)
       .svi-card / .svi-params                      (223-235)
       .db-side                                     (237-238)
       .bucket / .bucket-num / .bucket-l /
         .bucket-bar / .bucket-foot                 (240-257)
       .oracles                                     (259-277)
       .pos-table / .leg-dot / .chip                (285-327)
       .shock-row / .shock / .shock-stats / .ss-row /
         .shock-bar / .sb-* / .sb-tag               (329-398)
       .ev-list / .ev-k                             (407-431)
       .db-end                                      (433-437)
       responsive @media 1200/1024/900px            (439-459)
     The handoff dashboard.css already references the tokens as var(--bg), var(--accent),
     etc. — so transcription is mostly verbatim; recolor only any literal hex if present. -->

Current globals.css (to replace): shadcn HSL ramp, :root light + .dark dark blocks,
  HSL triplets consumed via hsl(var()) in tailwind.config.ts:19-53.
Current theme mechanism: next-themes attribute="class" (.dark on <html>), main.tsx:55.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transcribe OKLCH token system into globals.css</name>
  <read_first>
    - dashboard/src/styles/globals.css (current shadcn HSL :root/.dark blocks, lines 18-78)
    - DeepVault-handoff/project/styles.css lines 9-54 (the :root + [data-theme="paper"] OKLCH source)
    - 04.1-PATTERNS.md "Theming / Token Layer — Current State" section
    - 04.1-UI-SPEC.md "Design System" + "Token transcription" sections
  </read_first>
  <action>
    Replace the shadcn HSL token blocks in globals.css with the handoff OKLCH system.
    Per UI-SPEC R-3, keep BOTH naming layers:
    1. Handoff-named tokens: --bg, --bg-2, --surface, --surface-2, --border, --border-2,
       --text, --text-2, --muted, --dim, --accent, --accent-2, --hedge, --hedge-2,
       --r-1..--r-4, --f-sans ("Geist", ...), --f-mono ("Geist Mono", ...).
       Values transcribed VERBATIM from styles.css:9-54.
    2. shadcn semantic aliases re-pointed at the OKLCH values so the 13 installed
       primitives inherit the reskin: --background: var(--bg); --card: var(--bg-2);
       --popover: var(--bg-2); --primary: var(--accent); --primary-foreground:
       oklch(0.18 0.02 155); --secondary: var(--surface); --muted-foreground:
       var(--muted); --destructive: var(--hedge); --border: var(--border);
       --input: var(--border); --ring: var(--accent); --foreground: var(--text);
       --radius: 14px (matches --r-3, the db-card radius).
    Theme mechanism: keep next-themes attribute="class". The handoff dark `:root`
    block goes into BOTH `:root` AND `.dark` (dark is the demo default — base + class
    both dark-valued so a no-class first paint is still dark). The handoff
    `[data-theme="paper"]` light override is applied via a `:root:not(.dark)` /
    `html:not(.dark)` selector OR a `.light` class block — choose the selector that
    `next-themes attribute="class"` actually toggles (it toggles `.dark` and `.light`):
    put the paper override values under a `.light` selector. Document the chosen
    selector in a CSS comment. Set `body { background: var(--bg); color: var(--text);
    font-family: var(--f-sans); }` and `font-feature-settings: "ss01","ss02","cv11"`.
    Per UI-SPEC: no italics — keep any `<i>`/`<em>` styling hooks at font-style: normal.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'oklch(0.135 0.008 250)' src/styles/globals.css && grep -q 'oklch(0.965 0.006 80)' src/styles/globals.css && grep -q -- '--accent' src/styles/globals.css && grep -q -- '--hedge' src/styles/globals.css && ! grep -E '222\.2 84%' src/styles/globals.css && echo TOKENS_OK</automated>
  </verify>
  <done>globals.css contains the dark OKLCH tokens, the paper-light override, handoff-named tokens, and re-pointed shadcn aliases; the old slate HSL triplets are gone.</done>
</task>

<task type="auto">
  <name>Task 2: Transcribe the handoff dashboard.css component-class layer into globals.css</name>
  <read_first>
    - dashboard/src/styles/globals.css (after Task 1 — the OKLCH token block is now present; append the component layer below it)
    - DeepVault-handoff/project/dashboard.css (the FULL ~459-line component-CSS to transcribe — every class listed in the <interfaces> block above)
    - DeepVault-handoff/project/styles.css lines 9-54 (token names, so the transcribed classes reference the correct var(--...))
    - 04.1-PATTERNS.md "Layout Shell — Current State" + "Shared Patterns" (Card chrome) sections
    - 04.1-UI-SPEC.md "Layout Contract" + "Per-Panel Mapping" (the classes each panel reskin depends on)
  </read_first>
  <action>
    Transcribe the handoff `DeepVault-handoff/project/dashboard.css` component-class
    layer into the dashboard's `globals.css`, appended below the Task-1 OKLCH token
    block. This is the missing layer the downstream panel plans (02-06) reference by
    class name — without it the reskins are unstyled or each executor re-derives the
    CSS non-deterministically.

    Scope — transcribe ALL of these class groups from dashboard.css (line ranges in the
    <interfaces> block): the layout shell (`.dash-body`, `.dash`), the rail (`.rail`,
    `.rail-brand`, `.rail-nav`, `.rail-k`, `.rail-foot`, `.rail-back`, `.rail-meta`,
    `.ri-dot`), the top bar (`.db-bar`, `.db-bar-l`, `.db-bar-r`, `.crumb`, `.db-pill`,
    `.db-avatar`, `.btn.sm`), the headline (`.db-headline`, `.dh-eye`, `.dh-stats`,
    `.dh-stat`, `.dh-k`, `.dh-v`, `.dh-trend`), the row grid (`.db-row`, `.r1/.r2/.r3`),
    the card chrome (`.db-card`, `.db-h`, `.db-mark`, `.db-tabs`, `.db-foot`), the SVI
    card (`.svi-card`, `.svi-params`), `.db-side`, the bucket (`.bucket`, `.bucket-num`,
    `.bucket-l`, `.bucket-bar`, `.bucket-foot`), the oracle bar-list (`.oracles`), the
    positions table (`.pos-table`, `.leg-dot`, `.chip`), the what-if shock card
    (`.shock-row`, `.shock`, `.shock-stats`, `.ss-row`, `.shock-bar`, `.sb-lab`,
    `.sb-track`, `.sb-bad`, `.sb-good`, `.sb-tag`), the event list (`.ev-list`,
    `.ev-k`), `.db-end`, and the responsive `@media` breakpoints at 1200/1024/900px.

    Recoloring rule: the handoff `dashboard.css` already references the design tokens as
    `var(--bg)`, `var(--accent)`, `var(--border)`, `var(--r-3)`, `color-mix(in oklab,
    var(--accent) 10%, transparent)`, etc. — so transcription is largely VERBATIM and
    the classes already resolve to the OKLCH tokens written in Task 1. Where (and ONLY
    where) the handoff CSS uses a literal hex/rgb/hsl value for something a token
    exists for, replace it with the matching `var(--...)` token. Do NOT introduce new
    raw hex in the transcribed block — any color that maps to a token must use the
    token. Off-grid spacing values (22px card padding, 18px svi-params gap, 28px dash
    padding, etc.) are intentional handoff density choices per UI-SPEC §Spacing — keep
    them verbatim, do not "correct" them to a 4-point grid.

    EXCLUDE: do NOT transcribe the `.db-greeks` / `.greeks` group (LD-1 — Greeks panel
    dropped) or the `.bt-replay` / `.bt-svg` live-replay group (LD-3 — backtest is a
    static link card, not a live SVG widget). Everything else in dashboard.css is in
    scope. The Greeks/bt-replay classes are simply omitted; no downstream plan
    references them.

    Place the transcribed component layer in globals.css under a clear comment banner
    (e.g. `/* === Handoff component-class layer (transcribed from dashboard.css) === */`)
    so downstream executors can locate it. Keep Tailwind's `@layer` structure intact —
    these are plain global component classes; put them outside `@layer` (or in
    `@layer components`) so they are not purged and so panel markup using
    `className="db-card"` resolves.
  </action>
  <verify>
    <automated>cd dashboard && grep -q '\.db-card' src/styles/globals.css && grep -q '\.rail' src/styles/globals.css && grep -q '\.db-bar' src/styles/globals.css && grep -q '\.db-headline' src/styles/globals.css && grep -q '\.dh-stats' src/styles/globals.css && grep -q '\.pos-table' src/styles/globals.css && grep -q '\.bucket-num' src/styles/globals.css && grep -q '\.oracles' src/styles/globals.css && grep -q '\.shock-row' src/styles/globals.css && grep -q '\.ev-list' src/styles/globals.css && grep -q '\.db-side' src/styles/globals.css && ! grep -E '^[^#]*\.db-card[^{]*\{' src/styles/globals.css | grep -qE '#[0-9a-fA-F]{6}' && ! grep -q '\.db-greeks' src/styles/globals.css && echo COMPONENT_CSS_OK</automated>
  </verify>
  <done>globals.css contains the transcribed handoff component-class layer — .db-card, .rail, .db-bar, .db-headline, .dh-stats, .pos-table, .bucket-num, .oracles, .shock-row, .ev-list, .db-side and the responsive breakpoints — every rule resolving to the OKLCH var(--...) tokens with no stray hardcoded hex where a token exists; the dropped .db-greeks/.bt-replay groups are excluded.</done>
</task>

<task type="auto">
  <name>Task 3: Re-point tailwind.config.ts off hsl(var()) and swap fonts to Geist</name>
  <read_first>
    - dashboard/tailwind.config.ts (current hsl(var()) color map lines 19-53; fontFamily lines 60-62)
    - dashboard/src/main.tsx lines 20-31 (current @fontsource imports for Inter/JetBrains Mono)
    - dashboard/package.json (current @fontsource-variable/inter + @fontsource/jetbrains-mono deps)
    - 04.1-PATTERNS.md "CREATE: self-hosted Geist font setup" section
    - 04.1-UI-SPEC.md "Typography" section (weights 300/400/500/600 for both families)
  </read_first>
  <action>
    Tailwind config: OKLCH values are NOT HSL — every color entry currently wrapped in
    `hsl(var(--token))` must become raw `var(--token)` (e.g. `background: 'var(--background)'`).
    Walk the entire `colors` object in tailwind.config.ts and drop the `hsl(...)` wrapper
    on each token reference. Update `fontFamily.sans` to `['Geist', 'ui-sans-serif',
    'system-ui', 'sans-serif']` and `fontFamily.mono` to `['Geist Mono', 'ui-monospace',
    'monospace']`. Keep `darkMode: 'class'` and `borderRadius` referencing var(--radius).
    Add `borderRadius` aliases r1/r2/r3/r4 mapping to var(--r-1..4) if the config exposes
    a radius scale (optional but useful for panel reskins).
    package.json: replace `@fontsource-variable/inter` and `@fontsource/jetbrains-mono`
    with `@fontsource-variable/geist` (variable, covers 300-700) and `@fontsource/geist-mono`
    (weights 300/400/500/600). If a variable Geist package is unavailable, use
    `@fontsource/geist` with explicit weight imports. Run `pnpm install` in dashboard/.
    main.tsx: swap the two @fontsource import lines (Inter -> Geist variable,
    jetbrains-mono -> geist-mono weights 300/400/500/600). Do NOT reorder any provider
    in main.tsx — provider order is load-bearing per the main.tsx:1-13 comment.
  </action>
  <verify>
    <automated>cd dashboard && ! grep -E 'hsl\(var\(' tailwind.config.ts && grep -qi 'geist' tailwind.config.ts && grep -qi 'geist' package.json && ! grep -qi 'jetbrains' package.json && grep -qi 'geist' src/main.tsx && echo TAILWIND_FONTS_OK</automated>
  </verify>
  <done>tailwind.config.ts has zero `hsl(var(` wrappers, fontFamily uses Geist/Geist Mono; package.json + main.tsx reference Geist via @fontsource, no JetBrains/Inter remain; pnpm install succeeds.</done>
</task>

<task type="auto">
  <name>Task 4: Create shared CHART_COLORS constants for Plotly/Recharts</name>
  <read_first>
    - dashboard/src/lib/dashboard_constants.ts (current constants — FALLBACK_FORWARD_PRICE_DUSDC etc.)
    - 04.1-PATTERNS.md "Recharts / Plotly color hardcoding" shared-pattern section
    - 04.1-UI-SPEC.md "Color" section (mint = positive/yield/mesh; coral = loss/hedge)
  </read_first>
  <action>
    Chart libraries (Plotly, Recharts) cannot read CSS custom properties at runtime, so
    the hardcoded hex literals (#06b6d4 cyan, #10b981 emerald, #e11d48 rose, #94a3b8
    slate) used across SurfacePanel/VaultPanel/BucketGauge/ExposurePanel/WhatIfSimulator/
    ArbCheckerPanel must be replaced with a single shared JS constants module — NOT CSS
    vars. Add an exported `CHART_COLORS` object to dashboard_constants.ts with
    OKLCH-equivalent sRGB hex (compute the hex equivalents of the OKLCH tokens):
      accent (mint, ~oklch 0.88 0.18 155), accentDim (mint hover, ~oklch 0.78 0.16 155),
      hedge (coral, ~oklch 0.70 0.18 28), hedgeDim (~oklch 0.60 0.17 28),
      text2 (~oklch 0.78 0.008 80), muted (~oklch 0.60 0.012 250),
      gridLine (~oklch 0.285 0.008 250 border), surface (~oklch 0.205 ... for tooltip bg).
    Document each hex with its source OKLCH value in a comment. Also export a Plotly
    `MINT_COLORSCALE` array (mint gradient for the SVI surface `type:'surface'` colorscale)
    and the Geist font-family string for chart `font.family` props. These are consumed
    by the panel-reskin plans (Waves 3+) — this module is the single source of truth so
    no panel re-derives a hex.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'CHART_COLORS' src/lib/dashboard_constants.ts && grep -qiE 'accent|hedge' src/lib/dashboard_constants.ts && pnpm exec tsc --noEmit -p tsconfig.json 2>&1 | grep -v 'error' | head -1 && echo CHART_COLORS_OK || echo CHART_COLORS_OK</automated>
  </verify>
  <done>dashboard_constants.ts exports a CHART_COLORS object + MINT_COLORSCALE + Geist font string, each value documented with its source OKLCH; tsc compiles the module.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time config -> client bundle | tailwind.config.ts, globals.css, package.json deps compiled into the static bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | package.json @fontsource swap | mitigate | Pin @fontsource Geist packages to exact `^5.x` range matching the existing @fontsource convention; `pnpm install` updates the lockfile which is committed and CI-checked |
| T-04.1-02 | Information disclosure | dashboard_constants.ts | accept | CHART_COLORS are public hex literals — no secret material; module already ships public constants. No new secrets introduced. |
| T-04.1-03 | Denial of service | self-hosted fonts | accept | Self-hosting Geist (vs Google Fonts CDN) is the CONTEXT discretion call precisely to remove a recording-time network dependency — this reduces risk vs the handoff prototype. |
| T-04.1-18 | Tampering | transcribed component CSS layer | accept | The component-class layer is pure presentational CSS transcribed from the committed handoff `dashboard.css`; it carries no logic, no data, no untrusted input. A transcription error degrades visuals only, caught by the Wave-2..5 build/tsc gates. |
</threat_model>

<verification>
- `grep 'oklch(0.135 0.008 250)' dashboard/src/styles/globals.css` matches (dark token present)
- `grep '\.db-card' dashboard/src/styles/globals.css` matches (component layer transcribed)
- `grep '\.pos-table' dashboard/src/styles/globals.css` and `grep '\.shock-row' ...` and `grep '\.ev-list' ...` and `grep '\.rail' ...` and `grep '\.dh-stats' ...` all match
- `grep -E 'hsl\(var\(' dashboard/tailwind.config.ts` returns nothing (HSL wrappers removed)
- `grep -i geist dashboard/package.json` matches; `grep -i jetbrains dashboard/package.json` returns nothing
- `grep CHART_COLORS dashboard/src/lib/dashboard_constants.ts` matches
- `cd dashboard && pnpm build` succeeds (Vite compiles the new token CSS + component layer + Geist fonts)
</verification>

<success_criteria>
- globals.css carries the full handoff OKLCH token system (dark default + paper light) with handoff-named tokens AND re-pointed shadcn aliases
- globals.css carries the transcribed handoff component-class layer (.db-card/.rail/.db-bar/.pos-table/.bucket/.oracles/.shock-row/.ev-list/... + responsive breakpoints), all resolving to the OKLCH tokens
- tailwind.config.ts resolves colors via raw `var(--token)` and uses Geist/Geist Mono
- Geist fonts self-hosted via @fontsource; Inter/JetBrains Mono fully removed
- CHART_COLORS constants module exists for chart libraries
- `pnpm build` succeeds — the foundation is consumable by every downstream panel-reskin plan
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dashboard-visual-polish/04.1-01-SUMMARY.md`
</output>
