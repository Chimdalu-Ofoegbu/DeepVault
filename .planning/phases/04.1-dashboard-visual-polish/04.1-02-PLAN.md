---
phase: "04.1"
plan: "02"
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - dashboard/src/App.tsx
  - dashboard/src/components/layout/Rail.tsx
  - dashboard/src/components/layout/Header.tsx
requirements: ["D-01"]
autonomous: true
must_haves:
  truths:
    - "The dashboard renders a 232px left rail + 1fr main two-column shell"
    - "A grouped scroll-anchor rail navigates to in-page sections; disabled items show a tooltip"
    - "The headline section shows the eyebrow + h1 + a 4-cell stat strip"
    - "The 8 existing panels still render with live WebSocket data in the new 3-row grid"
  artifacts:
    - path: "dashboard/src/components/layout/Rail.tsx"
      provides: "232px aside.rail — brand, grouped nav (scroll anchors), footer"
      contains: "data-section"
    - path: "dashboard/src/App.tsx"
      provides: "rail + headline + 3-row 1.6fr/1fr grid layout shell"
    - path: "dashboard/src/components/layout/Header.tsx"
      provides: "db-bar top bar — breadcrumb + pills + Deposit button + wallet"
  key_links:
    - from: "dashboard/src/components/layout/Rail.tsx"
      to: "dashboard/src/App.tsx section[data-section]"
      via: "scrollIntoView targeting data-section attrs"
      pattern: "scrollIntoView"
    - from: "dashboard/src/App.tsx"
      to: "useWebSocket(env.relayWsUrl)"
      via: "data spine preserved — same hooks, same props"
      pattern: "useWebSocket"
---

<objective>
Restructure the dashboard layout shell from the flat `max-w-[1280px] space-y-8` stack
into the handoff's rail + headline + 3-row grid, build the new `Rail.tsx` grouped
scroll-anchor nav, and reskin `Header.tsx` into the `db-bar` top bar. The 8 panels are
re-placed into the 3-row grid unchanged (their internal reskin is Waves 3-5); the
WebSocket data spine and every panel prop stay byte-identical.

Purpose: The layout shell is the handoff's structural identity — rail, headline strip,
3-row grid. Doing it in Wave 2 (after tokens, before panel reskins) means panel reskins
land into their final grid cells. Restructuring is the highest-regression-risk task in
the phase, so it is isolated to one plan with an explicit no-regression gate.

Output: App.tsx layout shell, Rail.tsx (new), Header.tsx db-bar reskin.

Covers CONTEXT decision D-01 (layout-shell portion) and UI-SPEC #7, #11, #12, LD-1, LD-4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-UI-SPEC.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-PATTERNS.md
@DeepVault-handoff/project/dashboard.html
@DeepVault-handoff/project/dashboard.css
@dashboard/src/App.tsx
@dashboard/src/components/layout/Header.tsx

<interfaces>
<!-- Data spine — App.tsx:42-48, MUST be preserved unchanged -->
const { state, snapshot } = useWebSocket(env.relayWsUrl);
const surface   = useSurfaceSnapshot(snapshot);   // SurfacePanel, ArbCheckerPanel
const vaultView = useVaultState(snapshot);         // VaultPanel, dh-stats, DepositWithdrawPanel
const hedges    = useExposure(snapshot);           // ExposurePanel, WhatIfSimulator
const sigma     = useSigmaEstimates(snapshot);     // WhatIfSimulator
const positions = usePositions(snapshot);          // PositionViewer

<!-- data-section attrs already on App.tsx <section>s — rail anchors target these -->
hero, arb-checker, vault-bucket, exposure, what-if, deposit-withdraw, position-viewer

<!-- Navigation Contract (UI-SPEC) — rail item -> anchor -->
Vault/Overview -> top; Vault/Positions + Hedge ladder -> [data-section="position-viewer"];
Vault/History -> [data-section="exposure"]; Risk Studio/SVI surface -> [data-section="hero"];
Risk Studio/What-if -> [data-section="what-if"]; Risk Studio/Backtest + Account/Reports -> row3 backtest card;
Account/Wallet -> [data-section="deposit-withdraw"];
DISABLED (tooltip): Risk Studio/Drawdown replay, Composability/Single PTB, vUSDC share, Integrations

<!-- 3-row main grid, each db-row is 1.6fr 1fr (dashboard.css:170-187) -->
r1: SVI surface card | side column {Token bucket, Per-oracle exposure}  (Greeks DROPPED — LD-1)
r2: Positions/Hedge ladder card | What-if card
r3: Backtest link card | Event stream card
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Rail.tsx — 232px grouped scroll-anchor nav</name>
  <read_first>
    - dashboard/src/components/layout/Header.tsx (closest analog — sticky layout chrome, brand)
    - DeepVault-handoff/project/dashboard.html lines 16-54 (the <aside class="rail"> markup)
    - DeepVault-handoff/project/dashboard.css lines 12-68 (.rail, .rail-brand, .rail-nav, .rail-k, .rail-foot)
    - 04.1-PATTERNS.md "CREATE: src/components/layout/Rail.tsx" section
    - 04.1-UI-SPEC.md "Navigation Contract" — the full rail-item -> anchor map
  </read_first>
  <action>
    Create dashboard/src/components/layout/Rail.tsx. Build `<aside>` styled to the
    handoff `.rail`: sticky, height 100vh, width 232px, bg var(--bg-2), 1px right
    border var(--border), padding 24px 20px, flex-column gap 28px (use Tailwind
    utilities or a scoped class — match dashboard.css:12-20).
    Structure:
    1. rail-brand: inline SVG brand-mark (copy the 24x24 viewBox path from
       dashboard.html:18-22) + "DeepVault" wordmark (Geist 600).
    2. rail-nav: 4 groups with `rail-k` uppercase Geist Mono 10.5px labels:
       Vault (Overview, Positions, Hedge ladder, History),
       Risk Studio (SVI surface, What-if, Backtest, Drawdown replay),
       Composability (Single PTB, vUSDC share, Integrations),
       Account (Wallet, Reports).
       Scroll-anchor items: onClick -> document.querySelector('[data-section="X"]')
       .scrollIntoView({ behavior:'smooth' }) per the Navigation Contract map.
       Disabled items (Drawdown replay, Single PTB, vUSDC share, Integrations):
       rendered with --dim text, cursor:default, no hover bg, wrapped in a Radix
       <Tooltip> (ui/tooltip.tsx, already installed) — tooltip copy per UI-SPEC
       Navigation Contract ("Drawdown replay ships as the static backtest report" /
       "Composability detail lives in the project README and demo video").
    3. Active state: 10% accent-mix background + mint `ri-dot`. Use an
       IntersectionObserver over the `[data-section]` elements to update the active
       item as the user scrolls; default active = Overview.
    4. rail-foot: "back to site" link + network meta row with a `net-dot` + mono
       "testnet · 0x…" text (read the vault/admin address from TESTNET-DEPLOY.json
       if convenient, else show "testnet" — no fictional address).
    Map the rail item -> anchor exactly per the Navigation Contract; do NOT invent routes.
  </action>
  <verify>
    <automated>cd dashboard && test -f src/components/layout/Rail.tsx && grep -q 'scrollIntoView' src/components/layout/Rail.tsx && grep -q 'IntersectionObserver' src/components/layout/Rail.tsx && grep -qE 'Tooltip' src/components/layout/Rail.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo RAIL_OK</automated>
  </verify>
  <done>Rail.tsx exists, renders the 4 grouped nav sections, scroll-anchors via scrollIntoView, marks 4 items disabled with tooltips, tracks active via IntersectionObserver; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin Header.tsx into the db-bar top bar</name>
  <read_first>
    - dashboard/src/components/layout/Header.tsx (current 39-line Tailwind top bar)
    - DeepVault-handoff/project/dashboard.html lines 60-71 (the <header class="db-bar"> markup)
    - DeepVault-handoff/project/dashboard.css lines 76-114 (.db-bar, .db-bar-l/-r, .db-pill, .db-avatar, .crumb)
    - 04.1-PATTERNS.md "Header.tsx — top bar -> db-bar" section
    - 04.1-UI-SPEC.md "Top bar — db-bar" + Per-Panel Mapping #12
  </read_first>
  <action>
    Reskin Header.tsx to the handoff `db-bar`. Replace the 28px `<h1>PLP Risk Studio</h1>`
    with a left breadcrumb (`DeepVault / Risk Studio / Overview` — handoff `.crumb`,
    Geist Mono 11.5px, `<em>` separators at font-style:normal). Right side `db-bar-r`:
    - `live · testnet` pill — fold the existing `RelayStatusPill` here (reskinned in a
      later plan; for now just place it). Keep the `wsState` prop wiring.
    - `GlobalStalenessPill` — keep it in the pill row.
    - ⌘K search pill (`db-pill mono`, static, non-functional — visual only per handoff).
    - UTC clock pill — bind to a live `Date()` (client-side, updates per minute).
    - `Deposit DUSDC` primary button (`btn btn-primary sm`) — this is the trigger that
      opens the existing DepositWithdrawPanel deposit dialog. Wire it to the same dialog
      open mechanism the panel exposes (App.tsx coordinates — see Task 3); if the panel
      controls its own open state, lift a shared open-state to App and pass an `onClick`.
    - Wallet slot: the existing dapp-kit `<ConnectButton>` — reskin its wrapper to the
      `db-avatar` pill chrome (do NOT replace ConnectButton; it owns the wallet flow).
    Sticky (top:0, z-50), full-bleed via `margin: 0 -28px` + re-padding, background
    `color-mix(in oklab, var(--bg) 80%, transparent)` + `backdrop-filter: blur(12px)`,
    1px bottom border. Leave a placeholder slot in the `db-pill` row for the theme
    toggle — Plan 05 mounts the toggle control there. Copy: `Deposit DUSDC` (UI-SPEC
    R-6 — normalize handoff `dUSDC`).
    Preserve the `data-testid="header-controls"` attribute and the `wsState`/`snapshot`
    prop signature.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'ConnectButton' src/components/layout/Header.tsx && grep -qiE 'crumb|breadcrumb' src/components/layout/Header.tsx && grep -q 'Deposit DUSDC' src/components/layout/Header.tsx && grep -q 'wsState' src/components/layout/Header.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo HEADER_OK</automated>
  </verify>
  <done>Header.tsx renders the db-bar (breadcrumb, pills, Deposit DUSDC button, ConnectButton wallet pill); RelayStatusPill + GlobalStalenessPill + wsState wiring intact; theme-toggle slot reserved.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure App.tsx into the rail + headline + 3-row grid shell</name>
  <read_first>
    - dashboard/src/App.tsx (current flat space-y-8 stack, lines 42-109)
    - DeepVault-handoff/project/dashboard.html lines 13-101 (dash-body, db-headline, db-row r1)
    - DeepVault-handoff/project/dashboard.css lines 4-10, 71-74, 115-187 (.dash-body grid, .dash, .db-headline, .dh-stats, .db-row)
    - 04.1-PATTERNS.md "Layout Shell — Current State" section
    - 04.1-UI-SPEC.md "Layout Contract" — Shell, Headline, Main grid (3 rows), LD-1 side-column resolution
  </read_first>
  <action>
    Restructure App.tsx. Replace the `min-h-screen bg-slate-950` wrapper and the
    `mx-auto max-w-[1280px] px-6 py-12 space-y-8` main with the handoff `dash-body`
    shell:
    1. Outer: `dash-body` — `display:grid; grid-template-columns: 232px 1fr; min-height:100vh`.
    2. Left column: `<Rail />` (from Task 1).
    3. Right column `<main class="dash">` — `padding: 0 28px 80px`.
       - `<Header wsState={state} snapshot={snapshot} />` (db-bar) at the top.
       - The relay reconnect/down inline alert — KEEP the existing block verbatim
         (UI-SPEC marks the copy as already-shipped), recolor to --hedge in a later
         plan; structurally it stays above the row grid.
       - Headline section (`db-headline`, `grid-template-columns: 1fr auto`,
         padding 36px 0 32px, 1px bottom border): left = eyebrow kicker
         `PLP Risk Studio` + `<h1>` headline; right = `dh-stats` 4-cell strip.
         Headline copy has two variants (UI-SPEC R-5): if `hedges.length > 0` ->
         "Vault is healthy. <span>Hedge</span> is in position."; if zero hedges ->
         "Vault is live. <span>Hedge</span> engages on first deposit." The accent
         span is the word "Hedge" (Geist 300, mint-tinted).
         `dh-stats` 4 cells: Net APY / Hedge ratio / Utilization / Vault NAV. Bind
         each to `vaultView` (useVaultState) + `hedges` per UI-SPEC Data Binding
         table. Demo-open values: Net APY `—` "awaiting first cycle"; Hedge ratio
         `0.0%` "target {lo}–{hi}" from strategy_constants; Utilization real low %;
         Vault NAV `~$10`. No fictional inflation. (Building the headline strip
         here satisfies UI-SPEC #7; VaultPanel's gauge becomes feeder data.)
       - 3-row grid: three `<section class="db-row">` blocks, each
         `grid-template-columns: 1.6fr 1fr; gap: 20px; padding: 24px 0`, 1px bottom border.
         r1: left = `<section data-section="hero"><SurfacePanel surface={surface}/>
         + <ArbCheckerPanel surface={surface}/></section>` (SVI card; arb panel stays
         per R-2); right = `db-side` column (`display:grid; gap:20px; align-content:start`)
         holding `<BucketGauge/>` + `<section data-section="exposure"><ExposurePanel
         hedges={hedges} vault={snapshot?.vault ?? null}/></section>`. NO Greeks card (LD-1).
         r2: left = `<section data-section="position-viewer"><PositionViewer
         positions={positions} vault={snapshot?.vault ?? null}/></section>`; right =
         `<section data-section="what-if"><WhatIfSimulator hedges={hedges}
         surface={surface} sigma={sigma}/></section>`.
         r3: left = backtest link card (a small inline `db-card` — static "Backtest
         report" link card per LD-3; copy per UI-SPEC Copywriting; link to the Phase 3
         static HTML report; NO live widget); right = `<EventStreamPanel>` slot —
         Plan 05 creates the component, so for THIS plan leave a clearly-commented
         placeholder `<div data-section-placeholder="event-stream" />` (do not import a
         nonexistent module). App.tsx will be edited again by Plan 05 to mount it.
       - `<section data-section="deposit-withdraw"><DepositWithdrawPanel
         vaultView={vaultView}/></section>` — place it where the layout makes sense
         (below r3 or as a row); keep the data-section attr for the rail Wallet anchor.
    KEEP every data-section attribute (the rail anchors target them). KEEP the
    useWebSocket spine and every panel prop EXACTLY as in App.tsx:42-48 — this is a
    layout move, not a rewire. If the Deposit-button-opens-dialog needs shared open
    state, lift it to App.tsx and thread it to both Header and DepositWithdrawPanel.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'useWebSocket' src/App.tsx && grep -q 'useSurfaceSnapshot' src/App.tsx && grep -q 'usePositions' src/App.tsx && grep -c 'data-section' src/App.tsx && grep -q 'Rail' src/App.tsx && ! grep -q 'space-y-8' src/App.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo APP_SHELL_OK</automated>
  </verify>
  <done>App.tsx renders the dash-body rail+main shell with the db-headline + dh-stats strip and the 3-row grid; all 8 panels placed with unchanged props; data spine + data-section attrs preserved; event-stream slot is a placeholder; tsc clean and `pnpm build` succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App.tsx layout move | the panels' data props cross from the old stack into the new grid — wiring must not silently drop a prop |
| rail scroll-anchor | DOM query for [data-section] elements — a missing anchor is a dead nav link, not a security issue |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-04 | Tampering | App.tsx data spine | mitigate | The no-regression acceptance gate greps for useWebSocket + every derived hook in App.tsx after the restructure; tsc verifies every panel still receives its typed props. |
| T-04.1-05 | Information disclosure | Rail.tsx network meta | accept | The "testnet · 0x…" footer shows a public testnet vault address from TESTNET-DEPLOY.json — public on-chain data, no secret. |
| T-04.1-06 | Denial of service | IntersectionObserver in Rail | accept | A single observer over ~7 section elements — negligible cost; standard browser API, no untrusted input. |
</threat_model>

<verification>
- `grep useWebSocket dashboard/src/App.tsx` matches — data spine intact
- `grep -c data-section dashboard/src/App.tsx` >= 5 — scroll anchors preserved
- `grep scrollIntoView dashboard/src/components/layout/Rail.tsx` matches
- `grep 'Deposit DUSDC' dashboard/src/components/layout/Header.tsx` matches
- `cd dashboard && pnpm exec tsc --noEmit && pnpm build` both succeed
- Manual: dashboard renders rail + headline + 3 rows; all panels show live data (Phase 4 behavior intact — no DASH-01..13 regression)
</verification>

<success_criteria>
- App.tsx renders the rail + main + headline + 3-row grid shell faithfully to dashboard.html
- Rail.tsx exists with grouped scroll-anchor nav, disabled-item tooltips, IntersectionObserver active tracking
- Header.tsx reskinned to db-bar with breadcrumb, pills, Deposit DUSDC button, ConnectButton wallet pill
- Every panel still renders with live WebSocket data — no Phase 4 behavior regression
- `pnpm build` succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dashboard-visual-polish/04.1-02-SUMMARY.md`
</output>
