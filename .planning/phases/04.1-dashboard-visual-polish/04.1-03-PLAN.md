---
phase: "04.1"
plan: "03"
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - dashboard/src/components/panels/SurfacePanel.tsx
  - dashboard/src/components/panels/ArbCheckerPanel.tsx
  - dashboard/src/components/panels/VaultPanel.tsx
requirements: ["D-01"]
autonomous: true
must_haves:
  truths:
    - "The SVI surface renders inside db-card chrome with the §Surface mark, db-tabs, svi-params strip, db-foot"
    - "The arb-checker g(k) plot is retained, reskinned to db-card, recolored mint/coral"
    - "The Plotly surface still re-renders live on OracleSVIUpdated events"
    - "VaultPanel keeps its useVaultState-derived NAV/utilization data path as a pure feeder for the headline strip — it renders no standalone card"
  artifacts:
    - path: "dashboard/src/components/panels/SurfacePanel.tsx"
      provides: "SVI surface reskinned to db-card with mint Plotly colorway"
      contains: "useMemo"
    - path: "dashboard/src/components/panels/ArbCheckerPanel.tsx"
      provides: "g(k) plot reskinned to db-card, mint/coral recolor (R-2 retained)"
  key_links:
    - from: "dashboard/src/components/panels/SurfacePanel.tsx"
      to: "useSurfaceSnapshot"
      via: "surface prop — Phase 4 data hook unchanged"
      pattern: "surface"
---

<objective>
Reskin the two Risk Studio chart panels — SurfacePanel (3D SVI surface), ArbCheckerPanel
(g(k) plot) — to the handoff `db-card` visual language, and convert VaultPanel into a
pure data feeder (its card UI is dropped; its `useVaultState` derivation stays as the
source for the headline `dh-stats` strip built in Plan 02). Styling/markup/chart-color
changes only; the Phase 4 data hooks, Plotly `useMemo`+`revision` perf logic, `checkArb`
compute, and `useVaultState` derivation stay intact.

Purpose: SurfacePanel + ArbCheckerPanel form the SVI surface card (UI-SPEC #1) and the
g(k) differentiator (R-2). VaultPanel was reframed by UI-SPEC #7 into the headline
strip — Plan 02 builds that strip and renders NO standalone <VaultPanel> card, so this
plan strips VaultPanel's card UI while preserving the `useVaultState` data path the
strip consumes. Reskinning these in a dedicated plan keeps the chart-color and chrome
work focused and the no-regression check tight.

Output: SurfacePanel + ArbCheckerPanel reskinned in place; VaultPanel converted to a
pure data-feeder (card UI removed, data path preserved).

Covers CONTEXT decision D-01 (panel-reskin portion), UI-SPEC #1, #7, R-2.
This plan runs in parallel with Plan 04 — zero file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-UI-SPEC.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-PATTERNS.md
@.planning/phases/04-plp-risk-studio-dashboard-relay/04-VERIFICATION.md
@DeepVault-handoff/project/dashboard.html
@DeepVault-handoff/project/dashboard.css
@dashboard/src/components/panels/SurfacePanel.tsx
@dashboard/src/components/panels/ArbCheckerPanel.tsx
@dashboard/src/components/panels/VaultPanel.tsx

<interfaces>
<!-- Chart colors — import from Plan 01's CHART_COLORS, do NOT hardcode hex -->
import { CHART_COLORS, MINT_COLORSCALE } from '@/lib/dashboard_constants';
  CHART_COLORS.accent (mint), .hedge (coral), .text2, .muted, .gridLine, .surface

<!-- Component classes — defined by Plan 01 Task 2 in globals.css.
     .db-card/.db-h/.db-mark/.db-tabs/.db-foot/.svi-card/.svi-params resolve there;
     apply by className, do not re-derive the CSS. -->

<!-- Handoff db-card chrome reference (dashboard.css:181-235) -->
.db-card  -> bg var(--bg-2), 1px border var(--border), radius var(--r-3) 14px
.db-card.pad -> padding 20px 22px
.db-h header -> db-mark "§ X" (Geist Mono 10.5px uppercase) + h2 22px / h3 16px Geist 500
.db-tabs em -> Geist Mono 11.5px; em.on -> 1px mint border-bottom
.svi-params  -> a/b/ρ/m/σ row; .ok -> mint; .right -> margin-left:auto
.db-foot -> footer row, mono dim

<!-- Phase 4 data props — DO NOT change -->
SurfacePanel({ surface })       <- useSurfaceSnapshot(snapshot)
ArbCheckerPanel({ surface })    <- useSurfaceSnapshot(snapshot)
VaultPanel — feeder only: keeps useVaultState; renders NO card (Plan 02 renders the
  headline dh-stats strip from vaultView; <VaultPanel> is not mounted in App.tsx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin SurfacePanel to the §Surface db-card</name>
  <read_first>
    - dashboard/src/components/panels/SurfacePanel.tsx (current shadcn Card + Plotly, slate colorway lines 123-131)
    - DeepVault-handoff/project/dashboard.html lines 104-152 (the svi-card markup: db-h, db-tabs, svi-params, db-foot)
    - DeepVault-handoff/project/dashboard.css lines 181-235 (.db-card, .db-h, .db-tabs, .svi-params)
    - the component-class layer transcribed by Plan 01 Task 2 into dashboard/src/styles/globals.css (the .db-card/.db-h/.db-mark/.db-tabs/.svi-card/.svi-params classes resolve there — apply by className)
    - 04.1-PATTERNS.md "SurfacePanel.tsx — SVI 3D surface card" section
    - 04.1-UI-SPEC.md Per-Panel Mapping #1
  </read_first>
  <action>
    Reskin SurfacePanel to the handoff `svi-card`. Wrap the Plotly surface in `db-card`
    chrome (the `.db-card`/`.svi-card`/`.db-h`/`.db-tabs`/`.svi-params`/`.db-foot`
    classes are defined in globals.css by Plan 01 Task 2 — apply via className):
    - `db-h` header: `db-mark` "§ Surface" + `<h2>SVI · live</h2>`; `db-tabs` row with
      em items "3D mesh" (on), "Slice · τ=7d", "Slice · τ=14d", "replay ⏮" (dim).
      The tabs may be visual-only for v1 (the handoff tabs switch surface views) — if
      switching the Plotly view is trivial, wire 3D-mesh-active; otherwise render the
      tabs styled and only the 3D-mesh tab functional, with the slice tabs dim. Do not
      regress the existing surface render.
    - `svi-params` strip below the header: a/b/ρ/m/σ values from `surface` (the SVI
      params via useSurfaceSnapshot) + `butterfly · ok` / `calendar · ok` chips (mint
      `.ok`) + a right-aligned "Ns ago" mono dim. At demo-open (no event) show the
      empty-state copy per UI-SPEC ("Waiting for surface data" — heading + body).
    - The Plotly surface itself: KEEP `type:'surface'`, the `useMemo` data memoization
      and the `revision`-prop redraw logic (SurfacePanel.tsx:66-152) UNCHANGED — that
      is data/perf, not style. Recolor only: replace the slate Plotly colorway
      (`#94a3b8`, `font.family:'Inter'`) with `MINT_COLORSCALE` for the surface
      `colorscale`, `CHART_COLORS.text2` for axis `color`, and the Geist font string
      for `font.family`. Recolor axis/grid lines to `CHART_COLORS.gridLine`.
    - `db-foot`: "Source · OracleSVIUpdated via predict-server.testnet" + a mono dim
      event count (bind to a real count from the snapshot if available, else omit the
      number — no fictional "4,182").
    Import colors from CHART_COLORS (Plan 01) — no hardcoded hex. KEEP the `surface`
    prop and `useSurfaceSnapshot` data path.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'useMemo' src/components/panels/SurfacePanel.tsx && grep -q "type: 'surface'" src/components/panels/SurfacePanel.tsx && grep -qE 'CHART_COLORS|MINT_COLORSCALE' src/components/panels/SurfacePanel.tsx && ! grep -qE '#94a3b8|#06b6d4' src/components/panels/SurfacePanel.tsx && grep -q 'surface' src/components/panels/SurfacePanel.tsx && grep -q 'Waiting for surface data' src/components/panels/SurfacePanel.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo SURFACE_OK</automated>
  </verify>
  <done>SurfacePanel renders db-card chrome (§Surface mark, db-tabs, svi-params, db-foot), Plotly surface uses mint colorway via CHART_COLORS, no hardcoded slate/cyan hex; Plotly type:'surface' + useMemo/revision logic intact; empty-state copy present; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin ArbCheckerPanel — retain g(k) plot, recolor mint/coral</name>
  <read_first>
    - dashboard/src/components/panels/ArbCheckerPanel.tsx (current Card+Collapsible+Badge, hardcoded #06b6d4/#e11d48/emerald lines 126-145, 192-228)
    - DeepVault-handoff/project/dashboard.css lines 181-235 (.db-card chrome)
    - the component-class layer transcribed by Plan 01 Task 2 into dashboard/src/styles/globals.css (the .db-card/.db-h/.db-mark classes resolve there — apply by className)
    - 04.1-PATTERNS.md "ArbCheckerPanel.tsx — arb-checker g(k) plot" section
    - 04.1-UI-SPEC.md Per-Panel Mapping "Components reskinned but not structurally changed" + researcher decision R-2
  </read_first>
  <action>
    Reskin ArbCheckerPanel chrome to `db-card` (the `.db-card`/`.db-h`/`.db-mark`
    classes are defined in globals.css by Plan 01 Task 2 — apply via className). R-2 is
    a LOAD-BEARING KEEP: the g(k) 200-point plot panel is RETAINED (DASH-05 / MATH-04
    differentiator) even though the handoff folds arb status into the svi-params strip.
    Do NOT drop the g(k) plot.
    - Wrap in `db-card` chrome with a `db-h` header (`db-mark` "§ Arb-free" + h3 title).
      The panel sits inside/below the SVI surface card region (App.tsx Wave 2 already
      placed both under data-section="hero").
    - Recolor: the GREEN status `<Badge>` -> `CHART_COLORS.accent` mint;
      the RED status `<Badge>` -> `CHART_COLORS.hedge` coral;
      the Recharts `<Line stroke="#06b6d4">` -> `CHART_COLORS.accent`;
      the `<ReferenceLine stroke="#e11d48">` -> `CHART_COLORS.hedge`.
    - KEEP the `checkArb` `useMemo`, the 200-point g(k) data, and the
      auto-expand-on-RED `<Collapsible>` logic — those are Phase 4 verified behavior.
    Import colors from CHART_COLORS — no hardcoded hex.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'checkArb' src/components/panels/ArbCheckerPanel.tsx && grep -q 'CHART_COLORS' src/components/panels/ArbCheckerPanel.tsx && ! grep -qE '#06b6d4|#e11d48|#10b981' src/components/panels/ArbCheckerPanel.tsx && grep -qE 'Line|ReferenceLine' src/components/panels/ArbCheckerPanel.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo ARB_OK</automated>
  </verify>
  <done>ArbCheckerPanel reskinned to db-card, g(k) plot retained, status badge + line + reference-line recolored to mint/coral via CHART_COLORS; checkArb + auto-expand-on-RED logic intact; no hardcoded hex; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Convert VaultPanel to a pure data feeder — strip its card UI, keep useVaultState</name>
  <read_first>
    - dashboard/src/components/panels/VaultPanel.tsx (current Card + RadialBarChart, hardcoded #06b6d4 line 60/105-153)
    - dashboard/src/App.tsx (the db-headline dh-stats strip Plan 02 built — it consumes vaultView; <VaultPanel> is NOT mounted)
    - 04.1-PATTERNS.md "VaultPanel.tsx — feeds the headline 4-stat strip" section
    - 04.1-UI-SPEC.md Per-Panel Mapping #7 ("VaultPanel feeds the dh-stats headline strip")
  </read_first>
  <action>
    RESOLVED FATE (unconditional — Plan 02 made the decision concrete): UI-SPEC #7
    reframes VaultPanel's state into the headline `dh-stats` strip, which Plan 02 builds
    inside App.tsx. Plan 02 renders that strip directly from `vaultView`
    (`useVaultState(snapshot)`) and does NOT mount a standalone `<VaultPanel>` card.
    Therefore VaultPanel becomes a PURE DATA FEEDER — it must no longer render a card.

    Do this, unconditionally (no "if Plan 02 relocated..." branch):
    1. Remove VaultPanel's card UI — delete the shadcn `<Card>`/`<CardHeader>`/
       `<CardContent>` JSX, the local `Stat` helper render, and the
       `RadialBarChart`/Recharts gauge entirely. The component stops returning a
       dashboard card.
    2. KEEP the `useVaultState` hook usage and the `format.ts` calls — preserve the
       `VaultView`-derived figures (NAV, Net APY, hedge ratio, utilization, shares).
       Either: (a) keep VaultPanel as a thin module that re-exports a typed selector /
       helper (e.g. a `useVaultStats()` or a pure function) that the headline strip
       can consume, OR (b) if App.tsx already calls `useVaultState` directly for the
       strip (it does — `vaultView` in the spine), reduce VaultPanel to nothing that
       renders UI and remove the now-dead Recharts import.
    3. Because the Recharts `RadialBarChart` and its `fill:'#06b6d4'` are deleted,
       there is no chart hex left to recolor — but assert no `#06b6d4`/`#10b981`/
       `#94a3b8` literal survives anywhere in the file.
    4. No Phase-4 data regression: the `useVaultState` selector and the typed
       `VaultView` shape stay intact and importable — the headline strip's bindings
       (UI-SPEC Data Binding table) must still resolve. tsc must stay clean (no dead
       imports, no unused-export errors).
    Net effect: VaultPanel renders no UI; its data path lives on for the headline
    strip. The demo-open empty values (`—` / `0.0%` / real low % / `~$10`) are
    handled by the headline strip in Plan 02 — VaultPanel just supplies the numbers.
  </action>
  <verify>
    <automated>cd dashboard && grep -qE 'useVaultState|VaultView' src/components/panels/VaultPanel.tsx && ! grep -qE '#06b6d4|#10b981|#94a3b8' src/components/panels/VaultPanel.tsx && ! grep -qE 'RadialBarChart|<Card' src/components/panels/VaultPanel.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo VAULT_OK</automated>
  </verify>
  <done>VaultPanel renders no standalone card (shadcn Card + RadialBarChart removed), no hardcoded hex; the useVaultState/VaultView data path is intact and importable for the Plan-02 headline strip; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| relay snapshot -> chart render | SVI params + g(k) array from the relay flow into Plotly/Recharts — rendered as numeric data, not markup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-07 | Tampering | SurfacePanel Plotly data | accept | SVI params/surface grid are numeric arrays passed to Plotly's `type:'surface'` — Plotly renders them as geometry, no HTML injection surface. Data path unchanged from Phase 4 (verified). |
| T-04.1-08 | Information disclosure | CHART_COLORS import | accept | Chart colors are public constants — no secret material. |
| T-04.1-09 | Spoofing | arb-checker status badge | mitigate | The GREEN/RED status derives from `checkArb` over real g(k) — the reskin recolors only; the boolean/array logic is untouched so a false "GREEN" cannot be introduced by styling. |
</threat_model>

<verification>
- `grep checkArb dashboard/src/components/panels/ArbCheckerPanel.tsx` matches — g(k) logic intact
- `grep "type: 'surface'" dashboard/src/components/panels/SurfacePanel.tsx` matches — Plotly surface intact
- `grep useVaultState dashboard/src/components/panels/VaultPanel.tsx` matches — feeder data path intact
- No `#06b6d4` / `#e11d48` / `#94a3b8` / `#10b981` in any of the 3 files
- `grep CHART_COLORS` matches in SurfacePanel + ArbCheckerPanel
- `cd dashboard && pnpm exec tsc --noEmit && pnpm test` — Phase 4 panel tests still pass
- Manual: SVI surface still re-renders on a live OracleSVIUpdated event (DASH-04 not regressed); arb checker still flips GREEN/RED (DASH-05 not regressed); headline dh-stats strip still shows vault figures (DASH-06 not regressed)
</verification>

<success_criteria>
- SurfacePanel + ArbCheckerPanel reskinned to db-card chrome
- VaultPanel converted to a pure data feeder — no standalone card, useVaultState path intact
- All chart colors sourced from CHART_COLORS — zero hardcoded hex
- g(k) plot retained (R-2); Plotly surface render + checkArb + useVaultState logic intact
- No Phase 4 behavior regression — DASH-04/05/06 verified behavior preserved
- tsc clean, Phase 4 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dashboard-visual-polish/04.1-03-SUMMARY.md`
</output>
