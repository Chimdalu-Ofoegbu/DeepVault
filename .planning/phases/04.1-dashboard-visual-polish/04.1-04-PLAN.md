---
phase: "04.1"
plan: "04"
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02"]
files_modified:
  - dashboard/src/components/panels/BucketGauge.tsx
  - dashboard/src/components/panels/ExposurePanel.tsx
  - dashboard/src/components/panels/WhatIfSimulator.tsx
  - dashboard/src/components/panels/PositionViewer.tsx
  - dashboard/src/components/panels/DepositWithdrawPanel.tsx
  - dashboard/src/components/primitives/StalenessPill.tsx
  - dashboard/src/components/primitives/NumericValue.tsx
  - dashboard/src/components/primitives/TxDigestLink.tsx
  - dashboard/src/components/layout/RelayStatusPill.tsx
  - dashboard/src/components/layout/GlobalStalenessPill.tsx
requirements: ["D-01"]
autonomous: true
must_haves:
  truths:
    - "Token bucket renders the 44px mono bucket-num + mint bucket-bar in db-card chrome"
    - "Per-oracle exposure shows a single BTC row, no ETH/SOL/SUI"
    - "What-if simulator uses a 7-button shock-row, not 2 sliders"
    - "Positions table shows BTC legs only, no asset-filter tabs, in pos-table styling"
    - "Deposit/withdraw 3-step PTB sign flow is unchanged — chrome reskin only"
    - "Status pills render mint (live/fresh) / coral (stale/down)"
  artifacts:
    - path: "dashboard/src/components/panels/EventStreamPanel.tsx"
      provides: "N/A — created in Plan 05"
    - path: "dashboard/src/components/panels/WhatIfSimulator.tsx"
      provides: "7-button shock-row + shock-stats grid + shock-bar (whatIf.ts compute reused)"
      contains: "whatIf"
  key_links:
    - from: "dashboard/src/components/panels/DepositWithdrawPanel.tsx"
      to: "ptbBuilders.ts"
      via: "PTB construction + dapp-kit sign flow — unchanged"
      pattern: "ptb|Transaction"
---

<objective>
Reskin the Vault/Account panels and shared status primitives to the handoff visual
language: BucketGauge -> §Withdrawals bucket card, ExposurePanel -> BTC-only §Per-oracle
list, WhatIfSimulator -> 7-button shock-row (R-1), PositionViewer -> BTC-only pos-table
(LD-2), DepositWithdrawPanel -> db-card/btn/dialog chrome, and the 5 status primitives
recolored to mint/coral. Styling/markup/input-affordance changes only — every data
hook, the `whatIf.ts` compute, and the PTB sign flow are preserved.

Purpose: These 5 panels + 5 primitives are the rest of the 8-panel reskin (UI-SPEC #2,
#3, #5, #6, #8). The WhatIfSimulator input change (sliders -> buttons, R-1) and the
BTC-only enforcement (LD-2) are the substantive deltas. The DepositWithdrawPanel reskin
must not touch the wallet-flow code path.

Output: 5 panels + 5 primitives reskinned in place.

Covers CONTEXT decision D-01 (panel-reskin portion), UI-SPEC #2, #3, #5, #6, #8, #12,
LD-2, R-1, R-6.
This plan runs in parallel with Plan 03 — zero file overlap.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-UI-SPEC.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-PATTERNS.md
@.planning/phases/04-plp-risk-studio-dashboard-relay/04-VERIFICATION.md
@DeepVault-handoff/project/dashboard.html
@DeepVault-handoff/project/dashboard.css
@dashboard/src/components/panels/BucketGauge.tsx
@dashboard/src/components/panels/ExposurePanel.tsx
@dashboard/src/components/panels/WhatIfSimulator.tsx
@dashboard/src/components/panels/PositionViewer.tsx
@dashboard/src/components/panels/DepositWithdrawPanel.tsx

<interfaces>
import { CHART_COLORS } from '@/lib/dashboard_constants';  // mint/coral hex — Plan 01

<!-- Handoff markup refs (dashboard.html / dashboard.css) -->
bucket card: dashboard.html:155-169, css:241-258 — bucket-num 44px mono, bucket-bar mint, bucket-foot
oracles list: dashboard.html:175-181, css:260-277 — li {label, %, .bar with --w + ::after mint}
shock card: dashboard.html:288-322, css — shock-row 7 buttons, shock-stats ss-row grid, shock-bar
pos-table: dashboard.html:212-281, css — mono 12.5px, th uppercase 10.5px, leg-dot, chip ok/warn

<!-- Phase 4 data props — DO NOT change -->
BucketGauge()                           <- useBucketState + useCurrentAccount
ExposurePanel({ hedges, vault })        <- useExposure
WhatIfSimulator({ hedges, surface, sigma }) <- whatIf.ts client-side compute
PositionViewer({ positions, vault })    <- usePositions
DepositWithdrawPanel({ vaultView })     <- ptbBuilders.ts + dapp-kit signing
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reskin BucketGauge and ExposurePanel (BTC-only)</name>
  <read_first>
    - dashboard/src/components/panels/BucketGauge.tsx (current Card + RadialBarChart, hardcoded escalation hex line 56-57)
    - dashboard/src/components/panels/ExposurePanel.tsx (current Card+Table+BarChart, hardcoded #94a3b8 lines 118-150; empty-state lines 53-95)
    - DeepVault-handoff/project/dashboard.html lines 155-181 (bucket + oracles markup)
    - DeepVault-handoff/project/dashboard.css lines 241-277 (.bucket, .bucket-num, .bucket-bar, .oracles)
    - 04.1-PATTERNS.md "BucketGauge.tsx" + "ExposurePanel.tsx" sections
    - 04.1-UI-SPEC.md Per-Panel Mapping #2, #3; Copywriting empty-state rows for Token bucket + Per-oracle exposure
  </read_first>
  <action>
    BucketGauge -> `§ Withdrawals` `db-card.pad`. Replace the Recharts RadialBarChart
    with the handoff `bucket` layout: `bucket-num` = 44px Geist Mono big number showing
    real available % of NAV (with a 22px muted `<i>%</i>` suffix); `bucket-l` sub-label
    "of NAV available now"; `bucket-bar` = a track with a mint `<i>` fill at width =
    available %; `bucket-foot` = "queue · N" / "next refill · Ns". Header `db-mark`
    "§ Withdrawals" + `<h3>Token bucket</h3>` + a mono dim "refills X / s". Recolor the
    3-state escalation (the `>=70 / >=30 / else` hex ternary) to mint (`CHART_COLORS.accent`)
    for healthy and coral (`CHART_COLORS.hedge`) for low. KEEP the 3-state wallet logic
    and `useBucketState`. Demo-open: real available %, footer `queue · 0` (UI-SPEC copy).

    ExposurePanel -> `§ Per-oracle` `db-card.pad`. Replace the Recharts horizontal
    BarChart with the handoff CSS `oracles` bar-list: an `<ul>` of `<li>` rows, each
    {label / bold % / a `.bar` element with `style={{'--w': pct}}` and a mint `::after`
    fill}. **BTC row only** — drop the ETH/SOL/SUI rows entirely (LD-2). Empty state
    (zero exposure): a single BTC row at 0% with body copy "BTC oracle · 0% exposure —
    no hedge legs open yet." (UI-SPEC verbatim). KEEP `useExposure`. The Recharts import
    can be removed from this file if no chart remains.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'useBucketState' src/components/panels/BucketGauge.tsx && grep -qiE 'bucket-num|44' src/components/panels/BucketGauge.tsx && grep -q 'useExposure' src/components/panels/ExposurePanel.tsx && ! grep -qiE 'ETH|SOL|SUI' src/components/panels/ExposurePanel.tsx && ! grep -qE '#94a3b8|#10b981|#e11d48' src/components/panels/ExposurePanel.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo BUCKET_EXPOSURE_OK</automated>
  </verify>
  <done>BucketGauge renders the bucket-num/bucket-bar layout in §Withdrawals db-card; ExposurePanel renders the oracles bar-list with a single BTC row, no ETH/SOL/SUI, no hardcoded hex; data hooks intact; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Reskin WhatIfSimulator — 7 shock buttons replace the 2 sliders (R-1)</name>
  <read_first>
    - dashboard/src/components/panels/WhatIfSimulator.tsx (current Card + 2 Sliders lines 306-333, shockedPnL/whatIf compute lines 149-178, Recharts lines 300-375)
    - DeepVault-handoff/project/dashboard.html lines 288-322 (shock-row, shock-stats, shock-bar markup)
    - DeepVault-handoff/project/dashboard.js (the shocks() interaction reference)
    - 04.1-PATTERNS.md "WhatIfSimulator.tsx" section
    - 04.1-UI-SPEC.md Per-Panel Mapping #6, researcher decision R-1, Copywriting empty-state for What-if
  </read_first>
  <action>
    Reskin WhatIfSimulator to the handoff `§ What-if` "Shock the book" `db-card.pad`.
    Per R-1, the INPUT AFFORDANCE changes: REMOVE the 2 shadcn `<Slider>`s
    (WhatIfSimulator.tsx:306-333) and replace with a `shock-row` of 7 discrete buttons:
    −7σ, −5σ, −3σ, −2σ, flat, +2σ, +5σ. Clicking a button sets it `.active` (coral
    18%-mix bg + coral border via CHART_COLORS.hedge), clears the others, and feeds the
    corresponding σ multiplier into the EXISTING `shockedPnL` / `whatIf.ts` compute
    (WhatIfSimulator.tsx:149-178) — that compute is REUSED UNCHANGED; only the input
    widget changes from a continuous slider value to one of 7 discrete σ steps.
    Output layout: a `shock-stats` grid of `ss-row` items (Spot BTC / Vault NAV / Hedge
    payoff / Utilization / Withdraw budget / Liquidations) and a `shock-bar` loss-capped
    comparison bar (coral `sb-bad` unhedged vs mint `sb-good` DeepVault). Bind every
    figure to the real `whatIf.ts` output over real `hedges` — no fictional numbers.
    Empty state (zero hedges): shock buttons remain visible but the stats panel shows
    the disabled-state copy "Shock scenarios activate once the vault holds a hedge.
    Deposit DUSDC to open the first leg." (UI-SPEC verbatim). KEEP the Preview-mode
    logic. Recolor any retained Recharts to mint/coral via CHART_COLORS.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'whatIf\|shockedPnL' src/components/panels/WhatIfSimulator.tsx && ! grep -qE 'from .*ui/slider|<Slider' src/components/panels/WhatIfSimulator.tsx && grep -qiE 'shock' src/components/panels/WhatIfSimulator.tsx && grep -q 'Shock scenarios activate' src/components/panels/WhatIfSimulator.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo WHATIF_OK</automated>
  </verify>
  <done>WhatIfSimulator uses a 7-button shock-row (no Slider import), shock-stats grid + shock-bar; whatIf.ts/shockedPnL compute reused unchanged; empty-state copy present; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Reskin PositionViewer (BTC-only pos-table) and DepositWithdrawPanel chrome</name>
  <read_first>
    - dashboard/src/components/panels/PositionViewer.tsx (current Table+Tooltip, asset-filter tabs, hardcoded emerald/rose/cyan lines 181-184/202-229)
    - dashboard/src/components/panels/DepositWithdrawPanel.tsx (current Dialog+Tabs+Input, 3-step flow, PTB builders)
    - DeepVault-handoff/project/dashboard.html lines 201-286 (pos-table markup, leg-dot, chip)
    - 04.1-PATTERNS.md "PositionViewer.tsx" + "DepositWithdrawPanel.tsx" sections
    - 04.1-UI-SPEC.md Per-Panel Mapping #5, #8, LD-2, R-6, Copywriting (destructive actions + position-viewer empty states)
  </read_first>
  <action>
    PositionViewer -> full-width `db-card` (no pad) with the handoff `pos-table`:
    Geist Mono 12.5px cells, uppercase 10.5px `th`. Columns per UI-SPEC #5: Leg /
    Underlying / Strike / Expiry / Notional / Δ-eq / Premium / Mark / PnL / status-chip.
    `leg-dot` coral for hedge rows, mint `leg-dot.src` for the PLP-supply row.
    **BTC legs only (LD-2)**: DROP the `All/BTC/ETH/SOL/SUI` asset-filter tabs entirely,
    replace with a single disabled `BTC` indicator `chip`. Recolor PnL columns to
    mint/coral via CHART_COLORS (positive -> accent, negative -> hedge). `db-foot` with
    leg count + last/next rebalance + premium drag. Two empty states (UI-SPEC verbatim):
    wallet-not-connected "Connect a wallet to view your vault position and PnL
    attribution."; connected-no-position "No position yet. Deposit DUSDC to receive
    vault shares." KEEP `usePositions`, the zero-vs-unknown nullable invariant, and the
    tooltips.

    DepositWithdrawPanel -> CHROME RESKIN ONLY. Reskin the shadcn `<Card>`/`<Dialog>`/
    `<Tabs>`/`<Input>`/`<Button>` to the handoff `db-card`/`btn`/`dialog` language.
    Deposit confirm button -> `--accent` mint; withdraw-request + cancel confirm
    buttons -> `--hedge` coral (UI-SPEC §Destructive). Withdraw confirmation copy:
    "Request withdrawal of {N} DUSDC? A 1-hour cooldown applies before you can claim.";
    cancel copy: "Cancel pending withdrawal request? Your escrowed shares are returned
    immediately." Normalize any `dUSDC` -> `DUSDC` (R-6). CRITICAL: do NOT touch the
    3-step Input->Review->Execute flow, the PTB builders (`ptbBuilders.ts`), the
    balance-check, the dapp-kit transaction-signing path, or the Sonner toasts — the
    reskin changes button/dialog styling and copy only. The PTB construction + sign
    flow stays byte-identical.
  </action>
  <verify>
    <automated>cd dashboard && grep -q 'usePositions' src/components/panels/PositionViewer.tsx && ! grep -qiE '"ETH"|"SOL"|"SUI"|All/BTC' src/components/panels/PositionViewer.tsx && ! grep -qE '#06b6d4|#10b981|#e11d48' src/components/panels/PositionViewer.tsx && grep -qiE 'ptb|Transaction|signAndExecute' src/components/panels/DepositWithdrawPanel.tsx && grep -q '1-hour cooldown' src/components/panels/DepositWithdrawPanel.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo POS_DEPOSIT_OK</automated>
  </verify>
  <done>PositionViewer renders the BTC-only pos-table (no asset-filter tabs, single BTC chip), PnL recolored, two empty states present; DepositWithdrawPanel chrome reskinned to db-card/btn/dialog with coral destructive buttons, PTB + sign flow untouched; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 4: Recolor the 5 status primitives to mint/coral tokens</name>
  <read_first>
    - dashboard/src/components/primitives/StalenessPill.tsx (hardcoded cyan/amber/rose lines 29-34)
    - dashboard/src/components/primitives/NumericValue.tsx, TxDigestLink.tsx
    - dashboard/src/components/layout/RelayStatusPill.tsx (hardcoded lines 25-30), GlobalStalenessPill.tsx
    - 04.1-PATTERNS.md "StalenessPill / RelayStatusPill / GlobalStalenessPill" + "Status pill chrome" sections
    - 04.1-UI-SPEC.md Per-Panel Mapping #12 + "Components reskinned but not structurally changed"
  </read_first>
  <action>
    Recolor-only pass on the 5 shared primitives — KEEP all `useStaleness` / `WsState`
    logic. StalenessPill: fresh/`LIVE` -> mint (`--accent` token classes),
    warning/stale/`STALE` -> coral (`--hedge`). RelayStatusPill: connected/`live`
    state -> mint with the pulsing `live-dot` aesthetic (the pulse keyframe lands in
    Plan 06 motion pass — for now apply the mint color + the dot element);
    reconnecting/down -> coral. GlobalStalenessPill wraps StalenessPill — inherits.
    NumericValue + TxDigestLink: recolor any slate/cyan/rose Tailwind to the mint/coral
    token classes; positive numerics -> mint, negative -> coral, links -> accent.
    Replace hardcoded `cyan-*/amber-*/rose-*/emerald-*` Tailwind classes with classes
    that resolve to the OKLCH tokens (e.g. `text-[var(--accent)]` /
    `text-[var(--hedge)]` or the re-pointed shadcn semantic classes). No hardcoded hex,
    no slate/cyan/amber/rose/emerald color literals.
  </action>
  <verify>
    <automated>cd dashboard && ! grep -rqE 'cyan-[0-9]|amber-[0-9]|rose-[0-9]|emerald-[0-9]' src/components/primitives/StalenessPill.tsx src/components/primitives/NumericValue.tsx src/components/primitives/TxDigestLink.tsx src/components/layout/RelayStatusPill.tsx src/components/layout/GlobalStalenessPill.tsx && grep -q 'useStaleness\|WsState\|state' src/components/layout/RelayStatusPill.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo PRIMITIVES_OK</automated>
  </verify>
  <done>The 5 primitives recolored to mint/coral tokens — no cyan/amber/rose/emerald literals; useStaleness/WsState logic intact; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user -> DepositWithdrawPanel -> dapp-kit sign | the wallet transaction-signing path — the one security-load-bearing flow in this plan |
| relay snapshot -> panel render | position/exposure data rendered as React-escaped text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-10 | Tampering | DepositWithdrawPanel PTB flow | mitigate | The reskin is chrome+copy only — Task 3 acceptance criterion greps for the PTB/signAndExecute path still present; the PTB builders, balance-check, and dapp-kit signing code are explicitly out of the edit scope. PTB construction stays byte-identical. |
| T-04.1-11 | Spoofing | Withdraw confirmation copy | mitigate | The destructive-action confirmation dialog (1-hour cooldown warning) is preserved verbatim from UI-SPEC — the user is not deprived of the cooldown disclosure by the reskin. Coral buttons visually flag destructiveness. |
| T-04.1-12 | Information disclosure | PositionViewer | accept | Position data is the connected wallet's own on-chain state, rendered via React's default text escaping — no `dangerouslySetInnerHTML`. No cross-wallet leakage (usePositions is wallet-scoped, unchanged). |
</threat_model>

<verification>
- `grep usePositions dashboard/src/components/panels/PositionViewer.tsx` matches
- `grep -E '"ETH"|"SOL"|"SUI"' dashboard/src/components/panels/{ExposurePanel,PositionViewer}.tsx` returns nothing (BTC-only, LD-2)
- `grep -E 'signAndExecute|Transaction' dashboard/src/components/panels/DepositWithdrawPanel.tsx` matches (PTB flow intact)
- `grep -E 'Slider' dashboard/src/components/panels/WhatIfSimulator.tsx` returns nothing (sliders removed, R-1)
- No `cyan-N/amber-N/rose-N/emerald-N` in the 5 primitives
- `cd dashboard && pnpm exec tsc --noEmit && pnpm test` — Phase 4 panel tests pass
- Manual: deposit dialog still signs a PTB (DASH-11 not regressed); withdraw still shows the cooldown confirmation (DASH-12)
</verification>

<success_criteria>
- BucketGauge, ExposurePanel, WhatIfSimulator, PositionViewer, DepositWithdrawPanel reskinned to handoff db-card language
- BTC-only enforced (LD-2): no ETH/SOL/SUI rows, no asset-filter tabs
- WhatIfSimulator uses 7 shock buttons (R-1); whatIf.ts compute reused unchanged
- DepositWithdrawPanel PTB + dapp-kit sign flow untouched — chrome/copy only
- 5 status primitives recolored to mint/coral; logic intact
- No Phase 4 behavior regression; tsc clean, tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dashboard-visual-polish/04.1-04-SUMMARY.md`
</output>
