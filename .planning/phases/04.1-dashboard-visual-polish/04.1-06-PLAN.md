---
phase: "04.1"
plan: "06"
type: execute
wave: 5
depends_on: ["04.1-02", "04.1-03", "04.1-04", "04.1-05"]
files_modified:
  - dashboard/src/styles/globals.css
  - dashboard/src/components/layout/DashboardLoader.tsx
  - dashboard/src/App.tsx
  - dashboard/src/components/layout/Rail.tsx
  - dashboard/src/components/panels/WhatIfSimulator.tsx
  - dashboard/src/components/panels/EventStreamPanel.tsx
requirements: ["D-04"]
autonomous: false
must_haves:
  truths:
    - "A dashboard loader plays an SVI-draw animation on first paint, then fades out"
    - "Rail items, cards, and table rows have ~0.12s hover/focus transitions"
    - "The live/net dots pulse via a CSS keyframe"
    - "The shock-button active toggle and event-ticker flash animate per the handoff"
  artifacts:
    - path: "dashboard/src/components/layout/DashboardLoader.tsx"
      provides: "first-paint SVI-draw loader overlay that fades out"
      contains: "loader"
    - path: "dashboard/src/styles/globals.css"
      provides: "@keyframes pulse / dash + hover-transition rules"
      contains: "@keyframes"
  key_links:
    - from: "dashboard/src/App.tsx"
      to: "dashboard/src/components/layout/DashboardLoader.tsx"
      via: "loader rendered then dismissed on mount"
      pattern: "Loader"
---

<objective>
Apply the full motion pass per CONTEXT D-04: the dashboard loader (animated SVI-draw),
the live/net dot pulse, hover/focus transitions on rail/cards/table rows, the
shock-button active toggle, and the event-ticker flash. Motion "clarifies, not
decorates" — every animation in scope is in the handoff's motion language.

Purpose: D-04 chose full motion including the loader. This is the last wave by design:
the loader and transitions decorate components that must already exist and be styled
(Waves 2-5). Per the CONTEXT scope-vs-deadline note, this wave can slip without making
the dashboard unusable — Waves 1-5 land a demo-ready dashboard; this wave is the polish
layer.

Output: DashboardLoader.tsx (new), globals.css keyframes/transitions, motion wiring in
App.tsx + Rail + WhatIfSimulator + EventStreamPanel.

Covers CONTEXT decision D-04, UI-SPEC Interaction Contract.
This plan has a human-verify checkpoint — motion quality is visual.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-UI-SPEC.md
@.planning/phases/04.1-dashboard-visual-polish/04.1-PATTERNS.md
@DeepVault-handoff/project/styles.css
@DeepVault-handoff/project/dashboard.css
@dashboard/src/styles/globals.css
@dashboard/src/App.tsx

<interfaces>
<!-- Handoff motion sources -->
styles.css:86-119  — .loader, .loader-svi, @keyframes dash (the SVI line-draw loader)
styles.css:164-173 — @keyframes pulse (live-dot / net-dot ring pulse)
dashboard.css      — .rail-nav a:hover (.12s), .db-card / .pos-table tr:hover / .ev-list li:hover,
                     .shock active transition (background/border-color/color .12s)
dashboard.js       — shocks() (active-class swap), events() (700ms flash — already in EventStreamPanel)

<!-- In scope per UI-SPEC Interaction Contract: pulse dots, 700ms event flash,
     .12s hover transitions, shock toggle, Plotly redraw (already exists), the loader.
     NOT the landing-page lineDraw/fade-in/scroll-reveal (those belong to index.html). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add keyframes + hover/focus transitions to globals.css</name>
  <read_first>
    - dashboard/src/styles/globals.css (Plan 01 token system — append motion rules)
    - DeepVault-handoff/project/styles.css lines 164-173 (@keyframes pulse)
    - DeepVault-handoff/project/styles.css lines 86-119 (.loader, .loader-svi, @keyframes dash)
    - DeepVault-handoff/project/dashboard.css (.rail-nav a:hover, .pos-table tr:hover, .ev-list li:hover, .shock transitions, .db-tabs em.on)
    - 04.1-UI-SPEC.md "Interaction Contract" table
    - 04.1-PATTERNS.md "Motion (D-04)" shared-pattern section
  </read_first>
  <action>
    Append the motion CSS to globals.css (transcribe from the handoff, recolored to the
    OKLCH tokens already in the file):
    - `@keyframes pulse` (from styles.css:164-173) — the box-shadow ring pulse for
      `.live-dot` / `.net-dot`. Apply `animation: pulse 2s ease-out infinite` to those
      dot elements (used by RelayStatusPill connected state + Rail footer net-dot).
    - `@keyframes dash` (from styles.css:119) — `to { stroke-dashoffset: 0 }` for the
      loader SVI line-draw (consumed by Task 2's DashboardLoader).
    - Hover/focus transitions, all `.12s`–`.15s`: rail-nav item hover (background ->
      var(--surface), color -> var(--text)); `.db-card`/`oracles`/`db-pill`
      border-color shift; `pos-table tbody tr:hover` -> surface 60%-mix bg;
      `ev-list li:hover` -> surface 50%-mix bg.
    - Shock button toggle transition: `transition: background .12s, border-color .12s,
      color .12s` on `.shock`; the `.active` state (coral 18%-mix bg + coral border)
      is the WhatIfSimulator class from Plan 04 — this just adds the transition.
    - `db-tabs em` underline transition for the `.on` state.
    Use `prefers-reduced-motion: reduce` to disable the pulse + loader animation for
    accessibility (a media query wrapping the animation declarations). Do NOT add the
    landing-page `lineDraw` / `fade-in` / scroll-reveal keyframes — those are out of
    scope (index.html only, per UI-SPEC).
  </action>
  <verify>
    <automated>cd dashboard && grep -q '@keyframes pulse' src/styles/globals.css && grep -q '@keyframes dash' src/styles/globals.css && grep -q 'prefers-reduced-motion' src/styles/globals.css && grep -qE 'transition' src/styles/globals.css && ! grep -q 'lineDraw' src/styles/globals.css && echo MOTION_CSS_OK</automated>
  </verify>
  <done>globals.css has @keyframes pulse + dash, .12s hover/focus transitions for rail/card/table-row/shock/tabs, a prefers-reduced-motion guard; no landing-page lineDraw keyframe.</done>
</task>

<task type="auto">
  <name>Task 2: Build the DashboardLoader (SVI-draw animation) and mount it</name>
  <read_first>
    - DeepVault-handoff/project/styles.css lines 86-119 (.loader, .loader-wrap, .loader-mark, .loader-svi, @keyframes dash)
    - DeepVault-handoff/project/dashboard.js (loader dismiss behavior, if present)
    - dashboard/src/App.tsx (the dash-body shell from Plan 02 — loader mounts over it)
    - 04.1-CONTEXT.md D-04 (loader is in scope) + scope-vs-deadline note
    - 04.1-UI-SPEC.md "Interaction Contract"
  </read_first>
  <action>
    Create dashboard/src/components/layout/DashboardLoader.tsx — a full-viewport overlay
    that plays on first paint then fades out. Transcribe the handoff `.loader` /
    `.loader-svi` (styles.css:86-119): a centered `loader-wrap` containing the brand
    mark and an inline SVG `loader-svi` whose paths animate via `@keyframes dash`
    (stroke-dasharray + animated stroke-dashoffset -> 0, with the staggered
    nth-child delays from styles.css:117-118) — the "SVI curve draws itself" effect.
    Behavior: render the loader on mount, then after the draw animation completes
    (~0.8-1.2s) add the `.gone` class (opacity 0, pointer-events none) and unmount it
    shortly after. Use a `useEffect` + `useState` `mounted` flag — no external lib.
    Respect `prefers-reduced-motion`: if reduced motion is preferred, skip the
    animation and dismiss the loader near-immediately.
    Mount it in App.tsx: render `<DashboardLoader />` as a sibling overlay above the
    `dash-body` shell (it covers the screen, then fades to reveal the dashboard). The
    loader is purely presentational — it must NOT block or delay the `useWebSocket`
    connection or any data fetch (the data spine runs underneath it).
  </action>
  <verify>
    <automated>cd dashboard && test -f src/components/layout/DashboardLoader.tsx && grep -qiE 'loader' src/components/layout/DashboardLoader.tsx && grep -q 'prefers-reduced-motion\|reducedMotion\|matchMedia' src/components/layout/DashboardLoader.tsx && grep -qiE 'Loader' src/App.tsx && grep -q 'useWebSocket' src/App.tsx && pnpm exec tsc --noEmit -p tsconfig.json && echo LOADER_OK</automated>
  </verify>
  <done>DashboardLoader.tsx exists, plays the SVI-draw animation, fades out via .gone, respects reduced-motion; mounted in App.tsx as an overlay; the data spine is not blocked by it; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the interactive motion — rail hover, shock toggle, event flash, dots</name>
  <read_first>
    - dashboard/src/components/layout/Rail.tsx (Plan 02 — apply hover/active transition classes)
    - dashboard/src/components/panels/WhatIfSimulator.tsx (Plan 04 — the 7 shock buttons)
    - dashboard/src/components/panels/EventStreamPanel.tsx (Plan 05 — the 700ms flash; confirm it uses the globals.css class)
    - dashboard/src/components/layout/RelayStatusPill.tsx (Plan 04 — the live-dot)
    - 04.1-UI-SPEC.md "Interaction Contract" rows: rail hover, shock toggle, event ticker prepend, live/net dots, db-tabs select
  </read_first>
  <action>
    Wire the components to the Task-1 motion CSS:
    - Rail.tsx: ensure rail-nav items carry the `.12s` hover transition class and the
      active item the 10%-accent-mix bg; disabled items get NO hover (already styled
      in Plan 02 — this confirms the transition class is applied).
    - WhatIfSimulator.tsx: confirm the `.shock` buttons carry the transition (Task 1
      added the CSS; this verifies the class names match so the active-toggle animates).
    - EventStreamPanel.tsx: confirm the 700ms newest-row flash (Plan 05) uses a class
      defined/consistent with globals.css (a fading accent-mix background). If Plan 05
      implemented the flash inline, leave it; just ensure it visually matches the
      handoff timing (700ms) and the OKLCH accent token.
    - RelayStatusPill / Rail footer: apply `animation: pulse 2s ease-out infinite` to
      the `live-dot` / `net-dot` elements so the connected-state dot pulses.
    - db-tabs: confirm the `em.on` underline uses the mint border-bottom + transition.
    This task is mostly class-application + verification that the Wave-2..5 components'
    class names line up with the Task-1 CSS — no new logic. Do not change any data or
    behavior.
  </action>
  <verify>
    <automated>cd dashboard && grep -qiE 'live-dot|net-dot|pulse' src/components/layout/RelayStatusPill.tsx src/components/layout/Rail.tsx && grep -qiE 'shock' src/components/panels/WhatIfSimulator.tsx && pnpm exec tsc --noEmit -p tsconfig.json && pnpm build 2>&1 | tail -1 && echo INTERACTIVE_MOTION_OK</automated>
  </verify>
  <done>Rail hover, shock-button toggle, event-ticker flash, live/net dot pulse, and db-tabs underline all animate per the handoff; no behavior changed; tsc clean, pnpm build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification — motion pass quality</name>
  <what-built>
    Full motion pass: the SVI-draw dashboard loader, live/net dot pulse, rail/card/
    table-row hover transitions, the shock-button active toggle, the event-ticker
    prepend flash, and the db-tabs underline — all in the handoff motion language.
  </what-built>
  <how-to-verify>
    1. Run `cd dashboard && pnpm dev` and open the dashboard URL.
    2. On first load: the loader overlay should play the SVI line-draw, then fade out
       to reveal the dashboard (no white flash, no jank).
    3. Hover rail items, cards, and positions-table rows — each should have a smooth
       ~0.12s background/border transition. Disabled rail items should NOT react.
    4. The "Live · testnet" dot in the top bar should pulse gently.
    5. In the What-if card, click different shock buttons — the active button should
       animate to coral; only one active at a time.
    6. Watch the event stream — a new relay event should prepend with a brief
       (~700ms) accent flash on the newest row.
    7. Toggle the theme — confirm dark<->paper still works and motion still reads well
       in both themes.
    8. Set OS "reduce motion" and reload — the loader/pulse should be muted/skipped.
    Confirm motion "clarifies, not decorates" — nothing should feel gratuitous.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any motion that feels janky / decorative / off-brand.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client render only | This plan adds CSS animations + a presentational overlay — no data, no input, no network boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-16 | Denial of service | DashboardLoader overlay | mitigate | The loader is presentational only and explicitly must NOT block the useWebSocket connection (Task 2 acceptance criterion confirms the data spine runs underneath); it self-dismisses on a timer so it cannot trap the UI. |
| T-04.1-17 | Information disclosure | motion CSS | accept | Pure CSS keyframes/transitions — no data, no secret material. |
</threat_model>

<verification>
- `grep '@keyframes pulse' dashboard/src/styles/globals.css` matches
- `grep prefers-reduced-motion dashboard/src/styles/globals.css` matches
- `grep lineDraw dashboard/src/styles/globals.css` returns nothing (landing-page motion excluded)
- DashboardLoader.tsx exists and is mounted in App.tsx
- `cd dashboard && pnpm exec tsc --noEmit && pnpm build` both succeed
- Human checkpoint (Task 4): motion verified visually in dev
</verification>

<success_criteria>
- Dashboard loader plays the SVI-draw animation then fades out, respecting reduced-motion
- Pulse dots, .12s hover transitions, shock toggle, event flash, db-tabs underline all animate per the handoff
- No landing-page-only motion leaked in
- The data spine is never blocked by the loader
- tsc clean, pnpm build succeeds, human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-dashboard-visual-polish/04.1-06-SUMMARY.md`
</output>
