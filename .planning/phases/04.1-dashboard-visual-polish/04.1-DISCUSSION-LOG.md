# Phase 04.1: Dashboard Visual Polish - Discussion Log

**Date:** 2026-05-17
**Mode:** discuss (default, single batched question set)

> Human-reference record of the discuss-phase session. Not consumed by downstream agents — see `04.1-CONTEXT.md` for the decisions that flow into research/planning.

## Context entering discussion

Phase 04.1 was inserted after Phase 4 to map the Claude Design handoff bundle onto the already-built 8-panel dashboard. The substantive design decisions were already locked upstream:
- `04.1-UI-SPEC.md` (approved, verified by gsd-ui-checker) — token table, per-panel mapping, 5 scope decisions.
- The 5 scope decisions (Greeks dropped / BTC-only / static-backtest-card / single-page nav / real-data binding) were resolved with the user before the UI phase.

So this discussion covered only the **implementation gray areas** the UI-SPEC left open.

## Gray areas presented

Four implementation gray areas, presented as one batched AskUserQuestion set:

### Area 1 — Reskin strategy
Options presented:
- In-place edit (Recommended) — modify existing panels directly, keep data wiring/hooks/tests intact
- Parallel rebuild + swap — build fresh alongside, then swap

**User selected:** In-place edit.

### Area 2 — Light mode
Options presented:
- Dark-only now, defer toggle (Recommended) — transcribe both token sets, ship dark-only, defer toggle UI
- Wire the toggle in 4.1 — full light/dark toggle + persistence now

**User selected:** Wire the toggle in 4.1. *(Chose fuller scope over the recommendation.)*

### Area 3 — EventStreamPanel scope
Options presented:
- Full live WSS wire (Recommended) — build the panel AND wire it to the relay's live WebSocket stream
- Visual panel only, wire later — styled panel against a typed shape, defer the live subscription

**User selected:** Full live WSS wire.

### Area 4 — Motion budget
Options presented:
- Core motion only (Recommended) — hover/focus, event ticker, shock toggle, SVI render; skip loader
- Full motion incl. loader — everything including the animated loading screen
- Static reskin, motion later — no motion work in 4.1

**User selected:** Full motion incl. loader. *(Chose fuller scope over the recommendation.)*

## Outcome

Four decisions captured as D-01..D-04 in `04.1-CONTEXT.md`. User chose the fuller-scope option on two of four (light toggle, full motion) — consistent with PROJECT.md's "quality over component count" stance. Planner advised to wave the work so a partial landing (tokens + panel reskins) stays demo-usable if toggle/loader polish slips.

## Claude's Discretion (recorded, not asked)

- Geist font self-hosted/bundled (not Google Fonts CDN) — recording-target robustness per Phase 0 D-14.
- Theme toggle in the top-bar pill row; `localStorage` persistence; pre-paint theme application.
- OKLCH tokens transcribed into the existing shadcn token layer (re-point values, don't reinstall primitives).

## Deferred Ideas

- Landing/marketing site (handoff `index.html`)
- Multi-route dashboard navigation
- Greeks panel (v2)
- Multi-asset ETH/SOL/SUI (v2)

None were scope creep into 04.1 — all recorded so they aren't lost.
