---
phase: 04.2-dashboard-onboarding-declutter
verified: 2026-06-15T16:30:00Z
status: passed
score: 10/10 must-haves verified
overrides_applied: 0
---

# Phase 04.2: Dashboard Onboarding Declutter Verification Report

**Phase Goal:** Reorganize the existing Phase 4 / 04.1 dashboard so a first-time LP can understand and operate a deposit without wading through the quant — a deposit-first Vault view becomes the default landing, the risk-studio panels move behind a Vault | Risk Studio toggle, and the nav rail + dead header chrome are decluttered. REORGANIZATION, NOT a rewire: the `useWebSocket` data spine and all panel internals/props must be byte-identical.

**Verified:** 2026-06-15T16:30:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Mode toggle (LD-1, R-1): App.tsx renders a controlled Radix `Tabs` (`value={mode}`, default `'vault'`), with the two views as conditionally-mounted siblings (NOT inside TabsContent). `mode` useState declared BELOW the `useWebSocket` spine + 5 derived hooks (LD-3). | VERIFIED | `App.tsx:75` `const [mode, setMode] = useState<'vault' | 'risk'>('vault')` at char 4088, `useWebSocket` call at char 636 — mode is physically below spine. `App.tsx:229-242` shows controlled `<Tabs value={mode} onValueChange=...>` with no `TabsContent`. Content is a ternary at `App.tsx:244`. |
| 2 | Vault default + deposit-first (LD-4): initial render `mode==='vault'`; Vault view order is deposit-withdraw -> position-viewer -> hedge-status line; `dh-stats` strip visible in BOTH modes. | VERIFIED | `useState('vault')` default confirmed. `App.tsx:250` deposit-withdraw section, `App.tsx:256` position-viewer section, `App.tsx:266-277` hedge-status `<p>` — in that document order (chars 4849, 11772, 12374). `dh-stats` inside `db-headline` section at `App.tsx:188-218`, outside the mode conditional — present in both modes (OQ-2). |
| 3 | Risk Studio (LD-5): existing quant panels render under `mode==='risk'` at `App.tsx:284-348` — SurfacePanel, ArbCheckerPanel, BucketGauge, ExposurePanel, EventStreamPanel, WhatIfSimulator, Backtest card; all unchanged. | VERIFIED | `App.tsx:286-348` contains all LD-5 panels in the risk branch. EventStream pulled to r2 left cell (R-2 path a). All panel props byte-identical: `<SurfacePanel surface={surface} />`, `<ArbCheckerPanel surface={surface} />`, `<BucketGauge />`, `<ExposurePanel hedges={hedges} vault={snapshot?.vault ?? null} />`, `<EventStreamPanel events={snapshot?.ring_buffer ?? []} />`, `<WhatIfSimulator hedges={hedges} surface={surface} sigma={sigma} />`. |
| 4 | D-1: exactly ONE `<PositionViewer` mount; no third `[data-section]` anchor introduced. | VERIFIED | Grep count: `<PositionViewer` appears 1 time in App.tsx. `<section data-section="deposit-withdraw">` appears 1 time as a rendered section. PositionViewer is in Vault view only (R-2 path a). |
| 5 | D-2 (R-4): Rail's `OBSERVED_BY_MODE` includes `event-stream` and does NOT include `exposure` as an observed section. | VERIFIED | `Rail.tsx:79-82`: `OBSERVED_BY_MODE = { vault: ['deposit-withdraw', 'position-viewer'], risk: ['hero', 'what-if', 'backtest', 'event-stream'] }`. `event-stream` is present; `exposure` is absent from the observed array. |
| 6 | Rail declutter (LD-6): 6 live nav items in 2 groups; 4 disabled stubs + 3 duplicate anchors gone; Tooltip imports/wrapper removed; cross-mode click calls setMode then requestAnimationFrame scroll; RailGroup receives mode/setMode. | VERIFIED | `Rail.tsx:51-68`: NAV_GROUPS has exactly 6 items (3 vault + 3 risk) in 2 groups. No `Drawdown replay`, `Single PTB`, `vUSDC share`, `Integrations`, `Hedge ladder`, `'History'`, `Reports` in file. Zero `Tooltip` references. `setMode(item.mode)` + `requestAnimationFrame` at `Rail.tsx:216-218`. RailGroup signature at `Rail.tsx:186-196` includes `mode` and `setMode`. `[mode]` in useEffect dep array at `Rail.tsx:134`. Mode-gated active at `Rail.tsx:204-207`: `item.mode === mode &&`. |
| 7 | Header (LD-7): the `⌘K · Search` pill is gone; 6 controls remain (RelayStatusPill, GlobalStalenessPill, UTC clock, ThemeToggle, Deposit DUSDC, ConnectButton); breadcrumb mode-aware; `.db-clock` class on clock. | VERIFIED | `Header.tsx`: no `⌘K` or `Search` string in file. `Header.tsx:82` has `className="db-pill mono db-clock"`. `Header.tsx:67` breadcrumb: `{mode === 'vault' ? 'Vault' : 'Risk Studio'}`. All 6 controls present at lines 73-98. |
| 8 | D-3: globals.css responsive rules no longer use `.db-pill:nth-child(3)` or `.db-pill.mono:first-of-type`; `.db-clock` is hidden at <=1200px. | VERIFIED | `globals.css`: zero matches for `db-pill:nth-child(3)` or `db-pill.mono:first-of-type`. `globals.css:957`: `.db-bar .db-clock { display: none; }` inside the `@media (max-width: 1200px)` block. The `<=1024px` media block is removed entirely (the dead search-pill rule was its only content). |
| 9 | Runtime-copy preservation (LD-3): `PositionViewer.tsx` and `ExposurePanel.tsx` are byte-identical vs HEAD~6. | VERIFIED | `git diff HEAD~6 -- dashboard/src/components/panels/PositionViewer.tsx dashboard/src/components/panels/ExposurePanel.tsx` produces zero output (empty diff). PositionViewer's longer runtime testnet-limitation copy confirmed at `PositionViewer.tsx:146-153`. |
| 10 | Build gate: `pnpm --filter @deepvault/dashboard exec tsc --noEmit` exits 0. | VERIFIED | Running tsc produced no output (exit 0, no type errors). Executor also reported full `vite build` exit 0 + vitest 427/427 green in SUMMARY-02. |

**Score: 10/10 truths verified**

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard/src/App.tsx` | Mode state lifted above data spine; Vault + Risk conditional subtrees; mode-aware headline; mode-switch-then-scroll deposit callback | VERIFIED | `useState<'vault' | 'risk'>('vault')` at line 75 (below spine at line 63). Controlled Tabs at lines 229-242. Vault ternary at line 244. handleDepositClick at lines 82-90 calls setMode then requestAnimationFrame scroll. |
| `dashboard/src/styles/globals.css` | `.db-mode` segmented-control CSS block; `.db-mode-row` wrapper-margin; mint active state; focus ring; `.db-clock` responsive rule; nth-child/first-of-type gone | VERIFIED | `.db-mode` block at lines 264-298 (6 occurrences of `.db-mode*` selectors). `.db-mode-trigger[data-state='active']` at line 291 with `color-mix(in oklab, var(--accent) 12%, var(--surface))`. `:focus-visible` ring at line 295 with `var(--ring)`. `.db-bar .db-clock { display: none; }` at line 957. No nth-child(3) or first-of-type selectors remaining. |
| `dashboard/src/components/layout/Rail.tsx` | 6-item/2-group mode-coupled NAV_GROUPS; mode/setMode props; mode-switch-then-scroll; mode-keyed OBSERVED_BY_MODE; mode-gated active | VERIFIED | NAV_GROUPS at lines 51-68. Rail() signature at line 103 receives `{ mode, setMode }`. OBSERVED_BY_MODE at lines 79-82. onClick at lines 213-222 with setMode + requestAnimationFrame. isActive at lines 204-207 with `item.mode === mode`. |
| `dashboard/src/components/layout/Header.tsx` | Search pill removed; mode-aware breadcrumb; db-clock on clock; six kept controls | VERIFIED | No ⌘K or Search. `db-pill mono db-clock` at line 82. Mode-aware breadcrumb at line 67. All 6 controls at lines 73-98. `mode: 'vault' | 'risk'` in Props type at line 37. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `App.tsx` Tabs control | `mode` state | `value={mode} onValueChange={(v) => setMode(v as 'vault' | 'risk')}` | WIRED | App.tsx:229-231 |
| `App.tsx` mode conditional | Vault/Risk subtrees | `{mode === 'vault' ? <vault JSX> : <risk JSX>}` — siblings, not TabsContent | WIRED | App.tsx:244, ternary renders Vault at 247-278 and Risk at 284-348 |
| `App.tsx` call site | Rail | `<Rail mode={mode} setMode={setMode} />` | WIRED | App.tsx:126 |
| `App.tsx` call site | Header | `<Header ... mode={mode} />` | WIRED | App.tsx:128-133 |
| `Rail.tsx` onClick | App mode state | `setMode(item.mode)` then `requestAnimationFrame(() => scrollToSection(item.section))` | WIRED | Rail.tsx:215-218 |
| `Rail.tsx` OBSERVED_BY_MODE | App.tsx per-mode anchors | vault: `deposit-withdraw`, `position-viewer`; risk: `hero`, `what-if`, `backtest`, `event-stream` | WIRED | Rail.tsx:79-82 matches App.tsx mounted anchors |
| `Header.tsx` breadcrumb | mode prop | `{mode === 'vault' ? 'Vault' : 'Risk Studio'}` | WIRED | Header.tsx:67 |
| `handleDepositClick` | vault mode + deposit anchor | `setMode('vault')` then `requestAnimationFrame` scroll to `deposit-withdraw` | WIRED | App.tsx:83-89 |

---

### Data-Flow Trace (Level 4)

Not applicable — this phase is a client-side reorganization with no new data sources. The `useWebSocket` data spine and all 5 derived hooks are unchanged and verified by tsc exit 0 + 427/427 tests.

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| tsc --noEmit | `pnpm --filter @deepvault/dashboard exec tsc --noEmit` | Exit 0, no output | PASS |
| PositionViewer single mount | `grep -c "<PositionViewer" dashboard/src/App.tsx` | 1 | PASS |
| Rail 6 items | `node -e "... (c.match(/section:\s*'/g)||[]).length === 6"` | 6 | PASS |
| Search pill gone | `grep -c "⌘K" dashboard/src/components/layout/Header.tsx` | 0 | PASS |
| nth-child(3) gone | `grep -c "db-pill:nth-child(3)" dashboard/src/styles/globals.css` | 0 | PASS |
| db-clock hide rule | grep for `@media (max-width: 1200px)` + `.db-bar .db-clock { display: none }` | present | PASS |
| Panel files unchanged | `git diff HEAD~6 -- PositionViewer.tsx ExposurePanel.tsx` | empty (0 lines) | PASS |

---

### Requirements Coverage

No formal requirement IDs were mapped to this phase (ROADMAP Requirements: TBD). Verification was conducted against CONTEXT.md LD-1..LD-7, UI-SPEC R-1..R-4, and PATTERNS.md D-1/D-2/D-3.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|---------|
| LD-1 | Two view-modes, Vault default; segmented Vault/Risk Studio control | SATISFIED | `useState('vault')` default; controlled Radix Tabs at App.tsx:229-242 |
| LD-2 | Client-side toggle, no router, no persistence | SATISFIED | Mode is React state only; no localStorage, no URL; no router imports |
| LD-3 | Reorganization not rewire — spine and panel internals byte-identical | SATISFIED | tsc exit 0; panel files zero diff vs HEAD~6; all prop signatures match interfaces contract |
| LD-4 | Vault view: value-prop headline, Deposit first, Your position, dh-stats, hedge-status | SATISFIED | App.tsx layout: headline+dh-stats -> toggle -> deposit-withdraw -> position-viewer -> hedge-status p |
| LD-5 | Risk Studio: unchanged quant panels behind mode==='risk' | SATISFIED | Risk branch at App.tsx:284-348 contains all 7 LD-5 panels |
| LD-6 | Rail: 13->6 live items, 2 groups, 4 stubs + 3 dups removed | SATISFIED | Rail.tsx:51-68, 6 items, no stubs/dups/disabled kind |
| LD-7 | Header: search pill removed, 6 controls kept, db-clock class | SATISFIED | Header.tsx: no ⌘K; db-clock at line 82; 6 controls present |
| R-1 | Mode toggle built on installed Radix Tabs primitive | SATISFIED | App.tsx imports `Tabs, TabsList, TabsTrigger` from `./components/ui/tabs` (pre-existing shadcn primitive, not edited) |
| R-2 | PositionViewer in exactly one place (path a: single mount in Vault) | SATISFIED | 1 `<PositionViewer` mount in App.tsx; not in Risk subtree |
| R-3 | db-headline eyebrow + breadcrumb mode-aware | SATISFIED | App.tsx:166 eyebrow; Header.tsx:67 breadcrumb; both mode-conditional |
| R-4 | event-stream anchor inconsistency fixed; observer reconciled | SATISFIED | Rail.tsx OBSERVED_BY_MODE.risk includes `event-stream`; `exposure` dropped from observer |
| D-1 | No third [data-section] anchor; PositionViewer single mount | SATISFIED | Verified: 1 `<PositionViewer`, 1 `<section data-section="deposit-withdraw">` |
| D-2 | event-stream observer drift fixed | SATISFIED | OBSERVED_BY_MODE.risk: `['hero', 'what-if', 'backtest', 'event-stream']` |
| D-3 | Responsive selectors re-pointed from nth-child/first-of-type to .db-clock | SATISFIED | globals.css: zero nth-child(3) or first-of-type; .db-bar .db-clock at 1200px |

---

### Anti-Patterns Found

None. Scanned `App.tsx`, `Rail.tsx`, `Header.tsx`, `globals.css` for:
- TODO/FIXME/PLACEHOLDER: none relevant to new code
- Empty implementations: none (all conditional branches render real content)
- Hardcoded empty state: `useState('vault')` is a legitimate default, not a stub
- dangerouslySetInnerHTML: 0 occurrences in any modified file
- TabsContent in App.tsx: only in a comment (line 225), not as code

---

### Human Verification Required

The following items cannot be verified programmatically and require manual review in the running app:

#### 1. Toggle Visual Appearance

**Test:** Load `http://localhost:5174`, observe the Vault | Risk Studio segmented control below the headline.
**Expected:** The active tab shows a mint-tinted background (`color-mix(in oklab, var(--accent) 12%, var(--surface))`); inactive tabs show muted text on transparent background. Coral never appears on the toggle.
**Why human:** CSS rendering and color-mix output require visual inspection.

#### 2. Keyboard Operability of Mode Toggle

**Test:** Tab to the Vault | Risk Studio control. Press arrow right. Press Home/End.
**Expected:** Focus moves between tabs; the active tab changes on arrow key; focus ring is visible (2px mint outline). Screen reader announces aria-selected state changes.
**Why human:** Keyboard interaction and focus ring visibility require live browser testing; accessibility announcement requires AT testing.

#### 3. Cross-Mode Rail Click Lands on Target

**Test:** In Vault mode, click "SVI surface" in the rail. Wait for scroll.
**Expected:** Mode switches to Risk Studio, then the SVI surface section scrolls into view — the scroll never no-ops (OQ-3).
**Why human:** requestAnimationFrame timing and scroll behavior require live browser testing.

#### 4. Responsive Clock Hide at <=1200px

**Test:** Set browser width to 1150px. Observe the header pill row.
**Expected:** The UTC clock pill is hidden at <=1200px via `.db-clock { display: none }`.
**Why human:** Responsive CSS behavior at specific breakpoints requires live browser viewport testing.

#### 5. Deposit DUSDC Header CTA Switches to Vault Then Scrolls

**Test:** Switch to Risk Studio mode. Click the "Deposit DUSDC" button in the header.
**Expected:** Mode switches to Vault and the Deposit/Redeem panel scrolls into view (not a no-op while in risk mode).
**Why human:** Cross-mode CTA scroll coordination requires live browser testing with the mode already in risk.

---

### Gaps Summary

None. All 10 observable truths are VERIFIED against the actual codebase. All artifacts exist, are substantive, and are wired correctly. The data spine is intact. Panel files are byte-identical. tsc exits 0.

The 5 human verification items above are standard UI/interaction checks that cannot be confirmed programmatically. They do not indicate missing implementation — the code for all 5 is present and correct per source inspection.

---

_Verified: 2026-06-15T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
