---
phase: 05-mainnet-redeploy-smoke-test
plan: 05
plan_id: 05-05
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/strategy.toml
  - scripts/codegen.py
  - contracts/sources/strategy_constants.move
  - backtest/src/deepvault/strategy_constants.py
  - dashboard/src/lib/strategy_constants.ts
requirements:
  - DEPLOY-04
autonomous: true
tags:
  - codegen
  - strategy-toml
  - cooldown
must_haves:
  truths:
    - "shared/strategy.toml has a new [redemption] section with cooldown_ms = 3_600_000 (matching contracts/sources/redeem.move L40 const COOLDOWN_MS)"
    - "scripts/codegen.py emits REDEMPTION_COOLDOWN_MS to all three runtimes: Move (u64), Python (int), TypeScript (bigint with n suffix)"
    - "Three generated files updated in lockstep; CI codegen-drift job (existing) passes via the same --check flag"
    - "Move-side const COOLDOWN_MS in redeem.move L40 is acknowledged as the on-chain source-of-truth (not edited — Phase 5 honors code freeze); strategy.toml cooldown_ms documents the runtime-shared value used by smoke test wait logic"
    - "Plan 05-04 smoke test reads STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS from dashboard/src/lib/strategy_constants.ts (no hardcoded wait per CONTEXT.md 'Claude's Discretion')"
    - "load_strategy schema_version stays at 2 (no breaking schema change; this is a minor additive extension within v2)"
  artifacts:
    - path: "shared/strategy.toml"
      provides: "[redemption] block with cooldown_ms = 3_600_000"
      contains: "[redemption]\\ncooldown_ms"
    - path: "scripts/codegen.py"
      provides: "emit_redemption_{move,python,typescript} helpers that write REDEMPTION_COOLDOWN_MS to each runtime"
      contains: "emit_redemption|REDEMPTION_COOLDOWN_MS"
    - path: "contracts/sources/strategy_constants.move"
      provides: "Move-side REDEMPTION_COOLDOWN_MS const (or public fun returning u64) — informational; on-chain enforcement remains in redeem.move L40"
      contains: "REDEMPTION_COOLDOWN_MS|redemption_cooldown_ms"
    - path: "backtest/src/deepvault/strategy_constants.py"
      provides: "Python-side REDEMPTION_COOLDOWN_MS = 3_600_000"
      contains: "REDEMPTION_COOLDOWN_MS"
    - path: "dashboard/src/lib/strategy_constants.ts"
      provides: "TS-side REDEMPTION_COOLDOWN_MS: 3_600_000n (bigint with n suffix)"
      contains: "REDEMPTION_COOLDOWN_MS"
  key_links:
    - from: "shared/strategy.toml [redemption].cooldown_ms"
      to: "contracts/sources/redeem.move L40 const COOLDOWN_MS"
      via: "documented parity — value must match; CI parity sanity check"
      pattern: "3_600_000"
    - from: "dashboard/src/lib/strategy_constants.ts STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS"
      to: "scripts/mainnet-smoke-test.ts wait logic (Plan 05-04)"
      via: "TS import"
      pattern: "import.*STRATEGY_CONSTANTS"
---

<objective>
Extend `shared/strategy.toml` with `[redemption].cooldown_ms = 3_600_000` and regenerate cross-runtime constants via `scripts/codegen.py`. Honors CONTEXT.md "Claude's Discretion" instruction literally: smoke test wait logic reads from codegen, not from a hardcoded JS literal.

Purpose: Per RESEARCH finding #2 + Open Question #3 — the redeem cooldown is currently a Move `const` in `contracts/sources/redeem.move:40` and is NOT in `shared/strategy.toml`. Adding it is the cleanest way to satisfy CONTEXT.md "no hardcoded waits" without editing the on-chain code (code freeze 2026-05-30 already past). The Move const remains the on-chain authority; strategy.toml's value is the off-chain runtime mirror used by smoke-test + future replay tooling.

Output: ~5 line strategy.toml addition + ~15 line codegen.py extension across 3 emit functions + 3 regenerated files. Codegen pipeline already passes CI's codegen-drift job (existing); this plan extends it by 1 constant.

Scope decision (per CONTEXT.md re-route guidance): we choose option (a) from RESEARCH §"Token-bucket wait time" — extend strategy.toml + codegen. Option (b) (treat redeem.move const as source of truth + hardcode in TS) is REJECTED because it inverts the "shared/strategy.toml drives constants" pattern and would require Plan 05-04 to hardcode 3_600_000 in TS, violating "no hardcoded waits".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mainnet-redeploy-smoke-test/05-CONTEXT.md
@.planning/phases/05-mainnet-redeploy-smoke-test/05-RESEARCH.md
@.planning/phases/05-mainnet-redeploy-smoke-test/05-PATTERNS.md
@shared/strategy.toml
@scripts/codegen.py
@contracts/sources/redeem.move
@contracts/sources/strategy_constants.move
@backtest/src/deepvault/strategy_constants.py
@dashboard/src/lib/strategy_constants.ts

<interfaces>
Existing codegen pattern (PATTERNS.md + STATE.md Phase 02-02 entry):
- scripts/codegen.py has emit_move(strategy, out_path), emit_python(strategy, out_path), emit_typescript(strategy, out_path)
- Each emit function reads sub-sections of strategy.toml dict (e.g., strategy['token_bucket']['capacity_quote_micro_units']) and writes constants
- TS uses bigint `n` suffix on every u64-equivalent literal (Phase 02-02 lock — STATE.md decision log)
- Move emits as `public fun redemption_cooldown_ms(): u64 { 3_600_000 }` (or `const REDEMPTION_COOLDOWN_MS: u64 = 3_600_000` — match existing pattern in strategy_constants.move)
- Python emits as `REDEMPTION_COOLDOWN_MS: int = 3_600_000`
- CI's `codegen-drift` job runs `git diff --exit-code` on the 8 generated paths (Plan 01-04 extended to 8 paths; this plan does NOT add new paths, only adds a constant inside existing files)

From contracts/sources/redeem.move L40:
```move
const COOLDOWN_MS: u64 = 3_600_000;  // 1 hour
```

From shared/strategy.toml current state (line 11):
```
schema_version = 2
```
No bump needed — this is an additive minor extension.

Existing strategy.toml block structure (e.g. lines 31-37):
```
[token_bucket]
capacity_quote_micro_units = 100_000_000
refill_rate_quote_micro_units_per_ms = 1200
```

The new block should mirror this style at the bottom of the file (before or after [meta]):
```
[redemption]
# Off-chain mirror of contracts/sources/redeem.move L40 const COOLDOWN_MS.
# On-chain enforcement is the Move const; this value is used by off-chain
# smoke tests, replay tooling, and dashboard timers to compute wait windows.
# A future on-chain upgrade would change BOTH places; current code-freeze
# (CONTRIBUTING.md) prohibits Move edits, so the parity is documentational.
cooldown_ms = 3_600_000  # 1 hour = 60 * 60 * 1000
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend shared/strategy.toml + scripts/codegen.py with [redemption].cooldown_ms emitter</name>
  <read_first>
    - shared/strategy.toml (Read entire file — must understand existing block ordering before insert)
    - scripts/codegen.py (Read entire file — must understand emit_move / emit_python / emit_typescript signatures and existing constant-write patterns; required by codegen-drift CI invariant)
    - contracts/sources/redeem.move (lines 35-45 — confirm `COOLDOWN_MS = 3_600_000`)
    - contracts/sources/strategy_constants.move (existing generated file — confirm const/fun emission style)
    - backtest/src/deepvault/strategy_constants.py (existing generated file — confirm Python emission style + underscore-separated literal formatting)
    - dashboard/src/lib/strategy_constants.ts (existing generated file — confirm bigint `n` suffix usage on u64-equivalent constants)
    - .planning/phases/05-mainnet-redeploy-smoke-test/05-PATTERNS.md §"shared/strategy.toml (possibly modify)" + §"Codegen-driven constant import"
    - .planning/phases/05-mainnet-redeploy-smoke-test/05-RESEARCH.md Open Question #3 (cooldown source-of-truth analysis)
  </read_first>
  <action>
**Sub-step A — Extend strategy.toml:**
Edit `shared/strategy.toml` and insert this block after the existing `[token_bucket]` block (or before `[meta]` — pick wherever existing block ordering is alphabetical/logical):

```toml
[redemption]
# Off-chain mirror of contracts/sources/redeem.move L40 const COOLDOWN_MS.
# On-chain enforcement is the Move const; this value is used by off-chain
# smoke tests, replay tooling, and dashboard timers to compute wait windows.
# A future on-chain upgrade would change BOTH places; current code-freeze
# (CONTRIBUTING.md) prohibits Move edits, so this parity is documentational.
# Phase 5 Plan 05-05 extension; codegen-drift CI gate enforces lockstep emit.
cooldown_ms = 3_600_000   # 1 hour = 60 * 60 * 1000
```

Do NOT bump `schema_version` — this is an additive minor change within v2. The existing `load_strategy` schema guard (Plan 02-02 schema_version=2) continues to accept the file.

**Sub-step B — Extend scripts/codegen.py:**
Edit `scripts/codegen.py` to add a `load_redemption(strategy)` helper (or inline the 1-key read) and extend the three emit functions:

1. `emit_move`: add the constant in the same style as existing constants in strategy_constants.move. If the file uses `public fun <name>(): u64 { <value> }` pattern, emit `public fun redemption_cooldown_ms(): u64 { 3_600_000 }`. If it uses `const X: u64 = 3_600_000;`, emit `const REDEMPTION_COOLDOWN_MS: u64 = 3_600_000;`. **Match the existing file's pattern exactly** — Read strategy_constants.move first to confirm.
2. `emit_python`: add `REDEMPTION_COOLDOWN_MS: int = 3_600_000` using the existing underscore-formatter (`_fmt_underscored`) for readability. Place it adjacent to TOKEN_BUCKET constants for visual grouping.
3. `emit_typescript`: add `REDEMPTION_COOLDOWN_MS: 3_600_000n` (bigint `n` suffix mandatory per Phase 02-02 lock — STATE.md decision log). Place it adjacent to TOKEN_BUCKET constants.

After Edit, run codegen against the actual files:
```bash
cd backtest && uv run --no-project python ../scripts/codegen.py
```
Expected: contracts/sources/strategy_constants.move, backtest/src/deepvault/strategy_constants.py, and dashboard/src/lib/strategy_constants.ts all updated to contain the new constant.

**Sub-step C — Verify codegen-drift CI is satisfied:**
```bash
cd backtest && uv run --no-project python ../scripts/codegen.py --check
```
Must exit 0 (generated files match emit output).

**Sub-step D — Verify value parity with on-chain Move const:**
```bash
grep -E 'COOLDOWN_MS|cooldown_ms' contracts/sources/redeem.move
grep -E 'cooldown_ms|REDEMPTION_COOLDOWN_MS' shared/strategy.toml dashboard/src/lib/strategy_constants.ts backtest/src/deepvault/strategy_constants.py contracts/sources/strategy_constants.move
```
Manually confirm the literal `3_600_000` appears in all 5 places (redeem.move, strategy.toml, three generated files).
  </action>
  <verify>
    <automated>grep -q '\[redemption\]' shared/strategy.toml &amp;&amp; grep -q 'cooldown_ms = 3_600_000' shared/strategy.toml &amp;&amp; cd backtest &amp;&amp; uv run --no-project python ../scripts/codegen.py --check &amp;&amp; cd .. &amp;&amp; grep -q 'REDEMPTION_COOLDOWN_MS' dashboard/src/lib/strategy_constants.ts &amp;&amp; grep -q 'REDEMPTION_COOLDOWN_MS' backtest/src/deepvault/strategy_constants.py &amp;&amp; grep -qE 'REDEMPTION_COOLDOWN_MS|redemption_cooldown_ms' contracts/sources/strategy_constants.move</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '\[redemption\]' shared/strategy.toml` (new TOML section present)
    - `grep -q 'cooldown_ms = 3_600_000' shared/strategy.toml` (literal value present)
    - `grep -q 'schema_version = 2' shared/strategy.toml` (schema NOT bumped — additive change within v2)
    - `grep -q 'REDEMPTION_COOLDOWN_MS' dashboard/src/lib/strategy_constants.ts` (TS const emitted)
    - `grep -E 'REDEMPTION_COOLDOWN_MS.*3_?600_?000n' dashboard/src/lib/strategy_constants.ts` (bigint `n` suffix per STATE.md Phase 02-02 lock)
    - `grep -q 'REDEMPTION_COOLDOWN_MS.*3_600_000' backtest/src/deepvault/strategy_constants.py`
    - `grep -qE 'REDEMPTION_COOLDOWN_MS.*3_600_000|redemption_cooldown_ms.*3_600_000' contracts/sources/strategy_constants.move` (Move emits as const OR fun matching existing file style)
    - `cd backtest && uv run --no-project python ../scripts/codegen.py --check` exits 0 (codegen-drift CI passes locally)
    - `grep -c '3_600_000' contracts/sources/redeem.move` >= 1 (parity authority — UNCHANGED; this plan does NOT edit redeem.move)
    - `git diff contracts/sources/redeem.move` reports zero changes (code freeze 2026-05-30 honored — Phase 5 must NOT edit Move sources)
  </acceptance_criteria>
  <done>strategy.toml extended, codegen regenerated all 3 files, --check exits 0, redeem.move untouched (code freeze honored), value parity verified across 5 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer edits shared/strategy.toml → codegen run → 3 generated files | Codegen pipeline is the trust boundary; if codegen.py is buggy, generated files drift from source |
| Generated files → downstream consumers (Move tests, Python backtest, TS dashboard/smoke) | Consumers TRUST the generated value matches strategy.toml; CI codegen-drift gate enforces this |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | T (Tampering) | Drift between redeem.move L40 const and strategy.toml cooldown_ms | mitigate | Both literals share `3_600_000`; Plan 05-04 smoke test will fail loudly if cooldown wait is wrong (smoke fails to redeem at all if wait < cooldown). CI codegen-drift catches generated-file drift but NOT this Move-vs-TOML semantic parity — accepted documentational risk per CONTRIBUTING.md MATH: prefix discipline. |
| T-05-05-02 | T (Tampering) | Manual edit of generated strategy_constants.{move,py,ts} | mitigate | All three generated files contain AUTO-GENERATED header (Plan 00-03 lock). Manual edits get overwritten on next codegen; CI codegen-drift detects edits-without-source. |
| T-05-05-03 | I (Information disclosure) | cooldown value exposure | accept | The 1-hour cooldown is documented in CONTRIBUTING.md + svi-spec.md + ROADMAP.md. No confidentiality concern. |
| T-05-05-04 | D (DoS) | Smoke test (Plan 05-04) waits 1 hour real wall-clock | accept | Per CONTEXT.md D-02 ("staged with checkpoints"), the wait is INTENTIONAL. Test runs on local machine, not CI. |

**High-severity threats:** None. Documentational parity risk (T-05-05-01) is mitigated by Plan 05-04 fail-loud behavior.
</threat_model>

<verification>
End-to-end check (run by checker after task completes):

1. `grep -q 'cooldown_ms = 3_600_000' shared/strategy.toml`
2. `cd backtest && uv run --no-project python ../scripts/codegen.py --check` exits 0
3. `grep -q 'REDEMPTION_COOLDOWN_MS' dashboard/src/lib/strategy_constants.ts && grep -q 'REDEMPTION_COOLDOWN_MS' backtest/src/deepvault/strategy_constants.py`
4. `grep -q '3_600_000' contracts/sources/redeem.move` (unchanged — code freeze honored)
5. `git diff contracts/sources/redeem.move` reports no changes
6. Smoke import test: `cd dashboard && npx tsx -e "import {STRATEGY_CONSTANTS} from './src/lib/strategy_constants'; console.log(STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS)"` prints `3600000n`
</verification>

<success_criteria>
- shared/strategy.toml has new [redemption].cooldown_ms = 3_600_000 block
- scripts/codegen.py extended with 3 emit lines (one per runtime)
- 3 generated files updated in lockstep
- CI codegen-drift gate (`--check`) passes locally
- redeem.move untouched (code freeze 2026-05-30 honored)
- Plan 05-04 can `import { STRATEGY_CONSTANTS } from '../dashboard/src/lib/strategy_constants'` and read `STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS` as a bigint
- "no hardcoded waits" directive from CONTEXT.md Claude's Discretion is satisfied — Plan 05-04 uses the codegen constant, not a literal 3_600_000
</success_criteria>

<output>
After completion, create `.planning/phases/05-mainnet-redeploy-smoke-test/05-05-SUMMARY.md` per the standard summary template, including:
- All 5 files modified (1 source + 1 generator + 3 generated)
- Confirmation that redeem.move was NOT edited (code freeze)
- Codegen drift `--check` output
- Resume signal: "REDEMPTION_COOLDOWN_MS now codegen-distributed across 3 runtimes; Plan 05-04 smoke test can import STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS as 3600000n"
</output>
