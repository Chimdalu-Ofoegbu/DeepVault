---
phase: 05-testnet-demo-hardening
plan: 02
plan_id: 05-02
type: execute
wave: 2
depends_on:
  - 05-03
files_modified:
  - scripts/mainnet-deploy.sh
  - scripts/mainnet-smoke-test.sh
  - scripts/mainnet-smoke-test.ts
  - .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json
requirements:
  - DEPLOY-02
  - DEPLOY-03
autonomous: true

must_haves:
  truths:
    - "scripts/mainnet-deploy.sh exists, passes `bash -n` + `shellcheck`, and is parameter-binding-section-equivalent to scripts/e2e-vault-deploy.sh (same structure, mainnet divergences applied)"
    - "scripts/mainnet-smoke-test.sh + scripts/mainnet-smoke-test.ts exist; bash file passes lint; TS file passes `tsc --noEmit`; structural parity with Plan 05-03's testnet-smoke-test"
    - "MAINNET-DEPLOY.json placeholder file exists at .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json with the discretion-spec'd content"
    - "None of the 3 scripts actually executes a mainnet PTB / publish / mutation during plan-execute time (D-05 write-but-don't-execute)"
    - "scripts/mainnet-deploy.sh reads quote_type_tag from config/mainnet.toml (not hardcoded), uses gas-budget 1_000_000_000 (1.0 SUI), and asserts active env == mainnet"
    - "scripts/mainnet-smoke-test.ts uses getFullnodeUrl('mainnet') and reads quote_type_tag from MAINNET-DEPLOY.json"
    - "AdminCap owner verification step present in mainnet-deploy.sh (DEPLOY-03 closure)"
  artifacts:
    - path: "scripts/mainnet-deploy.sh"
      provides: "Mainnet publish + create_vault + MAINNET-DEPLOY.json capture (deferred execution)"
      contains: "sui_config_mainnet"
      min_lines: 200
    - path: "scripts/mainnet-smoke-test.sh"
      provides: "Mainnet smoke test orchestrator (deferred execution)"
      contains: "mainnet-smoke-test.ts"
      min_lines: 50
    - path: "scripts/mainnet-smoke-test.ts"
      provides: "Mainnet TS smoke driver — structural parity with testnet variant"
      contains: "quote_type_tag"
      min_lines: 350
    - path: ".planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json"
      provides: "Placeholder deploy record"
      contains: "not_deployed"
  key_links:
    - from: "scripts/mainnet-deploy.sh"
      to: "config/mainnet.toml"
      via: "section-aware awk parse for quote_type_tag and predict.package_id"
      pattern: "config/mainnet.toml"
    - from: "scripts/mainnet-smoke-test.ts"
      to: ".planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json"
      via: "readFileSync"
      pattern: "MAINNET-DEPLOY.json"
    - from: "scripts/mainnet-smoke-test.ts"
      to: "scripts/testnet-smoke-test.ts"
      via: "structural fork — same 7 checkpoints + same dual ±10 bps math"
      pattern: "navPerShareScaled1e9"
---

<objective>
Write the mainnet-readiness toolkit's deploy + smoke-test scripts as write-but-don't-execute deliverables, and commit a MAINNET-DEPLOY.json placeholder. Three new scripts and one JSON file. All lint-clean; structurally parallel to their testnet analogs; targeted at the post-submission deploy window when DeepBook Predict ships on mainnet.

Wave 2 — depends on Plan 05-03 because the mainnet smoke test is structurally a fork of the testnet smoke test (same 7 checkpoints, same dual ±10 bps math; only env vars, RPC URL, type tag, amount differ).

Purpose: close DEPLOY-02 (deploy script written, not executed) and DEPLOY-03 (vault-creation parameter parity verified) per the reshape note.

Output: 4 files committed; lint-clean; verified by static gates only (D-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-testnet-demo-hardening/05-CONTEXT.md
@.planning/phases/05-testnet-demo-hardening/05-RESEARCH.md
@.planning/phases/05-testnet-demo-hardening/05-PATTERNS.md
@.planning/phases/05-testnet-demo-hardening/05-03-SUMMARY.md
@scripts/e2e-vault-deploy.sh
@scripts/testnet-smoke-test.sh
@scripts/testnet-smoke-test.ts
@config/mainnet.toml
@config/testnet.toml
@.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json

<interfaces>
Mainnet-vs-testnet divergence cheatsheet (mainnet-deploy.sh fork of e2e-vault-deploy.sh):
- Active env: mainnet (not testnet)
- SUI_CONFIG_DIR scoping: ~/.sui/sui_config_mainnet (Phase 0 D-06)
- RPC: https://fullnode.mainnet.sui.io:443 (implicit via active env)
- Gas budget for publish: 1_000_000_000 (1.0 SUI), not 500_000_000
- Quote type: read from config/mainnet.toml [assets].quote_type_tag (NOT hardcoded)
- Predict pkg/registry/top_level: read from config/mainnet.toml [predict] block
- Output target: .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json
- Network field in JSON: "mainnet"
- AdminCap owner gate: NEW assertion that AdminCap object owner equals DEPLOYER_ADDR
- quote_type_tag canonical, dusdc_type_tag retained as alias for Phase 4 dashboard back-compat

Mainnet-vs-testnet divergence cheatsheet (mainnet-smoke-test.ts fork of testnet-smoke-test.ts):
- getFullnodeUrl('mainnet')
- SUPPLY_AMOUNT_MICRO = 50_000_000n (same $50-equivalent size; both targets identical)
- DeployJson field: quote_type_tag canonical
- Reads MAINNET-DEPLOY.json
- Active-env gate: mainnet
- All other math identical

Placeholder MAINNET-DEPLOY.json content (per CONTEXT.md D-discretion):
{
  "status": "not_deployed",
  "reason": "Predict mainnet pending - DeepBook Predict not shipped on Sui mainnet as of submission window 2026-06-16",
  "scripts_ready": true,
  "toolkit_path": "scripts/mainnet-deploy.sh",
  "preflight_path": "scripts/preflight.sh",
  "predict_check_path": "scripts/predict-mainnet-check.sh",
  "smoke_test_path": "scripts/mainnet-smoke-test.sh",
  "runbook": "docs/MAINNET-READINESS.md"
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/mainnet-deploy.sh (mainnet publish + create_vault + JSON capture; write-but-don't-execute)</name>
  <files>scripts/mainnet-deploy.sh</files>
  <read_first>
    - scripts/e2e-vault-deploy.sh FULL (237 LOC; this is a ~90% fork)
    - .planning/phases/05-testnet-demo-hardening/05-PATTERNS.md (line-by-line divergence cheatsheet for mainnet-deploy.sh)
    - .planning/phases/05-testnet-demo-hardening/05-RESEARCH.md sections "Sui Mainnet Publish + USDsui Specifics" and "AdminCap verification"
    - config/mainnet.toml
    - .planning/phases/05-testnet-demo-hardening/05-CONTEXT.md D-05 (write-but-don't-execute discipline)
  </read_first>
  <action>
    Fork scripts/e2e-vault-deploy.sh into scripts/mainnet-deploy.sh with the divergences listed in the interfaces block. Structure:

    1. Header comment block citing DEPLOY-02 / DEPLOY-03 / D-05 (write-but-don't-execute) and the 9 steps. Document that this script is INTENTIONALLY UNEXECUTED in the submission window; the post-submission operator runs it after predict-mainnet-check.sh reports shipped:true and preflight.sh exits 0.

    2. Strict mode plus repo-root prologue (e2e-vault-deploy.sh L31-34 verbatim).

    3. SUI_CONFIG_DIR scoping gate: defensive assertion at the top, exit 1 if SUI_CONFIG_DIR is unset or does not end with sui_config_mainnet.

    4. Config-driven constants (replace e2e-vault-deploy.sh L36-40 hardcoded testnet values). Use a section-aware awk helper to avoid section-ordering brittleness:
       - Define an extract_config_value() bash function that takes (section, key) args and walks config/mainnet.toml respecting [section] boundaries; returns the unquoted string value
       - Set QUOTE_TYPE, PREDICT_PACKAGE_ID, PREDICT_REGISTRY_ID, PREDICT_TOP_LEVEL_ID by calling the helper
       - Set SEED_AMOUNT_MICRO=10000000
       - Assert all four are non-empty AND not equal to "TBD". If any is TBD, exit 1 with the message "config/mainnet.toml has TBD slot <name>; run preflight.sh to enumerate." (This is a defensive echo, not a re-invocation of preflight.sh — that is the operator's job.)

    5. Pin verification + Move build (verbatim from e2e-vault-deploy.sh L42-53).

    6. Active-env gate: swap testnet to mainnet (e2e-vault-deploy.sh L61-66).

    7. Publish: same shape as L71-83 with gas-budget 1_000_000_000 (1.0 SUI mainnet, vs 0.5 SUI testnet, per RESEARCH).

    8. Parse PendingTreasury (verbatim L85-100).

    9. Locate + split seed coin (verbatim L102-137 but use QUOTE_TYPE instead of hardcoded DUSDC_TYPE).

    10. Call vault::create_vault (verbatim L139-150) with --type-args "${QUOTE_TYPE}".

    11. Parse vault_id, initial_shared_version, admin_cap_id, predict_manager_id, predict_manager_initial_shared_version (verbatim L152-200) — also captures ADMIN_CAP_ID.

    12. AdminCap owner verification gate (NEW per RESEARCH "AdminCap verification" + DEPLOY-03 closure). Use jq with --arg pkg to select the AdminCap created object and read .owner.AddressOwner; exit 1 if it does not equal DEPLOYER_ADDR.

    13. OracleSVI ID capture from config (carry-forward — populates MAINNET-DEPLOY.json oracle_svi_id field; empty string if not yet known at deploy time, the operator updates it post-deploy from Mysten's Predict server registry before invoking mainnet-smoke-test.sh).

    14. Write MAINNET-DEPLOY.json. Use a heredoc that emits a JSON object with: network=mainnet, status=deployed, deployed_at_iso, deploy_tx_digest, deployer, package_id, vault_id, vault_initial_shared_version, admin_cap_id, predict_manager_id, predict_manager_initial_shared_version, predict_package_id, predict_registry_id, predict_top_level_id, quote_type_tag (canonical), dusdc_type_tag (alias = same value as quote_type_tag — back-compat for Phase 4 dashboard), oracle_svi_id (empty string default).

    15. Sanity check (verbatim L233-237).

    Forbidden tokens — script MUST NOT contain:
    - The testnet DUSDC type tag literal (0xe95040...::dusdc::DUSDC) — must come from config
    - The testnet Predict package ID (0xf5ea2b3749c65d6e56507cc35388719aadb28f9cab873696a2f8687f5c785138) — must come from config
    - `sui client switch` — never use ambient env switching (D-discretion)
    - `bash -n scripts/preflight.sh` or any invocation of preflight.sh (this script does NOT re-run preflight; separation of concerns)
  </action>
  <verify>
    <automated>bash -n scripts/mainnet-deploy.sh && shellcheck scripts/mainnet-deploy.sh && grep -q 'sui_config_mainnet' scripts/mainnet-deploy.sh && grep -q 'MAINNET-DEPLOY.json' scripts/mainnet-deploy.sh && grep -q 'AdminCap' scripts/mainnet-deploy.sh && grep -q '1000000000\|1_000_000_000' scripts/mainnet-deploy.sh && grep -q 'quote_type_tag' scripts/mainnet-deploy.sh && ! grep -q 'sui client switch' scripts/mainnet-deploy.sh</automated>
  </verify>
  <done>
    - bash -n + shellcheck exit 0
    - Grep confirms sui_config_mainnet scoping, MAINNET-DEPLOY.json output target, AdminCap owner gate, 1.0 SUI gas budget, config-driven quote_type_tag, NO sui client switch
    - Forbidden-token grep (the testnet Predict pkg and DUSDC type tag literals) returns nothing — verified by acceptance script
    - Plan-execute time does NOT run the deploy (D-05); script is committed and the operator executes it post-submission
  </done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/mainnet-smoke-test.sh + scripts/mainnet-smoke-test.ts (mirror Plan 05-03 with mainnet divergences; write-but-don't-execute)</name>
  <files>scripts/mainnet-smoke-test.sh, scripts/mainnet-smoke-test.ts</files>
  <read_first>
    - scripts/testnet-smoke-test.sh and scripts/testnet-smoke-test.ts (Plan 05-03 output — this is a structural fork)
    - .planning/phases/05-testnet-demo-hardening/05-PATTERNS.md (mainnet-smoke-test.sh and .ts patterns)
    - .planning/phases/05-testnet-demo-hardening/05-RESEARCH.md sections "Smoke Test Verification Mechanics" and "MAINNET-DEPLOY.json Schema"
  </read_first>
  <action>
    Fork the two Plan 05-03 testnet smoke test files into their mainnet variants. Maintain structural parity — the only differences are environment-scoping and type-tag fields.

    SCRIPT 1: scripts/mainnet-smoke-test.sh
    - Header comment block citing DEPLOY-02 / DEPLOY-03 / D-05 + the 7 staged checkpoints. Document that this script is INTENTIONALLY UNEXECUTED in the submission window; runs post-submission after preflight + deploy.
    - Strict mode + repo-root prologue.
    - SUI_CONFIG_DIR scoping gate (same as mainnet-deploy.sh).
    - Deploy-JSON gate: assert .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json exists AND status=="deployed". If status=="not_deployed" (placeholder), exit 1 with the message "Mainnet vault not deployed - run scripts/mainnet-deploy.sh first (post-submission, after Predict mainnet ships)."
    - Invoke the TS driver: cd dashboard && npx tsx ../scripts/mainnet-smoke-test.ts.
    - Exit code propagates.

    SCRIPT 2: scripts/mainnet-smoke-test.ts
    - Header comment block citing DEPLOY-02 / DEPLOY-03 / D-05 + the 7 staged checkpoints + "fork of testnet-smoke-test.ts with mainnet divergences."
    - Imports identical to testnet variant, plus reads MAINNET-DEPLOY.json instead of TESTNET-DEPLOY.json.
    - DeployJson type: field `quote_type_tag: string` is canonical; field `dusdc_type_tag: string` is an alias both populated by mainnet-deploy.sh (same value). All other fields identical to testnet variant (predict_package_id, etc).
    - Constants block identical to testnet variant (SUPPLY_AMOUNT_MICRO=50_000_000n, COOLDOWN_MS_MS from STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS+5000, ALLOC_BPS=1000n, NAV_TOLERANCE_BPS=10n).
    - Sui client: const client = new SuiClient({ url: getFullnodeUrl('mainnet') });
    - Active-env defensive note in comments: this script assumes the operator has scoped SUI_CONFIG_DIR=~/.sui/sui_config_mainnet; the bash wrapper enforces.
    - PTB shapes verbatim from testnet-smoke-test.ts, but typeArguments uses deploy.quote_type_tag (NOT dusdc_type_tag — the canonical field on mainnet).
    - 7 staged checkpoints with PASS markers identical in shape to testnet variant.
    - Dual ±10 bps gate math identical.
    - process.exit handling identical.

    Forbidden tokens in mainnet-smoke-test.ts:
    - `getFullnodeUrl('testnet')` — must be 'mainnet'
    - Hardcoded COOLDOWN_MS like `60 * 60 * 1000` — must come through STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS
    - Reading TESTNET-DEPLOY.json — must read MAINNET-DEPLOY.json
  </action>
  <verify>
    <automated>bash -n scripts/mainnet-smoke-test.sh && shellcheck scripts/mainnet-smoke-test.sh && (cd dashboard && npx tsc --noEmit ../scripts/mainnet-smoke-test.ts) && grep -q "getFullnodeUrl..mainnet" scripts/mainnet-smoke-test.ts && grep -q 'MAINNET-DEPLOY.json' scripts/mainnet-smoke-test.ts && grep -q 'REDEMPTION_COOLDOWN_MS' scripts/mainnet-smoke-test.ts && grep -q 'quote_type_tag' scripts/mainnet-smoke-test.ts && grep -q "CHECKPOINT PASS" scripts/mainnet-smoke-test.ts && ! grep -q "getFullnodeUrl..testnet" scripts/mainnet-smoke-test.ts && ! grep -q 'TESTNET-DEPLOY.json' scripts/mainnet-smoke-test.ts</automated>
  </verify>
  <done>
    - bash -n + shellcheck + tsc --noEmit all exit 0
    - Grep confirms: mainnet fullnode URL, MAINNET-DEPLOY.json target, REDEMPTION_COOLDOWN_MS (no hardcoded waits), quote_type_tag field, at least one CHECKPOINT PASS marker present
    - Forbidden-token grep: no 'testnet' RPC literal, no TESTNET-DEPLOY.json path
    - Structural parity with testnet-smoke-test.ts confirmed by side-by-side diff (operator can run `diff scripts/testnet-smoke-test.ts scripts/mainnet-smoke-test.ts` and the result is a small handful of contextual line changes, not a rewrite)
    - Plan-execute time does NOT run the smoke test (D-05); script is committed and the operator executes it post-submission
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit MAINNET-DEPLOY.json placeholder with D-discretion content</name>
  <files>.planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json</files>
  <read_first>
    - .planning/phases/05-testnet-demo-hardening/05-CONTEXT.md `<decisions>` section "Claude's Discretion / MAINNET-DEPLOY.json placeholder content"
    - .planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json (placeholder shape reference)
  </read_first>
  <action>
    Create the file `.planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json` with EXACTLY this content (carry-forward from CONTEXT.md D-discretion):

    {
      "status": "not_deployed",
      "reason": "Predict mainnet pending - DeepBook Predict not shipped on Sui mainnet as of submission window 2026-06-16",
      "scripts_ready": true,
      "toolkit_path": "scripts/mainnet-deploy.sh",
      "preflight_path": "scripts/preflight.sh",
      "predict_check_path": "scripts/predict-mainnet-check.sh",
      "smoke_test_path": "scripts/mainnet-smoke-test.sh",
      "runbook": "docs/MAINNET-READINESS.md"
    }

    Notes:
    - Use ASCII hyphen-minus, not em-dash, for downstream consumer simplicity (cross-OS portability — same discipline as Phase 0 codegen header convention)
    - jq validation: `jq -e '.status == "not_deployed" and .scripts_ready == true' .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json` exits 0
    - This file is the placeholder; mainnet-deploy.sh (Task 1) OVERWRITES this file when invoked post-submission with a full deploy record (per Task 1 step 14)
    - Per CONTEXT.md `<decisions>` "Claude's Discretion / MAINNET-DEPLOY.json placeholder content": the Phase 6 dashboard reads the `status` field and renders a "mainnet-readiness" badge in the UI. The "status" string takes one of two values: "not_deployed" (placeholder) or "deployed" (post-mainnet-deploy).
  </action>
  <verify>
    <automated>jq -e '.status == "not_deployed" and .scripts_ready == true and .toolkit_path == "scripts/mainnet-deploy.sh"' .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json && test "$(jq -r '.runbook' .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json)" = "docs/MAINNET-READINESS.md"</automated>
  </verify>
  <done>
    - File exists at the canonical path
    - jq validates the JSON structure (well-formed; status=not_deployed; scripts_ready=true; runbook field present)
    - Field values match the CONTEXT.md D-discretion specification exactly
    - File is committed to master; downstream Phase 6 dashboard work can consume the status field for mainnet-readiness badge rendering
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed script -> future post-submission operator | mainnet-deploy.sh and mainnet-smoke-test.sh are the documents the operator trusts to publish on mainnet; correctness validated by lint + dry-run audit + structural parity with testnet analogs |
| config/mainnet.toml -> mainnet-deploy.sh parameter binding | config-driven values (quote_type_tag, predict.*) are the ONLY surface for mainnet addresses; no hardcoding |
| MAINNET-DEPLOY.json placeholder -> Phase 6 dashboard | status field drives the "mainnet-readiness badge"; placeholder shape must be parseable JSON |

## STRIDE Threat Register (ASVS L1 coverage)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-18 | Tampering | mainnet-deploy.sh hardcodes a testnet address by accidental copy-paste from the analog | mitigate | Forbidden-token grep in acceptance: the testnet Predict pkg literal 0xf5ea2b... and the testnet DUSDC type tag literal 0xe95040... must be absent. All mainnet addresses come from config/mainnet.toml via extract_config_value(). |
| T-05-19 | Tampering | The mainnet-smoke-test.ts has subtle math drift from the testnet variant (off-by-one in bps gate) | mitigate | Plan 05-02 forks Plan 05-03's exact code path; Task 2 acceptance includes a `diff` discipline note. Both scripts read the same STRATEGY_CONSTANTS values. CI will eventually run both — testnet on push, mainnet post-submission. |
| T-05-20 | Elevation of Privilege | mainnet-deploy.sh accidentally executes during CI on master push | mitigate | Per D-05: script is committed but not invoked by any CI workflow. There is NO `.github/workflows/mainnet-deploy.yml` (Plan does not create one). Plan 05-01 also drops the predict-mainnet-check cron. Default-branch CI surface for Phase 5 is unchanged. |
| T-05-21 | Information Disclosure | DEPLOYER_ADDR or AdminCap ID logged to CI logs reveals operator wallet identity | accept | These are public on-chain values. Every successful publish reveals them. No mitigation needed. |
| T-05-22 | Repudiation | Post-submission operator runs mainnet-deploy.sh without running preflight.sh first | mitigate | mainnet-deploy.sh defensive: Step 4 asserts no TBD slots in config/mainnet.toml. This is partial preflight coverage; full preflight is invoked separately by the operator. Plan 05-05's MAINNET-READINESS.md ≤30-min procedure lists preflight as step 1 and mainnet-deploy as step 3 — operator discipline gates the order. |
| T-05-23 | Tampering | MAINNET-DEPLOY.json placeholder edited by hand and breaks JSON parsing in Phase 6 dashboard | mitigate | Task 3 acceptance includes a jq validation gate; jq -e exits non-zero on any structural break. Phase 6 dashboard's own consumer will catch downstream too. |

All ASVS L1-applicable threats addressed.
</threat_model>

<verification>
Static gates:
- bash -n scripts/mainnet-deploy.sh + shellcheck => exit 0
- bash -n scripts/mainnet-smoke-test.sh + shellcheck => exit 0
- (cd dashboard && npx tsc --noEmit ../scripts/mainnet-smoke-test.ts) => exit 0
- jq -e '.status' .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json => exit 0

Structural grep gates:
- mainnet-deploy.sh: sui_config_mainnet, MAINNET-DEPLOY.json, AdminCap, 1.0 SUI gas budget, quote_type_tag from config — ALL present
- mainnet-smoke-test.ts: getFullnodeUrl('mainnet'), MAINNET-DEPLOY.json, REDEMPTION_COOLDOWN_MS, quote_type_tag, CHECKPOINT PASS markers — ALL present

Forbidden-token grep gates:
- mainnet-deploy.sh: 0xe95040... + 0xf5ea2b3749c65d6e56507cc35388719aadb28f9cab873696a2f8687f5c785138 + `sui client switch` — ALL absent
- mainnet-smoke-test.ts: getFullnodeUrl('testnet') + TESTNET-DEPLOY.json + hardcoded 60*60*1000 — ALL absent

NO CI workflow file created:
- ! test -f .github/workflows/predict-mainnet-check.yml (Plan 05-01 enforcement)
- ! test -f .github/workflows/mainnet-deploy.yml (Plan 05-02 implicit — write-but-don't-execute means no CI cron)

Execution discipline (D-05):
- Plan execution does NOT invoke `bash scripts/mainnet-deploy.sh` or `bash scripts/mainnet-smoke-test.sh` or `npx tsx scripts/mainnet-smoke-test.ts` against live mainnet. The only invocations are static gates: bash -n, shellcheck, tsc --noEmit.
</verification>

<success_criteria>
- 3 scripts + 1 JSON placeholder committed
- All pass lint + tsc --noEmit
- Structural parity with their testnet analogs (e2e-vault-deploy.sh for mainnet-deploy.sh; Plan 05-03's testnet-smoke-test.{sh,ts} for mainnet-smoke-test.{sh,ts})
- Mainnet divergences (env scoping, RPC URL, type tag, gas budget, output target, AdminCap owner gate) all present and verified by grep
- NO actual mainnet execution; NO new CI workflows
- MAINNET-DEPLOY.json placeholder is well-formed JSON matching CONTEXT.md D-discretion spec
- DEPLOY-02 + DEPLOY-03 closed by static-gate acceptance per the reshape note's "scripts written, lint-clean, deferred execution" framing
</success_criteria>

<output>
After completion, create `.planning/phases/05-testnet-demo-hardening/05-02-SUMMARY.md` capturing:
- Files created (4): mainnet-deploy.sh + mainnet-smoke-test.sh + mainnet-smoke-test.ts + MAINNET-DEPLOY.json
- Static-gate results (bash -n + shellcheck + tsc --noEmit + jq -e)
- Structural parity evidence: a side-by-side diff summary against the analog files showing only the documented divergences
- Forbidden-token grep results: no testnet literals, no sui client switch, no TESTNET-DEPLOY.json reads in mainnet variants
- Explicit confirmation that NO mainnet execution happened during plan-execute time (D-05 closure)
- Resume signal for Plan 05-05: README and MAINNET-READINESS.md now have actual script paths to cite
</output>
