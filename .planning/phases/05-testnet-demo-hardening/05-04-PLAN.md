---
phase: 05-testnet-demo-hardening
plan: 04
plan_id: 05-04
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/strategy.toml
  - scripts/codegen.py
  - contracts/sources/strategy_constants.move
  - contracts/sources/redeem.move
  - backtest/src/deepvault/strategy_constants.py
  - dashboard/src/lib/strategy_constants.ts
requirements:
  - DEPLOY-04
autonomous: true

must_haves:
  truths:
    - "shared/strategy.toml contains a `[redemption]` section with `cooldown_ms = 3_600_000`"
    - "scripts/codegen.py parses the new section and emits `REDEMPTION_COOLDOWN_MS` (or `redemption_cooldown_ms()`) to all 3 runtime files with byte-identical numeric value (3_600_000)"
    - "contracts/sources/redeem.move no longer declares its own `const COOLDOWN_MS: u64 = 3_600_000;` — it reads from `strategy_constants::redemption_cooldown_ms()` instead"
    - "All existing tests pass: `sui move test` clean for the contracts package; backtest pytest clean; dashboard vitest clean"
    - "`make codegen` is idempotent — running it twice produces no diff"
    - "`make codegen` followed by `python scripts/codegen.py --check` exits 0 (no drift)"
  artifacts:
    - path: "shared/strategy.toml"
      provides: "Source of truth for cross-runtime cooldown constant"
      contains: "[redemption]"
      contains_2: "cooldown_ms"
    - path: "scripts/codegen.py"
      provides: "Parser + emitter for [redemption] section"
      contains: "redemption"
    - path: "contracts/sources/strategy_constants.move"
      provides: "Move-side accessor `redemption_cooldown_ms()` returning u64 3_600_000"
      contains: "redemption_cooldown_ms"
    - path: "backtest/src/deepvault/strategy_constants.py"
      provides: "Python-side `REDEMPTION_COOLDOWN_MS: Final[int] = 3_600_000`"
      contains: "REDEMPTION_COOLDOWN_MS"
    - path: "dashboard/src/lib/strategy_constants.ts"
      provides: "TS-side `REDEMPTION_COOLDOWN_MS: 3_600_000n`"
      contains: "REDEMPTION_COOLDOWN_MS"
    - path: "contracts/sources/redeem.move"
      provides: "Cooldown gate now reads from strategy_constants — single source of truth"
      contains: "strategy_constants::redemption_cooldown_ms"
  key_links:
    - from: "shared/strategy.toml [redemption].cooldown_ms"
      to: "all 3 runtime constants"
      via: "scripts/codegen.py emit_move / emit_python / emit_typescript"
      pattern: "redemption.cooldown_ms"
    - from: "contracts/sources/redeem.move (cooldown assertion)"
      to: "deepvault::strategy_constants::redemption_cooldown_ms()"
      via: "Move function call (replaces local const)"
      pattern: "strategy_constants::redemption_cooldown_ms()"
    - from: "scripts/testnet-smoke-test.ts (Plan 05-03)"
      to: "STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS"
      via: "TS import from dashboard/src/lib/strategy_constants.ts"
      pattern: "REDEMPTION_COOLDOWN_MS"
---

<objective>
Extend `shared/strategy.toml` with a `[redemption]` section containing `cooldown_ms = 3_600_000`, extend `scripts/codegen.py` to parse + emit the new constant to all 3 runtimes (Move / Python / TS) with byte-identical value, and refactor `contracts/sources/redeem.move` to read from the codegen-emitted accessor instead of its local `const COOLDOWN_MS = 3_600_000` at L40.

Purpose: enable Plan 05-03's testnet smoke test to import `STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS` rather than hardcoding 3_600_000 in the smoke test driver. Honors CONTEXT.md D-08's "single source of truth, no hardcoded waits" directive and the long-standing project pattern (D-50 from Phase 0).

Output: 6 files modified, all tests still green, golden-vector parity preserved, CI codegen-drift gate continues to enforce.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-testnet-demo-hardening/05-CONTEXT.md
@.planning/phases/05-testnet-demo-hardening/05-RESEARCH.md
@.planning/phases/05-testnet-demo-hardening/05-PATTERNS.md
@scripts/codegen.py
@shared/strategy.toml
@contracts/sources/redeem.move
@contracts/sources/strategy_constants.move
@backtest/src/deepvault/strategy_constants.py
@dashboard/src/lib/strategy_constants.ts
@Makefile

<interfaces>
<!-- Existing codegen.py emission pattern (concise excerpts the executor needs). -->

From scripts/codegen.py:
- `load_strategy()` reads `shared/strategy.toml`, asserts `schema_version == 2`, returns a dict
- `emit_move(data)` builds a string of `public fun NAME(): u64 { VALUE }` accessors inside `module deepvault::strategy_constants { ... }`
- `emit_python(data)` builds `NAME: Final[int] = VALUE` constant lines
- `emit_typescript(data)` builds `NAME: VALUE,` (with `n` suffix for u64-equivalent fields) inside `export const STRATEGY_CONSTANTS = { ... } as const;`
- `_fmt_underscored(n)` returns `f"{n:_d}"` for grouped digit display (matches TOML source)
- The `--check` mode reads the existing files from disk and compares against fresh emission output; any diff exits 1

From contracts/sources/redeem.move:
- L21: `use deepvault::strategy_constants;` is ALREADY imported
- L40: `const COOLDOWN_MS: u64 = 3_600_000;` is the value to replace
- L120: `assert!(now_ms >= slot_ts + COOLDOWN_MS, ECooldownNotMet);` is the consumer
- Other strategy_constants accessors already called (L126, L151 use `strategy_constants::nav_scale()`)

From contracts/sources/strategy_constants.move (codegen output):
- All accessors are `public fun NAME(): u64 { VALUE }` form (or `u8` for decimals, `vector<u8>` for strings)
- Module path is `deepvault::strategy_constants`
- File starts with the AUTO-GENERATED header block; codegen.py overwrites it on every run

From dashboard/src/lib/strategy_constants.ts (codegen output):
- u64-equivalent fields use `n` BigInt suffix (e.g. `TOKEN_BUCKET_CAPACITY: 100_000_000n,`)
- Member key naming convention: SCREAMING_SNAKE matching TOML keys with section prefix where relevant

`schema_version` stays at 2 (additive extension; no breaking schema change).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared/strategy.toml + scripts/codegen.py to emit REDEMPTION_COOLDOWN_MS to all 3 runtimes</name>
  <files>shared/strategy.toml, scripts/codegen.py, contracts/sources/strategy_constants.move, backtest/src/deepvault/strategy_constants.py, dashboard/src/lib/strategy_constants.ts</files>
  <read_first>
    - shared/strategy.toml (current schema; how [token_bucket] section is shaped — model after this)
    - scripts/codegen.py (every emit_* function — the new constant must be added in three places with the existing comment/grouping pattern)
    - contracts/sources/strategy_constants.move (current header + module body shape — for visual diff inspection after regen)
    - backtest/src/deepvault/strategy_constants.py (current section ordering)
    - dashboard/src/lib/strategy_constants.ts (BigInt suffix convention)
  </read_first>
  <action>
    **Sub-step 1 — Extend `shared/strategy.toml`:** Add a new top-level section, placed AFTER `[oracle]` and BEFORE `[svi]` (so the schema groups by concern: time-related sections together):

    ```toml
    [redemption]
    # 1-hour cooldown between redeem_request and redeem_fulfill (D-01 of Phase 2).
    # Single source of truth for the on-chain redeem.move gate AND the testnet-smoke-test wait.
    # Plan 05-04 extracted this from contracts/sources/redeem.move const COOLDOWN_MS.
    cooldown_ms = 3_600_000   # 1 hour = 60 * 60 * 1000
    ```

    Do NOT bump `schema_version`; the addition is purely additive (codegen.py supports schema_version=2 already; new section is consumed by extending `load_strategy()`'s downstream emit_* calls, no schema gate change).

    **Sub-step 2 — Extend `scripts/codegen.py`:**

    (a) In `emit_move(data)`: read `red = data["redemption"]`. After the existing `// Oracle` block (around L134-137 in current file), insert a new `// Redemption` block:
    ```python
    parts.append("\n    // Redemption (1h cooldown between redeem_request and redeem_fulfill)\n")
    parts.append(
        f"    public fun redemption_cooldown_ms(): u64 "
        f"{{ {red['cooldown_ms']} }}\n"
    )
    ```

    (b) In `emit_python(data)`: after the existing `# Oracle` block (around L199-200), insert:
    ```python
    parts.append("\n# Redemption (1h cooldown between redeem_request and redeem_fulfill)\n")
    parts.append(f"REDEMPTION_COOLDOWN_MS: Final[int] = {red['cooldown_ms']}\n")
    ```
    (Hoist `red = data["redemption"]` near the top alongside `oracle = data["oracle"]`.)

    (c) In `emit_typescript(data)`: after the existing `// Oracle` block (around L255), insert (note BigInt `n` suffix because the value is u64-equivalent in Move — PATTERNS.md "TS bigint convention" + Phase 01-02 lesson on Pitfall B):
    ```python
    parts.append("\n  // Redemption (1h cooldown between redeem_request and redeem_fulfill)\n")
    parts.append(f"  REDEMPTION_COOLDOWN_MS: {red['cooldown_ms']}n,\n")
    ```
    (Hoist `red = data["redemption"]` alongside `oracle = data["oracle"]`.)

    (d) **Numeric value formatting:** The TOML literal `3_600_000` deserializes to Python int `3600000` (tomllib strips underscores). Emit the value as-is (`{red['cooldown_ms']}`) — Move accepts plain digits, Python accepts plain digits, TypeScript accepts plain digits with `n` suffix. (Existing emit code uses plain `{...}` interpolation for token_bucket too, so the pattern is established. There is no need to call `_fmt_underscored` — that helper is reserved for the very large Cody-Phi coefficients where underscored grouping aids audit.)

    **Sub-step 3 — Regenerate via `make codegen`:** Run `(cd backtest && uv run --no-project python ../scripts/codegen.py)` to overwrite `contracts/sources/strategy_constants.move`, `backtest/src/deepvault/strategy_constants.py`, and `dashboard/src/lib/strategy_constants.ts`. Verify all three files now contain the new constant (3600000 / 3_600_000 / 3600000n respectively).

    **Sub-step 4 — Idempotency + drift check:** Run `make codegen` a second time; `git diff` should show no further changes. Then run `(cd backtest && uv run --no-project python ../scripts/codegen.py --check)` and confirm exit 0.

    Notes:
    - This change is additive (new section, new accessor); no existing accessor changes. All Phase 1/2/3 tests should remain green automatically.
    - The CI codegen-drift job (5-job matrix per `.github/workflows/ci.yml`) will FAIL on any commit that touches `shared/strategy.toml` without regenerating the 3 output files — this task does the regen, so CI stays green.
  </action>
  <verify>
    <automated>(cd backtest && uv run --no-project python ../scripts/codegen.py --check) && grep -q '\[redemption\]' shared/strategy.toml && grep -q 'cooldown_ms.*3_600_000' shared/strategy.toml && grep -q 'redemption_cooldown_ms' contracts/sources/strategy_constants.move && grep -q 'REDEMPTION_COOLDOWN_MS.*3600000' backtest/src/deepvault/strategy_constants.py && grep -q 'REDEMPTION_COOLDOWN_MS.*3_600_000n\|REDEMPTION_COOLDOWN_MS.*3600000n' dashboard/src/lib/strategy_constants.ts</automated>
  </verify>
  <done>
    - `make codegen` runs cleanly; `--check` immediately after exits 0 (drift = none)
    - All 3 generated files contain a `REDEMPTION_COOLDOWN_MS` (or `redemption_cooldown_ms()` for Move) accessor with byte-equivalent numeric value 3_600_000
    - `shared/strategy.toml` has the new `[redemption]` section with `cooldown_ms = 3_600_000`
    - Running `make codegen` twice produces no diff between the runs (idempotency)
  </done>
</task>

<task type="auto">
  <name>Task 2: Switch contracts/sources/redeem.move to read cooldown from strategy_constants — preserve Move test green status</name>
  <files>contracts/sources/redeem.move</files>
  <read_first>
    - contracts/sources/redeem.move L1-50 (imports + const block — note line 21 already imports `deepvault::strategy_constants`)
    - contracts/sources/redeem.move L104-125 (`redeem_fulfill` function — the cooldown assertion at L120)
    - contracts/sources/strategy_constants.move (post Task 1 regen — confirm `redemption_cooldown_ms()` exists)
    - .planning/phases/05-testnet-demo-hardening/05-CONTEXT.md §"D-08" — directs replacing the local const, with the cleanest Move-level import semantics path
  </read_first>
  <action>
    **Replace the local `const COOLDOWN_MS: u64 = 3_600_000;` at L40 and update the consumer at L120.**

    Option chosen: **Delete the local const entirely, call `strategy_constants::redemption_cooldown_ms()` inline.** Justification:
    - The import is already in place (L21 `use deepvault::strategy_constants;`).
    - Inline calls are the established pattern in this very file (L126: `math::mul_div_round_down(outstanding, nav, strategy_constants::nav_scale())`).
    - Move-level visibility is `public fun` per codegen output — no `public(package)` gating to work around.
    - Eliminates the duplicate-truth risk (the prior plan run flagged "if Move-level import semantics work" — they do; the existing call sites prove it).

    Concretely:

    1. **Delete L37-40** (the comment + const declaration):
       ```
       // === Constants ===

       /// D-01: 1-hour cooldown between redeem_request and redeem_fulfill.
       const COOLDOWN_MS: u64 = 3_600_000;
       ```
       (Keep `// === Errors ===` block above unchanged. The new file structure goes directly from errors to events with no constants block.)

    2. **Update L120** (inside `redeem_fulfill`):
       ```
       assert!(now_ms >= slot_ts + COOLDOWN_MS, ECooldownNotMet);
       ```
       becomes:
       ```
       assert!(now_ms >= slot_ts + strategy_constants::redemption_cooldown_ms(), ECooldownNotMet);
       ```

    3. **Verify no other references** — `grep -n COOLDOWN_MS contracts/sources/redeem.move` must return 0 results after the edit. (Search the whole `contracts/` tree to ensure no test fixture references the local const by name; if any do, update them to call the strategy_constants accessor too.)

    4. **Run `sui move test --gas-limit 100000000000` in contracts/** (or document in the SUMMARY that the executor's environment lacks Sui CLI — in that case, the CI move job is the empirical gate; static review confirms the call signature matches the existing `strategy_constants::nav_scale()` call sites). The 8 existing redeem-related tests in `redeem_test.move` exercise the cooldown gate; they MUST still pass because the runtime value is unchanged (3_600_000 → 3_600_000 via codegen).

    Notes:
    - This is a 1-line behavior-preserving refactor at runtime; the test-suite green status is the empirical proof of correctness.
    - The strategy_constants import was added in Plan 02-05 (see redeem.move L21); this task just expands its usage.
    - No CONTRIBUTING.md POLICY: or MATH: prefix is required — this is a refactor of a redemption parameter, not a hedge_policy or SVI math change. A normal `feat(05-04):` or `refactor(05-04):` commit prefix is sufficient.
  </action>
  <verify>
    <automated>! grep -n 'const COOLDOWN_MS' contracts/sources/redeem.move && grep -q 'strategy_constants::redemption_cooldown_ms()' contracts/sources/redeem.move && grep -c 'COOLDOWN_MS' contracts/sources/redeem.move | grep -E '^0$|^$' && (cd contracts && sui move build 2>&1 || echo "BUILD-CHECK-DEFERRED-TO-CI")</automated>
  </verify>
  <done>
    - `contracts/sources/redeem.move` no longer declares `const COOLDOWN_MS`
    - The cooldown assertion now reads `strategy_constants::redemption_cooldown_ms()`
    - `grep -c 'COOLDOWN_MS' contracts/sources/redeem.move` = 0 (no stale references)
    - `sui move build` in contracts/ exits 0 (or, if Sui CLI is unavailable in execute environment, the build gate falls to CI and a SUMMARY note records the deferral — see Plan 01-05 precedent for this fallback documentation pattern)
    - All existing `sui move test` cases still pass (8 redeem tests, Plan 02-05; CI move job verifies empirically on push)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared/strategy.toml → 3 runtime constants | Codegen.py is the trusted translator; CI's codegen-drift job is the integrity gate |
| Move bytecode → Move tests | Phase 2 redeem tests verify the on-chain cooldown gate behaviorally |
| TS smoke test → STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS | Plan 05-03 consumer; trusts the codegen pipeline |

## STRIDE Threat Register (ASVS L1 coverage)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | Move on-chain const (3_600_000) drifts from TS smoke-test wait (e.g. someone bumps strategy.toml but forgets to redeploy the package) | mitigate | The Move const is removed entirely — it now reads from `strategy_constants::redemption_cooldown_ms()`. If strategy.toml changes, codegen regenerates the Move module, which would require a redeploy. Mismatch is structurally impossible within a single deployed package because both Move + TS read the same source. |
| T-05-09 | Tampering | Codegen emits different numeric values to the 3 runtimes (e.g. typo in emit_typescript drops the `n` BigInt suffix) | mitigate | Acceptance criteria includes a grep gate for the exact pattern `3_600_000n` in TS (or `3600000n` post-tomllib strip). CI's codegen-drift job verifies that re-running codegen produces no diff against committed files. golden-vector parity (Phase 1) is not directly affected by this constant (it's a time, not an SVI parameter), but the cross-runtime numeric-equality discipline is identical. |
| T-05-10 | Repudiation | Future developer edits the generated Move/Python/TS file directly instead of strategy.toml | mitigate | Existing AUTO-GENERATED header on every emitted file warns. Existing CI codegen-drift job rejects PRs that touch generated files without regenerating from source. No new mitigation needed. |
| T-05-11 | Denial of Service | The new accessor `redemption_cooldown_ms()` adds a function call to redeem_fulfill's hot path | accept | Move function calls to public accessors are inlined at compile time for simple `public fun () : u64 { CONST }` shapes (see Sui Move docs). Gas cost is identical to the inline const. Same pattern is used at L126 `strategy_constants::nav_scale()` with no observed gas penalty. |

All ASVS L1-applicable threats addressed.
</threat_model>

<verification>
- `grep -q '\[redemption\]' shared/strategy.toml` AND `grep -q 'cooldown_ms.*3_600_000' shared/strategy.toml`
- `grep -q 'redemption_cooldown_ms' contracts/sources/strategy_constants.move`
- `grep -q 'REDEMPTION_COOLDOWN_MS.*3600000' backtest/src/deepvault/strategy_constants.py`
- `grep -q 'REDEMPTION_COOLDOWN_MS.*3600000n\|REDEMPTION_COOLDOWN_MS.*3_600_000n' dashboard/src/lib/strategy_constants.ts`
- `grep -q 'strategy_constants::redemption_cooldown_ms()' contracts/sources/redeem.move`
- `! grep -q 'const COOLDOWN_MS' contracts/sources/redeem.move`
- `(cd backtest && uv run --no-project python ../scripts/codegen.py --check)` exits 0 (no drift after first regen)
- `make codegen && git diff --exit-code shared/strategy.toml scripts/codegen.py contracts/sources/strategy_constants.move backtest/src/deepvault/strategy_constants.py dashboard/src/lib/strategy_constants.ts` shows no changes on the second run (idempotency)
- Existing tests stay green: `sui move test` (CI move job), `cd backtest && uv run pytest` (CI python job), `pnpm -r run test` (CI ts job)
</verification>

<success_criteria>
- One new TOML section + 3 codegen emit_* extensions land in a single coherent commit
- All 3 runtime files emit the new constant with byte-identical numeric value (3_600_000)
- `redeem.move` switches its cooldown gate from a local const to the strategy_constants accessor — no duplicate-truth path remains
- CI codegen-drift job stays green
- All existing Move/Python/TS tests still pass
- DEPLOY-04 enabler in place — Plan 05-03 can now import `STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS` without hardcoding
</success_criteria>

<output>
After completion, create `.planning/phases/05-testnet-demo-hardening/05-04-SUMMARY.md` capturing:
- Files modified (6): strategy.toml + codegen.py + 3 generated files + redeem.move
- Diff summary: 1 new TOML section, 3 emit_* extensions in codegen.py, 1 line replaced in redeem.move
- CI drift check result (codegen-drift exit 0)
- Move test status (sui move test in contracts/ exits 0, or CI-deferred per Plan 01-05 pattern)
- Three-way byte-identical numeric value verified: 3_600_000 in Move, 3600000 in Python (Final[int]), 3600000n in TS BigInt
- Resume signal for Plan 05-03 (which consumes STRATEGY_CONSTANTS.REDEMPTION_COOLDOWN_MS)
</output>
