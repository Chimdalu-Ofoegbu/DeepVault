---
phase: 05-testnet-demo-hardening
plan: 05
plan_id: 05-05
type: execute
wave: 3
depends_on:
  - 05-01
  - 05-02
  - 05-03
files_modified:
  - docs/MAINNET-READINESS.md
  - README.md
  - Makefile
requirements:
  - DEPLOY-09
autonomous: true

must_haves:
  truths:
    - "docs/MAINNET-READINESS.md has 3 new top sections — (a) Why deferred, (b) <=30-minute post-submission deploy procedure with concrete CLI sequence, (c) Architecture mainnet compatibility via single-config-flip — appended ABOVE the existing funding playbook"
    - "README.md has: testnet contract addresses from TESTNET-DEPLOY.json, `make demo` pointer, light-touch laypitch + glossary (Phase 6 polishes further), mainnet-readiness section linking to MAINNET-READINESS.md, Sui testnet explorer URLs"
    - "Makefile `demo:` target replaces the `@echo 'TODO: Phase 6 fills this in'` placeholder with `bash scripts/testnet-smoke-test.sh`"
    - "`make demo` invokes Plan 05-03's testnet smoke test successfully (where credentials present) OR documents the operator-runnable recipe (where not)"
    - "MAINNET-READINESS.md preserves the original funding playbook content (wallet setup, $80 budget, Cetus path) below the new top sections — post-submission operational reference unchanged"
    - "README cold-read smoke test passes: a judge with 10 minutes can understand the mainnet posture in under 2 minutes (claim verified by checklist in SUMMARY)"
  artifacts:
    - path: "docs/MAINNET-READINESS.md"
      provides: "Two-audience runbook: judges (why deferred) + operator (≤30-min deploy)"
      contains: "Why deferred"
      contains_2: "30-minute"
      contains_3: "single-config-flip"
    - path: "README.md"
      provides: "Judge-skimmable project entrypoint with mainnet-readiness posture + reproducible demo"
      contains: "make demo"
      contains_2: "MAINNET-READINESS.md"
      contains_3: "TESTNET-DEPLOY.json"
    - path: "Makefile"
      provides: "make demo wires to scripts/testnet-smoke-test.sh"
      contains: "scripts/testnet-smoke-test.sh"
  key_links:
    - from: "Makefile demo target"
      to: "scripts/testnet-smoke-test.sh"
      via: "bash invocation"
      pattern: "bash scripts/testnet-smoke-test.sh"
    - from: "README.md mainnet-readiness section"
      to: "docs/MAINNET-READINESS.md"
      via: "Markdown link"
      pattern: "docs/MAINNET-READINESS.md"
    - from: "docs/MAINNET-READINESS.md ≤30-min procedure"
      to: "scripts/predict-mainnet-check.sh + scripts/preflight.sh + scripts/mainnet-deploy.sh + scripts/mainnet-smoke-test.sh"
      via: "shell command sequence in the procedure section"
      pattern: "preflight.sh.*mainnet-deploy.sh"
---

<objective>
Close out Phase 5 with the documentation + reproducible-run plumbing that makes the toolkit + testnet smoke test consumable by judges and the post-submission operator. Three files modified:

1. **docs/MAINNET-READINESS.md** — prepend three new top sections explaining (a) why mainnet deploy is deferred, (b) the ≤30-minute post-submission deploy procedure with the concrete CLI sequence, (c) the architecture's mainnet compatibility via single-config-flip. Preserve the original funding playbook content below as the operational reference.
2. **README.md** — harden with: testnet contract addresses, `make demo` pointer, light-touch laypitch + glossary refinement (Phase 6 polishes), mainnet-readiness section, Sui testnet explorer URLs. Per D-11 this is a "judge-skimmable" pass; Phase 6 makes it "judge-readable."
3. **Makefile** — fill in the `demo:` target placeholder with `bash scripts/testnet-smoke-test.sh`.

Wave 3 — depends on Plans 05-01, 05-02, 05-03 because the docs cite actual script paths and the Makefile wires Plan 05-03's smoke test.

Purpose: close DEPLOY-09 + materially anticipate DEPLOY-06 (Phase 6's cold-read README) per the reshape note.

Output: Phase 5 closes. Judges can run `make demo` from a fresh clone and reproduce the testnet smoke test; mainnet posture is clearly explained.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-testnet-demo-hardening/05-CONTEXT.md
@.planning/phases/05-testnet-demo-hardening/05-RESEARCH.md
@.planning/phases/05-testnet-demo-hardening/05-01-SUMMARY.md
@.planning/phases/05-testnet-demo-hardening/05-02-SUMMARY.md
@.planning/phases/05-testnet-demo-hardening/05-03-SUMMARY.md
@.planning/phases/05-testnet-demo-hardening/05-04-SUMMARY.md
@docs/MAINNET-READINESS.md
@README.md
@Makefile
@.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json
@scripts/testnet-smoke-test.sh

<interfaces>
<!-- The actual script paths Plans 05-01/05-02/05-03 created (Plan 05-05 cites these). -->

From Plan 05-01:
- `scripts/preflight.sh` — mainnet-readiness gate; exits non-zero today (Predict TBD)
- `scripts/predict-mainnet-check.sh` — manual RPC probe; emits `"shipped":false` today

From Plan 05-02:
- `scripts/mainnet-deploy.sh` — write-but-don't-execute publish
- `scripts/mainnet-smoke-test.sh` + `scripts/mainnet-smoke-test.ts` — write-but-don't-execute smoke test
- `.planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json` — placeholder `{status: "not_deployed"}`

From Plan 05-03:
- `scripts/testnet-smoke-test.sh` + `scripts/testnet-smoke-test.ts` — JUDGE-FACING; `make demo` invokes this

From Plan 05-04:
- `shared/strategy.toml [redemption].cooldown_ms = 3_600_000` + codegen accessors in all 3 runtimes

The ≤30-minute post-submission deploy procedure (concrete CLI sequence to embed in MAINNET-READINESS.md):
```bash
# Step 1: verify Predict has shipped on mainnet
PREDICT_MAINNET_CANDIDATE=<package-id-from-mysten-announcement> \
  bash scripts/predict-mainnet-check.sh
# Expect: stdout contains "shipped":true

# Step 2: fill in the now-known TBD slots in config/mainnet.toml
# (predict.package_id, predict.registry_id, predict.top_level_shared_object_id,
#  predict.plp_type_tag, oracle_svi.event_module_full, assets.quote_type_tag,
#  contingency.predict_mainnet_shipped=true).
$EDITOR config/mainnet.toml

# Step 3: run preflight to verify config + golden vectors + parity tests
SUI_CONFIG_DIR=~/.sui/sui_config_mainnet bash scripts/preflight.sh
# Expect: exit 0

# Step 4: publish the deepvault package on mainnet + create vault
SUI_CONFIG_DIR=~/.sui/sui_config_mainnet bash scripts/mainnet-deploy.sh
# Expect: .planning/phases/05-testnet-demo-hardening/MAINNET-DEPLOY.json
#         updated with status=deployed and real ids

# Step 5: smoke test the deployed mainnet vault (~1h wall-clock)
SUI_CONFIG_DIR=~/.sui/sui_config_mainnet \
  SUI_PRIVATE_KEY=<...> ORACLE_SVI_ID=<...> \
  bash scripts/mainnet-smoke-test.sh
# Expect: 7 CHECKPOINT PASS markers + dual ±10 bps green
```

Testnet contract addresses (read from .planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json at edit time; placeholder fields are PENDING until Phase 2 actually runs the testnet deploy — README cites the JSON path AND any populated fields):
- package_id, vault_id, admin_cap_id, predict_manager_id (all readable via jq)
- Sui testnet explorer URL pattern: https://suiscan.xyz/testnet/object/<package_id>
- Sui testnet tx digest URL pattern: https://suiscan.xyz/testnet/tx/<deploy_tx_digest>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepend 3 new top sections to docs/MAINNET-READINESS.md; preserve existing funding playbook</name>
  <files>docs/MAINNET-READINESS.md</files>
  <read_first>
    - docs/MAINNET-READINESS.md FULL (current content; the existing "Reshape note" header is already in place per Plan 00-06; the funding playbook content below is the post-submission operational reference)
    - .planning/phases/05-testnet-demo-hardening/05-CONTEXT.md `<decisions>` D-09 (rationale for the rename + the 3 new top sections)
    - .planning/phases/05-testnet-demo-hardening/05-01-SUMMARY.md / 05-02-SUMMARY.md / 05-03-SUMMARY.md (actual script paths to cite)
    - The `<interfaces>` block above (the concrete CLI sequence for the ≤30-min procedure)
  </read_first>
  <action>
    Edit `docs/MAINNET-READINESS.md` to ADD three new top sections AFTER the existing top-of-file `> **Reshape note (2026-05-13):**` block but BEFORE the existing `**Purpose:** Two audiences.` line. **Preserve everything below** — the existing funding playbook (wallets, funding flow, smoke test, risk flag, contingency, demo recording, post-submission, references) remains untouched as the post-submission operational reference.

    The three new sections:

    **Section A — "## Why mainnet deploy is deferred to post-submission":**
    - One paragraph explaining: DeepBook Predict mainnet has not shipped (per Mysten's "later in 2026" timeline; testnet only as of 2026-05-05 launch). Executing a mainnet smoke test before Predict ships would degrade to `vault::supply` + `vault::redeem` with `allocation_bps=0` — a no-hedge variant that's strictly worse as a judge story than the full-PTB testnet demo.
    - Second paragraph: the post-submission window mainnet push remains intended if DeepVault pursues mainnet launch (e.g. winning Sui Overflow or otherwise commercializing). The toolkit (preflight + predict-mainnet-check + mainnet-deploy + mainnet-smoke-test) is committed and lint-clean, ready to invoke in ≤30 minutes when Predict ships.
    - Cross-reference: link to `.planning/phases/05-testnet-demo-hardening/05-CONTEXT.md` for the full reshape rationale.

    **Section B — "## ≤30-minute post-submission deploy procedure":**
    - Numbered list of the 5 concrete CLI steps from the `<interfaces>` block above. Each step:
      - Has the exact command (code-fenced)
      - States the expected output
      - Estimates the wall-clock duration (predict-check ~5s, config edit ~5min, preflight ~30s, deploy ~2-3min RPC, smoke ~62min wall-clock for the cooldown)
    - End with a callout: "Total active-operator time: <30 min editing + waiting on commands. Total wall-clock: ~1h15m including the smoke test cooldown."
    - Link to each script's source so the operator can audit before invoking: `scripts/predict-mainnet-check.sh`, `scripts/preflight.sh`, `scripts/mainnet-deploy.sh`, `scripts/mainnet-smoke-test.sh`.

    **Section C — "## Architecture mainnet compatibility (single-config-flip)":**
    - One paragraph stating the design intent: every mainnet-specific value lives in `config/mainnet.toml`; no Move code, indexer code, or dashboard code hardcodes a mainnet address. The testnet→mainnet switch is a config edit, not a code edit.
    - Bulleted list of the config keys that the operator fills at deploy time (predict.*, oracle_svi.event_module_full, assets.quote_type_tag, deepvault.* output from mainnet-deploy.sh, contingency.predict_mainnet_shipped).
    - Cross-reference: the Pitfall 14 mitigation (config drift between testnet and mainnet) — link to `.planning/research/PITFALLS.md §"Pitfall 14"`.
    - Mention that `Move.toml [addresses].predict` will need updating to the mainnet Predict package address at deploy time (one-line edit; CI will catch drift via the existing SHA-pin gate).

    Below these 3 sections, the existing content STARTING from `**Purpose:** Two audiences.` is preserved verbatim — the wallet setup, funding flow, Cetus path, smoke test outline, risk flag, contingency note, demo recording, post-submission, and references all stand as the post-submission operational reference.

    Notes:
    - Use ASCII characters throughout (hyphens, not em-dashes; ASCII quotes, not smart quotes) to keep grep gates simple and avoid cross-OS rendering surprises (matches Phase 0 codegen header convention).
    - The existing "Reshape note" header at the top of the file stays — it's the breadcrumb explaining why the rename happened.
    - The Step 2 budget correction (Cetus swap: 60 USDsui not 50 — per RESEARCH §"Mainnet-specific gotchas" final row) — verify this is reflected in the preserved Section "Step 2: Acquire USDsui via Cetus DEX". If the existing file still says "$50" / "50 USDsui", patch the Step 1 funding target and Step 2 swap amount to "60 USDsui" with an inline comment "(10 USDsui seed + 50 USDsui smoke test — Plan 05-05 correction per RESEARCH carry-forward)". This is a targeted edit, not a full Section rewrite.
  </action>
  <verify>
    <automated>grep -q 'Why mainnet deploy is deferred' docs/MAINNET-READINESS.md && grep -q '30-minute' docs/MAINNET-READINESS.md && grep -q 'single-config-flip\|single config flip' docs/MAINNET-READINESS.md && grep -q 'scripts/predict-mainnet-check.sh' docs/MAINNET-READINESS.md && grep -q 'scripts/preflight.sh' docs/MAINNET-READINESS.md && grep -q 'scripts/mainnet-deploy.sh' docs/MAINNET-READINESS.md && grep -q 'scripts/mainnet-smoke-test.sh' docs/MAINNET-READINESS.md && grep -q 'Cetus' docs/MAINNET-READINESS.md && grep -q '60 USDsui\|60 USDSUI' docs/MAINNET-READINESS.md</automated>
  </verify>
  <done>
    - Three new top sections (A/B/C) present, each with the required substrings
    - All four mainnet-toolkit script paths cited in the procedure section
    - Funding playbook (Cetus, $80 budget, contingency) preserved below the new sections
    - Step 2 USDsui amount corrected to 60 (10 seed + 50 smoke)
    - File parses cleanly as Markdown (no broken fences); cold-read test from SUMMARY confirms "mainnet posture clear in <2 minutes" — see Task 4 verification recipe
  </done>
</task>

<task type="auto">
  <name>Task 2: Harden README.md with testnet addresses + make demo pointer + mainnet-readiness section</name>
  <files>README.md</files>
  <read_first>
    - README.md FULL (current Status, Laypitch, Glossary, Architecture, Quick Start, Stack, Demo, Repo layout, Hosting, Key policies, Build log sections)
    - .planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json (testnet contract addresses if populated; else cite the JSON path with "pending first deploy" status)
    - .planning/phases/05-testnet-demo-hardening/05-CONTEXT.md `<decisions>` D-11 (partial README hardening; Phase 6 polishes further)
    - docs/MAINNET-READINESS.md (Plan 05-05 Task 1 output — Task 2 cross-references it)
  </read_first>
  <action>
    Edit `README.md` with the following targeted edits — DO NOT rewrite the whole file; preserve the existing Laypitch, Glossary, Stack, Repo layout, Quick Start sections.

    **Edit 1 — Update the `## Demo` section (current L113-116ish: "make demo  # Phase 6 fills this in"):**
    Replace with:
    ```markdown
    ## Demo

    ```bash
    # Reproduces the full testnet vault cycle end-to-end:
    # deposit -> hedge mint -> redeem_request -> 1h cooldown -> redeem_fulfill
    # with a dual ±10 bps NAV verification gate. Takes ~1h wall-clock.
    #
    # Requires SUI_PRIVATE_KEY (ephemeral testnet keypair) and ORACLE_SVI_ID
    # (BTC-USD OracleSVI shared object id) env vars. See docs/DEV-BOOTSTRAP.md
    # for setup. Phase 6 records the demo video against this same flow.
    SUI_PRIVATE_KEY=<...> ORACLE_SVI_ID=<...> make demo
    ```

    Or, equivalently:

    ```bash
    bash scripts/testnet-smoke-test.sh
    ```
    ```

    **Edit 2 — Update the existing `## Hosting` table to also include testnet contract pointers (add rows or add a new `### Testnet contracts` subsection):**
    ```markdown
    ### Testnet contracts

    Live testnet deployment captured in [`.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json`](.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json).
    Field-by-field references (auto-cited from the JSON; placeholders show `PENDING` until Phase 2's testnet deploy actually runs):

    | Object | Sui testnet explorer |
    |--------|---------------------|
    | `deepvault` package | `https://suiscan.xyz/testnet/object/<package_id>` |
    | Vault shared object | `https://suiscan.xyz/testnet/object/<vault_id>` |
    | AdminCap | `https://suiscan.xyz/testnet/object/<admin_cap_id>` |
    | Deploy tx | `https://suiscan.xyz/testnet/tx/<deploy_tx_digest>` |

    The literal addresses populate after `bash scripts/e2e-vault-deploy.sh` runs on testnet (Phase 2 deliverable). `make demo` (Phase 5) consumes the same JSON.
    ```

    **Edit 3 — Add a NEW top-level section after `## Hosting` and before `## Key policies`:**
    ```markdown
    ## Mainnet readiness

    DeepBook Predict mainnet has not shipped during the submission window (per Mysten's "later in 2026" timeline; testnet launched 2026-05-05). DeepVault's mainnet deploy is **deferred to post-submission**.

    The mainnet toolkit is committed and lint-clean — when Predict ships on mainnet, the post-submission operator runs a ≤30-minute deploy procedure:

    ```bash
    bash scripts/predict-mainnet-check.sh   # Verify Predict shipped on mainnet
    bash scripts/preflight.sh                # Verify config + tests
    bash scripts/mainnet-deploy.sh           # Publish + create_vault
    bash scripts/mainnet-smoke-test.sh       # $50-equivalent USDsui round-trip
    ```

    Full procedure + rationale: [`docs/MAINNET-READINESS.md`](docs/MAINNET-READINESS.md).

    The architecture is **mainnet-compatible via a single config flip** in [`config/mainnet.toml`](config/mainnet.toml) — no Move/TS/Python code changes required.
    ```

    **Edit 4 — Update the existing build log Week 1 section with a Week 4 / Week 5 entry (preserves the existing append-only discipline):**
    Append (DO NOT delete or modify previous weeks):
    ```markdown
    ### Week 5 (2026-06-09 to 2026-06-15)

    - **Phase 5 (Testnet Demo Hardening + Mainnet-Readiness Toolkit) completed** — testnet smoke test green end-to-end with dual ±10 bps NAV verification; mainnet-readiness toolkit (preflight + predict-mainnet-check + mainnet-deploy + mainnet-smoke-test) committed and lint-clean, ready to invoke post-submission when DeepBook Predict ships on mainnet.
    - `scripts/testnet-smoke-test.sh` (judge-facing): staged 7-checkpoint cycle, $50-equivalent DUSDC, per-depositor return ratio ≥ 99.9% AND vault NAV drift ≤ 10 bps. Reproducible via `make demo`.
    - `shared/strategy.toml` extended with `[redemption].cooldown_ms = 3_600_000`; codegen propagates to Move/Python/TS; redeem.move no longer holds a local const.
    - `docs/MAINNET-READINESS.md` rewritten: judges read "why deferred"; the post-submission operator reads "≤30-min deploy procedure"; original funding playbook preserved.
    - DEPLOY-01 through DEPLOY-04 + DEPLOY-09 closed per the Phase 5 reshape.
    ```

    Notes:
    - These edits are surgical inserts, not rewrites. The existing Laypitch, Glossary, Stack, Repo layout, Quick Start, Key policies sections are unchanged.
    - Per D-11: Phase 5 makes the README "judge-skimmable"; Phase 6 makes it "judge-readable" (polishes the laypitch, adds an architecture diagram, runs the cold-read test).
  </action>
  <verify>
    <automated>grep -q 'make demo' README.md && grep -q 'scripts/testnet-smoke-test.sh' README.md && grep -q 'MAINNET-READINESS.md' README.md && grep -q 'suiscan.xyz/testnet' README.md && grep -q 'Mainnet readiness' README.md && grep -q 'single config flip\|single-config-flip\|single config' README.md && grep -q 'Week 5' README.md && grep -q 'TESTNET-DEPLOY.json' README.md</automated>
  </verify>
  <done>
    - Demo section updated with the testnet-smoke-test invocation and env var documentation
    - Testnet contract pointer block present, links to TESTNET-DEPLOY.json and Sui testnet explorer
    - Mainnet readiness section present, cross-links to MAINNET-READINESS.md
    - Build log Week 5 entry appended (append-only discipline preserved)
    - Existing laypitch / glossary / stack sections unchanged
    - No broken Markdown (every code fence balanced; every link target exists)
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Makefile demo target to scripts/testnet-smoke-test.sh</name>
  <files>Makefile</files>
  <read_first>
    - Makefile FULL (the existing demo target at L40-41: `demo:` + `@echo "TODO: Phase 6 fills this in — should reproduce demo end-to-end from fresh clone"`)
    - scripts/testnet-smoke-test.sh (Plan 05-03 output — verify the orchestrator invocation contract)
  </read_first>
  <action>
    Replace the existing Makefile `demo:` target body with the scripts/testnet-smoke-test.sh invocation.

    Current state (Makefile L40-41):
    ```makefile
    demo:
    	@echo "TODO: Phase 6 fills this in — should reproduce demo end-to-end from fresh clone"
    ```

    New state:
    ```makefile
    demo:
    	@echo "==> Running testnet smoke test (judge-facing demo cycle)."
    	@echo "    Requires SUI_PRIVATE_KEY + ORACLE_SVI_ID env vars."
    	@echo "    See docs/DEV-BOOTSTRAP.md for ephemeral testnet keypair setup."
    	@echo "    Wall-clock duration: ~1 hour (REDEMPTION_COOLDOWN_MS + RPC latency)."
    	bash scripts/testnet-smoke-test.sh
    ```

    Use tab indentation for the recipe lines (Makefile syntax requires tabs, not spaces).

    Per CONTEXT.md `<decisions>` D-10: "make demo invokes scripts/testnet-smoke-test.sh end-to-end. Judges should be able to `git clone && make install && make demo` to reproduce the full vault cycle."
  </action>
  <verify>
    <automated>grep -q 'bash scripts/testnet-smoke-test.sh' Makefile && ! grep -q 'TODO: Phase 6' Makefile && grep -q '^demo:' Makefile && (cd $(git rev-parse --show-toplevel) && make -n demo 2>&1 | grep -q 'testnet-smoke-test')</automated>
  </verify>
  <done>
    - Makefile demo target invokes scripts/testnet-smoke-test.sh
    - Old "TODO: Phase 6 fills this in" placeholder removed
    - `make -n demo` (dry-run) shows the testnet-smoke-test invocation
    - The recipe lines use tab indentation (Makefile syntax compliance)
  </done>
</task>

<task type="auto">
  <name>Task 4: README cold-read smoke test + Phase 5 closure SUMMARY</name>
  <files>.planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md</files>
  <read_first>
    - All four prior Plan 05 SUMMARYs (05-01, 05-02, 05-03, 05-04) for cross-referencing closed requirements
    - README.md (post Task 2 edits)
    - docs/MAINNET-READINESS.md (post Task 1 edits)
    - Makefile (post Task 3 edits)
  </read_first>
  <action>
    Create `.planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md` documenting:

    1. **Files modified** (3 production files + 1 SUMMARY): docs/MAINNET-READINESS.md, README.md, Makefile, plus this SUMMARY.

    2. **README cold-read smoke test checklist** (per D-11 / DEPLOY-06 anticipation; sets up Phase 6 verification):
       - [ ] A judge reading only `## Status` + `## Mainnet readiness` + `## Demo` understands the mainnet posture in <2 minutes
       - [ ] `make demo` is a discoverable single command; no archaeology required to find the smoke test entry point
       - [ ] Mainnet-readiness section links to MAINNET-READINESS.md so the curious judge has a deeper-reading path
       - [ ] No claims in the README falsely imply a live mainnet deployment (specifically: no "mainnet contract at <id>" entries; the placeholder MAINNET-DEPLOY.json status drives any "deployed" claim)
       - [ ] Testnet contract pointer subsection works whether TESTNET-DEPLOY.json is populated or in placeholder state (cites the JSON; placeholder = "PENDING until Phase 2 testnet deploy runs")

    3. **Phase 5 closure traceability matrix** (mapping each DEPLOY-XX to the plan that closed it):
       | Requirement | Plan | Closure evidence |
       |-------------|------|------------------|
       | DEPLOY-01 | 05-01 | scripts/preflight.sh + scripts/predict-mainnet-check.sh, both lint-clean |
       | DEPLOY-02 | 05-02 | scripts/mainnet-deploy.sh + MAINNET-DEPLOY.json placeholder, lint-clean |
       | DEPLOY-03 | 05-02 | AdminCap owner verification + structural parity with e2e-vault-deploy.sh |
       | DEPLOY-04 | 05-03 + 05-04 | scripts/testnet-smoke-test.sh+.ts; dual ±10 bps gate; `make demo` wires it; cooldown codegen sources the wait time |
       | DEPLOY-09 | 05-05 | docs/MAINNET-READINESS.md three new top sections + ≤30-min procedure + single-config-flip explanation + funding playbook preserved |

    4. **Dropped pieces vs the prior plan run** (proof that the reshape carried through):
       - GitHub Actions cron for predict-mainnet-check.sh — NOT created (D-07)
       - `runSplitDemoPath` — not implemented anywhere
       - AdminCap pause recovery procedure — not added to testnet smoke test (per CONTEXT.md `<specifics>`)
       - `SMOKE-TEST-SPLIT-DEMO.json` artifact — not created
       - Mainnet smoke test execution — deferred (D-05)
       - The 7th plan from the prior run — not created (Phase 5 is 5 plans)

    5. **Resume signal for Phase 6:**
       - DEPLOY-06 (README cold-read polish) — Phase 5 set up the structural pieces; Phase 6 runs the cold-read test against a judge persona
       - DEPLOY-05 (demo video) — Phase 5's testnet smoke test IS the demo flow; Phase 6 records the video against `make demo`
       - DEPLOY-07 (architecture diagram) — Phase 6
       - DEPLOY-08 (strategy whitepaper) — Phase 6
       - DEPLOY-10 (Devpost submission) — Phase 6
       - The mainnet-readiness sidebar in the demo video — Phase 6 reads MAINNET-READINESS.md and records a ~10-second segment explaining the post-submission posture

    Notes:
    - This is a closure SUMMARY for Plan 05-05 specifically, not the whole-Phase-5 closure SUMMARY (each plan gets its own).
    - The Phase 5 closure SUMMARY in aggregate is the union of the 5 plan SUMMARYs.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md && grep -q 'DEPLOY-09' .planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md && grep -q 'cold-read' .planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md && grep -q 'Dropped pieces' .planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md && grep -q 'Phase 6' .planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md</automated>
  </verify>
  <done>
    - SUMMARY file exists at the canonical path
    - Phase 5 closure traceability matrix complete (all 5 requirements mapped)
    - README cold-read checklist documents the verification criteria for Phase 6 to run
    - Dropped pieces (cron yml, split-demo, etc.) explicitly listed as proof of reshape adherence
    - Phase 6 resume signals present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| judge reading README -> understanding mainnet posture | The Mainnet readiness section is the single source of truth for "is this on mainnet?" — must be accurate (deferred, not live) |
| make demo -> testnet smoke test execution | The Makefile target is the single entry point judges trust to reproduce the demo |
| docs/MAINNET-READINESS.md -> post-submission operator | The runbook is the operational contract; an incorrect step here causes a real-money deploy mistake |

## STRIDE Threat Register (ASVS L1 coverage)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-24 | Information Disclosure / Misrepresentation | README falsely implies mainnet is live (e.g. an old "mainnet redeploy completed" sentence survives an edit) | mitigate | Task 2 acceptance includes a forbidden-claim grep: the README must NOT contain "mainnet redeploy completed" / "deployed on mainnet" / "mainnet vault at ..." (without a clear "deferred" qualifier). The mainnet-readiness section's only "deployed" claim is via the MAINNET-DEPLOY.json status field (currently "not_deployed") — see Task 2 verify gate. |
| T-05-25 | Tampering | MAINNET-READINESS.md ≤30-min procedure has wrong CLI sequence (e.g. forgets the predict-mainnet-check step) | mitigate | Task 1 acceptance: grep for all 4 mainnet script paths present in the procedure section. The sequence comes from the `<interfaces>` block which is itself derived from CONTEXT.md D-07. |
| T-05-26 | Repudiation | `make demo` invokes the smoke test but fails silently when SUI_PRIVATE_KEY is missing | mitigate | scripts/testnet-smoke-test.sh (Plan 05-03 Task 1) asserts both env vars at the top with explicit error messages; the Makefile recipe echoes the env var requirement. Failure is loud, not silent. |
| T-05-27 | Tampering | A future edit to docs/MAINNET-READINESS.md adds a contradictory claim (e.g. "mainnet deploy completed in Phase 5") | mitigate | Plan 06's plans will read this doc; build-log discipline (CONTRIBUTING.md) requires append-only weekly entries that would surface any post-submission deploy. No code-level enforcement; risk is bounded by solo-build review. |
| T-05-28 | Information Disclosure | The README mistakenly lists a real testnet wallet address in the contract pointers section | accept | All on-chain addresses (package_id, vault_id, admin_cap_id, treasury_cap_holder) are public by construction. The deployer wallet address that holds AdminCap is referenced in TESTNET-DEPLOY.json (already on disk); README cites the JSON path, not the address. Acceptable disclosure. |
| T-05-29 | Denial of Service | `make demo` recipe consumes testnet faucet funds repeatedly if judges run it multiple times | mitigate | Per CONTEXT.md `<critical_design_principles>` "Makefile demo target abuse": document that `make demo` runs against a faucet-funded testnet wallet, not a user's wallet. Task 2 Edit 1 already calls this out in the Demo section ("Requires ephemeral testnet keypair") and the README hardening note explains the env var pattern. |

All ASVS L1-applicable threats addressed.
</threat_model>

<verification>
- `grep -q 'Why mainnet deploy is deferred' docs/MAINNET-READINESS.md` (Section A header)
- `grep -q '30-minute' docs/MAINNET-READINESS.md` (Section B header)
- `grep -q 'single-config-flip\|single config flip' docs/MAINNET-READINESS.md` (Section C header)
- All 4 mainnet-toolkit script paths cited in MAINNET-READINESS.md
- `grep -q '60 USDsui\|60 USDSUI' docs/MAINNET-READINESS.md` (Cetus swap amount corrected per RESEARCH carry-forward)
- README: `make demo`, `scripts/testnet-smoke-test.sh`, `MAINNET-READINESS.md`, `TESTNET-DEPLOY.json`, `suiscan.xyz/testnet`, "Mainnet readiness" section, Week 5 build log entry — all present
- Forbidden README claim: no `mainnet redeploy completed` or `deployed on mainnet` (without a "deferred" qualifier)
- Makefile demo target invokes `bash scripts/testnet-smoke-test.sh`; old TODO placeholder gone
- `make -n demo` dry-run shows the testnet-smoke-test invocation
- 05-05-SUMMARY.md exists with the Phase 5 closure traceability matrix and the dropped-pieces verification

Optional integration check (if testnet credentials available at plan-execute time):
- `SUI_PRIVATE_KEY=<...> ORACLE_SVI_ID=<...> make demo` exits 0 with the 7-checkpoint stdout from Plan 05-03's testnet-smoke-test (this is the load-bearing acceptance for the Makefile target; the script itself is already validated by Plan 05-03)
</verification>

<success_criteria>
- docs/MAINNET-READINESS.md hardened with three new top sections (why deferred / ≤30-min procedure / single-config-flip)
- Existing funding playbook preserved as post-submission operational reference; Step 2 USDsui amount corrected to 60
- README hardened: testnet contract pointers, `make demo` documentation, mainnet-readiness section, Week 5 build log
- Makefile `demo:` target wired to scripts/testnet-smoke-test.sh
- Phase 5 closure SUMMARY documents all 5 plans' contributions to the 5 DEPLOY-XX requirements
- Dropped pieces (cron yml / split-demo / 7th plan) explicitly verified as absent
- DEPLOY-09 closed; DEPLOY-06 (cold-read) anticipated for Phase 6
</success_criteria>

<output>
After completion, the `.planning/phases/05-testnet-demo-hardening/05-05-SUMMARY.md` is the closure artifact. Phase 5 is complete; Phase 6 (Submission Package) takes the baton with the testnet smoke test as the demo flow + the mainnet-readiness toolkit as the post-submission contingency.
</output>
