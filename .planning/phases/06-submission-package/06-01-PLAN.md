---
phase: 06-submission-package
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backtest/reports/full-365d.json
  - backtest/reports/full-365d-report.html
  - docs/architecture.svg
  - .planning/phases/06-submission-package/NUMBERS-CANONICAL.md
autonomous: true
requirements: [DEPLOY-07]
must_haves:
  truths:
    - "The backtest report JSON and HTML are committed git artifacts (force-added past .gitignore), so every number published downstream traces to a committed file (LD-1)."
    - "A single canonical numbers ledger exists declaring which window each headline figure comes from and which committed file is its source, so no downstream artifact mixes windows."
    - "docs/architecture.svg renders on GitHub and depicts the four real tiers plus the two-protocol single-PTB composability moment."
  artifacts:
    - path: "backtest/reports/full-365d.json"
      provides: "Committed OOS-window headline numbers + sensitivity table + 7 hedge_trades (the machine-generated snapshot)"
      contains: "oos_apy"
    - path: "backtest/reports/full-365d-report.html"
      provides: "Committed attachable HTML report for the Devpost bundle"
      min_lines: 1
    - path: "docs/architecture.svg"
      provides: "Hand-authored 4-tier architecture diagram, GitHub-renderable, referenced by README"
      contains: "Python backtest"
    - path: ".planning/phases/06-submission-package/NUMBERS-CANONICAL.md"
      provides: "Single source-of-truth mapping every publishable figure -> window label -> committed file:field"
      contains: "full-365d.json"
  key_links:
    - from: "backtest/reports/full-365d.json"
      to: "git index"
      via: "git add -f past .gitignore:54"
      pattern: "git ls-files .* full-365d.json"
    - from: "NUMBERS-CANONICAL.md"
      to: "backtest/reports/full-365d.json + .planning/backtest-assumptions.md"
      via: "every figure cites a committed file + window label"
      pattern: "full-window|OOS"
---

<objective>
Establish the two upstream artifacts that every other Phase-6 plan references: the committed backtest report (force-added past .gitignore so LD-1's "if a number isn't in a committed artifact, it is not published" is satisfied) and the hand-authored four-tier architecture SVG. Also produce the canonical numbers ledger that fixes which window each headline figure comes from, so the whitepaper, README, and Devpost draft never mix windows or invent a number.

Purpose: This is the honesty-bar gate. No downstream plan may quote a backtest number until the report is committed and NUMBERS-CANONICAL.md declares its window + source. The SVG is referenced by the README (Plan 03) and the whitepaper (Plan 02), so it must exist first.

Output: Force-committed `backtest/reports/full-365d.{json,html}`, `docs/architecture.svg`, `.planning/phases/06-submission-package/NUMBERS-CANONICAL.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-submission-package/CONTEXT.md
@.planning/phases/06-submission-package/06-RESEARCH.md

# The number sources (read for exact field values + window labels):
@.planning/backtest-assumptions.md
# The architecture content source (45 KB; tier-by-tier component map):
@.planning/research/ARCHITECTURE.md

<facts>
<!-- Verified file:line facts the executor must use verbatim — do NOT re-derive. -->

REPORT FILES (exist on disk, GITIGNORED at .gitignore:54):
- backtest/reports/full-365d.json (25,557 bytes)
- backtest/reports/full-365d-report.html (4,957,703 bytes ~= 4.96 MB)
- `git ls-files backtest/reports/` currently returns EMPTY (untracked/ignored).

TWO NUMBER SETS / TWO WINDOWS (06-RESEARCH.md §1, Pitfall 2):
- OOS-window headline lives in backtest/reports/full-365d.json (top-level keys):
    oos_apy = -0.022970276023766778  (-2.30%)
    oos_sharpe = -1.8690399608930919 (-1.87)
    oos_sortino = -0.712804447615134 (-0.71)
    oos_max_drawdown_bps = -98       (-0.98%)
    unhedged_max_drawdown_bps = -2802 (-28.02% buy-and-hold BTC over OOS)
    n_hedge_cycles = 7
    n_hedge_payoffs = 0              (calm OOS regime; hedge was net cost)
    strategy_attribution.total_return = -0.006947216841157473 (-0.69% OOS)
- FULL-WINDOW headline lives ONLY in the git-tracked ledger
  .planning/backtest-assumptions.md (lines 185-208) + 03-10-SUMMARY.md (L123-158):
    PLP yield +7.14% / PLP LVR drag -4.16% / hedge cost -5.43% /
    hedge payoff +9.98% (1 payoff fired) / TOTAL RETURN +7.52% /
    hedged max DD -1.66% vs unhedged buy-and-hold BTC -52.86%.
- The +7.52% number is NOT in the JSON. The JSON only has OOS.
- Minor JSON-vs-ledger OOS drift (different runs): OOS Sharpe -1.87 (JSON) vs -1.92 (ledger);
  OOS APY -2.30% (JSON) vs -2.37% (ledger). Prefer the JSON values (machine-generated artifact)
  for OOS claims; cite full-window claims to backtest-assumptions.md.

DO NOT REGENERATE the report: walk_forward re-fetches live BTC from Binance and caches to the
gitignored backtest/data/ — regenerated numbers WILL differ from this snapshot (06-RESEARCH.md §1).
The committed JSON is the only stable snapshot. Force-add it; do not re-run the CLI.

FOUR TIERS (06-RESEARCH.md §5; full component lists in .planning/research/ARCHITECTURE.md):
- Tier 1 — Move package (contracts/sources/): vault.move, supply.move, redeem.move,
  rebalance.move, share.move, ltv.move, svi_view.move, predict_adapter.move + helpers.
- Tier 2 — Event relay / indexer (indexer/src/, Node): pollOracleSVI.ts (queryEvents 2s),
  pollVaultEvents.ts, wsServer.ts (replay-on-connect), snapshot.ts, cursor.ts. Deploys to Render.
- Tier 3 — React dashboard (dashboard/src/, Vite + dapp-kit): 11 panels; App.tsx has the
  Vault | Risk-Studio mode split (Phase 04.2). Deploys to Vercel.
- Tier 4 — Python backtest harness (backtest/src/deepvault/): strategy_sim.py, walk_forward.py,
  vault_state.py, lookahead_audit.py, report.py, svi.py.
- Two-protocol single-PTB centerpiece (scripts/two-protocol-ptb-demo.ts, 5-call shape):
  margin deposit -> borrow_quote -> withdraw (bridge) -> vault::supply::supply (atomic deposit +
  hedge mint) -> optional VAULT_SHARE re-deposit. ATOMIC: any call fails -> whole PTB reverts.
  Data flow: strategy.toml --codegen--> Move/Py/TS constants; vault emits OracleSVIUpdated +
  Supplied + HedgeMinted -> indexer queryEvents -> WS -> dashboard.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Force-commit the backtest report past .gitignore</name>
  <read_first>
    - 06-RESEARCH.md §1 + Pitfall 1 (the force-add recipe and the "do NOT regenerate" rule)
    - .gitignore line 54 (confirm `backtest/reports/` is the ignore rule)
  </read_first>
  <files>backtest/reports/full-365d.json, backtest/reports/full-365d-report.html</files>
  <action>
    Force-add the two EXISTING report files past .gitignore so they become committed artifacts (the LD-1 fix from 06-RESEARCH.md Pitfall 1). Run exactly:

      git add -f backtest/reports/full-365d.json backtest/reports/full-365d-report.html

    Do NOT modify `.gitignore` (future regens stay ignored). Do NOT regenerate the report — re-running `walk_forward` re-fetches live BTC and changes the numbers (06-RESEARCH.md §1). Do NOT hand-edit any number in either file (LD-7).

    This staging is the deliverable for this task; the phase-end commit (git_commit step) will carry these staged files with a message that documents the deliberate Phase-6 force-add exception (e.g. "docs(06): force-add verified backtest report past gitignore — LD-1 honesty-bar artifact, snapshot 2026-06-15, do not regenerate").
  </action>
  <verify>
    <automated>git add -f backtest/reports/full-365d.json backtest/reports/full-365d-report.html && git ls-files backtest/reports/full-365d.json backtest/reports/full-365d-report.html | grep -c 'full-365d' | grep -qx 2 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files backtest/reports/full-365d.json` is non-empty (file is now tracked).
    - `git ls-files backtest/reports/full-365d-report.html` is non-empty.
    - The JSON still contains `oos_apy` with value `-0.022970276023766778` (unchanged; not hand-edited): `grep -c '\-0.022970276023766778' backtest/reports/full-365d.json` returns >= 1.
    - `.gitignore` line 54 still reads `backtest/reports/` (NOT removed): `grep -c '^backtest/reports/$' .gitignore` returns 1.
  </acceptance_criteria>
  <done>Both report files are git-tracked (force-added), .gitignore unchanged, numbers unmodified.</done>
</task>

<task type="auto">
  <name>Task 2: Write the canonical numbers ledger (window-labeled, source-cited)</name>
  <read_first>
    - 06-RESEARCH.md §1 (both number tables + the JSON-vs-ledger drift note)
    - .planning/backtest-assumptions.md lines 185-208 (full-window figures)
    - backtest/reports/full-365d.json (confirm the OOS field values listed in <facts>)
  </read_first>
  <files>.planning/phases/06-submission-package/NUMBERS-CANONICAL.md</files>
  <action>
    Create NUMBERS-CANONICAL.md — the single source-of-truth that every downstream plan (whitepaper, README, Devpost) MUST cite when quoting a number, so no artifact mixes windows (06-RESEARCH.md Pitfall 2). Structure it as a table with columns: Figure | Value | Window | Committed source file:field. Populate it verbatim from <facts> — paste, do not recompute:

    FULL-WINDOW (365-day) block — source `.planning/backtest-assumptions.md` L185-208 (git-tracked):
      | Total return | +7.52% | full-window 365d | backtest-assumptions.md L197 |
      | PLP yield | +7.14% | full-window | L193 |
      | PLP LVR drag | -4.16% | full-window | L194 |
      | Hedge cost | -5.43% | full-window | L195 |
      | Hedge payoff | +9.98% (1 payoff fired) | full-window | L196 |
      | Hedged max drawdown | -1.66% | full-window | L198 |
      | Unhedged buy-and-hold BTC max DD | -52.86% | full-window | L199 |

    OOS-HOLDOUT (30%) block — source `backtest/reports/full-365d.json` (now committed):
      | OOS APY | -2.30% | OOS holdout | full-365d.json oos_apy |
      | OOS Sharpe | -1.87 | OOS holdout | full-365d.json oos_sharpe |
      | OOS Sortino | -0.71 | OOS holdout | full-365d.json oos_sortino |
      | OOS hedged max DD | -0.98% | OOS holdout | full-365d.json oos_max_drawdown_bps (-98) |
      | OOS unhedged BTC max DD | -28.02% | OOS holdout | full-365d.json unhedged_max_drawdown_bps (-2802) |
      | OOS hedge cycles / payoffs | 7 / 0 | OOS holdout | full-365d.json n_hedge_cycles / n_hedge_payoffs |
      | OOS total return | -0.69% | OOS holdout | full-365d.json strategy_attribution.total_return |

    Add a prominent "## Honest framing (LD-1)" section with the exact one-paragraph story downstream artifacts reuse: "Over the full 365-day window the strategy returned +7.52% (one -15% breach fired; payoff +9.98%); in the calm out-of-sample 30% holdout the hedge was a net cost (APY -2.30%, Sharpe -1.87) — the honest cost-of-carry of crash insurance." Add a "## Rules" note: (a) every published figure MUST carry its window label; (b) never place a full-window return next to an OOS Sharpe without labeling both; (c) prefer JSON values for OOS claims (the machine artifact) over the ledger's slightly-drifted OOS values (-1.92 / -2.37); (d) the +7.52% lives only in the ledger, not the JSON.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-submission-package/NUMBERS-CANONICAL.md && grep -q 'full-window' .planning/phases/06-submission-package/NUMBERS-CANONICAL.md && grep -q 'OOS' .planning/phases/06-submission-package/NUMBERS-CANONICAL.md && grep -q '7.52' .planning/phases/06-submission-package/NUMBERS-CANONICAL.md && grep -q '2.30' .planning/phases/06-submission-package/NUMBERS-CANONICAL.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists and contains both a "full-window" and an "OOS" labeled block: `grep -c 'full-window' ...NUMBERS-CANONICAL.md` >= 1 AND `grep -c 'OOS' ...NUMBERS-CANONICAL.md` >= 1.
    - Contains `7.52` (full-window total return) cited to `backtest-assumptions.md`: `grep -q 'backtest-assumptions.md' ...NUMBERS-CANONICAL.md`.
    - Contains `2.30` and `1.87` (OOS APY + Sharpe) cited to `full-365d.json`: `grep -q 'full-365d.json' ...NUMBERS-CANONICAL.md`.
    - Contains a "Honest framing" section: `grep -qi 'honest framing' ...NUMBERS-CANONICAL.md`.
  </acceptance_criteria>
  <done>NUMBERS-CANONICAL.md maps every publishable figure to its window + committed source; the honest-framing paragraph is present for downstream reuse.</done>
</task>

<task type="auto">
  <name>Task 3: Author the four-tier architecture SVG (DEPLOY-07)</name>
  <read_first>
    - 06-RESEARCH.md §5 (the four-tier inventory + the 5-call two-protocol PTB shape + data-flow)
    - .planning/research/ARCHITECTURE.md (the authoritative component map / content source)
    - CONTEXT.md LD-3 (the diagram contract: 4 tiers + data-flow arrows + PTB composability callout)
  </read_first>
  <files>docs/architecture.svg</files>
  <action>
    Hand-author `docs/architecture.svg` — a committed, GitHub-renderable SVG (no build step) implementing CONTEXT.md LD-3. Use a fixed viewBox (e.g. `viewBox="0 0 1200 800"`), inline styles only (GitHub strips external CSS/JS), and <text> labels (not paths) so the tier names are greppable in the file. Depict the FOUR tiers from <facts> / §5, each as a labeled box with its key modules listed:
      1. "Move package (Sui)" — vault.move, supply/redeem/rebalance, svi_view (binary_price), ltv.move (worst-case NAV), predict_adapter.move.
      2. "Event relay" (Node indexer) — pollOracleSVI (queryEvents 2s) + pollVaultEvents -> wsServer (replay-on-connect). Label "Render".
      3. "React dashboard" — 11 panels; explicitly label the "Vault | Risk Studio" mode split. Label "Vercel".
      4. "Python backtest" — strategy_sim, walk_forward (OOS + sensitivity), vault_state (Move-parity), lookahead_audit, report.
    Draw data-flow arrows (<line>/<path> with arrowhead markers): strategy.toml --codegen--> the three runtimes; vault emits OracleSVIUpdated + Supplied + HedgeMinted --> indexer queryEvents --> WS --> dashboard; backtest replays Move traces (vault_state 1-wei parity).
    Add a visually distinct callout box labeled with the two-protocol single-PTB composability moment: "Single PTB (atomic): Margin borrow -> vault deposit -> Predict hedge mint". Per 06-RESEARCH.md Pitfall 3, annotate it honestly: "architecturally proven via mock_margin_pool integration test; live on testnet pending Mysten DUSDC Margin pool" — do NOT depict a live LoanBorrowed event. Keep total file well under 1 MB (vector text, no embedded raster).
  </action>
  <verify>
    <automated>test -f docs/architecture.svg && head -c 200 docs/architecture.svg | grep -qi '<svg' && grep -q 'Move package' docs/architecture.svg && grep -q 'Event relay' docs/architecture.svg && grep -q 'React dashboard' docs/architecture.svg && grep -q 'Python backtest' docs/architecture.svg && grep -qi 'PTB' docs/architecture.svg && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File begins with an `<svg` root element (valid SVG, GitHub-renderable): `head -c 200 docs/architecture.svg | grep -qi '<svg'`.
    - All four tier labels are present as text: `grep -q 'Move package' docs/architecture.svg`, `grep -q 'Event relay' docs/architecture.svg`, `grep -q 'React dashboard' docs/architecture.svg`, `grep -q 'Python backtest' docs/architecture.svg`.
    - The Vault/Risk-Studio split is labeled: `grep -qi 'Risk Studio' docs/architecture.svg`.
    - The single-PTB composability callout is present: `grep -qi 'PTB' docs/architecture.svg` AND the honest mock/pending annotation is present: `grep -qi 'mock' docs/architecture.svg`.
    - File is under 1 MB: `test $(wc -c < docs/architecture.svg) -lt 1000000`.
  </acceptance_criteria>
  <done>docs/architecture.svg renders on GitHub, shows the four real tiers + data-flow arrows + the honestly-annotated two-protocol single-PTB composability moment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public (commit/push) | The force-added report files + the SVG become public on push. The only real concern is that no secret, private key, or private address is embedded. |

This is a docs/artifacts plan: no new code, no data-flow into a running system, no auth surface. STRIDE categories S/T/R/I/D/E do not map to executable behavior here.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | backtest/reports/full-365d.json + .html (force-added) | mitigate | These contain only synthetic backtest output (BTC OHLC-derived PnL, no keys, no PII). Acceptance confirms numbers are unmodified machine output; no credential fields exist in the report schema. |
| T-06-02 | Information Disclosure | docs/architecture.svg | accept | Diagram depicts public module names + public testnet composability shape only. No secret/address content. Low risk. |
</threat_model>

<verification>
- `git ls-files backtest/reports/full-365d.json` and `... .html` both non-empty (force-add landed).
- `.gitignore` line 54 unchanged.
- NUMBERS-CANONICAL.md has both window blocks + honest-framing section + citations to both committed sources.
- docs/architecture.svg renders (valid `<svg`), shows all 4 tier labels + PTB callout + honest mock annotation, < 1 MB.
- No backtest number was hand-edited (oos_apy value intact).
</verification>

<success_criteria>
- The backtest report is a committed git artifact (LD-1 honesty-bar gate satisfied for all downstream number-quoting).
- A canonical, window-labeled numbers ledger exists; downstream plans cite it, never recompute.
- The four-tier architecture SVG exists and is GitHub-renderable, ready for the README to reference (Plan 03) and the whitepaper to cite (Plan 02).
</success_criteria>

<output>
After completion, create `.planning/phases/06-submission-package/06-01-SUMMARY.md`.
</output>
