---
phase: 06-submission-package
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - docs/WHITEPAPER.md
autonomous: true
requirements: [DEPLOY-08]
must_haves:
  truths:
    - "A reader of docs/WHITEPAPER.md sees the raw 5-parameter SVI math + the binary/digital hedge price formula with a citation to Gatheral & Jacquier 2014 (arXiv:1204.0646)."
    - "The whitepaper states the locked sizing-policy bounds (allocation 10%, -15% OTM, 14d tenor, 2d roll) verbatim and explains the honest negative-OOS framing via the locked-policy re-tuning rule."
    - "The whitepaper has a liquidation-under-worst-case-Predict-outcome section grounded in the committed -60% compound shock anchors, and a risk-disclosures section stating the v1 is unaudited / admin-paused / single-key."
    - "Every published return/risk number carries a window label and traces to a committed artifact per NUMBERS-CANONICAL.md (LD-1)."
  artifacts:
    - path: "docs/WHITEPAPER.md"
      provides: "Gatheral-style strategy whitepaper: SVI math, hedge price formula, sizing bounds, worst-case liquidation, risk disclosures, honest backtest numbers"
      min_lines: 200
      contains: "arXiv:1204.0646"
  key_links:
    - from: "docs/WHITEPAPER.md"
      to: ".planning/phases/06-submission-package/NUMBERS-CANONICAL.md + backtest/reports/full-365d.json"
      via: "every number cites a committed source + window label"
      pattern: "full-window|OOS holdout"
    - from: "docs/WHITEPAPER.md"
      to: "shared/svi-spec.md (binary_price pseudocode) + Gatheral & Jacquier 2014"
      via: "math section cites the spec + the paper"
      pattern: "arXiv:1204.0646"
---

<objective>
Author `docs/WHITEPAPER.md` — a Gatheral-style strategy whitepaper (6-12 "pages" of Markdown) implementing CONTEXT.md LD-2: (a) raw 5-parameter SVI math, (b) the binary/digital hedge price formula off the SVI surface, (c) the locked sizing-policy bounds, (d) a liquidation-under-worst-case-Predict-outcome section, (e) risk disclosures. Every number is window-labeled and cited to a committed artifact (LD-1).

Purpose: This is the credibility document a judge reads to verify the math is correct and the returns are honest. It depends on the committed report + NUMBERS-CANONICAL.md from Plan 01 (force-added in Wave 1) so it can cite real numbers without mixing windows.

Output: `docs/WHITEPAPER.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-submission-package/CONTEXT.md
@.planning/phases/06-submission-package/06-RESEARCH.md

# The canonical numbers ledger from Plan 01 — cite this for EVERY number:
@.planning/phases/06-submission-package/NUMBERS-CANONICAL.md
# The committed report (Plan 01 force-added it):
@backtest/reports/full-365d.json
# The math contract + the policy ADR + the assumptions ledger:
@shared/svi-spec.md
@shared/strategy.toml
@docs/HEDGE-POLICY.md
@.planning/backtest-assumptions.md

<facts>
<!-- Verified file:line facts; paste, do NOT re-derive. -->

PARAMETERIZATION (06-RESEARCH.md §2, Pitfall 5): the on-chain evaluator is RAW 5-parameter SVI,
NOT SSVI (strategy.toml:64 parameterization = "raw_svi_5param"). CONTEXT/REQUIREMENTS say "SSVI"
loosely. Describe the on-chain math as "raw 5-parameter SVI" (a, b, rho, m, sigma); you MAY note
the dashboard renders an SSVI-style surface across tenors but the on-chain pricer is per-slice raw SVI.

SVI param shape (svi-spec.md:226, verbatim from vendored oracle.move:72-83):
  SVIParams { a: u64, b: u64, rho: i64, m: i64, sigma: u64 }   (1e9 fixed-point, scale=9)

BINARY/DIGITAL HEDGE PRICE FORMULA (svi-spec.md:234-251; cloned from vendored
oracle.move::compute_nd2 at oracle.move:400-429) — a zero-drift Black-Scholes digital put:
  k = ln(strike * F / forward)
  total_var = a + b * |rho*(k-m) + sqrt((k-m)^2 + sigma^2)|     (raw SVI total variance w(k))
  d2 = -(k + total_var/2) / sqrt(total_var)
  binary_price = Phi(d2)                                        (normal CDF, u64 in [0, F])
  Phi = Cody 1969 piecewise rational Chebyshev (svi-spec.md:107-155).
  Pricing convention D-06: r = 0 hardcoded (14d discount ~0.998, sub-bp; document the assumption).

GATHERAL CITATION (exact, 06-RESEARCH.md §2): Gatheral & Jacquier (2014),
"Arbitrage-free SVI volatility surfaces", arXiv:1204.0646 — https://arxiv.org/abs/1204.0646.
(Cited in backtest/tests/test_gatheral_paper_vectors.py:7.)

THREE-WAY PARITY GATE (06-RESEARCH.md §2): shared/golden-vectors.json holds 141 vectors
(A=21 Gatheral worked examples, B=100 synthetic + arb-violating, C=20 cross-checks). Tolerance
1 unit at 1e9; empirically all 141 pass at exact equality across Move/Python/TS. CI `parity` job
enforces bit-equality + forbidden-token grep (no Number/Math.X/parseFloat) on the TS evaluator.
NOTE: svi-spec.md:305-318 still says "120 vectors / 20 Gatheral" (stale) — use the REAL numbers
141 total / 21 Gatheral.

FAIR-VALUE-VS-PREDICT ABSTAIN (06-RESEARCH.md §2, rebalance.move:264-270): on-chain ships
theoretical fair value; vault::rebalance compares against predict.get_trade_amounts ask and
ABSTAINS (EPredictMisquote) if Predict ask > SVI fair value by > 0.5% (max_price_premium_bps = 50).

LOCKED SIZING BOUNDS (strategy.toml:24-29, [hedge_policy]; verbatim):
  allocation_bps = 1000        (10% of new deposit)
  strike_otm_bps = 1500        (-15% OTM)
  tenor_seconds = 1209600      (14 days)
  roll_trigger_seconds = 172800 (roll when expiry < 2 days)
  sizing_function = "fixed"    (v1; "dynamic" reserved for v2)
  max_price_premium_bps = 50   (0.5% misquote-abstain threshold)

RE-TUNING POLICY (docs/HEDGE-POLICY.md:55-68) — the honest framing for the negative OOS numbers:
  walk-forward only (60d in-sample / 14d out-of-sample / 30% held-out, never touched during
  calibration); "If the locked policy underperforms in backtest, document the underperformance
  and ship with the principled choice" (HEDGE-POLICY.md:68). Forbidden: re-tuning after testnet/
  mainnet/demo. The locked 0.10 ratio is NOT the OOS-optimal row (0.05 is) — by design, no
  retrospective re-tuning.

LIQUIDATION / WORST-CASE PREDICT (06-RESEARCH.md §4; contracts/sources/ltv.move):
  worst_case_nav_per_share (ltv.move:60-68) = liquid balance / total_shares — assumes ALL open
  hedges expire worthless (D-14); excludes hedge cost basis. Parity anchors (hardcoded 1-wei in
  BOTH runtimes): wcn_pre = 9_009_900_990, wcn_post = 6_306_930_693 under a -30% balance shock.
  Compound -60% shock (worthless hedges + 30% collateral haircut) -> risk_ratio_bps = 8_101 <
  11_500 LIQUIDATION_LTV_BPS -> liquidation fires. Proven bit-equal across Move
  (contracts/tests/liquidation_test.move, 3 tests) and Python
  (backtest/tests/test_liquidation_parity.py, 11 tests). Policy anchor:
  strategy.toml [ltv] worst_case_settlement_haircut_bps = 10000 (100% adverse outcome assumed).

BACKTEST MODEL ASSUMPTIONS (06-RESEARCH.md §3; .planning/backtest-assumptions.md) — MUST disclose:
  - PLP_APY = 0.08 (8%) is an ASSUMPTION (strategy_sim.py:88), not a measured Predict yield.
  - PLP_LVR_COEFF = 0.25 LVR drag (Milionis-Moallemi-Roughgarden 2022).
  - Backtest hedge pricing = zero-drift BS digital put using trailing-30d realized vol as the IV
    proxy — a DIFFERENT pricing path from the on-chain audited SVI evaluator (no historical IV
    surface; Deribit deferred to v2). State both pricing paths explicitly.
  - Coverage-based sizing (target payout = hedge_ratio x NAV, capped premium), settlement =
    expiry-spot (not path-minimum), fees_bps=0, gas=1bp/PTB, rf=0, BARS_PER_YEAR=8760.

SECURITY CLAIMS (safe to publish; 06-RESEARCH.md Security Domain):
  - Inflation-attack defense: virtual shares (10^6) + 10-DUSDC seed burned to @0xdead (ERC-4626 v5).
  - AdminCap is key-only (non-transferable v1); cannot relax Predict's 30s oracle staleness gate.
  - Misquote abstain (EPredictMisquote, 0.5% cap) on every hedge mint.
  - Capability containment (TradeCap/TreasuryCap never escape) proven by ptb_capability_test.move
    + test_ptb_capability_grep.py + two Sui Prover specs (VAULT-10).
  - NO third-party audit (v2 per AUDIT-V2-01): risk-disclosures MUST say "unaudited; admin-paused
    single-key v1." Do NOT imply an audit. The HTML report's shuffled-label/hand-recompute blocks
    are STUBS in the summary render path (06-RESEARCH.md §1 renderer caveat) — for the lookahead-
    audit claim cite backtest/src/deepvault/lookahead_audit.py + backtest-assumptions.md
    §"Lookahead-Bias Audit" (D-06 gate |alpha| <= 0.005; D-07 3-row hand-recompute seed 42),
    NOT the HTML stub block.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Whitepaper part A — overview, SVI math, hedge price formula, sizing policy</name>
  <read_first>
    - 06-RESEARCH.md §2 (SVI math + parity gate + abstain) and §3 (sizing bounds)
    - shared/svi-spec.md:224-275 (param shape, binary_price pseudocode, pricing convention)
    - docs/HEDGE-POLICY.md:54-68 (re-tuning policy = honest framing)
    - NUMBERS-CANONICAL.md (the window labels you will use in Part B)
  </read_first>
  <files>docs/WHITEPAPER.md</files>
  <action>
    Create `docs/WHITEPAPER.md` and write the front matter + the math/policy half. Sections (Gatheral-style, Markdown headings):

    1. "# DeepVault Strategy Whitepaper" + a one-paragraph abstract: a composable PLP+Hedge structured product on Sui DeepBook Predict — "PLP yield minus crash insurance". Note it is a Sui Overflow 2026 submission, v1, unaudited (forward-reference the risk section).
    2. "## 1. The SVI Volatility Surface" — describe the RAW 5-parameter SVI total-variance function w(k) = a + b(rho(k-m) + sqrt((k-m)^2 + sigma^2)) using the param shape from <facts>. Cite Gatheral & Jacquier (2014), "Arbitrage-free SVI volatility surfaces", arXiv:1204.0646 (https://arxiv.org/abs/1204.0646) as the arbitrage-free-SVI framework. Note (Pitfall 5) the on-chain evaluator is per-slice raw SVI; the dashboard renders an SSVI-style surface across tenors. State the 1e9 fixed-point scale.
    3. "## 2. The Binary Hedge Price" — reproduce the binary_price pseudocode block from <facts> (zero-drift BS digital put: total_var -> d2 -> Phi(d2)). State the Cody-1969 Phi and the D-06 r=0 assumption (14d discount ~0.998, sub-bp). Cite shared/svi-spec.md (the locked contract, cloned from vendored oracle.move:400-429). Explain the on-chain misquote abstain: vault::rebalance compares the SVI fair value against predict.get_trade_amounts ask and refuses the mint if Predict's ask exceeds fair value by > 0.5% (EPredictMisquote; rebalance.move:264-270).
    4. "## 3. Three-Way Parity Gate" — the math is proven bit-equal across Move, Python, and TypeScript on 141 golden vectors (21 Gatheral worked examples / 100 synthetic + arb-violating / 20 cross-checks) at 1-unit tolerance at 1e9; CI's `parity` job enforces it plus a forbidden-token grep on the TS evaluator. (Use the REAL 141/21 numbers from <facts>, not the stale 120/20.)
    5. "## 4. Sizing Policy" — state the locked bounds VERBATIM from <facts>: 10% allocation (allocation_bps=1000), -15% OTM (strike_otm_bps=1500), 14-day tenor (tenor_seconds=1209600), 2-day roll trigger (roll_trigger_seconds=172800), fixed sizing v1. Cite shared/strategy.toml [hedge_policy] + docs/HEDGE-POLICY.md. Include the re-tuning policy (walk-forward 60/14/30%-held-out) and quote the honest principle: "If the locked policy underperforms in backtest, document the underperformance and ship with the principled choice" — this sets up Part B's honest negative-OOS framing.

    Every claim that references an implementation detail cites the source file (svi-spec.md, strategy.toml, HEDGE-POLICY.md, rebalance.move). Do NOT quote any return/Sharpe number yet — those land in Part B with window labels.
  </action>
  <verify>
    <automated>test -f docs/WHITEPAPER.md && grep -q 'arXiv:1204.0646' docs/WHITEPAPER.md && grep -q 'allocation_bps' docs/WHITEPAPER.md && grep -q 'raw' docs/WHITEPAPER.md && grep -qi 'binary' docs/WHITEPAPER.md && grep -q '141' docs/WHITEPAPER.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists with the SVI math + hedge price + sizing sections.
    - Cites the Gatheral paper exactly: `grep -q 'arXiv:1204.0646' docs/WHITEPAPER.md`.
    - States the locked allocation param: `grep -q 'allocation_bps' docs/WHITEPAPER.md` AND `grep -q '1000' docs/WHITEPAPER.md`.
    - Describes raw (not SSVI) on-chain math: `grep -qi 'raw' docs/WHITEPAPER.md` and the on-chain pricer is described as 5-parameter: `grep -qi '5-param\|5 param\|five-param' docs/WHITEPAPER.md`.
    - Uses the real parity count: `grep -q '141' docs/WHITEPAPER.md`.
    - Names the misquote abstain: `grep -q 'EPredictMisquote' docs/WHITEPAPER.md`.
  </acceptance_criteria>
  <done>Part A (overview + SVI math + binary hedge price + parity gate + sizing policy) is written with all implementation citations and the Gatheral reference; no unlabeled numbers.</done>
</task>

<task type="auto">
  <name>Task 2: Whitepaper part B — backtest results (window-labeled), worst-case liquidation, model assumptions, risk disclosures</name>
  <read_first>
    - 06-RESEARCH.md §1 (numbers + renderer caveat), §3 (model assumptions), §4 (liquidation)
    - NUMBERS-CANONICAL.md (window labels + honest-framing paragraph — reuse verbatim)
    - .planning/backtest-assumptions.md §"Strategy Simulation Model" + §"Lookahead-Bias Audit"
    - contracts/sources/ltv.move (worst_case_nav formula) — already summarized in <facts>
  </read_first>
  <files>docs/WHITEPAPER.md</files>
  <action>
    Append the results/risk half to `docs/WHITEPAPER.md`. Sections:

    6. "## 5. Backtest Results" — present BOTH windows with explicit labels, pulling values from NUMBERS-CANONICAL.md (do NOT recompute):
       - Full-window (365d, cite backtest-assumptions.md): total return +7.52%, attribution PLP yield +7.14% / LVR drag -4.16% / hedge cost -5.43% / hedge payoff +9.98% (one -15% breach fired), hedged max DD -1.66% vs unhedged buy-and-hold BTC -52.86%.
       - OOS holdout 30% (cite backtest/reports/full-365d.json): APY -2.30%, Sharpe -1.87, Sortino -0.71, hedged max DD -0.98%, 7 hedge cycles / 0 payoffs (calm regime).
       - Lead with the honest-framing paragraph from NUMBERS-CANONICAL.md: full-window +7.52% (one breach fired, payoff +9.98%); in the calm OOS holdout the hedge was a net cost (-2.30% APY, -1.87 Sharpe) — the honest cost-of-carry of crash insurance. NEVER place a full-window return next to an OOS Sharpe without labeling both.
       - Note the sensitivity table shows monotonic insurance cost-of-carry and that the locked 0.10 ratio is NOT the OOS-optimal row (0.05 is) — by design (no retrospective re-tuning, per Part A's policy).
    7. "## 6. Model Assumptions" — disclose, from <facts>/§3: PLP_APY=8% is a defensible ASSUMPTION (not measured Predict yield); PLP_LVR_COEFF=0.25 LVR drag (Milionis-Moallemi-Roughgarden 2022); the backtest prices hedges with a zero-drift BS digital put using trailing-30d realized vol as the IV proxy — a DIFFERENT pricing path from the on-chain audited SVI evaluator (state both paths explicitly); coverage-based sizing; settlement = expiry-spot; rf=0; gas=1bp/PTB. Lookahead-bias audit: cite backtest/src/deepvault/lookahead_audit.py + backtest-assumptions.md §"Lookahead-Bias Audit" (shuffled-label |alpha| <= 0.005 gate D-06; 3-row hand-recompute seed 42 D-07) — explicitly do NOT cite the HTML report's stub block.
    8. "## 7. Liquidation Under Worst-Case Predict Outcome" — use the §4 framing: under the worst-case Predict outcome all open binaries expire worthless, so worst_case_nav_per_share collapses to the liquid quote balance / total_shares (ltv.move:60-68, excludes the spent hedge cost basis). A compound -60% shock (worthless hedges + 30% collateral haircut) drives risk_ratio_bps = 8_101 below the 11_500 bps LIQUIDATION_LTV threshold, firing liquidation — proven bit-equal across Move (liquidation_test.move) and Python (test_liquidation_parity.py) with parity anchors wcn_pre=9_009_900_990 / wcn_post=6_306_930_693. Policy anchor: worst_case_settlement_haircut_bps=10000 (100% adverse outcome assumed). 
    9. "## 8. Risk Disclosures" — state plainly: v1 is UNAUDITED (no third-party audit; audit deferred to v2); admin-paused single-key (AdminCap is key-only, non-transferable v1, cannot relax Predict's 30s staleness gate); known mitigations that ARE in place — inflation-attack defense (virtual shares 10^6 + 10-DUSDC seed burned to @0xdead, ERC-4626 v5 port), per-hedge misquote abstain, capability containment proven by ptb_capability_test.move + test_ptb_capability_grep.py + two Sui Prover specs. The two-protocol single-PTB is architecturally proven via the mock_margin_pool integration test; the live testnet Margin leg is pending Mysten's DUSDC Margin pool (06-RESEARCH.md Pitfall 3) — do NOT claim a live Margin PTB. Do NOT imply an audit anywhere.
    10. "## References" — Gatheral & Jacquier (2014) arXiv:1204.0646; Milionis, Moallemi, Roughgarden (2022) Loss-Versus-Rebalancing; shared/svi-spec.md; docs/HEDGE-POLICY.md; the committed backtest report.

    Final length target 6-12 pages of content (>= ~200 lines total across both tasks).
  </action>
  <verify>
    <automated>grep -q '7.52' docs/WHITEPAPER.md && grep -q '2.30' docs/WHITEPAPER.md && grep -qi 'full-window' docs/WHITEPAPER.md && grep -qi 'out-of-sample\|OOS' docs/WHITEPAPER.md && grep -qi 'unaudited' docs/WHITEPAPER.md && grep -q 'worst_case_nav\|worst-case' docs/WHITEPAPER.md && grep -q '8_101\|8101' docs/WHITEPAPER.md && test $(wc -l < docs/WHITEPAPER.md) -ge 200 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Full-window +7.52% present and window-labeled: `grep -q '7.52' docs/WHITEPAPER.md` AND `grep -qi 'full-window' docs/WHITEPAPER.md`.
    - OOS numbers present and labeled: `grep -q '2.30' docs/WHITEPAPER.md` AND `grep -q '1.87' docs/WHITEPAPER.md` AND `grep -qi 'out-of-sample\|OOS' docs/WHITEPAPER.md`.
    - PLP_APY assumption disclosed: `grep -qi 'PLP_APY\|8% APY\|assumption' docs/WHITEPAPER.md` AND the two-pricing-paths disclosure present: `grep -qi 'trailing-30d\|trailing 30\|realized vol' docs/WHITEPAPER.md`.
    - Worst-case liquidation section grounded in the committed anchor: `grep -q '8_101\|8101' docs/WHITEPAPER.md` AND `grep -qi 'worthless' docs/WHITEPAPER.md`.
    - Risk disclosure is honest about audit status: `grep -qi 'unaudited' docs/WHITEPAPER.md` and does NOT claim a completed audit (manual review: no "audited by" / "passed audit" phrasing).
    - Length >= 200 lines: `test $(wc -l < docs/WHITEPAPER.md) -ge 200`.
  </acceptance_criteria>
  <done>Part B (window-labeled backtest results, model-assumption disclosures, worst-case liquidation, honest risk disclosures, references) is written; every number traces to a committed source per NUMBERS-CANONICAL.md; no audit is implied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| whitepaper claims -> public reader | The whitepaper makes math + performance + security claims. The real surface is claim accuracy, not executable attack surface. |

No new code, data-flow, or auth. The honesty bar (LD-1) and the "no implied audit" rule are the controls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Spoofing (of credibility) | docs/WHITEPAPER.md performance claims | mitigate | Every number window-labeled + cited to a committed artifact via NUMBERS-CANONICAL.md (acceptance greps the figures). No mixed-window numbers. |
| T-06-04 | Repudiation / overclaim | docs/WHITEPAPER.md security + audit claims | mitigate | Risk-disclosures section states "unaudited; admin-paused single-key v1"; acceptance requires `unaudited` and forbids audit-implied phrasing. Two-protocol PTB labeled mock-proven, not live. |
| T-06-05 | Information Disclosure | docs/WHITEPAPER.md | accept | Whitepaper cites only public module names + public testnet figures; no secret/address/key content. |
</threat_model>

<verification>
- docs/WHITEPAPER.md exists, >= 200 lines, GitHub-renderable Markdown.
- Math section cites arXiv:1204.0646 + describes raw 5-param SVI + reproduces the binary_price formula.
- Sizing section states the locked bounds verbatim.
- Results section labels every figure by window (full-window vs OOS holdout) and cites the committed source.
- Liquidation section grounded in the wcn anchors + the 8_101 risk-ratio.
- Risk disclosures honest (unaudited, single-key); no audit implied; two-protocol PTB labeled mock-proven.
</verification>

<success_criteria>
- A judge can read docs/WHITEPAPER.md and verify the hedge math is the audited SVI digital-put formula (with the Gatheral citation), the sizing policy is the locked one, and the returns are honest (modest full-window gain, negative OOS cost-of-carry), each window-labeled and traceable to a committed artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/06-submission-package/06-02-SUMMARY.md`.
</output>
