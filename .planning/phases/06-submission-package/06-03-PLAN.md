---
phase: 06-submission-package
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - README.md
autonomous: true
requirements: [DEPLOY-06]
must_haves:
  truths:
    - "A judge cold-reading README.md sees an accurate current status (Phases 0-5 + dashboard complete, submission-ready), not the stale 'Phase 0 next-up Phase 1' block."
    - "Every docs/MAINNET-FUNDING.md reference is repointed to docs/MAINNET-READINESS.md (zero broken links)."
    - "The testnet contract table shows the real addresses from TESTNET-DEPLOY.json (no <placeholder> strings) with working suiscan links."
    - "README links to the whitepaper, the committed backtest report, and the architecture SVG, and the demo section honestly scopes make demo (supply+hedge+redeem; two-protocol Margin PTB is mock-proven/documented-future)."
  artifacts:
    - path: "README.md"
      provides: "Cold-read-passing judge-facing front door: laypitch, glossary, prerequisites, reproducible make demo, real testnet addresses, links to whitepaper/report/SVG/MAINNET-READINESS"
      contains: "docs/WHITEPAPER.md"
  key_links:
    - from: "README.md"
      to: "docs/MAINNET-READINESS.md"
      via: "all former MAINNET-FUNDING links repointed"
      pattern: "MAINNET-READINESS.md"
    - from: "README.md"
      to: "TESTNET-DEPLOY.json real addresses"
      via: "package_id/vault_id/admin_cap_id/deploy_tx_digest pasted into suiscan URLs"
      pattern: "0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862"
    - from: "README.md"
      to: "docs/WHITEPAPER.md + backtest/reports/full-365d-report.html + docs/architecture.svg"
      via: "links added to the three Phase-6 artifacts"
      pattern: "architecture.svg"
---

<objective>
Polish the EXISTING `README.md` (do NOT rewrite) to pass the DEPLOY-06 cold-read: fix the broken MAINNET-FUNDING links, replace the placeholder testnet addresses with the real ones, correct the stale status block, and add links to the whitepaper + committed backtest report + architecture SVG. Then do a fresh-eyes cold-read pass. Implements CONTEXT.md LD-4.

Purpose: The README is the judge's front door. It currently claims "Phase 0 next-up Phase 1" (the project is submission-ready), has broken links to a renamed doc, and shows `<placeholder>` addresses. This plan makes it accurate and honest. It depends on Plan 01 (the SVG + committed report it links to) and Plan 02 (the whitepaper it links to).

Output: polished `README.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-submission-package/CONTEXT.md
@.planning/phases/06-submission-package/06-RESEARCH.md

# The file being polished (read in full before editing):
@README.md
# The real testnet addresses (canonical source — paste verbatim):
@.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json

<facts>
<!-- Verified file:line facts; paste, do NOT invent. -->

README GAPS (06-RESEARCH.md §6; verified via Grep):
- G1 stale status: README.md L8-14 says "Phase 0 COMPLETE / Phase 1 next-up". Reality: Phases 0-5
  + dashboard (Phase 04.1/04.2) complete; submission-ready. (Trust artifacts over STATE.md, which
  is also stale — Pitfall 4.)
- G2 BROKEN LINKS: README.md references docs/MAINNET-FUNDING.md (renamed to MAINNET-READINESS.md)
  at lines 41, 184, 213 (3 markdown links). ALL occurrences must become docs/MAINNET-READINESS.md.
- G3 PLACEHOLDER ADDRESSES: README.md L124-133 testnet table uses <package_id>/<vault_id>/
  <admin_cap_id>/<deploy_tx_digest> template strings + a "PENDING until Phase 2 runs" caveat.
  Replace with the real values below + drop the "PENDING/placeholder" caveat (it IS deployed).
- G4/G5/G6 MISSING LINKS: no link to docs/WHITEPAPER.md (Plan 02 output); no link to the committed
  backtest report backtest/reports/full-365d-report.html (Plan 01 force-added it); architecture
  link at L54 points at .planning/research/ARCHITECTURE.md (internal) — add/repoint to
  docs/architecture.svg (Plan 01 output) and embed it.
- G7 DEMO HONESTY: README.md L101-120 demo section. Keep make demo (it is real). Add a one-line
  honesty note: make demo exercises supply + REAL on-chain hedge mint + redeem; the two-protocol
  Margin PTB is architecturally proven via the mock_margin_pool test + documented-future on live
  testnet (no DUSDC Margin pool on testnet — 06-RESEARCH.md Pitfall 3). Note ORACLE_SVI_ID comes
  from the Mysten Predict testnet OracleSVI registry.
- G8 PLACEHOLDER OWNER: CI badge (L5) + clone URL (L61) use <owner>. Repo push is user-gated (A6).
  Leave <owner> as-is but ensure it is clearly a fill-at-push placeholder (a brief inline note is
  acceptable); do NOT fabricate a GitHub owner.

REAL TESTNET ADDRESSES (TESTNET-DEPLOY.json, git-tracked, status: deployed) — paste verbatim:
  package_id      = 0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862
  vault_id        = 0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911
  admin_cap_id    = 0x9e40150e07ce223019afbaca425cb08b84c541ad402b428ee4a9942dfaaba3e7
  deploy_tx_digest= ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS
  deployed_at     = 2026-05-16T14:48:36Z
  Suiscan URL patterns: https://suiscan.xyz/testnet/object/<id> and
                        https://suiscan.xyz/testnet/tx/<digest>
  NOTE: TESTNET-DEPLOY.json lives at
  .planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json (NOT repo root).
  README.md L124 already links there — keep that link, just fill the address table.

CONSTRAINT: polish, do NOT rewrite. Preserve the existing laypitch (L17-23), glossary (L24-32),
Quick Start (L56-87), and the Week-1/Week-5 build-log entries. Apply targeted edits only.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken links, stale status, and placeholder testnet addresses</name>
  <read_first>
    - README.md in full (you will make targeted edits, not a rewrite)
    - 06-RESEARCH.md §6 (the itemized gap list G1-G8)
    - TESTNET-DEPLOY.json (confirm the real addresses match <facts>)
  </read_first>
  <files>README.md</files>
  <action>
    Apply targeted edits to README.md (preserve everything not listed):
    1. G2 — Repoint ALL `docs/MAINNET-FUNDING.md` occurrences to `docs/MAINNET-READINESS.md`: the markdown links at L41 (Architecture-at-a-Glance table row + label), L184 (Key policies), and L213 (References "For deploy"). After this edit there must be ZERO occurrences of the string `MAINNET-FUNDING` anywhere in README.md.
    2. G1 — Replace the stale Status block (L8-14). Write an accurate, honest status: Phases 0 through 5 complete plus the PLP Risk Studio dashboard (Phase 04.1 reskin + 04.2 Vault | Risk Studio split); testnet vault deployed (cite deploy date 2026-05-16); submission-ready for Sui Overflow 2026; ship target 2026-06-16. Do NOT claim mainnet is deployed (it is deferred — link MAINNET-READINESS.md). Do NOT overstate (no "audited").
    3. G3 — Fill the testnet contract table (L124-133) with the real addresses from <facts>: `deepvault` package -> https://suiscan.xyz/testnet/object/0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862 ; Vault shared object -> .../object/0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911 ; AdminCap -> .../object/0x9e40150e07ce223019afbaca425cb08b84c541ad402b428ee4a9942dfaaba3e7 ; Deploy tx -> https://suiscan.xyz/testnet/tx/ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS . Remove the "placeholders show PENDING until Phase 2's testnet deploy actually runs" caveat (L124) and the L133 "literal addresses populate after ... runs on testnet" line — the deploy has happened. Keep the link to TESTNET-DEPLOY.json at its real phase-02 path.
  </action>
  <verify>
    <automated>test $(grep -c 'MAINNET-FUNDING' README.md) -eq 0 && grep -q 'MAINNET-READINESS.md' README.md && grep -q '0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862' README.md && grep -q 'ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS' README.md && test $(grep -c '<package_id>\|<vault_id>\|<admin_cap_id>\|<deploy_tx_digest>' README.md) -eq 0 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Zero broken MAINNET-FUNDING links: `grep -c 'MAINNET-FUNDING' README.md` returns 0, and `grep -q 'MAINNET-READINESS.md' README.md` succeeds.
    - Real package_id present: `grep -q '0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862' README.md`.
    - Real vault_id present: `grep -q '0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911' README.md`.
    - Real deploy tx digest present: `grep -q 'ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS' README.md`.
    - Zero placeholder address tokens remain: `grep -c '<package_id>\|<vault_id>\|<admin_cap_id>\|<deploy_tx_digest>' README.md` returns 0.
    - Status no longer says Phase 1 is next-up: `grep -c 'next-up' README.md` returns 0.
  </acceptance_criteria>
  <done>All MAINNET-FUNDING links repointed, status block accurate and honest, testnet table shows real addresses with working suiscan links, no placeholder address tokens remain.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase-6 artifact links, demo honesty note, and run the cold-read pass</name>
  <read_first>
    - README.md (post-Task-1 state)
    - 06-RESEARCH.md §6 (G4/G5/G6/G7) and Pitfall 3 (demo re-scope)
    - ROADMAP.md Phase 6 success criterion #2 (the cold-read bar)
  </read_first>
  <files>README.md</files>
  <action>
    1. G6 — Repoint the "Full architecture diagram" line (L54) from `.planning/research/ARCHITECTURE.md` to `docs/architecture.svg` and embed it (GitHub renders a committed SVG via `![Architecture](docs/architecture.svg)` or a relative link). Keep a one-line pointer to the internal ARCHITECTURE.md for depth if desired.
    2. G4/G5 — Add links to the two new artifacts. In the Architecture-at-a-Glance table (or a fitting nearby section) add a link to `docs/WHITEPAPER.md` ("Strategy whitepaper: SVI math, hedge price, sizing, worst-case liquidation, risk disclosures"). In the Demo / References section add a link to the committed backtest report `backtest/reports/full-365d-report.html` ("Backtest report: 365-day walk-forward, OOS holdout, PnL attribution"). Both files now exist (Plans 01/02).
    3. G7 — In the Demo section (L101-120) add an honest scope note: `make demo` runs supply + a REAL on-chain Predict hedge mint + redeem (the 7-checkpoint dual ±10 bps cycle); the flagship two-protocol single-PTB (Margin borrow + Predict + vault hedge) is architecturally proven via the `mock_margin_pool` integration test and documented as live-on-testnet-pending — there is no DUSDC Margin pool on testnet yet. Note `ORACLE_SVI_ID` is the BTC-USD OracleSVI shared object from the Mysten Predict testnet registry. Do NOT claim a live Margin PTB.
    4. COLD-READ PASS (LD-4 acceptance): re-read the whole README as a first-time judge with 10 minutes. Verify, and fix in-place if any fail: (a) the one-paragraph laypitch is understandable without prior context; (b) the glossary defines PLP/SVI/PTB/NAV; (c) prerequisites + `make demo` are runnable (Windows fallback present); (d) every internal link resolves to a file that exists (whitepaper, report, SVG, MAINNET-READINESS, HEDGE-POLICY, TESTNET-DEPLOY.json, CONTRIBUTING). Record the cold-read outcome (pass + any fixes applied) in the SUMMARY. Do NOT introduce any unlabeled performance number into the README — if you mention returns, label the window and link the whitepaper or NUMBERS-CANONICAL.md.
  </action>
  <verify>
    <automated>grep -q 'docs/WHITEPAPER.md' README.md && grep -q 'docs/architecture.svg' README.md && grep -q 'full-365d-report.html' README.md && grep -qiE 'mock_margin_pool|mock margin|documented-future|pending' README.md && grep -q 'make demo' README.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Whitepaper link present: `grep -q 'docs/WHITEPAPER.md' README.md`.
    - Architecture SVG referenced/embedded: `grep -q 'docs/architecture.svg' README.md`.
    - Committed backtest report linked: `grep -q 'full-365d-report.html' README.md`.
    - Demo honesty note present (two-protocol PTB scoped as mock-proven/pending): `grep -qiE 'mock_margin_pool|mock margin|documented-future|pending' README.md`.
    - `make demo` still documented: `grep -q 'make demo' README.md`.
    - No new unlabeled performance number: any percentage return in README is accompanied by a window label or a link to the whitepaper (manual check during cold-read; record in SUMMARY).
  </acceptance_criteria>
  <done>README links the three Phase-6 artifacts, the demo section is honestly scoped, and the cold-read pass is recorded (laypitch/glossary/prereqs/links all verified, fixes applied if any).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public reader | README is the public front door. The real surface is link integrity, address accuracy, and not leaking secrets / not implying a live capability that does not exist. |

No new code, data-flow, or auth. The honesty bar (LD-1) + accurate addresses + the demo re-scope (Pitfall 3) are the controls.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Information Disclosure | README testnet addresses | accept | Only PUBLIC testnet object IDs + tx digest are published (from the git-tracked TESTNET-DEPLOY.json). No private keys, no deployer mnemonic. The deployer address is already public on-chain. Low risk. |
| T-06-07 | Spoofing (of capability) | README demo claims | mitigate | Demo section honestly scopes the live flow (supply+hedge+redeem) vs the mock-proven two-protocol PTB; acceptance greps the mock/pending note. No live-Margin overclaim. |
| T-06-08 | Tampering (broken links / stale status) | README links + status | mitigate | Acceptance requires zero MAINNET-FUNDING links, zero placeholder address tokens, zero "next-up" stale status, and that the three artifact links exist. Cold-read confirms all internal links resolve. |
</threat_model>

<verification>
- `grep -c 'MAINNET-FUNDING' README.md` == 0; `MAINNET-READINESS.md` present.
- Real package_id / vault_id / admin_cap_id / deploy tx digest present; zero `<...>` address placeholders.
- Status block accurate (no "next-up Phase 1").
- Links to docs/WHITEPAPER.md, docs/architecture.svg, backtest/reports/full-365d-report.html present.
- Demo section carries the honest two-protocol-PTB scope note + ORACLE_SVI_ID source.
- Cold-read pass recorded in SUMMARY (laypitch/glossary/prereqs/links verified).
</verification>

<success_criteria>
- A judge with 10 minutes can read README.md, understand the laypitch + glossary, run `make demo`, follow working links to the whitepaper / report / architecture diagram / mainnet-readiness, and see the real deployed testnet addresses — with no broken link, no stale status, and no overclaimed capability.
</success_criteria>

<output>
After completion, create `.planning/phases/06-submission-package/06-03-SUMMARY.md`.
</output>
