---
phase: 06-submission-package
plan: 04
type: execute
wave: 3
depends_on: [06-01, 06-03]
files_modified:
  - docs/DEMO-SCRIPT.md
autonomous: false
requirements: [DEPLOY-05]
user_setup:
  - service: demo-recording
    why: "Recording the demo video requires a human operator and a funded testnet wallet running live PTBs on camera — Claude cannot record video or operate a wallet UI."
    dashboard_config:
      - task: "Record the ~3-minute demo video following docs/DEMO-SCRIPT.md (funded testnet wallet on camera, supply+hedge+redeem, tx digest pasted into suiscan)"
        location: "Local screen recording + the deployed dashboard + a Slush/testnet wallet"
must_haves:
  truths:
    - "docs/DEMO-SCRIPT.md is a shot-by-shot ~3-minute storyboard for the FILMABLE flow (make demo: supply + real on-chain hedge mint + redeem, 7 checkpoints, tx digest on suiscan)."
    - "The script presents the two-protocol single-PTB as mock-proven + documented-future, never as a live testnet Margin PTB (Pitfall 3)."
    - "The script includes the ~10s mainnet-readiness sidebar narrated from docs/MAINNET-READINESS.md."
    - "A human-action checkpoint hands the actual recording to the user (autonomous: false)."
  artifacts:
    - path: "docs/DEMO-SCRIPT.md"
      provides: "Shot-by-shot demo storyboard: timestamps, on-screen narration, exact make demo command, where the wallet-diff and pasteable tx digest appear, mainnet-readiness sidebar"
      contains: "make demo"
  key_links:
    - from: "docs/DEMO-SCRIPT.md"
      to: "scripts/testnet-smoke-test.sh (make demo) + the 7 checkpoints"
      via: "every filmed command exists in the repo"
      pattern: "make demo|testnet-smoke-test"
    - from: "docs/DEMO-SCRIPT.md"
      to: "docs/MAINNET-READINESS.md"
      via: "the ~10s sidebar narrates the deferral + 30-min procedure"
      pattern: "MAINNET-READINESS"
---

<objective>
Produce `docs/DEMO-SCRIPT.md` — the autonomous, shot-by-shot ~3-minute demo-video storyboard for DEPLOY-05 (CONTEXT.md LD-5) — then hold a human-action checkpoint for the actual recording. The script films ONLY the honestly-real flow (`make demo`: supply + real on-chain hedge mint + redeem with the tx digest on suiscan) and presents the two-protocol single-PTB as architecturally-proven-via-mock + documented-future, never as a live testnet Margin PTB.

Purpose: DEPLOY-05's literal wording ("single PTB opening Margin + Predict + vault share atomically") CANNOT be filmed live — there is no DUSDC Margin pool on testnet (06-RESEARCH.md Pitfall 3). The script delivers the only honest demo; the recording is the user's (needs a funded testnet wallet on camera).

Output: `docs/DEMO-SCRIPT.md` + a `checkpoint:human-action` for the recording.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-submission-package/CONTEXT.md
@.planning/phases/06-submission-package/06-RESEARCH.md

# The real demo entry point + the mainnet-sidebar source + the real addresses:
@README.md
@docs/MAINNET-READINESS.md
@.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json

<facts>
<!-- Verified file:line facts; the script storyboards THESE, do NOT invent a flow. -->

THE FILMABLE COMMAND (06-RESEARCH.md §7; Makefile demo target verified):
  SUI_PRIVATE_KEY=<ephemeral testnet key> ORACLE_SVI_ID=<BTC-USD OracleSVI shared object id> make demo
  -> bash scripts/testnet-smoke-test.sh -> npx tsx scripts/testnet-smoke-test.ts (run from dashboard/)

7 STAGED CHECKPOINTS (each prints [CHECKPOINT PASS]; 06-RESEARCH.md §7):
  1. pre-deposit snapshot
  2. supply tx (atomic deposit + REAL on-chain hedge mint via Predict testnet)
  3. assert Supplied + HedgeMinted events
  4. redeem_request
  5. wait REDEMPTION_COOLDOWN_MS (1h) + 5s
  6. redeem_fulfill
  7. dual ±10 bps gate (Gate A per-depositor ratio >= 99.9%, prints ratio_bps=...;
     Gate B NAV drift <= 10 bps, prints nav_delta_bps=...)
  Demo amount: 50 DUSDC (SUPPLY_AMOUNT_MICRO = 50_000_000n). Wall-clock ~= 1h5m (the cooldown).
  PRACTICAL FILMING NOTE: the 1h cooldown is not filmable in real time — script must instruct the
  operator to cut/timelapse between checkpoint 4 and checkpoint 6 (record request, stop, resume at
  fulfill), or pre-run once and narrate over the captured terminal log.

WHERE THE TX DIGEST + WALLET-DIFF SURFACE (06-RESEARCH.md §7):
  - supply tx digest is returned from client.signAndExecuteTransaction and PRINTED; operator pastes
    it into https://suiscan.xyz/testnet/tx/<digest> ON CAMERA.
  - wallet-diff (DUSDC -50, then redeemed back) visible in the wallet extension between checkpoints 1 and 7.
  - real testnet objects to show on suiscan (from TESTNET-DEPLOY.json):
      package 0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862
      vault   0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911
      deploy tx ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS

TWO-PROTOCOL PTB RE-SCOPE (06-RESEARCH.md Pitfall 3 — NON-NEGOTIABLE):
  No DUSDC Margin pool exists on testnet (MARGIN-WHITELIST = UNDETERMINED-FALLBACK-TO-MOCK; live
  pool is DBUSDC, a different token). scripts/two-protocol-ptb-demo.ts exits 0 (graceful skip) when
  Margin pool IDs are absent. The script MUST present the two-protocol PTB as "architecturally
  proven via the mock_margin_pool integration test (5-call shape compiles + runs); live on testnet
  pending Mysten's DUSDC Margin pool". A storyboard shot showing a live LoanBorrowed event is
  FORBIDDEN. The 5-call shape (for narration over the test, not a live tx): margin deposit ->
  borrow_quote -> withdraw (bridge) -> vault::supply::supply (atomic deposit + hedge mint) ->
  optional VAULT_SHARE re-deposit.

MAINNET-READINESS SIDEBAR (~10s; 06-RESEARCH.md §7; docs/MAINNET-READINESS.md:5-13):
  Narrate from MAINNET-READINESS.md §"Why mainnet deploy is deferred" + the 5-step <=30-min
  procedure. Honest line (MAINNET-READINESS.md:7): a mainnet smoke test today "would degrade to
  vault::supply + vault::redeem with allocation_bps=0 (no hedge mint) ... strictly worse than the
  full-PTB testnet demo."
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the shot-by-shot demo storyboard (DEPLOY-05 preparatory artifact)</name>
  <read_first>
    - 06-RESEARCH.md §7 (the filmable command, 7 checkpoints, digest/wallet-diff locations) + Pitfall 3 (re-scope)
    - README.md Demo section (the real command + env vars)
    - docs/MAINNET-READINESS.md:5-13 (the sidebar narration source)
    - TESTNET-DEPLOY.json (the real objects to show on suiscan)
  </read_first>
  <files>docs/DEMO-SCRIPT.md</files>
  <action>
    Create `docs/DEMO-SCRIPT.md` — a ~3-minute shot-by-shot storyboard. Structure as a timestamped table/sequence (e.g. 0:00-0:20, 0:20-0:45, ...) with three columns per shot: ON SCREEN (what the viewer sees), NARRATION (the spoken line), and ACTION/COMMAND (what the operator does). Build the storyboard from <facts>:

    - Cold open (~0:00-0:20): the laypitch ("PLP yield minus crash insurance") + one line on the composability thesis. Show the README / dashboard landing.
    - Setup (~0:20-0:40): show `SUI_PRIVATE_KEY=<ephemeral> ORACLE_SVI_ID=<BTC-USD OracleSVI> make demo` and state the demo amount (50 DUSDC) and that the hedge mint is REAL on-chain against Predict testnet. Note ORACLE_SVI_ID comes from the Mysten Predict testnet OracleSVI registry.
    - Supply + hedge (~0:40-1:30): walk checkpoints 1-3 — pre-deposit snapshot, the supply tx (atomic deposit + real hedge mint), and the Supplied + HedgeMinted event assertions. Capture the printed supply tx digest and PASTE it into https://suiscan.xyz/testnet/tx/<digest> ON CAMERA. Show the wallet-diff (DUSDC -50) in the wallet extension. Optionally show the vault object on suiscan (vault 0x2824...a911).
    - Redeem (~1:30-2:10): checkpoints 4-6 — redeem_request, the 1h cooldown (INSTRUCT the operator to cut/timelapse here — do not film an hour of waiting; resume at fulfill), redeem_fulfill. Show the wallet-diff restored.
    - Verdict (~2:10-2:30): checkpoint 7 dual ±10 bps gate — show the printed ratio_bps=... (Gate A >= 99.9%) and nav_delta_bps=... (Gate B <= 10 bps) both annotated OK.
    - Composability callout (~2:30-2:50): narrate the two-protocol single-PTB as the architectural centerpiece — the 5-call shape (margin deposit -> borrow_quote -> withdraw bridge -> vault::supply::supply -> optional SHARE re-deposit), ATOMIC. State plainly it is proven via the mock_margin_pool integration test and is documented live-on-testnet-pending Mysten's DUSDC Margin pool. DO NOT script a shot showing a live LoanBorrowed event.
    - Mainnet-readiness sidebar (~2:50-3:00, ~10s): narrate from MAINNET-READINESS.md — mainnet deploy is deferred to post-submission; a mainnet smoke test today would degrade to allocation_bps=0 (no hedge), strictly worse than this full testnet demo; the toolkit runs in <=30 minutes when Predict ships on mainnet.

    Add a "## Pre-recording checklist" the operator follows: funded testnet wallet (>= 60 DUSDC + gas), ephemeral SUI_PRIVATE_KEY exported, ORACLE_SVI_ID resolved, dashboard running, suiscan tab open, screen recorder + mic ready, and the cooldown cut/timelapse plan. Add a "## Honesty guardrails" box restating Pitfall 3 (no live-Margin shot) and the LD-1 rule (any number shown on screen must be the live demo's own output, not a quoted backtest figure).
  </action>
  <verify>
    <automated>test -f docs/DEMO-SCRIPT.md && grep -q 'make demo' docs/DEMO-SCRIPT.md && grep -qiE 'CHECKPOINT|checkpoint' docs/DEMO-SCRIPT.md && grep -q 'suiscan.xyz/testnet/tx' docs/DEMO-SCRIPT.md && grep -qiE 'mock_margin_pool|documented-future|pending' docs/DEMO-SCRIPT.md && grep -q 'MAINNET-READINESS' docs/DEMO-SCRIPT.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File exists and storyboards the real command: `grep -q 'make demo' docs/DEMO-SCRIPT.md`.
    - Walks the staged checkpoints: `grep -qiE 'checkpoint' docs/DEMO-SCRIPT.md` AND references the dual gate (`grep -qiE 'ratio_bps|nav_delta_bps' docs/DEMO-SCRIPT.md`).
    - The pasteable tx digest shot is scripted: `grep -q 'suiscan.xyz/testnet/tx' docs/DEMO-SCRIPT.md`.
    - Two-protocol PTB is scoped as mock-proven/pending (NOT live): `grep -qiE 'mock_margin_pool|documented-future|pending' docs/DEMO-SCRIPT.md`. Manual check: no shot depicts a live LoanBorrowed event.
    - Mainnet-readiness sidebar present and sourced: `grep -q 'MAINNET-READINESS' docs/DEMO-SCRIPT.md`.
    - Cooldown handling addressed: `grep -qiE 'cooldown|timelapse|cut' docs/DEMO-SCRIPT.md`.
  </acceptance_criteria>
  <done>docs/DEMO-SCRIPT.md is a complete shot-by-shot ~3-min storyboard for the filmable supply+hedge+redeem flow, with the two-protocol PTB honestly scoped and the ~10s mainnet sidebar; every filmed command exists in the repo.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    docs/DEMO-SCRIPT.md — the complete shot-by-shot ~3-minute demo storyboard (filmable make-demo flow: supply + real on-chain hedge mint + redeem, 7 checkpoints, tx digest pasted into suiscan, two-protocol PTB framed as mock-proven/documented-future, ~10s mainnet-readiness sidebar). The script is ready to film; the recording itself requires a human operator + a funded testnet wallet on camera, which Claude cannot do.
  </what-built>
  <how-to-verify>
    1. Open docs/DEMO-SCRIPT.md and skim the storyboard end to end.
    2. Complete the "Pre-recording checklist": fund a throwaway testnet wallet (>= 60 DUSDC + gas), export an EPHEMERAL `SUI_PRIVATE_KEY`, resolve `ORACLE_SVI_ID` (BTC-USD OracleSVI from the Mysten Predict testnet registry), start the dashboard, open a suiscan tab.
    3. Start your screen recorder + mic and follow the storyboard shot by shot. Run `SUI_PRIVATE_KEY=<...> ORACLE_SVI_ID=<...> make demo`; cut/timelapse across the 1-hour redeem cooldown (record checkpoints 1-4, stop, resume at checkpoint 6); paste the printed supply tx digest into https://suiscan.xyz/testnet/tx/<digest> on camera; show the wallet-diff; end on the dual ±10 bps verdict + the two-protocol/mainnet narration.
    4. Save the recording and upload it (e.g. to YouTube/Loom). Keep the share URL — Plan 05 (Devpost filing) needs it as the demo-video link.
    HONESTY GUARDRAILS: do not film a live Margin LoanBorrowed event (there is no testnet DUSDC Margin pool); do not show any quoted backtest number as if it were the live demo's output.
  </how-to-verify>
  <resume-signal>Type "recorded" plus the video share URL, or describe any blocker (e.g. wallet funding, OracleSVI id).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| demo recording -> public video | The recorded video is public. The real surface is: not exposing the ephemeral private key on camera, and not depicting a capability (live Margin PTB) that does not exist. The script itself is text and embeds no secret. |

No new code, data-flow, or auth in the artifact. The controls are Pitfall 3 (no live-Margin shot) and ephemeral-key hygiene (the demo wallet is a throwaway; the script never embeds a key).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | demo recording (operator-controlled) | mitigate | Script's pre-recording checklist mandates an EPHEMERAL throwaway `SUI_PRIVATE_KEY`; docs/DEMO-SCRIPT.md embeds NO key (acceptance: no private-key literal in the file). On-camera exposure risk is owned by the operator and flagged in the checkpoint. |
| T-06-10 | Spoofing (of capability) | demo narration | mitigate | Script frames the two-protocol PTB as mock-proven/pending; acceptance greps the framing and forbids a live LoanBorrowed shot. |
</threat_model>

<verification>
- docs/DEMO-SCRIPT.md exists; storyboards `make demo` + the 7 checkpoints + the dual ±10 bps verdict.
- The pasteable tx-digest shot + wallet-diff are scripted; the 1h cooldown is handled (cut/timelapse).
- Two-protocol PTB framed as mock-proven/documented-future; no live LoanBorrowed shot.
- ~10s mainnet-readiness sidebar present, sourced from MAINNET-READINESS.md.
- The artifact embeds no private key.
- Human-action checkpoint present (autonomous: false); recording held for the user.
</verification>

<success_criteria>
- The user has a film-ready, honest ~3-minute demo storyboard; the only remaining step (recording, which needs a funded testnet wallet on camera) is clearly handed to the user via the human-action checkpoint, and its output (the video URL) feeds Plan 05's Devpost filing.
</success_criteria>

<output>
After completion, create `.planning/phases/06-submission-package/06-04-SUMMARY.md`.
</output>
