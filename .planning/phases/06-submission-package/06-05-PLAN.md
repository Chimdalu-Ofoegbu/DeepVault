---
phase: 06-submission-package
plan: 05
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04]
files_modified:
  - docs/DEVPOST-SUBMISSION.md
autonomous: false
requirements: [DEPLOY-10]
user_setup:
  - service: devpost-portal
    why: "Filing on the Devpost / Sui Overflow portal is external publishing that needs the user's account + the recorded demo-video link — Claude cannot log in or submit on the portal."
    dashboard_config:
      - task: "Paste docs/DEVPOST-SUBMISSION.md into the Devpost / Sui Overflow submission form and submit (fill the demo-video link from Plan 04's recording + the repo URL after push)"
        location: "Devpost / Sui Overflow 2026 submission portal"
must_haves:
  truths:
    - "docs/DEVPOST-SUBMISSION.md is a complete, paste-ready Devpost draft: title, tagline, all standard sections (inspiration / what it does / how we built it / challenges / accomplishments / what's next), testnet addresses, repo URL placeholder, demo-video-link placeholder, backtest-report link, mainnet-readiness pointer."
    - "Every number in the draft is window-labeled and traces to a committed artifact per NUMBERS-CANONICAL.md (LD-1); the two-protocol PTB is framed as mock-proven/documented-future (Pitfall 3)."
    - "A human-action checkpoint hands the actual portal filing to the user (autonomous: false)."
  artifacts:
    - path: "docs/DEVPOST-SUBMISSION.md"
      provides: "Complete paste-ready Devpost submission draft with all fields filled + placeholders clearly marked for the user to fill at filing time"
      contains: "What's next"
  key_links:
    - from: "docs/DEVPOST-SUBMISSION.md"
      to: ".planning/phases/06-submission-package/NUMBERS-CANONICAL.md"
      via: "every quoted figure carries its window label + committed source"
      pattern: "full-window|out-of-sample|OOS"
    - from: "docs/DEVPOST-SUBMISSION.md"
      to: "TESTNET-DEPLOY.json + backtest report + MAINNET-READINESS.md"
      via: "addresses + report link + mainnet pointer embedded"
      pattern: "0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862"
---

<objective>
Produce `docs/DEVPOST-SUBMISSION.md` — the complete, paste-ready Devpost submission draft for DEPLOY-10 (CONTEXT.md LD-6) — then hold a human-action checkpoint for the actual portal filing. Every field is filled; the demo-video link and repo URL are clearly-marked placeholders the user fills at filing time (the video comes from Plan 04's recording; the repo URL after the user-gated push).

Purpose: DEPLOY-10 is "submission package complete on the portal." The draft is the autonomous deliverable; filing on Devpost is external publishing that needs the user's account + the recorded video link. This plan depends on Plans 01-04 (it reuses the committed numbers, the README framing, the whitepaper, and points at the demo from Plan 04).

Output: `docs/DEVPOST-SUBMISSION.md` + a `checkpoint:human-action` for the filing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-submission-package/CONTEXT.md
@.planning/phases/06-submission-package/06-RESEARCH.md

# Reuse, do not re-derive: numbers ledger, README framing, addresses, mainnet pointer:
@.planning/phases/06-submission-package/NUMBERS-CANONICAL.md
@README.md
@.planning/phases/02-vault-move-package-testnet-deploy/TESTNET-DEPLOY.json
@docs/MAINNET-READINESS.md

<facts>
<!-- Verified facts; paste, do NOT invent. -->

TITLE / TAGLINE (from CLAUDE.md project blurb): DeepVault — a composable PLP+Hedge structured
product on Sui DeepBook Predict that fuses PLP yield with automated tail-risk hedging, paired with
an institutional-grade PLP Risk Studio dashboard streaming live SVI volatility surfaces. Built for
Sui Overflow 2026's DeepBook specialized track. Tagline candidate: "PLP yield minus crash insurance,
as one composable Sui deposit." Foundation-aligned narrative: "the third primitive in the DeepBook
stack."

DEVPOST SECTIONS (LD-6): title, tagline, inspiration, what it does, how we built it, challenges,
accomplishments, what's next.

NUMBERS (reuse NUMBERS-CANONICAL.md — window-labeled, LD-1):
  - Full-window 365d (cite backtest-assumptions.md): total return +7.52%; hedge payoff +9.98%
    (one -15% breach fired); hedged max DD -1.66% vs unhedged buy-and-hold BTC -52.86%.
  - OOS holdout 30% (cite backtest/reports/full-365d.json): APY -2.30%, Sharpe -1.87, 7 cycles /
    0 payoffs (calm regime; honest cost-of-carry).
  - The honest-framing one-liner from NUMBERS-CANONICAL.md is the "accomplishments" return story.
  - NEVER place +7.52% next to -1.87 without labeling both windows.

REAL TESTNET ADDRESSES (TESTNET-DEPLOY.json):
  package 0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862
  vault   0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911
  admin_cap 0x9e40150e07ce223019afbaca425cb08b84c541ad402b428ee4a9942dfaaba3e7
  deploy tx ETYPnLemp761HsXeWigdh7h5hMvqEa2id4KDm8auBBCS  (deployed 2026-05-16)
  suiscan: https://suiscan.xyz/testnet/object/<id> , https://suiscan.xyz/testnet/tx/<digest>

LINKS / POINTERS (06-RESEARCH.md §7):
  - backtest report (committed by Plan 01): backtest/reports/full-365d-report.html
  - mainnet-readiness pointer: docs/MAINNET-READINESS.md
  - whitepaper: docs/WHITEPAPER.md ; architecture: docs/architecture.svg
  - repo URL = the GitHub URL once pushed -> PLACEHOLDER (push is user-gated, A6); mark clearly.
  - demo-video link = PLACEHOLDER (filled from Plan 04's recording); mark clearly.

THE COMPOSABILITY / CHALLENGES STORY (honest, 06-RESEARCH.md Pitfall 3 + §5):
  - "how we built it": Move vault (supply/redeem/rebalance + svi_view binary_price + ltv worst-case
    NAV + predict_adapter), three-way SVI parity gate (141 golden vectors bit-equal across
    Move/Python/TS), Node queryEvents->WS relay, React + Plotly Risk Studio (Vault | Risk Studio
    split), Python walk-forward backtest with lookahead audit. Single-PTB composability: 5-call
    Margin+Predict+vault-hedge shape.
  - "challenges": the two-protocol single-PTB cannot run live on testnet (no DUSDC Margin pool;
    DBUSDC is a different token) — proven via the mock_margin_pool integration test + documented as
    live-on-testnet-pending; DeepBook Predict mainnet did not ship in the submission window so the
    demo targets testnet with a ~10s mainnet-readiness sidebar; enforcing cross-runtime bit-equal
    SVI math; the honest backtest (negative OOS cost-of-carry by design, no retrospective tuning).
  - "what's next": mainnet deploy when Predict ships (toolkit ready, <=30 min); live VAULT_SHARE-as-
    Margin-collateral once the DUSDC pool exists; dynamic hedge sizing (v2); multi-asset; third-party
    audit (v2). Do NOT imply an audit happened.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the complete paste-ready Devpost draft (DEPLOY-10 preparatory artifact)</name>
  <read_first>
    - 06-RESEARCH.md §7 (addresses + links + repo/video placeholders) + Pitfall 3 (honest framing)
    - NUMBERS-CANONICAL.md (window-labeled numbers + the honest-framing one-liner)
    - README.md (reuse the laypitch/glossary framing for consistency)
    - docs/MAINNET-READINESS.md:5-9 (the mainnet-deferral story for "challenges"/"what's next")
  </read_first>
  <files>docs/DEVPOST-SUBMISSION.md</files>
  <action>
    Create `docs/DEVPOST-SUBMISSION.md` — a complete, paste-ready Devpost draft. Use clear Markdown section headers matching the Devpost form, all populated from <facts>:

    1. "# Title" — DeepVault (+ the one-line descriptor).
    2. "## Tagline" — "PLP yield minus crash insurance, as one composable Sui deposit" (or equivalent), plus the foundation-aligned "third primitive in the DeepBook stack" framing.
    3. "## Inspiration" — DeepBook Predict opens a binary-options venue on Sui; LPs earn PLP yield but carry tail risk. DeepVault packages PLP yield + automated crash insurance into a single deposit and proves Sui composability at the protocol layer.
    4. "## What it does" — single-deposit vault: ~90% to Predict PLP for yield, ~10% buys binary tail hedges priced off a live SVI surface; redeem with a per-user token-bucket + 1h cooldown; an institutional PLP Risk Studio dashboard streams the live SVI surface + arb checker + what-if simulator + exposure/position panels (Vault | Risk Studio split).
    5. "## How we built it" — the five tiers from <facts> + the three-way SVI parity gate (141 golden vectors bit-equal across Move/Python/TS) + the 5-call single-PTB composability shape. Cite docs/WHITEPAPER.md for the math and docs/architecture.svg for the diagram.
    6. "## Challenges we ran into" — the HONEST list from <facts>: two-protocol PTB unfilmable live (no testnet DUSDC Margin pool -> mock-proven + documented-future); Predict mainnet didn't ship in the window (testnet demo + mainnet sidebar); cross-runtime bit-equal SVI; the honest backtest (negative OOS cost-of-carry by design — no retrospective tuning per the locked hedge policy).
    7. "## Accomplishments we're proud of" — deployed testnet vault (real addresses), bit-equal three-way SVI math, and the honest backtest story: lead with the NUMBERS-CANONICAL.md one-liner — full-window +7.52% (one -15% breach fired, payoff +9.98%; hedged max DD -1.66% vs unhedged BTC -52.86%) AND, window-labeled, the calm OOS holdout where the hedge was a net cost (APY -2.30%, Sharpe -1.87). Link the committed report backtest/reports/full-365d-report.html. NEVER mix the two windows in one unlabeled claim.
    8. "## What's next" — mainnet deploy when Predict ships (toolkit ready, <=30 min; docs/MAINNET-READINESS.md); live VAULT_SHARE-as-Margin-collateral once the DUSDC pool exists; dynamic hedge sizing (v2); multi-asset; third-party audit (v2). Do NOT imply an audit happened.
    9. "## Submission details" — a block the user pastes into the portal fields:
        - Testnet contract addresses (package / vault / admin_cap / deploy tx) with suiscan links, from <facts>.
        - Backtest report: backtest/reports/full-365d-report.html (committed).
        - Mainnet-readiness: docs/MAINNET-READINESS.md.
        - Repo URL: `<REPO-URL-PLACEHOLDER — fill after the user-gated GitHub push>`.
        - Demo video: `<DEMO-VIDEO-URL-PLACEHOLDER — fill from Plan 04's recording>`.
    10. "## Fill-at-filing checklist" — the two placeholders above + a reminder that any number shown must keep its window label.

    Mark the two placeholders with an obvious literal token (e.g. `<REPO-URL-PLACEHOLDER>` and `<DEMO-VIDEO-URL-PLACEHOLDER>`) so they are greppable and impossible to miss. Do NOT fabricate a repo URL or a video link.
  </action>
  <verify>
    <automated>test -f docs/DEVPOST-SUBMISSION.md && grep -qi 'inspiration' docs/DEVPOST-SUBMISSION.md && grep -qiE "what it does" docs/DEVPOST-SUBMISSION.md && grep -qiE "how we built it" docs/DEVPOST-SUBMISSION.md && grep -qiE "what's next" docs/DEVPOST-SUBMISSION.md && grep -q '0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862' docs/DEVPOST-SUBMISSION.md && grep -q '7.52' docs/DEVPOST-SUBMISSION.md && grep -q '2.30' docs/DEVPOST-SUBMISSION.md && grep -q 'PLACEHOLDER' docs/DEVPOST-SUBMISSION.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - All standard Devpost sections present: `grep -qi 'inspiration'`, `grep -qiE 'what it does'`, `grep -qiE 'how we built it'`, `grep -qiE 'challenges'`, `grep -qiE 'accomplishments'`, `grep -qiE "what's next"` — all against docs/DEVPOST-SUBMISSION.md.
    - Real testnet package + vault addresses present: `grep -q '0xbc9aaeaa237400179e4c55cf49209dcf6ed0492be6eeb0088677e754ebd6e862' docs/DEVPOST-SUBMISSION.md` AND `grep -q '0x2824d97e221413660fd9f8e23155bd4d1d459c06a893b1d350eb279c3bf7a911' docs/DEVPOST-SUBMISSION.md`.
    - Numbers present AND window-labeled: `grep -q '7.52' docs/DEVPOST-SUBMISSION.md` AND `grep -q '2.30' docs/DEVPOST-SUBMISSION.md` AND `grep -qiE 'full-window|out-of-sample|OOS' docs/DEVPOST-SUBMISSION.md`.
    - Committed report + mainnet pointer linked: `grep -q 'full-365d-report.html' docs/DEVPOST-SUBMISSION.md` AND `grep -q 'MAINNET-READINESS' docs/DEVPOST-SUBMISSION.md`.
    - Repo URL + demo-video placeholders clearly marked: `grep -c 'PLACEHOLDER' docs/DEVPOST-SUBMISSION.md` >= 2.
    - Two-protocol PTB framed honestly (mock-proven/pending): `grep -qiE 'mock_margin_pool|documented-future|pending' docs/DEVPOST-SUBMISSION.md`. No audit implied (manual check).
  </acceptance_criteria>
  <done>docs/DEVPOST-SUBMISSION.md is a complete paste-ready draft with all Devpost sections, real testnet addresses, window-labeled numbers traced to committed artifacts, the committed report + mainnet pointers, and the two clearly-marked placeholders (repo URL + demo video).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    docs/DEVPOST-SUBMISSION.md — the complete paste-ready Devpost / Sui Overflow submission draft (title, tagline, all standard sections, real testnet addresses + suiscan links, window-labeled honest numbers traced to the committed backtest report, mainnet-readiness pointer, and two clearly-marked placeholders for the repo URL and the demo-video link). Filing on the portal is external publishing that needs your account + the recorded video link, which Claude cannot do.
  </what-built>
  <how-to-verify>
    1. Open docs/DEVPOST-SUBMISSION.md and read it end to end.
    2. Fill the two `<...PLACEHOLDER>` tokens: the repo URL (after you run the user-gated `git push` to GitHub) and the demo-video link (the share URL from Plan 04's recording — type "recorded" with the URL there if you haven't yet).
    3. Go to the Devpost / Sui Overflow 2026 submission portal, log in, and paste each section into the matching form field (inspiration / what it does / how we built it / challenges / accomplishments / what's next), attach the testnet addresses + backtest report link + mainnet-readiness pointer, and add the demo-video link + repo URL.
    4. Submit before the deadline (ROADMAP: 2026-06-16; your working target 2026-06-19).
    HONESTY GUARDRAILS: keep every number's window label when pasting (do not collapse +7.52% full-window and -1.87 OOS Sharpe into one unlabeled claim); do not add any figure not in NUMBERS-CANONICAL.md; do not imply a third-party audit.
  </how-to-verify>
  <resume-signal>Type "filed" (optionally with the Devpost submission URL), or describe any blocker (missing repo URL, video link, or portal field that doesn't map).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| draft -> public Devpost submission | The submission is public. The real surface is claim accuracy (honest, window-labeled numbers; no implied audit; PTB framed as mock-proven) and not publishing secrets. The draft is text and embeds no secret/key. |

No new code, data-flow, or auth. Controls: LD-1 honesty (window labels + committed sources), Pitfall 3 framing, and only-public-testnet-IDs.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-11 | Spoofing (of credibility) | Devpost numbers | mitigate | Every figure reused from NUMBERS-CANONICAL.md with its window label + committed source; acceptance greps the figures and a window label. No mixed-window claim. |
| T-06-12 | Repudiation / overclaim | Devpost "accomplishments" + "what's next" | mitigate | Two-protocol PTB framed mock-proven/pending (acceptance greps it); audit framed as v2 future, never claimed; the negative OOS result is presented honestly as cost-of-carry by design. |
| T-06-13 | Information Disclosure | docs/DEVPOST-SUBMISSION.md | accept | Contains only public testnet IDs + public report links + clearly-marked placeholders. No private key/address. The repo URL + video link are placeholders the user fills. Low risk. |
</threat_model>

<verification>
- docs/DEVPOST-SUBMISSION.md exists with all standard Devpost sections.
- Real testnet package + vault addresses present with suiscan links.
- Numbers window-labeled and traced to committed artifacts (NUMBERS-CANONICAL.md / the committed report).
- Committed backtest report + mainnet-readiness pointer linked; whitepaper + architecture referenced.
- Repo URL + demo-video placeholders clearly marked (>= 2 PLACEHOLDER tokens); none fabricated.
- Two-protocol PTB framed mock-proven/pending; no audit implied.
- Human-action checkpoint present (autonomous: false); portal filing held for the user.
</verification>

<success_criteria>
- The user has a complete, paste-ready Devpost submission whose only remaining steps are filling two clearly-marked placeholders (repo URL after push, demo-video link from Plan 04) and pasting into the portal — every claim honest, window-labeled, and traceable to a committed artifact.
</success_criteria>

<output>
After completion, create `.planning/phases/06-submission-package/06-05-SUMMARY.md`.
</output>
