# @deepvault/dashboard

Phase 4 placeholder — React + Vite dashboard with live SVI surface, vault panels, what-if simulator, and dApp Kit deposit/withdraw (DASH-04..DASH-13 in `.planning/REQUIREMENTS.md`).

Phase 0 ships only the workspace stub so `pnpm install` recognizes this directory.
