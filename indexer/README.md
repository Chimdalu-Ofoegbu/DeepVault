# @deepvault/indexer

Phase 4 placeholder — Sui RPC `queryEvents` relay with disk-persisted cursor and WebSocket fan-out to dashboard clients (DASH-01..DASH-03 in `.planning/REQUIREMENTS.md`).

Phase 0 ships only the workspace stub so `pnpm install` recognizes this directory.
