// indexer/src/index.ts — Phase 4 Wave 1 (Plan 04-02) replaces this stub
// with relay.ts (OracleSVIUpdated tail polling + WebSocket fan-out).
//
// Placeholder export exists so `tsc --noEmit` finds at least one input
// file. Removing it requires that Plan 04-02 land relay.ts in the same
// commit.

export const RELAY_VERSION = '0.0.0-scaffold' as const;
